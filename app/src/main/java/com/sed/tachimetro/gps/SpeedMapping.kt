package com.sed.tachimetro.gps

// TODO(RED): stub implementation, intentionally not yet correct — see SpeedMappingTest.
fun mapSpeedToKmh(
    hasAccuracy: Boolean,
    accuracyMeters: Float,
    hasSpeed: Boolean,
    speedMetersPerSecond: Float,
    accuracyThresholdMeters: Float = 50f,
    noiseFloorKmh: Double = 2.0,
): Int? {
    return 0
}
