<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Deliberately fine-only: this app needs precise instantaneous speed for a
         speedometer display, so coarse-only location would not satisfy the core
         requirement. Not adding ACCESS_COARSE_LOCATION is an explicit project
         constraint (Phase 2 threat model T-02-EP: no additional permission scope). -->
    <uses-permission
        android:name="android.permission.ACCESS_FINE_LOCATION"
        tools:ignore="CoarseFineLocation" />

    <application
        android:name=".TachimetroApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tachimetro">

        <!-- D-00a: dichiara l'esperienza a template Android Auto (categoria POI, non
             NAVIGATION, vedi service sotto). minCarApiLevel 1 massimizza la compatibilita'
             con head unit vecchi; tutte le API usate in questa fase sono disponibili da
             CarAppApiLevel 1. -->
        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />
        <meta-data
            android:name="androidx.car.app.minCarApiLevel"
            android:value="1" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Tachimetro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- D-00a: categoria POI (non NAVIGATION), app di sola lettura velocita', nessuna
             funzionalita' di instradamento reale. D-04: riusa l'icona di lancio esistente,
             nessun asset grafico dedicato per l'auto. android:exported="true" e' un requisito
             di piattaforma perche' l'host possa bindare il servizio (vedi 08-02-PLAN.md
             threat_model T-08-05 per la disposizione del rischio: HostValidator permissivo
             accettato solo per questa fase di scaffold, sostituzione con allow-list reale
             pianificata in Fase 11). -->
        <service
            android:name=".car.TachimetroCarAppService"
            android:exported="true"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="androidx.car.app.CarAppService" />
                <category android:name="androidx.car.app.category.POI" />
            </intent-filter>
        </service>

    </application>

</manifest>