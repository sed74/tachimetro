---
context: default
phase: none — mid /gsd:new-milestone workflow, before roadmap exists
task: 8
total_tasks: 14
status: in_progress
last_updated: 2026-08-30T21:21:28.846Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Assuming forced screen-off is implementable | User asked for "phone screen turns off/locks" when Android Auto connects. A normal Android app cannot force the physical screen off without special permissions (device admin etc.). | advisory | When writing REQUIREMENTS.md, phrase this requirement in terms of what's actually achievable (release `FLAG_KEEP_SCREEN_ON`, show a neutral "connected to Android Auto" state, let system idle timeout apply) — confirm the precise mechanism with the user or via research before locking it into a REQ-ID. |

<current_state>
Mid-execution of `/gsd:new-milestone v2.0 Android Auto Support`. PROJECT.md and STATE.md have already been updated and committed for the new milestone (commit `a078ff8`). Leftover v1.1 phase directories were cleared. The workflow was paused right at **Step 8 — Research Decision**: the user was asked whether to research Android Auto / Car App Library constraints before defining requirements, and interrupted to pause the session before answering.

No REQUIREMENTS.md or ROADMAP.md content exists yet for v2.0 — those are still fully ahead.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Gathered milestone goals via 3 rounds of AskUserQuestion — Done
- Task 2: Verified milestone understanding summary with user, confirmed "Va bene, procedi" — Done
- Task 3: Updated PROJECT.md (Status line, new "Current Milestone: v2.0" section, Active requirements placeholder, footer date) — Done
- Task 4: Reset STATE.md via `gsd-sdk query state.milestone-switch --milestone v2.0 --name "Android Auto Support"` — Done
- Task 5: Cleared 2 leftover phase directories from v1.1 via `gsd-sdk query phases.clear --confirm` — Done
- Task 6: Committed PROJECT.md + STATE.md as `a078ff8` "docs: start milestone v2.0 Android Auto Support" — Done
- Task 7: Loaded `init.new-milestone` context — Done. Key values: `research_enabled=false`, `agents_installed=true`, `roadmapper_model=sonnet`/`researcher_model=sonnet`/`synthesizer_model=sonnet`, `latest_completed_milestone=v1.1` (last phase was Phase 7 → new milestone continues numbering starting at **Phase 8**, no `--reset-phase-numbers` was requested), `phase_archive_path=.planning/milestones/v1.1-phases`.
- Task 8: Asked the Research Decision question — **in progress**, user interrupted before answering (see next_action).
</completed_work>

<remaining_work>

- Task 9: Re-ask/resolve the Research Decision (research first vs skip straight to requirements).
- Task 10: **If "Research first" chosen** — spawn 4 parallel `gsd-project-researcher` agents (STACK.md, FEATURES.md, ARCHITECTURE.md, PITFALLS.md dimensions) scoped to "Android Auto / Car App Library integration for a fullscreen speed display", using the existing PROJECT.md as `EXISTING_CONTEXT`. Then spawn `gsd-research-synthesizer` to write SUMMARY.md. Display key findings.
- Task 11: Define Requirements — write `.planning/REQUIREMENTS.md` (categories, REQ-IDs continuing from existing numbering, "Active"/"Future"/"Out of Scope" sections), present full list for confirmation, commit.
- Task 12: Create Roadmap — spawn `gsd-roadmapper` with instruction to start at **Phase 8** (continuing numbering, not resetting), map every v2.0 requirement to a phase, present roadmap table for approval, commit `ROADMAP.md` + `STATE.md` + `REQUIREMENTS.md`.
- Task 13: Scan `.planning/todos/pending/` for matches to new roadmap phases (directory currently doesn't exist — likely a no-op, skip silently if still absent).
- Task 14: Print the "MILESTONE INITIALIZED" summary block and point the user to `/gsd:discuss-phase 8`.
</remaining_work>

<decisions_made>

- Scope of v2.0: project the current GPS speed onto the Android Auto car/moto screen using Google's **Car App Library**, not just phone-side compatibility with Android Auto being active.
- Likely technical approach (to validate in research): declare the app under the **navigation** Car App Library category, since that's the category known to expose a raw drawable `Surface` — there's no generic "just show a big number" template in the library. This needs explicit research verification before it's written into REQUIREMENTS.md as a committed decision, since navigation-category apps may carry extra Play Console review requirements.
- Car display shows **only** current speed + "no GPS signal" state. Max speed and distance are explicitly **out of scope** for the car display this milestone (stay phone-only, unchanged from v1.1).
- Phone screen behavior when Android Auto connects: user wants it to "turn off/lock" — flagged as technically uncertain (see Critical Anti-Patterns table above). Needs to be resolved to a concrete, implementable requirement (e.g., release `FLAG_KEEP_SCREEN_ON` + neutral status screen) rather than assumed literal screen-off.
</decisions_made>

<blockers>
- None blocking further automated progress — the only open item is a **pending decision from the user** (see next_action), not a technical blocker.
</blockers>

## Required Reading (in order)
1. `.planning/PROJECT.md` — updated "Current Milestone: v2.0" section has the confirmed goal/target features/out-of-scope for this milestone
2. `.planning/HANDOFF.json` — structured version of this same state
3. `$HOME/.claude/get-shit-done/workflows/new-milestone.md` — the workflow being resumed, currently at Step 8 (Research Decision)

## Critical Anti-Patterns (do NOT repeat these)
- Do not silently decide the "Research first vs skip" question on the user's behalf — it was deliberately left open when the user asked to pause. Ask again on resume.
- Do not write REQUIREMENTS.md assuming literal phone screen-off is implementable without checking platform constraints first (see table above).

## Infrastructure State
- No running servers/watchers/background processes were part of this workflow.
- Git: clean after WIP commit below; last substantive commit is `a078ff8` on `master`.

<context>
This is the very start of a new major milestone (v2.0) for an app that through v1.1 was a plain fullscreen-number speedometer with no external integrations. The interesting technical unknown is whether/how Android's Car App Library lets an app draw a large custom number on the car's screen — this is NOT free-form like a normal Activity; Google restricts UI to specific templates per app category. The user's intent (confirmed via questions) is clear: real speed on the car display, minimal (just speed + no-signal state), and the phone becoming secondary/dormant once Android Auto is active. The one soft spot is the "phone screen turns off" request, which may not be literally achievable — that nuance must survive into REQUIREMENTS.md rather than get glossed over.
</context>

<next_action>
Start with: resume `/gsd:new-milestone` (or continue the workflow inline) at **Step 8 — Research Decision**. Re-ask the user: "Vuoi fare ricerca sul dominio Android Auto / Car App Library prima di definire i requisiti?" with options "Research first (Recommended)" and "Skip research (current default)" — recommending research given the real technical uncertainty about which Car App Library category/template can show a custom speed display. Then proceed through Steps 9-14 as outlined in `<remaining_work>`.
</next_action>
