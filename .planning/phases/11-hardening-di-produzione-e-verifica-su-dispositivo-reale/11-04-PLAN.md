---
phase: 11-hardening-di-produzione-e-verifica-su-dispositivo-reale
plan: 04
type: execute
wave: 3
depends_on: ["11-02", "11-03"]
files_modified:
  - docs/android-auto-hardening-verification.md
  - .planning/STATE.md
autonomous: false
requirements: [AA-01, AA-02, AA-03, CONN-01]

must_haves:
  truths:
    - "Si sa empiricamente, e non per congettura, se la velocita' continua ad aggiornarsi sullo schermo auto con il telefono bloccato durante un tragitto reale"
    - "Se non continua, il limite di piattaforma e' scritto e rintracciabile, non un ricordo di sessione"
    - "Nessun ACCESS_BACKGROUND_LOCATION e' entrato nel manifest, qualunque sia l'esito (D-03/D-04)"
    - "Il gap di Pitfall 6, aperto dalla ricerca v2.0 e portato avanti da Fase 8, e' chiuso con un esito dichiarato"
    - "Il test dura 5-10 minuti su un tragitto reale, la stessa soglia gia' validata empiricamente in Fase 8 (D-06)"
    - "L'esito di SC2 e' stabilito da un checkpoint umano bloccante, non da un test automatico (D-07)"
  artifacts:
    - path: "docs/android-auto-hardening-verification.md"
      provides: "Esito datato di SC2 nella tabella 'Esiti registrati' e, se FAIL, il limite di piattaforma documentato"
      contains: "SC2"
    - path: ".planning/STATE.md"
      provides: "Blocker Fase 11 su background location risolto o convertito in limite noto e accettato"
  key_links:
    - from: "docs/android-auto-hardening-verification.md"
      to: ".planning/research/PITFALLS.md"
      via: "cross-reference a Pitfall 6 come enunciato del gap"
      pattern: "Pitfall 6"
    - from: ".planning/STATE.md"
      to: "docs/android-auto-hardening-verification.md"
      via: "rimando dal blocker all'esito registrato"
      pattern: "android-auto-hardening-verification"
---

<objective>
Eseguire il test su strada di SC2 — telefono fisicamente bloccato, Android Auto connesso, tragitto
reale di 5-10 minuti — e registrarne l'esito, chiudendo il gap di piattaforma piu' incerto della
milestone (Pitfall 6) in un verso o nell'altro.

Purpose: e' l'unico rischio della fase che nessun test automatico puo' toccare (D-07). D-03/D-04
stabiliscono in anticipo che un esito negativo si chiude documentando il limite, NON aggiungendo
`ACCESS_BACKGROUND_LOCATION`: entrambi gli esiti chiudono la fase.
Output: tabella `## Esiti registrati` completa; `.planning/STATE.md` aggiornato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-CONTEXT.md
@docs/android-auto-hardening-verification.md
@.planning/research/PITFALLS.md

<locked_decisions>
<!-- NON rinegoziabili in esecuzione, qualunque cosa mostri il test. -->
- D-03: se il GPS smette di aggiornare a telefono bloccato, la fase si chiude documentando il
  limite di piattaforma come noto e accettato. Motivazione dell'utente: coerenza con la
  minimizzazione dei permessi gia' seguita dal progetto (threat model T-02-EP di Fase 2) ed
  evitare l'onere di disclosure data-safety su Play Store.
- D-04: aggiungere `ACCESS_BACKGROUND_LOCATION` e' un'opzione ESCLUSA per questa fase. Se in una
  milestone futura il gap risultasse inaccettabile, va riaperta come decisione a se', non
  anticipata qui.
- D-06: durata target 5-10 minuti, stessa soglia gia' validata empiricamente in Fase 8 (D-08).
- D-07: checkpoint umano bloccante. Nessuna sostituzione con un test automatico, un AVD o una
  sessione DHU: solo un dispositivo fisico reale, bloccato, su un tragitto reale.
- SC2 della ROADMAP ammette esplicitamente l'esito negativo ("o il limite di piattaforma viene
  documentato esplicitamente se non risolvibile"). Un FAIL NON e' un fallimento della fase e non
  richiede rilavorazione.
</locked_decisions>

<build_note>
SC2 va eseguito su un build di **debug** (a differenza di SC1/SC3, che richiedono il release
firmato): serve il log `onGetTemplate #<n>` sul tag `TachimetroCar`, che `SpeedScreen.kt` emette
solo sotto `BuildConfig.DEBUG`, e il validator reale e' irrilevante per questo criterio. Non
esiste script di misura per SC2 e la ragione e' strutturale: durante una sessione Android Auto
cablata la porta USB e' occupata dall'head unit, quindi `adb` non e' disponibile in parallelo.
SC2 e' verifica puramente umana.
</build_note>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint SC2 — test su strada con telefono bloccato</name>
  <read_first>
    - docs/android-auto-hardening-verification.md sezione SC2 (prerequisiti, procedura,
      osservazioni, criteri di esito, contingenza D-03/D-04)
    - .planning/research/PITFALLS.md sezione Pitfall 6 (enunciato del gap: la documentazione
      Google non chiarisce se gli aggiornamenti di posizione continuino a fluire verso lo schermo
      auto con il telefono bloccato/in background)
    - .planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-CONTEXT.md
      (D-03, D-04, D-06, D-07)
  </read_first>
  <what-built>
    Nulla di nuovo e' stato costruito per SC2: la catena GPS -> schermo auto esiste dalla Fase 8
    (`GpsSpeedProvider` Application-scoped -> `SpeedScreen` che ricostruisce il `PaneTemplate` a
    1 Hz) ed e' gia' stata verificata dal vivo su DHU con schermo acceso (Fase 8, 586 refresh in
    608 s). Cio' che non e' MAI stato osservato e' il comportamento con il telefono fisicamente
    bloccato e lo schermo spento durante una guida reale: un AVD con DHU non riproduce le
    politiche di risparmio energetico e di background location di un telefono vero.
  </what-built>
  <action>
    Prima di sospendere per il checkpoint, l'esecutore automatizza e riporta l'esito di:
    `./gradlew.bat :app:installDebug --console=plain`;
    `adb shell dumpsys package com.sed.tachimetro | findstr /C:"ACCESS_FINE_LOCATION"` per
    confermare che il permesso sia concesso;
    `grep -c ACCESS_BACKGROUND_LOCATION app/src/main/AndroidManifest.xml`, che deve dare 0 (D-04).
    Poi sospendere e presentare all'utente la sessione su strada descritta in `<how-to-verify>`,
    ricordando esplicitamente che un esito FAIL e' previsto e valido (D-03) e che in nessun caso
    vanno aggiunti permessi al manifest per "far passare" il test.
    L'esecutore non modifica alcun file in questo task: la registrazione dell'esito e' il Task 2.
  </action>
  <verify>
    <automated>./gradlew.bat :app:installDebug --console=plain && test "$(grep -c ACCESS_BACKGROUND_LOCATION app/src/main/AndroidManifest.xml)" = "0" && adb shell dumpsys package com.sed.tachimetro | grep -q ACCESS_FINE_LOCATION && echo "SC2_PREREQ_OK"</automated>
    <human-check>Sessione di 5-10 minuti su tragitto reale con telefono bloccato per l'intera durata; risposta ai quattro punti di osservazione del passo 7 e, se FAIL, al dato del passo 8.</human-check>
  </verify>
  <done>L'utente ha dichiarato l'esito di SC2 (PASS / FAIL / INCONCLUSIVO) con la durata effettiva della sessione e, in caso di FAIL, il tempo trascorso prima del blocco degli aggiornamenti.</done>
  <how-to-verify>
    Preparazione automatizzabile dall'esecutore prima del checkpoint:
    1. `./gradlew.bat :app:installDebug --console=plain`
    2. Confermare che il permesso `ACCESS_FINE_LOCATION` sia concesso:
       `adb shell dumpsys package com.sed.tachimetro | findstr /C:"ACCESS_FINE_LOCATION"`.
    3. Confermare che il manifest non dichiari `ACCESS_BACKGROUND_LOCATION`:
       `grep -c ACCESS_BACKGROUND_LOCATION app/src/main/AndroidManifest.xml` deve dare 0 (D-04).

    Sessione umana (non automatizzabile):
    4. Collegare il telefono fisico all'auto e avviare Android Auto; aprire Tachimetro sullo
       schermo auto.
    5. **Bloccare il telefono** (schermo spento) e lasciarlo bloccato per l'INTERA durata. Nessuna
       interazione col telefono durante il test.
    6. Guidare per 5-10 minuti su un tragitto reale con velocita' variabile (D-06).
    7. Osservare a occhio, sullo schermo auto:
       - il numero cambia visibilmente e in modo coerente con la velocita' reale del veicolo;
       - non si blocca su un valore evidentemente scaduto mentre il veicolo e' in movimento;
       - non resta permanentemente su "Ricerca segnale...";
       - non torna da solo alla lista app dell'head unit.
    8. Annotare, se il numero si ferma, DOPO QUANTO TEMPO dal blocco del telefono si e' fermato e
       se e' ripartito riaccendendo lo schermo del telefono (informazione decisiva per distinguere
       una perdita di segnale GPS da una sospensione degli update in background).

    Esito atteso (criteri del runbook, sezione SC2):
    - PASS: il numero continua ad aggiornarsi per l'intera sessione di 5-10 minuti a telefono
      bloccato, senza blocchi superiori a 30 secondi consecutivi.
    - FAIL: il numero smette di aggiornarsi (resta fermo, o resta su "Ricerca segnale...") per
      piu' di 30 secondi consecutivi mentre il veicolo e' chiaramente in movimento, in una
      sessione di almeno 5 minuti a telefono bloccato.
    - INCONCLUSIVO: sessione piu' corta di 5 minuti, telefono sbloccato durante il test, o veicolo
      fermo per gran parte del tragitto.

    Se FAIL: NON e' un fallimento della fase e non va rilavorato. NON aggiungere
    `ACCESS_BACKGROUND_LOCATION` (D-04), non aggiungere un foreground service, non modificare il
    manifest. Si passa al Task 2, che documenta il limite. Sicurezza prima di tutto: il test
    richiede un passeggero che osservi lo schermo auto, oppure va eseguito senza distogliere
    l'attenzione dalla guida.
  </how-to-verify>
  <acceptance_criteria>
    - `:app:installDebug` esce con codice 0 e il permesso `ACCESS_FINE_LOCATION` risulta concesso.
    - `grep -c ACCESS_BACKGROUND_LOCATION app/src/main/AndroidManifest.xml` restituisce 0 sia
      prima sia dopo il checkpoint.
    - L'utente dichiara la durata effettiva della sessione (>= 5 minuti per un esito non
      INCONCLUSIVO) e conferma che il telefono e' rimasto bloccato per tutta la durata.
    - L'utente risponde ai quattro punti di osservazione del passo 7.
    - In caso di FAIL, l'utente riporta il dato del passo 8 (tempo trascorso prima del blocco e
      se riaccendere lo schermo ha fatto ripartire gli aggiornamenti).
    - L'esito dichiarato e' uno fra PASS / FAIL / INCONCLUSIVO.
  </acceptance_criteria>
  <resume-signal>Rispondere con l'esito (`PASS`, `FAIL` o `INCONCLUSIVO`), la durata effettiva della sessione, e — se FAIL — dopo quanti minuti dal blocco del telefono il numero ha smesso di aggiornarsi e se e' ripartito riaccendendo lo schermo.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrare l'esito SC2 e, se FAIL, documentare il limite di piattaforma</name>
  <files>docs/android-auto-hardening-verification.md, .planning/STATE.md</files>
  <read_first>
    - docs/android-auto-hardening-verification.md (tabella `## Esiti registrati`, sezione SC2 e la
      sua `### Contingenza se FAIL`)
    - .planning/STATE.md sezione `### Blockers/Concerns` (in particolare la voce "Fase 11: il
      comportamento del GPS in background quando il telefono e' bloccato... non e' documentato" e
      il pattern con cui la voce sulla quota di refresh e' stata chiusa: testo originale barrato
      con `~~...~~` seguito da **Risolto in Fase 8** e dal rimando all'artefatto)
    - .planning/research/PITFALLS.md sezione Pitfall 6 (da referenziare, non riscrivere)
  </read_first>
  <action>
    1. In `docs/android-auto-hardening-verification.md`, compilare la riga SC2 della tabella
       `## Esiti registrati`: data della sessione (`AAAA-MM-GG`), esito dichiarato dall'utente,
       durata effettiva e, in Note, l'osservazione decisiva (per un PASS: "numero aggiornato per
       l'intera sessione a telefono bloccato"; per un FAIL: il tempo trascorso prima del blocco e
       se riaccendere lo schermo ha fatto ripartire gli aggiornamenti).

    2. Se l'esito e' **FAIL**, aggiungere subito sotto la sezione SC2 del runbook una sottosezione
       `### Limite di piattaforma documentato (esito della sessione del <data>)` che contenga, in
       italiano con apostrofi ASCII:
       - la descrizione precisa del comportamento osservato (cosa si ferma, dopo quanto, cosa lo
         fa ripartire);
       - il rimando a `.planning/research/PITFALLS.md` Pitfall 6 come enunciato del gap (non
         riscriverlo);
       - la dichiarazione che il limite e' **noto e accettato** per la milestone v2.0 (D-03), con
         la motivazione dell'utente: minimizzazione dei permessi coerente con il threat model
         T-02-EP di Fase 2, ed evitare l'onere di disclosure data-safety su Play Store;
       - la riaffermazione che `ACCESS_BACKGROUND_LOCATION` resta **esplicitamente scartato** e
         **non va riaperto** in questa fase (D-04), e che un'eventuale riapertura e' una decisione
         a se' di una milestone futura;
       - una nota operativa per l'utente finale: se il gap si manifesta, tenere lo schermo del
         telefono acceso durante la guida e' l'unico aggiramento disponibile con i permessi
         attuali.
       Se l'esito e' **PASS**, non aggiungere questa sottosezione: aggiungere invece una riga alla
       sezione SC2 che dichiara Pitfall 6 chiuso empiricamente con la data della sessione.

    3. In `.planning/STATE.md`, sezione `### Blockers/Concerns`, aggiornare la voce esistente sul
       GPS in background usando lo stesso pattern gia' impiegato per la voce sulla quota di
       refresh: barrare il testo originale con `~~...~~` e farlo seguire da un esito esplicito.
       - Se PASS: `**Risolto in Fase 11 (11-04)**` + descrizione dell'esito + rimando a
         `docs/android-auto-hardening-verification.md`.
       - Se FAIL: `**Chiuso in Fase 11 (11-04) come limite di piattaforma noto e accettato**` +
         descrizione del comportamento + rimando a `docs/android-auto-hardening-verification.md`
         e a `Pitfall 6` + la nota che `ACCESS_BACKGROUND_LOCATION` resta escluso (D-03/D-04).
       - Se INCONCLUSIVO: lasciare il blocker APERTO, aggiungendo che la sessione del <data> non
         e' stata conclusiva e perche'. Non barrare nulla.
       Aggiornare inoltre `## Current Position` / `Last activity` con la data della sessione.

    4. Verificare che `app/src/main/AndroidManifest.xml` non sia stato modificato e non contenga
       `ACCESS_BACKGROUND_LOCATION`, qualunque sia l'esito.

    Non modificare `app/src/`, `playstore/` ne' `ROADMAP.md` in questo task.
  </action>
  <verify>
    <automated>test "$(grep -c 'da eseguire' docs/android-auto-hardening-verification.md)" = "0" && grep -qF "SC2" docs/android-auto-hardening-verification.md && test "$(grep -c ACCESS_BACKGROUND_LOCATION app/src/main/AndroidManifest.xml)" = "0" && git status --short app/ | wc -l | grep -qx 0 && grep -qF "android-auto-hardening-verification" .planning/STATE.md && echo "SC2_RECORDED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - La tabella `## Esiti registrati` non contiene piu' alcuna occorrenza di `da eseguire`: tutte
      e tre le righe (SC1, SC2, SC3) hanno data ed esito.
    - `grep -c ACCESS_BACKGROUND_LOCATION app/src/main/AndroidManifest.xml` restituisce 0 e
      `git status --short app/` e' vuoto: nessuna riga di codice ne' di manifest e' stata toccata
      da questo piano.
    - `.planning/STATE.md` cita `docs/android-auto-hardening-verification.md` e la voce di
      Blockers/Concerns sul GPS in background riporta un esito esplicito (risolto, chiuso come
      limite accettato, o ancora aperto con la motivazione).
    - Se l'esito e' FAIL: il runbook contiene una sottosezione
      `### Limite di piattaforma documentato` che cita `Pitfall 6`, `D-03`, `D-04`,
      `ACCESS_BACKGROUND_LOCATION`, le parole "scartato" e "non va riaperto", e la motivazione
      dell'utente (minimizzazione dei permessi / disclosure data-safety).
    - Se l'esito e' PASS: il runbook dichiara Pitfall 6 chiuso empiricamente con la data, e la
      sottosezione sul limite di piattaforma NON esiste.
  </acceptance_criteria>
  <done>L'esito di SC2 e' registrato con il suo dato oggettivo; il gap Pitfall 6 e' chiuso in un verso o nell'altro; il manifest e' invariato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sistema operativo del telefono -> processo dell'app a schermo bloccato | Il confine reale di SC2: e' l'OS a decidere se gli aggiornamenti di posizione continuano a raggiungere il processo quando il telefono e' bloccato. L'app non controlla questo confine, puo' solo osservarlo. |
| esito ambiguo della sessione -> decisione sui permessi dichiarati | Un FAIL crea pressione a "risolvere" allargando i permessi: il confine e' il punto in cui una decisione gia' presa potrebbe essere silenziosamente ribaltata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-16 | Elevation of Privilege | `app/src/main/AndroidManifest.xml` — aggiunta reattiva di `ACCESS_BACKGROUND_LOCATION` dopo un FAIL | mitigate | D-03/D-04 sono riportati come `<locked_decisions>` nel piano e come contingenza scritta nel runbook; gate automatico su entrambi i task: `grep -c ACCESS_BACKGROUND_LOCATION app/src/main/AndroidManifest.xml` deve dare 0, e `git status --short app/` deve essere vuoto a fine piano. |
| T-11-17 | Repudiation | limite di piattaforma osservato ma non registrato, che riemerge come sorpresa in una milestone futura | mitigate | Registrazione a due livelli, come da precedente di Fase 8: nota tecnica durevole in `docs/android-auto-hardening-verification.md` piu' voce in `.planning/STATE.md` Blockers/Concerns, entrambe con cross-reference a `PITFALLS.md` Pitfall 6. Gate automatico: zero occorrenze residue di `da eseguire` nella tabella degli esiti. |
| T-11-18 | Spoofing (falso PASS) | sessione condotta a schermo acceso o piu' corta di 5 minuti, dichiarata PASS | mitigate | `INCONCLUSIVO` e' un esito esplicito e legittimo del runbook; il resume-signal impone di dichiarare la durata effettiva e la conferma che il telefono e' rimasto bloccato, non un semplice "ha funzionato". |
| T-11-19 | Information Disclosure | dati del tragitto (posizioni, velocita') registrati negli artefatti della verifica | mitigate | Nessuna cattura logcat per SC2 (adb non disponibile durante la sessione): si registrano solo esito, durata e comportamento osservato. Nessuna coordinata, nessun percorso, nessun identificatore di dispositivo negli artefatti. |
| T-11-SC | Tampering | installazioni npm/pip/cargo/Gradle | accept | Nessuna nuova dipendenza: questo piano esegue un `installDebug` e modifica due file Markdown. |
</threat_model>

<verification>
1. `grep -c ACCESS_BACKGROUND_LOCATION app/src/main/AndroidManifest.xml` restituisce 0.
2. `git status --short app/` e' vuoto.
3. `docs/android-auto-hardening-verification.md` ha SC1, SC2 e SC3 tutti compilati nella tabella
   `## Esiti registrati`, senza occorrenze di `da eseguire`.
4. `.planning/STATE.md` riporta un esito esplicito per il blocker sul GPS in background e cita il
   runbook.
5. `./gradlew.bat :app:test --console=plain` resta verde (nessuna regressione: questo piano non
   tocca codice, il controllo serve solo a confermarlo).
</verification>

<success_criteria>
- SC2 della ROADMAP e' chiuso con un esito dichiarato: la velocita' continua ad aggiornarsi a
  telefono bloccato, oppure il limite di piattaforma e' documentato esplicitamente — entrambi
  esiti validi per SC2 come formulato.
- Il gap Pitfall 6, aperto dalla ricerca v2.0 e portato avanti da Fase 8, non e' piu' un'incognita.
- Nessun permesso e' stato aggiunto al manifest, coerentemente con D-03/D-04 e con la
  minimizzazione dei permessi seguita dal progetto.
- I tre Success Criteria della Fase 11 hanno tutti un esito registrato e rintracciabile.
</success_criteria>

<output>
Create `.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-04-SUMMARY.md` when done
</output>
