---
context: phase
phase: 11-hardening-di-produzione-e-verifica-su-dispositivo-reale
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-09-03T09:33:16.202Z
---

## Critical Anti-Patterns

_Nessuna — nessun anti-pattern bloccante scoperto in questa sessione. Nota advisory sotto._

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Post-merge build/test gate non copre Gradle | Il gate generico di execute-phase.md (step 5.6) sniffa solo npm/cargo/go/python/make/Xcode — un progetto Gradle/Kotlin come questo non viene mai rilevato e il gate si auto-skippa silenziosamente | advisory | Eseguire manualmente `./gradlew.bat assembleDebug` e `./gradlew.bat test` dopo ogni merge di wave finche' `workflow.build_command`/`workflow.test_command` non vengono impostati in `.planning/config.json` |

<current_state>
Phase 11 in esecuzione (`/gsd:execute-phase 11`, mode: yolo). Wave 1 completata e mergiata su master (commit `5ec7e6d`). Wave 2 (piano 11-03, Task 1 di 3) non ancora iniziata: richiede un build di release firmato e hardware Android Auto reale (DHU o head unit), non accessibili in questo momento. L'utente ha scelto di occuparsi personalmente sia delle credenziali del keystore sia della generazione/installazione dell'APK firmato, e testera' fisicamente SC1/SC3 quando l'hardware sara' disponibile.
</current_state>

<completed_work>

Wave 1 (piani 11-01, 11-02) — Done, mergiata su master:
- 11-01: `CarHostValidation.kt` (nuovo) con `createCarHostValidator(context, allowAllHosts)`; `TachimetroCarAppService.createHostValidator()` ora delega passando `BuildConfig.DEBUG`; 6 test strumentati verdi (`CarHostValidationTest.kt`). Commit di merge: `a2582f7`.
- 11-02: `docs/android-auto-hardening-verification.md` (runbook SC1/SC2/SC3) e `scripts/aa-connect-cycle-check.ps1` (misura cicli rapidi). Commit di merge: `fb2e66d`.
- Post-merge build gate: `./gradlew.bat assembleDebug` → BUILD SUCCESSFUL.
- Post-merge test gate: `./gradlew.bat test` → BUILD SUCCESSFUL, nessun conflitto cross-plan.
- Tracking aggiornato: `docs(phase-11): update tracking after wave 1` → commit `5ec7e6d`.
- `keystore.properties` creato da `keystore.properties.example` (storeFile gia' corretto verso `C:\Users\fedes\AndroidStudioProjects\keystore\keystore`; storePassword/keyAlias/keyPassword ancora `CHANGE_ME`, file gitignorato, nessun segreto committato).
</completed_work>

<remaining_work>

- **11-03 Task 1** (checkpoint:human-verify, blocking): SC1 — host Android Auto reale su build di release firmato con allow-list. Bloccato su: keystore.properties compilato con credenziali reali (a cura dell'utente) + build/installazione release firmata (a cura dell'utente) + DHU/head unit reale (non accessibile ora).
- **11-03 Task 2** (checkpoint:human-verify, blocking): SC3 — 10 cicli rapidi di connessione/disconnessione via `scripts/aa-connect-cycle-check.ps1 -Cycles 10`, riusando lo stesso build/setup del Task 1.
- **11-03 Task 3** (auto): compilare la tabella "Esiti registrati" in `docs/android-auto-hardening-verification.md` per SC1/SC3 e ritirare/aggiornare la nota di rischio in `playstore/README.md` in base all'esito di SC1 (PASS → ritira la nota; non-PASS → aggiorna indicando che la fase e' in corso).
- **11-04** (wave 3, non iniziata): checkpoint SC2 — test su strada reale, telefono bloccato/schermo spento, 5-10 minuti, registrazione esito in `docs/android-auto-hardening-verification.md` e in `.planning/STATE.md`.
</remaining_work>

<decisions_made>

- L'utente gestisce personalmente sia le credenziali del keystore sia la generazione/installazione dell'APK di release firmato — l'executor NON deve tentare di automatizzare `installRelease` per questo piano, l'utente lo fa fuori dal flusso GSD.
- Nessun ripiego su build di debug per SC1: e' esplicitamente vietato dal runbook come falso PASS (vedi `docs/android-auto-hardening-verification.md` sezione SC1 e D-01/D-02 in `11-CONTEXT.md`).
- Il gate di copertura decisioni CONTEXT.md di plan-phase (`check.decision-coverage-plan`) cerca citazioni `D-NN` solo nel blocco YAML `must_haves.truths` del frontmatter dei piani, non nel corpo XML (`&lt;objective&gt;` ecc.) — se in futuro si ripianifica questa fase e il gate torna a fallire su D-02/D-05/D-06/D-07, la causa e' questa (gia' risolta una volta aggiungendo le citazioni nel campo giusto).
</decisions_made>

<blockers>
- Android Auto reale (DHU o head unit) non accessibile ora — bloccante per 11-03 Task 1/2 e per 11-04. Nessun workaround: e' un checkpoint umano per design (D-07).
- keystore.properties con credenziali placeholder — bloccante per generare un build di release firmato. Workaround: l'utente lo compila e gestisce la firma personalmente.
</blockers>

## Required Reading (in order)
1. `.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-03-PLAN.md` — Task 1/2/3 completi, con `&lt;how-to-verify&gt;` e criteri di esito PASS/FAIL/INCONCLUSIVO
2. `docs/android-auto-hardening-verification.md` — runbook autorevole per la procedura SC1/SC2/SC3
3. `.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-CONTEXT.md` — decisioni D-01..D-07 e contingenze gia' chiuse (NON riaprire D-03/D-04: niente `ACCESS_BACKGROUND_LOCATION` se SC2 fallisce)

## Infrastructure State
- Working tree pulito su `master`, HEAD a `5ec7e6d`. Nessuna worktree residua (`git worktree list` mostra solo il checkout principale).
- `keystore.properties` presente ma con placeholder — non idoneo a un build di release firmato finche' l'utente non lo compila.
- Nessun processo in background in esecuzione.

<context>
Esecuzione autonoma di `/gsd:execute-phase 11` in mode yolo, arrivata naturalmente al limite dell'automazione: i due piani rimanenti (11-03, 11-04) sono entrambi checkpoint umani bloccanti su hardware reale che nessun agente puo' soddisfare. L'utente ha confermato di voler gestire personalmente keystore e build firmato, e testera' su Android Auto reale appena possibile. Il lavoro autonomo (Wave 1) e' stato completato, verificato e mergiato correttamente prima della pausa.
</context>

<next_action>
Quando l'utente riporta l'esito di SC1 (PASS/FAIL/INCONCLUSIVO + riga di log `CarApp.Val` osservata) e di SC3 (PASS/FAIL/INCONCLUSIVO + verdetto e conteggi dello script `aa-connect-cycle-check.ps1`), eseguire direttamente 11-03 Task 3: aggiornare `docs/android-auto-hardening-verification.md` (tabella "Esiti registrati") e `playstore/README.md` (ritira o aggiorna la nota di rischio secondo l'esito di SC1), poi continuare con Wave 3 (piano 11-04, test su strada SC2). Non ri-eseguire l'automazione dei Task 1/2 di 11-03, gia' svolta manualmente dall'utente.
</next_action>
