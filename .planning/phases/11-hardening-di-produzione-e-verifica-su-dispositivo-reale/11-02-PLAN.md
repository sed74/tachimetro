---
phase: 11-hardening-di-produzione-e-verifica-su-dispositivo-reale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/aa-connect-cycle-check.ps1
  - docs/android-auto-hardening-verification.md
autonomous: true
requirements: [AA-01, AA-02, AA-03, AA-04, CONN-01, CONN-02]

must_haves:
  truths:
    - "Chi esegue la verifica sa esattamente quale build installare per ciascuno dei tre Success Criteria, e perche' non e' lo stesso binario"
    - "Il criterio di esito di ogni verifica e' scritto PRIMA della sessione, non deciso a posteriori"
    - "La contingenza se SC2 fallisce e' gia' decisa (documentare il limite, nessun ACCESS_BACKGROUND_LOCATION) e le opzioni chiuse sono riaffermate come chiuse"
    - "Le catture logcat finiscono sotto build/ (gitignorata), mai nel repository"
  artifacts:
    - path: "scripts/aa-connect-cycle-check.ps1"
      provides: "Misura riproducibile di crash/ANR/rifiuti host durante cicli rapidi di connessione-disconnessione (SC3)"
      contains: "FATAL EXCEPTION"
    - path: "docs/android-auto-hardening-verification.md"
      provides: "Runbook dei tre gate empirici SC1/SC2/SC3 con prerequisiti, procedura, criteri di esito e contingenze"
      contains: "## Criteri di esito"
  key_links:
    - from: "docs/android-auto-hardening-verification.md"
      to: "scripts/aa-connect-cycle-check.ps1"
      via: "riferimento esplicito nella procedura SC3"
      pattern: "aa-connect-cycle-check\\.ps1"
    - from: "docs/android-auto-hardening-verification.md"
      to: ".planning/research/PITFALLS.md"
      via: "cross-reference a Pitfall 6 come enunciato del gap di piattaforma di SC2"
      pattern: "PITFALLS\\.md"
---

<objective>
Produrre gli strumenti della verifica empirica di questa fase: un runbook riproducibile per i tre
Success Criteria e uno script di misura per il test di connessione/disconnessione rapida.

Purpose: D-07 impone che SC2 e SC3 siano checkpoint umani bloccanti; il pattern del progetto
(Fase 8) e' "script euristico + runbook per occhi umani, nessuno dei due sostituisce l'altro".
Scrivere i criteri di esito PRIMA della sessione impedisce di razionalizzare un risultato ambiguo
a posteriori.
Output: `scripts/aa-connect-cycle-check.ps1` (nuovo), `docs/android-auto-hardening-verification.md`
(nuovo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-CONTEXT.md
@.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-PATTERNS.md
@docs/dhu-quota-verification.md
@scripts/dhu-quota-check.ps1

<interfaces>
<!-- Segnali di log disponibili, VERIFICATI (sources-jar androidx.car.app:app:1.7.0 e
     SpeedScreen.kt). Sono la base dei criteri di esito automatici: non inventarne altri. -->

Tag `CarApp.Val` (androidx/car/app/utils/LogTags.java, TAG_HOST_VALIDATION):
  Log.d "Accepted - Validator disabled, all hosts allowed"   -> il build in esecuzione e' DEBUG (validator permissivo)
  Log.d "Accepted - Host in allow-list"                      -> il build in esecuzione e' RELEASE e l'allow-list ha accettato l'host
  Log.w "Rejected - package name <pkg> not found"            -> host rifiutato
  I due Log.d sono dietro Log.isLoggable("CarApp.Val", DEBUG): vanno abilitati con
  `adb shell setprop log.tag.CarApp.Val DEBUG` PRIMA di avviare la sessione.
  Il Log.w di rifiuto e' invece sempre emesso.

Tag `TachimetroCar` (app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt:303-314):
  Log.d "onGetTemplate #<n> content=<label>"  -- EMESSO SOLO SE BuildConfig.DEBUG.
  Conseguenza operativa da scrivere nel runbook: questo contatore NON esiste in un build di
  release, quindi non puo' essere usato come misura in una sessione SC1 (che richiede il build
  di release). Il conteggio dei refresh e' disponibile solo nelle sessioni su build di debug.

Convenzioni adb gia' usate dal progetto (scripts/dhu-quota-check.ps1):
  applicationId = "com.sed.tachimetro"
  porta DHU     = `adb forward tcp:5277 tcp:5277`
  output        = sotto build/ (regola `/build` in .gitignore)
</interfaces>

<build_matrix>
<!-- Vincolo strutturale scoperto in pianificazione, da riportare nel runbook: i tre SC NON
     possono essere verificati dallo stesso binario. -->
| SC | Build richiesto | Perche' |
|----|-----------------|---------|
| SC1 (HostValidator reale) | **release** (firmato) | D-01: il build di debug usa di proposito `ALLOW_ALL_HOSTS_VALIDATOR`. Verificare SC1 su un debug sarebbe un falso PASS. |
| SC2 (velocita' a telefono bloccato) | **debug** | Serve il log `onGetTemplate #` e non serve il validator reale; SC2 riguarda il GPS in background, non l'host. |
| SC3 (connect/disconnect rapido) | **release** (firmato) | Piu' informativo: esercita il binario che si spedisce, il validator reale a ogni ri-bind, e non dipende da log DEBUG-only. |
</build_matrix>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Script di misura aa-connect-cycle-check.ps1 per SC3</name>
  <files>scripts/aa-connect-cycle-check.ps1</files>
  <read_first>
    - scripts/dhu-quota-check.ps1 (file intero, 334 righe): blocco di comment-based help,
      `[CmdletBinding()] param(...)`, `$ErrorActionPreference = 'Stop'`, costanti `$AppId`/`$LogTag`
      letterali, `function Write-Section`, verifica di `adb` sul PATH con `Get-Command`,
      selezione del serial, `adb forward tcp:5277 tcp:5277` con retry, `adb logcat -c` +
      `Start-Process` in background con redirect su file, `Read-Host` per i punti non
      automatizzabili, scrittura del riepilogo con `Set-Content`
    - .planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-CONTEXT.md
      sezione D-05 (testa prima, patcha solo se si rompe davvero)
  </read_first>
  <action>
    Creare `scripts/aa-connect-cycle-check.ps1` ricalcando la struttura di
    `scripts/dhu-quota-check.ps1`, adattata al problema di misura di SC3.

    Comment-based help obbligatorio con `.SYNOPSIS` (che nomina D-05 e D-07 e il criterio SC3
    della ROADMAP), `.DESCRIPTION` che rimanda a `docs/android-auto-hardening-verification.md`,
    una `.PARAMETER` per ogni parametro, `.EXAMPLE`. Nella `.DESCRIPTION` riportare i due
    vincoli ereditati dall'analogo, testualmente: lo script NON installa e NON scarica nulla
    (usa solo `adb` gia' sul PATH), e il suo esito euristico NON sostituisce la conferma umana
    richiesta dal checkpoint del Piano 03.

    Parametri: `[int]$Cycles = 10`, `[string]$OutputDir = "build/aa-hardening"`,
    `[string]$Serial = ""`.

    Costanti letterali in testa (stessa motivazione scritta dell'analogo: nessun parsing del
    build script): `$AppId = "com.sed.tachimetro"`, `$HostValidationTag = "CarApp.Val"`.

    Flusso:
    1. Verificare `adb` sul PATH con `Get-Command adb`; errore esplicito se assente.
    2. Selezionare il serial (parametro esplicito, altrimenti unico dispositivo in stato
       `device`); errore se ambiguo. A differenza dell'analogo NON preferire i serial
       `emulator-`: SC3 gira su telefono fisico + DHU o head unit reale.
    3. Creare `$OutputDir` se assente; nomi file con timestamp `yyyyMMdd-HHmmss`:
       `aa-connect-cycle-<ts>.log` e `aa-connect-cycle-<ts>-summary.txt`.
    4. `adb -s <serial> shell setprop log.tag.CarApp.Val DEBUG` per abilitare i Log.d di
       validazione host, e stampare che e' stato fatto.
    5. `adb forward tcp:5277 tcp:5277` con lo stesso retry dell'analogo (necessario solo per il
       DHU; se il test usa un head unit reale il forward e' innocuo — segnalarlo a video, non
       fallire).
    6. `adb -s <serial> logcat -c`, poi avviare in background
       `adb -s <serial> logcat -v time CarApp.Val:D *:W` con redirect su `$logFile`.
    7. Campionare il PID iniziale con `adb -s <serial> shell pidof com.sed.tachimetro`.
    8. Ciclo `for` da 1 a `$Cycles`: stampare l'istruzione del ciclo corrente ("collegare
       Android Auto, attendere che Tachimetro sia visibile sullo schermo auto, poi scollegare
       subito"), attendere `Read-Host`, campionare il PID a fine ciclo e accumularlo in una
       lista insieme al numero di ciclo.
    9. Terminare la cattura logcat (`Stop-Process` sul processo avviato, gestendo il caso in cui
       sia gia' terminato).
    10. Analizzare `$logFile` contando le occorrenze di: `FATAL EXCEPTION`,
        `ANR in com.sed.tachimetro`, e delle righe del tag `CarApp.Val` che contengono
        `Rejected -`. Contare anche le righe `Accepted - Host in allow-list` e
        `Accepted - Validator disabled` per poter dichiarare a video QUALE build era in
        esecuzione.
    11. Verdetto euristico e sua scrittura sia a video sia in `$summaryFile`:
        - `FAIL` se il conteggio di `FATAL EXCEPTION` > 0 OPPURE `ANR in com.sed.tachimetro` > 0
          OPPURE `Rejected -` > 0.
        - `PASS` se tutti e tre i conteggi sono 0 E sono stati completati tutti i `$Cycles` cicli.
        - `INCONCLUSIVO` in ogni altro caso.
        Il riepilogo deve includere: serial, numero di cicli completati, PID campionato a ogni
        ciclo, i cinque conteggi, il verdetto e il percorso del log.

    Nota da scrivere come commento nel punto in cui si campiona il PID: un cambio di PID fra un
    ciclo e l'altro NON e' di per se' un FAIL — alla disconnessione il sistema puo' legittimamente
    terminare il processo se l'app non e' in primo piano sul telefono. Il segnale di crash e'
    `FATAL EXCEPTION`/`ANR`, non il PID. Il PID viene comunque registrato perche' e' il contesto
    che serve a un umano per interpretare un log ambiguo.

    Scrivere i messaggi a video in italiano con apostrofi ASCII, come l'analogo.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'scripts/aa-connect-cycle-check.ps1'), [ref]$null, [ref]$errs); if ($errs.Count -gt 0) { $errs | Out-String | Write-Error; exit 1 }; Get-Help ./scripts/aa-connect-cycle-check.ps1 -Full | Out-Null; exit 0" && grep -c "FATAL EXCEPTION" scripts/aa-connect-cycle-check.ps1 && grep -c "build/aa-hardening" scripts/aa-connect-cycle-check.ps1 && git check-ignore -q build && echo "OUTPUT_DIR_GITIGNORED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - Lo script si analizza senza errori di sintassi PowerShell e `Get-Help -Full` restituisce un
      help popolato (comment-based help presente e ben formato).
    - `param()` dichiara esattamente `$Cycles` (default 10), `$OutputDir` (default
      `build/aa-hardening`), `$Serial` (default stringa vuota).
    - Lo script contiene le stringhe letterali `FATAL EXCEPTION`, `ANR in com.sed.tachimetro`,
      `Rejected -`, `Accepted - Host in allow-list`, `setprop log.tag.CarApp.Val DEBUG`.
    - `git check-ignore -q build` esce con codice 0 (`OUTPUT_DIR_GITIGNORED_OK` stampato): le
      catture non possono finire nel repository.
    - `grep -nE "Invoke-WebRequest|curl|Install-|choco|winget|sdkmanager" scripts/aa-connect-cycle-check.ps1`
      non trova nulla: lo script non installa e non scarica nulla.
    - Le tre righe di verdetto `FAIL`/`PASS`/`INCONCLUSIVO` sono presenti e le loro condizioni
      corrispondono a quelle elencate nell'action.
  </acceptance_criteria>
  <done>Lo script esiste, e' sintatticamente valido, ha help completo, scrive solo sotto build/ e implementa i tre verdetti con i segnali di log verificati.</done>
</task>

<task type="auto">
  <name>Task 2: Runbook docs/android-auto-hardening-verification.md per SC1, SC2, SC3</name>
  <files>docs/android-auto-hardening-verification.md</files>
  <read_first>
    - docs/dhu-quota-verification.md (file intero, 115 righe): scheletro delle sezioni,
      tono, tabella "Criteri di esito", sezione "Contingenza se FAIL" che riafferma le opzioni
      chiuse, sezione "Cosa e' fuori scope di questa verifica"
    - .planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-CONTEXT.md
      (decisioni D-01, D-03, D-04, D-05, D-06, D-07)
    - .planning/research/PITFALLS.md sezione Pitfall 6 (enunciato del gap di piattaforma di SC2 --
      va referenziato, NON riscritto)
    - scripts/aa-connect-cycle-check.ps1 (creato al Task 1: parametri e verdetti reali da citare)
    - app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt righe 300-315 (per confermare che
      il log `onGetTemplate #` e' dietro BuildConfig.DEBUG)
  </read_first>
  <action>
    Creare `docs/android-auto-hardening-verification.md` in italiano, apostrofi ASCII, ricalcando
    l'ordine delle sezioni di `docs/dhu-quota-verification.md`:
    titolo + paragrafo di inquadramento; `## Perche' questa verifica esiste`; `## Quale build per
    quale criterio`; poi, per ciascuno dei tre criteri, un blocco `## SC1 ...` / `## SC2 ...` /
    `## SC3 ...` contenente `### Prerequisiti`, `### Procedura passo-passo`, `### Cosa osservare a
    occhio`, `### Criteri di esito` (tabella), `### Contingenza se FAIL`; chiudere con
    `## Cosa e' fuori scope di questa verifica` e `## Esiti registrati` (tabella vuota con le
    colonne `Criterio | Data | Esito | Note`, compilata dai Piani 03 e 04).

    Contenuto obbligatorio, sezione per sezione:

    **Quale build per quale criterio** — riprodurre la tabella `<build_matrix>` del contesto di
    questo piano (SC1 release firmato, SC2 debug, SC3 release firmato) con la motivazione di
    ciascuna riga. Aggiungere la conseguenza operativa verificata: il contatore
    `onGetTemplate #<n>` del tag `TachimetroCar` esiste SOLO nei build di debug, quindi non e'
    utilizzabile come misura in una sessione SC1/SC3; e i build di release richiedono
    `keystore.properties` (assente dal repository per progetto — vedi `keystore.properties.example`),
    senza il quale `assembleRelease` produce un APK non firmato e non installabile.

    **SC1** — prerequisiti: build release firmato installato (`gradlew.bat :app:installRelease`),
    DHU dall'SDK (`<sdk>\extras\google\auto\desktop-head-unit.exe`) oppure head unit reale,
    permesso di localizzazione gia' concesso.
    Procedura: `adb shell setprop log.tag.CarApp.Val DEBUG`; `adb logcat -s CarApp.Val`;
    `adb forward tcp:5277 tcp:5277`; avviare il DHU; selezionare Tachimetro.
    Osservazioni a occhio: l'app compare nella lista app dell'head unit, si apre, mostra le cifre
    della velocita', non torna alla lista app.
    Criteri di esito (tabella, FAIL per primo):
    - `FAIL` se in logcat compare `Rejected -` sul tag `CarApp.Val`, oppure l'app non si apre
      sull'head unit, oppure compare `Accepted - Validator disabled, all hosts allowed`
      (significa che si sta testando un build di debug: la verifica non e' valida).
    - `PASS` se compare `Accepted - Host in allow-list` E l'app si apre e mostra la velocita'
      sullo schermo auto.
    - `INCONCLUSIVO` ogni altro caso, in particolare se nessuna riga `CarApp.Val` viene emessa
      (setprop dimenticato).
    Contingenza se FAIL: NON reintrodurre `ALLOW_ALL_HOSTS_VALIDATOR` per far passare il test.
    Un `Rejected` indica un'allow-list sbagliata (tipicamente l'inversione `<package>,<digest>`
    documentata in `11-PATTERNS.md`) o un host non ufficiale: si corregge il validator, si
    riesegue il test strumentato `CarHostValidationTest` e si ripete la sessione.

    **SC2** — inquadrare il criterio citando `.planning/research/PITFALLS.md` Pitfall 6 come
    enunciato del gap (cross-reference, non riscrittura).
    Prerequisiti: **telefono fisico reale** (non AVD), build **debug** installato, Android Auto
    funzionante su un'auto vera, permesso di localizzazione concesso, tragitto reale disponibile.
    Procedura: durata 5-10 minuti (D-06, stessa soglia gia' validata in Fase 8), telefono
    fisicamente bloccato/schermo spento per l'INTERA durata, nessuna interazione col telefono.
    Scrivere esplicitamente che non esiste script di misura per SC2 e perche': durante una
    sessione Android Auto cablata la porta USB e' occupata dall'head unit, quindi `adb` non e'
    disponibile in parallelo — SC2 e' verifica puramente umana (D-07).
    Cosa osservare a occhio: il numero sullo schermo auto cambia visibilmente e in modo coerente
    con la velocita' reale del veicolo; non si blocca su un valore scaduto; non resta
    permanentemente su "Ricerca segnale..."; il telefono resta bloccato per tutta la durata.
    Criteri di esito:
    - `FAIL` se il numero smette di aggiornarsi (resta fermo sullo stesso valore, o resta su
      "Ricerca segnale...") per piu' di 30 secondi consecutivi mentre il veicolo e' chiaramente
      in movimento, in una sessione di almeno 5 minuti a telefono bloccato.
    - `PASS` se il numero continua ad aggiornarsi per l'intera sessione di 5-10 minuti a telefono
      bloccato, senza blocchi superiori a 30 secondi.
    - `INCONCLUSIVO` ogni altro caso (sessione piu' corta di 5 minuti, telefono sbloccato durante
      il test, veicolo fermo per gran parte del tragitto).
    Contingenza se FAIL (D-03/D-04) — questa sezione e' la piu' importante del documento e deve
    dichiarare che la decisione e' GIA' presa, non da prendere sul momento: si chiude la fase
    documentando il limite di piattaforma come noto e accettato, registrandolo in questo runbook
    (`## Esiti registrati`) e in `.planning/STATE.md` fra i Blockers/Concerns, con cross-reference
    a Pitfall 6. L'aggiunta di `ACCESS_BACKGROUND_LOCATION` al manifest resta **esplicitamente
    scartata** e **non va riaperta** (formula ricalcata da
    `docs/dhu-quota-verification.md:106-107`): motivazione dell'utente = coerenza con la
    minimizzazione dei permessi gia' seguita dal progetto (threat model T-02-EP di Fase 2) ed
    evitare l'onere di disclosure data-safety su Play Store. Se in una milestone futura il gap
    risultasse inaccettabile, va riaperto come decisione a se' (D-04), non anticipato qui.
    Aggiungere che un FAIL su SC2 NON e' un fallimento della fase: SC2 della ROADMAP ammette
    esplicitamente questo esito ("o il limite di piattaforma viene documentato esplicitamente se
    non risolvibile").

    **SC3** — prerequisiti: build **release** firmato installato, DHU o head unit reale, `adb`
    disponibile (sessione da scrivania, non in guida).
    Procedura: `powershell -File scripts/aa-connect-cycle-check.ps1 -Cycles 10`, poi seguire le
    istruzioni per ciclo dello script (collegare, attendere che Tachimetro sia visibile, scollegare
    subito). Citare i parametri reali dello script e il percorso di output `build/aa-hardening/`.
    Cosa osservare a occhio: a ogni riconnessione lo schermo auto torna a mostrare la velocita' o
    "Ricerca segnale..." — mai una schermata vuota, mai un valore evidentemente vecchio congelato,
    mai un errore dell'host; il telefono non mostra crash ne' dialog "L'app si e' interrotta".
    Criteri di esito: gli stessi tre verdetti dello script (`FAIL` se compare `FATAL EXCEPTION`,
    `ANR in com.sed.tachimetro` o `Rejected -`; `PASS` se zero occorrenze su tutti i cicli
    completati; `INCONCLUSIVO` altrimenti), PIU' la conferma visiva umana dei punti sopra —
    dichiarando che l'uno non sostituisce l'altro (D-10 di Fase 8, D-07 qui).
    Contingenza se FAIL (D-05): solo in caso di crash o stato incoerente REALMENTE osservato si
    aggiungono guardie difensive, e il punto di intervento indicato dal pattern del progetto e'
    `app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt` (analogo: la guardia in memoria
    `requestInFlight` alle righe 61-67). Scrivere esplicitamente che nessun hardening preventivo
    va scritto per uno scenario non ancora osservato, e che un fix va pianificato come lavoro a
    se' con l'utente, non improvvisato durante la sessione di verifica.

    **Cosa e' fuori scope** — almeno: il passaggio a `NavigationTemplate`+`SurfaceCallback`
    (rimandato a v2.1, D-14 di `08-CONTEXT.md`); velocita' massima e distanza sullo schermo auto
    (fuori scope di milestone); qualunque nuova capability oltre ai tre Success Criteria;
    Scenario G/Pitfall 1 di Fase 9 (`requestPermissions()` ignorabile dall'host a veicolo in
    movimento), gia' accettato e non riaperto qui.

    Nel paragrafo di apertura dichiarare che questo runbook serve i gate SC1/SC2/SC3 della Fase 11
    e che sostituisce, per questa fase, il ruolo che `docs/dhu-quota-verification.md` aveva per il
    gate SC4 della Fase 8.
  </action>
  <verify>
    <automated>test -f docs/android-auto-hardening-verification.md && for s in "## Perche' questa verifica esiste" "## Quale build per quale criterio" "## Cosa e' fuori scope di questa verifica" "## Esiti registrati"; do grep -qF "$s" docs/android-auto-hardening-verification.md || { echo "MISSING SECTION: $s"; exit 1; }; done && test "$(grep -c '^### Criteri di esito' docs/android-auto-hardening-verification.md)" = "3" && test "$(grep -c '^### Contingenza se FAIL' docs/android-auto-hardening-verification.md)" = "3" && grep -qF "aa-connect-cycle-check.ps1" docs/android-auto-hardening-verification.md && grep -qF "PITFALLS.md" docs/android-auto-hardening-verification.md && grep -qF "ACCESS_BACKGROUND_LOCATION" docs/android-auto-hardening-verification.md && echo "RUNBOOK_STRUCTURE_OK"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/android-auto-hardening-verification.md` esiste e contiene le quattro sezioni di primo
      livello elencate nel comando di verifica (`RUNBOOK_STRUCTURE_OK` stampato).
    - Esistono esattamente 3 sezioni `### Criteri di esito` e 3 sezioni `### Contingenza se FAIL`
      (una per SC1, SC2, SC3).
    - Ogni tabella "Criteri di esito" ha le tre righe `FAIL`, `PASS`, `INCONCLUSIVO` con una
      condizione misurabile, non un giudizio soggettivo; la riga `FAIL` compare per prima.
    - La sezione SC2 contiene sia `ACCESS_BACKGROUND_LOCATION` sia le parole "scartata" e "non va
      riaperta", e cita `D-03` e `D-04`.
    - La sezione SC3 cita `D-05` e nomina `SpeedScreen.kt` come punto di intervento SOLO in caso
      di FAIL osservato.
    - Il documento cita `scripts/aa-connect-cycle-check.ps1` con i suoi parametri reali e
      `.planning/research/PITFALLS.md` Pitfall 6.
    - La tabella `## Esiti registrati` esiste con intestazione `Criterio | Data | Esito | Note` e
      tre righe (SC1, SC2, SC3) con esito `da eseguire`.
    - Il documento non contiene caratteri accentati nelle formule tecniche (convenzione ASCII del
      progetto per i documenti tecnici: `perche'`, `e'`, `unita'`).
  </acceptance_criteria>
  <done>Il runbook copre i tre criteri con prerequisiti, procedura, osservazioni umane, criteri di esito misurabili e contingenze gia' decise; la tabella degli esiti e' pronta per essere compilata dai Piani 03 e 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispositivo sotto test -> filesystem del repository | Lo script cattura logcat, che puo' contenere valori di velocita', identificatori di dispositivo e nomi di package; il confine e' il punto in cui questi dati vengono scritti su disco. |
| operatore umano -> esito del gate | Il verdetto di un gate empirico e' un'affermazione di sicurezza/qualita': il confine e' il momento in cui un'osservazione ambigua viene registrata come PASS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Information Disclosure | catture logcat di `scripts/aa-connect-cycle-check.ps1` | mitigate | `$OutputDir` di default `build/aa-hardening`, sotto la regola `/build` di `.gitignore`; gate automatico `git check-ignore -q build` nel Task 1 (stessa disciplina di T-08-12 in Fase 8). |
| T-11-07 | Tampering | supply chain dello script di misura | mitigate | Lo script usa esclusivamente `adb` gia' presente sul PATH: nessun download, nessuna installazione. Gate automatico: `grep -nE "Invoke-WebRequest\|curl\|Install-\|choco\|winget\|sdkmanager"` deve non trovare nulla. |
| T-11-08 | Repudiation | esito del gate deciso a posteriori su un'osservazione ambigua | mitigate | Criteri `FAIL`/`PASS`/`INCONCLUSIVO` misurabili scritti nel runbook PRIMA della sessione, con `INCONCLUSIVO` come esito esplicito e legittimo; verdetto euristico dello script registrato su file insieme ai conteggi grezzi. |
| T-11-09 | Spoofing (falso PASS) | verifica SC1 eseguita su un build di debug | mitigate | La tabella "Quale build per quale criterio" e il criterio di esito SC1 rendono `Accepted - Validator disabled, all hosts allowed` un **FAIL esplicito** della verifica, non un PASS: e' il segnale che distingue oggettivamente i due binari. |
| T-11-10 | Elevation of Privilege | contingenza SC2 "risolta" aggiungendo `ACCESS_BACKGROUND_LOCATION` | mitigate | D-03/D-04 sono scritti nel runbook come decisione gia' presa e opzione esplicitamente chiusa, con la motivazione (minimizzazione permessi, T-02-EP; onere data-safety su Play Store), cosi' che l'esecutore di una sessione futura non possa riaprirla per comodita'. |
| T-11-SC | Tampering | installazioni npm/pip/cargo/Gradle | accept | Nessuna dipendenza aggiunta: questo piano produce solo un documento Markdown e uno script PowerShell che usa `adb`. |
</threat_model>

<verification>
1. `powershell -NoProfile -File scripts/aa-connect-cycle-check.ps1 -?` stampa l'help completo
   senza eseguire nulla.
2. Il parser PowerShell non riporta errori di sintassi sullo script.
3. `docs/android-auto-hardening-verification.md` contiene 3 tabelle di criteri di esito e 3
   sezioni di contingenza.
4. `git check-ignore -q build` esce con codice 0.
5. `git status --short` non mostra file sotto `build/` come non tracciati e non ignorati.
6. Nessun file sotto `app/src/` e' stato modificato da questo piano
   (`git status --short app/` vuoto).
</verification>

<success_criteria>
- Esiste un runbook riproducibile che chiunque puo' seguire per eseguire SC1, SC2 e SC3 senza
  dover reinterpretare le decisioni della fase.
- I criteri di esito sono misurabili e scritti prima della sessione; `INCONCLUSIVO` e' un esito
  legittimo e previsto.
- Le contingenze (D-03/D-04 per SC2, D-05 per SC3) sono documentate come decisioni gia' prese e
  le opzioni chiuse sono riaffermate come chiuse.
- Lo script di SC3 misura crash/ANR/rifiuti host senza installare nulla e senza scrivere dentro
  il repository.
</success_criteria>

<output>
Create `.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-02-SUMMARY.md` when done
</output>
