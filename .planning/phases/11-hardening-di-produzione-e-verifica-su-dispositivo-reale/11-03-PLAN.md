---
phase: 11-hardening-di-produzione-e-verifica-su-dispositivo-reale
plan: 03
type: execute
wave: 2
depends_on: ["11-01", "11-02"]
files_modified:
  - docs/android-auto-hardening-verification.md
  - playstore/README.md
autonomous: false
requirements: [AA-01, AA-02, AA-03, AA-04, CONN-01, CONN-02]

user_setup:
  - service: android-release-signing
    why: "SC1 e SC3 richiedono un APK di release FIRMATO: senza firma l'APK non e' installabile e il validator reale non puo' essere esercitato su un host vero. Il build di debug usa di proposito ALLOW_ALL_HOSTS_VALIDATOR (D-01), quindi non e' un sostituto valido."
    env_vars:
      - name: keystore.properties
        source: "File alla radice del repository, gia' gitignorato. Copiare keystore.properties.example e compilare storeFile / storePassword / keyAlias / keyPassword con il keystore di rilascio Play Store (lo stesso richiesto dai passi di pubblicazione ancora aperti in playstore/README.md)."
    dashboard_config:
      - task: "Installare il Desktop Head Unit (pacchetto extras/google/auto) oppure avere accesso a un head unit Android Auto reale, e attivare la Developer Mode dell'app Android Auto sul telefono"
        location: "Android Studio SDK Manager -> SDK Tools -> Android Auto Desktop Head Unit Emulator; app Android Auto sul telefono -> Info -> tocco ripetuto sulla versione"

must_haves:
  truths:
    - "Un host Android Auto reale si collega con successo a un build di RELEASE che usa l'allow-list, provato dal log 'Accepted - Host in allow-list'"
    - "La velocita' compare sullo schermo auto con il validator reale attivo, quindi SC1 non ha causato regressioni su AA-01..AA-03"
    - "Cicli rapidi di connessione/disconnessione non producono crash, ANR o rifiuti host, e lo schermo auto si riprende in modo coerente a ogni riconnessione"
    - "La nota di rischio interna su ALLOW_ALL_HOSTS_VALIDATOR in playstore/README.md non descrive piu' uno stato falso"
  artifacts:
    - path: "docs/android-auto-hardening-verification.md"
      provides: "Esiti datati di SC1 e SC3 nella tabella 'Esiti registrati'"
      contains: "SC1"
    - path: "playstore/README.md"
      provides: "Nota di rischio ritirata e sostituita dalla spiegazione di cosa e' cambiato"
  key_links:
    - from: "playstore/README.md"
      to: "app/src/main/java/com/sed/tachimetro/car/CarHostValidation.kt"
      via: "riferimento al validator reale che ha chiuso il rischio"
      pattern: "CarHostValidation|allow-list"
---

<objective>
Verificare empiricamente, con un host Android Auto reale e un build di RELEASE firmato, che
l'allow-list introdotta dal Piano 01 accetti gli host legittimi (SC1) e che cicli rapidi di
connessione/disconnessione non rompano l'app (SC3); poi registrare gli esiti e ritirare la nota
di rischio interna di `playstore/README.md`.

Purpose: SC1 e SC3 sono sessioni da scrivania con lo STESSO setup (release firmato + DHU/head
unit + adb disponibile), quindi vengono eseguiti insieme. SC3 su build di release e' piu'
informativo di SC3 su debug: esercita il binario che si spedisce e ri-valida l'host a ogni bind.
Output: tabella `## Esiti registrati` compilata per SC1 e SC3; `playstore/README.md` aggiornato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-CONTEXT.md
@docs/android-auto-hardening-verification.md
@scripts/aa-connect-cycle-check.ps1
@playstore/README.md

<interfaces>
<!-- Segnale oggettivo che distingue i due binari. Verificato nel sources-jar di
     androidx.car.app:app:1.7.0 (androidx/car/app/validation/HostValidator.java) e in
     androidx/car/app/utils/LogTags.java. -->
tag `CarApp.Val`:
  "Accepted - Host in allow-list"                     -> build RELEASE, allow-list attiva  => SC1 valido
  "Accepted - Validator disabled, all hosts allowed"  -> build DEBUG, validator permissivo => SC1 NON valido
  "Rejected - package name <pkg> not found"           -> host rifiutato                    => SC1 FAIL
I due Log.d richiedono `adb shell setprop log.tag.CarApp.Val DEBUG` PRIMA della sessione.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Checkpoint SC1 — collegamento di un host reale a un build di release con allow-list</name>
  <read_first>
    - docs/android-auto-hardening-verification.md sezione SC1 (prerequisiti, procedura, criteri
      di esito, contingenza) — il runbook e' la fonte autorevole della procedura, questo task
      non la duplica ma la esegue
    - app/src/main/java/com/sed/tachimetro/car/CarHostValidation.kt (creato dal Piano 01)
    - app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt
  </read_first>
  <what-built>
    Piano 01 ha sostituito `HostValidator.ALLOW_ALL_HOSTS_VALIDATOR` con
    `HostValidator.Builder(context).addAllowedHosts(androidx.car.app.R.array.hosts_allowlist_sample).build()`
    nei soli build di release (D-01/D-02), estratto nel seam `createCarHostValidator(context, allowAllHosts)`
    e lockato da `CarHostValidationTest` (6 test strumentati verdi). Il test dimostra il CONTENUTO
    dell'allow-list ma non puo' dimostrare che un host Android Auto reale la superi: nessun test
    automatico puo' produrre un bind da `com.google.android.projection.gearhead`.
  </what-built>
  <action>
    Prima di sospendere per il checkpoint, l'esecutore automatizza e riporta l'esito di ciascuno
    di questi comandi, nell'ordine: verifica della presenza di `keystore.properties` alla radice
    (FERMARSI e chiedere all'utente se assente, vedi `user_setup`);
    `./gradlew.bat :app:installRelease --console=plain`;
    `adb shell dumpsys package com.sed.tachimetro | findstr /C:"flags="` per confermare l'assenza
    di `DEBUGGABLE`; `adb shell setprop log.tag.CarApp.Val DEBUG`;
    `adb forward tcp:5277 tcp:5277`; avvio della cattura `adb logcat -v time -s CarApp.Val`
    con redirect su file sotto `build/aa-hardening/`.
    Solo dopo che tutti sono riusciti, sospendere e presentare all'utente i passi non
    automatizzabili elencati in `<how-to-verify>` (avvio del DHU/head unit, selezione dell'app,
    osservazione visiva). L'esecutore NON dichiara l'esito al posto dell'utente, non ripiega sul
    build di debug per far procedere la sessione, e non modifica alcun file sorgente in questo task.
  </action>
  <verify>
    <automated>test -f keystore.properties && ./gradlew.bat :app:installRelease --console=plain && ! adb shell dumpsys package com.sed.tachimetro | grep -q DEBUGGABLE && echo "RELEASE_BUILD_INSTALLED_OK"</automated>
    <human-check>Riga `Accepted - Host in allow-list` presente e zero `Rejected -` nel logcat catturato, piu' conferma visiva dei quattro punti del passo 9.</human-check>
  </verify>
  <done>L'utente ha dichiarato l'esito di SC1 (PASS / FAIL / INCONCLUSIVO) allegando la riga di log `CarApp.Val` osservata.</done>
  <how-to-verify>
    Prima del checkpoint l'esecutore automatizza tutto cio' che e' automatizzabile e riporta
    l'esito di ogni comando:
    1. Verificare la presenza di `keystore.properties` alla radice. Se assente, FERMARSI e
       chiedere all'utente di crearlo da `keystore.properties.example` (vedi `user_setup`): senza
       firma non esiste una verifica SC1 valida, e un ripiego sul build di debug e' un FALSO PASS
       esplicitamente vietato dal criterio di esito del runbook.
    2. `./gradlew.bat :app:installRelease --console=plain`
    3. `adb shell dumpsys package com.sed.tachimetro | findstr /C:"flags="` e confermare che
       `DEBUGGABLE` NON compare: e' la prova che il binario installato e' quello di release.
    4. `adb shell setprop log.tag.CarApp.Val DEBUG`
    5. `adb forward tcp:5277 tcp:5277`
    6. Avviare la cattura: `adb logcat -v time -s CarApp.Val`

    Poi l'utente esegue la parte non automatizzabile:
    7. Avviare `desktop-head-unit.exe` (`<sdk>\extras\google\auto\`) oppure collegare il telefono
       a un head unit Android Auto reale.
    8. Selezionare **Tachimetro** nella lista app dell'head unit.
    9. Confermare a occhio, sullo schermo auto: l'app si apre; mostra le cifre della velocita' (o
       "Ricerca segnale..." se il GPS non ha ancora il fix); NON torna da sola alla lista app; non
       compare alcun messaggio di errore dell'host.
    10. Nella cattura logcat deve comparire `Accepted - Host in allow-list`.

    Esito atteso (criteri del runbook, sezione SC1):
    - PASS: `Accepted - Host in allow-list` presente E l'app si apre e mostra la velocita'.
    - FAIL: compare `Rejected -`, oppure l'app non si apre, oppure compare
      `Accepted - Validator disabled, all hosts allowed` (si sta testando un debug: verifica non
      valida).
    - INCONCLUSIVO: nessuna riga `CarApp.Val` emessa (setprop dimenticato o host che non ha
      ancora tentato il bind).

    Se FAIL: NON reintrodurre `ALLOW_ALL_HOSTS_VALIDATOR` per far passare la sessione. Riportare
    la riga di log esatta e fermarsi.
  </how-to-verify>
  <acceptance_criteria>
    - `keystore.properties` esiste e `:app:installRelease` esce con codice 0.
    - `adb shell dumpsys package com.sed.tachimetro` mostra un blocco `flags=` privo di
      `DEBUGGABLE`.
    - La cattura logcat contiene almeno una riga `CarApp.Val` con `Accepted - Host in allow-list`
      e zero righe con `Rejected -`.
    - L'utente conferma esplicitamente i quattro punti di osservazione visiva del passo 9.
    - L'esito dichiarato dall'utente e' uno fra PASS / FAIL / INCONCLUSIVO, mai un giudizio
      libero.
  </acceptance_criteria>
  <resume-signal>Rispondere con l'esito (`PASS`, `FAIL` o `INCONCLUSIVO`), incollando la riga di log `CarApp.Val` osservata e descrivendo eventuali anomalie sullo schermo auto.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint SC3 — cicli rapidi di connessione/disconnessione Android Auto</name>
  <read_first>
    - docs/android-auto-hardening-verification.md sezione SC3
    - scripts/aa-connect-cycle-check.ps1 (parametri, verdetti, percorso di output)
    - .planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-CONTEXT.md D-05
  </read_first>
  <what-built>
    Nessun hardening preventivo e' stato scritto: D-05 impone di testare prima e patchare SOLO se
    il test rivela un crash o uno stato incoerente reale. Questo checkpoint E' il test.
    `scripts/aa-connect-cycle-check.ps1` (Piano 02) automatizza la misura: abilita i log di
    validazione host, cattura logcat (`CarApp.Val:D *:W`), campiona il PID a ogni ciclo e conta
    `FATAL EXCEPTION`, `ANR in com.sed.tachimetro` e `Rejected -`.
  </what-built>
  <action>
    L'esecutore riutilizza il build di release gia' installato dal Task 1 (stessa sessione, stesso
    setup) e avvia `powershell -File scripts/aa-connect-cycle-check.ps1 -Cycles 10`, che si ferma
    da solo a ogni ciclo in attesa di INVIO. Sospendere e presentare all'utente i passi non
    automatizzabili elencati in `<how-to-verify>` (collegare/scollegare rapidamente per 10 cicli,
    osservazione visiva dello schermo auto). Al termine l'esecutore legge il riepilogo prodotto
    sotto `build/aa-hardening/` e lo riporta integralmente.
    Nessun codice va modificato in questo task: D-05 vieta l'hardening preventivo, e un eventuale
    fix va pianificato come lavoro a se' con l'utente, mai improvvisato durante la sessione.
  </action>
  <verify>
    <automated>ls build/aa-hardening/*-summary.txt >/dev/null 2>&1 && grep -qE "PASS|FAIL|INCONCLUSIVO" $(ls -t build/aa-hardening/*-summary.txt | head -1) && echo "SC3_SUMMARY_PRESENT_OK"</automated>
    <human-check>A ogni riconnessione lo schermo auto torna a mostrare la velocita' o "Ricerca segnale...", mai una schermata vuota, un valore congelato o un errore dell'host; nessun dialog di crash sul telefono.</human-check>
  </verify>
  <done>L'utente ha dichiarato l'esito di SC3 (PASS / FAIL / INCONCLUSIVO) e il verdetto dello script con i cinque conteggi e' registrato.</done>
  <how-to-verify>
    Riutilizzare il build di release gia' installato dal Task 1 (stesso setup, stessa sessione).
    1. Eseguire `powershell -File scripts/aa-connect-cycle-check.ps1 -Cycles 10`.
    2. Per ciascuno dei 10 cicli, quando lo script lo chiede: collegare Android Auto (avviare il
       DHU o collegare il cavo all'head unit), attendere che Tachimetro sia visibile sullo schermo
       auto, poi scollegare SUBITO, senza pause deliberate — l'obiettivo e' la successione rapida,
       non una sessione stabile.
    3. Osservare a occhio, a ogni riconnessione: lo schermo auto torna a mostrare la velocita' o
       "Ricerca segnale..."; mai una schermata vuota; mai un valore vecchio congelato; nessun
       messaggio di errore dell'host; sul telefono nessun dialog "L'app si e' interrotta".
    4. A fine sessione leggere il riepilogo prodotto dallo script in `build/aa-hardening/`.

    Esito atteso:
    - PASS: verdetto `PASS` dello script (zero `FATAL EXCEPTION`, zero `ANR`, zero `Rejected -`,
      10 cicli completati) E conferma visiva dei punti del passo 3. L'uno non sostituisce l'altro.
    - FAIL: verdetto `FAIL` dello script oppure uno stato incoerente osservato a occhio.
    - INCONCLUSIVO: sessione interrotta prima dei 10 cicli, o log illeggibile.

    Se FAIL (D-05): NON improvvisare un fix durante la sessione. Riportare all'utente il verdetto,
    lo stack trace o la descrizione dello stato incoerente, e il ciclo in cui si e' manifestato; il
    fix (il cui punto di intervento naturale e' la guardia in memoria di
    `app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt:61-67`) va pianificato come lavoro a
    se' con l'utente.

    Nota: un cambio di PID fra un ciclo e l'altro NON e' di per se' un FAIL — alla disconnessione
    il sistema puo' legittimamente terminare il processo se l'app non e' in primo piano sul
    telefono. Il segnale di crash e' `FATAL EXCEPTION`/`ANR`.
  </how-to-verify>
  <acceptance_criteria>
    - `scripts/aa-connect-cycle-check.ps1 -Cycles 10` e' stato eseguito fino in fondo e ha
      prodotto un file di riepilogo sotto `build/aa-hardening/`.
    - Il riepilogo riporta 10 cicli completati e i cinque conteggi di log.
    - Il verdetto euristico dello script e' registrato testualmente.
    - L'utente conferma esplicitamente i quattro punti di osservazione visiva del passo 3.
    - L'esito dichiarato e' uno fra PASS / FAIL / INCONCLUSIVO.
  </acceptance_criteria>
  <resume-signal>Rispondere con l'esito (`PASS`, `FAIL` o `INCONCLUSIVO`), il verdetto stampato dallo script e la descrizione di qualunque comportamento anomalo osservato sullo schermo auto.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Registrare gli esiti SC1/SC3 e ritirare la nota di rischio in playstore/README.md</name>
  <files>docs/android-auto-hardening-verification.md, playstore/README.md</files>
  <read_first>
    - docs/android-auto-hardening-verification.md (tabella `## Esiti registrati` da compilare)
    - playstore/README.md righe 14-45 (la sezione "## Rischio noto accettato per questo rilascio
      (nota interna)" da ritirare, e alle righe 16-20 il precedente in-repo di come si ritira una
      nota superata: la si sostituisce con la spiegazione di cosa e' cambiato, non la si cancella)
    - app/src/main/java/com/sed/tachimetro/car/CarHostValidation.kt (per citare correttamente cosa
      ha chiuso il rischio)
  </read_first>
  <action>
    1. In `docs/android-auto-hardening-verification.md`, compilare le righe SC1 e SC3 della tabella
       `## Esiti registrati` con: data della sessione (formato `AAAA-MM-GG`), esito dichiarato
       dall'utente ai Task 1 e 2, e nelle Note il segnale oggettivo osservato — per SC1 la riga
       `CarApp.Val` (`Accepted - Host in allow-list`), per SC3 il verdetto dello script e i
       conteggi. Non toccare la riga SC2 (resta `da eseguire`, e' del Piano 04).

    2. In `playstore/README.md` sostituire l'intera sezione
       `## Rischio noto accettato per questo rilascio (nota interna)` (attualmente righe 24-40).
       - Se SC1 e' PASS: intitolare la sezione sostitutiva `## Rischio host validation: chiuso in
         Fase 11 (nota interna)` e scrivere che `TachimetroCarAppService.createHostValidator()`
         non restituisce piu' `HostValidator.ALLOW_ALL_HOSTS_VALIDATOR` nei build di release ma
         un validator con allow-list reale (`CarHostValidation.kt`, allow-list ufficiale
         `androidx.car.app` limitata a `com.google.android.projection.gearhead` e
         `com.google.android.apps.automotive.templates.host`); che i build di DEBUG restano
         permissivi di proposito (D-01) e non vengono distribuiti; e che la chiusura e' stata
         verificata con host reale il <data> (rimandare a
         `docs/android-auto-hardening-verification.md`). Mantenere l'ultimo capoverso esistente
         (quello che vieta di copiare la nota in `listing/`, `release_notes/`, `data_safety.md`,
         `content_rating.md`, `privacy_policy.html`) adattandolo al nuovo testo: questa resta una
         nota interna, non una voce di sicurezza dei dati, e non va copiata altrove.
       - Se SC1 NON e' PASS: NON ritirare la nota. Aggiornarla invece indicando che la Fase 11 e'
         in corso, quale esito ha prodotto la sessione e che il rischio resta aperto. Non
         dichiarare chiuso cio' che non lo e'.

    3. Se e solo se SC1 e' PASS, verificare che nessun altro file di `playstore/` affermi il
       contrario (`grep -rn "ALLOW_ALL" playstore/`); l'unica occorrenza attesa dopo la modifica e'
       quella nella nota storica del README.

    Non modificare `listing/`, `release_notes/`, `data_safety.md`, `content_rating.md` ne'
    `privacy_policy.html`: il cambiamento e' interno e non ha significato per l'utente finale.
  </action>
  <verify>
    <automated>grep -qF "Rischio host validation" playstore/README.md && ! grep -qF "restituisce ancora" playstore/README.md && test "$(grep -c 'da eseguire' docs/android-auto-hardening-verification.md)" = "1" && git status --short playstore/listing playstore/release_notes 2>/dev/null | wc -l | grep -qx 0 && echo "OUTCOME_RECORDED_OK"</automated>
  </verify>
  <acceptance_criteria>
    - La tabella `## Esiti registrati` del runbook ha le righe SC1 e SC3 compilate con data, esito
      e segnale oggettivo; la riga SC2 riporta ancora `da eseguire` (unica occorrenza rimasta di
      quella stringa nel file).
    - `playstore/README.md` non contiene piu' la frase `restituisce ancora` (il testo che
      affermava lo stato superato).
    - Se SC1 = PASS: `playstore/README.md` contiene la sezione `## Rischio host validation: chiuso
      in Fase 11 (nota interna)`, cita `CarHostValidation.kt` e rimanda a
      `docs/android-auto-hardening-verification.md`, e conserva il divieto di copiare la nota negli
      altri file di `playstore/`.
    - Se SC1 != PASS: la sezione resta una nota di rischio APERTO, con l'esito della sessione e la
      data; il gate `grep -qF "Rischio host validation"` va comunque soddisfatto usando quel
      titolo con la parola "aperto" al posto di "chiuso" — in quel caso l'esecutore adatta il
      comando di verifica e lo documenta nel SUMMARY.
    - `git status --short playstore/listing playstore/release_notes` e' vuoto: nessun testo
      rivolto all'utente finale e' stato toccato.
  </acceptance_criteria>
  <done>Gli esiti SC1 e SC3 sono registrati con il loro segnale oggettivo e la nota interna di playstore/README.md riflette lo stato reale del codice.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host Android Auto reale -> `TachimetroCarAppService` di un APK di release | E' il confine che il Piano 01 ha chiuso in teoria; questo piano lo attraversa davvero per la prima volta. |
| credenziali di firma (`keystore.properties`, `.jks`) -> filesystem/repository | Il build di release richiede il keystore di rilascio: il confine e' il punto in cui quelle credenziali potrebbero entrare in git. |
| esito della sessione -> documentazione di rilascio (`playstore/README.md`) | Dichiarare chiuso un rischio non chiuso e' esso stesso un rischio di sicurezza documentale. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Spoofing (falso PASS) | verifica SC1 eseguita su un binario di debug | mitigate | Doppio controllo oggettivo prima della conferma umana: `adb shell dumpsys package com.sed.tachimetro` deve mostrare `flags=` senza `DEBUGGABLE`, e il log `CarApp.Val` deve riportare `Accepted - Host in allow-list` e non `Accepted - Validator disabled`. Quest'ultima riga e' definita come FAIL, non come PASS. |
| T-11-12 | Denial of Service (regressione SC1) | allow-list che rifiuta l'host legittimo | mitigate | `Rejected -` su `CarApp.Val` e' criterio di FAIL immediato; la contingenza vieta esplicitamente di reintrodurre `ALLOW_ALL_HOSTS_VALIDATOR` per far passare la sessione e rimanda alla correzione dell'allow-list + rilancio di `CarHostValidationTest`. |
| T-11-13 | Information Disclosure | `keystore.properties` / file `.jks` committati per abilitare `installRelease` | mitigate | Entrambi sono gia' coperti da `.gitignore` (`keystore.properties`, `*.jks`, `*.keystore`). Il Task 1 non chiede mai di incollare password in chat o nel SUMMARY; il SUMMARY registra solo l'esito, mai il contenuto del file. |
| T-11-14 | Repudiation | nota di rischio ritirata senza che SC1 sia passato | mitigate | Il Task 3 ramifica esplicitamente su PASS / non-PASS e vieta di dichiarare chiuso cio' che non lo e'; il gate automatico verifica che la frase superata `restituisce ancora` sia sparita ma non impone la parola "chiuso". |
| T-11-15 | Tampering | cicli rapidi che lasciano lo schermo auto in uno stato incoerente (SC3) | mitigate | Misura oggettiva (`FATAL EXCEPTION` / `ANR` / `Rejected -`) piu' conferma visiva; nessun hardening preventivo (D-05), fix pianificato come lavoro a se' se e solo se osservato. |
| T-11-SC | Tampering | installazioni npm/pip/cargo/Gradle | accept | Nessuna nuova dipendenza: questo piano esegue build e comandi adb e modifica due file Markdown. |
</threat_model>

<verification>
1. `ls keystore.properties` esiste (fornito dall'utente) e `git check-ignore -q keystore.properties`
   esce con codice 0.
2. `./gradlew.bat :app:installRelease` esce con codice 0.
3. `adb shell dumpsys package com.sed.tachimetro | findstr /C:"flags="` non contiene `DEBUGGABLE`.
4. Il log `CarApp.Val` catturato contiene `Accepted - Host in allow-list` e zero `Rejected -`.
5. `build/aa-hardening/` contiene un riepilogo con 10 cicli e il verdetto dello script.
6. `docs/android-auto-hardening-verification.md` ha SC1 e SC3 compilati nella tabella degli esiti.
7. `git status --short playstore/` mostra solo `README.md` modificato.
</verification>

<success_criteria>
- SC1 della ROADMAP e' chiuso empiricamente: un host Android Auto reale si collega a un build di
  release con allow-list e la velocita' compare sullo schermo auto.
- SC3 della ROADMAP e' chiuso empiricamente: 10 cicli rapidi di connessione/disconnessione senza
  crash, ANR, rifiuti host ne' stato incoerente osservato.
- Gli esiti sono registrati con il segnale oggettivo che li supporta, non con un giudizio.
- `playstore/README.md` descrive lo stato reale del codice.
</success_criteria>

<output>
Create `.planning/phases/11-hardening-di-produzione-e-verifica-su-dispositivo-reale/11-03-SUMMARY.md` when done
</output>
