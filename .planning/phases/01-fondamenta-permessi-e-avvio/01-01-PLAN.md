---
phase: 01-fondamenta-permessi-e-avvio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
autonomous: true
requirements: [APP-01]
tags: [android, gradle, kotlin, build-config]

must_haves:
  truths:
    - "Il progetto compila con il plugin Kotlin Android applicato (nessun file .kt ancora, ma il build passa)"
    - "Il plugin Kotlin e la libreria ConstraintLayout sono dichiarati nella version catalog e referenziati via alias libs.*"
    - "Il target bytecode Kotlin e allineato a Java 11 (coerente con le compileOptions esistenti)"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "Versione + alias plugin kotlin-android e libreria constraintlayout"
      contains: "kotlin-android"
    - path: "app/build.gradle.kts"
      provides: "Applicazione plugin kotlin.android, jvmTarget 11, dependency constraintlayout"
      contains: "libs.plugins.kotlin.android"
  key_links:
    - from: "app/build.gradle.kts"
      to: "gradle/libs.versions.toml"
      via: "alias reference libs.plugins.kotlin.android e libs.constraintlayout"
      pattern: "libs\\.plugins\\.kotlin\\.android"
---

<objective>
Abilitare Kotlin nel progetto (attualmente scaffold Java senza plugin Kotlin) e aggiungere la dipendenza di layout ConstraintLayout, in modo che i file `.kt` e il layout XML della Fase 1 (Plan 02) possano compilare.

Purpose: Nessun file `.kt` puo esistere finche il plugin `org.jetbrains.kotlin.android` non e applicato (vincolo confermato in CONVENTIONS.md e ARCHITECTURE.md). Questa e la fondamenta build su cui poggia tutto il resto della fase e delle fasi successive (decisione bloccata PROJECT.md: Kotlin + layout XML, no Compose).
Output: version catalog e `app/build.gradle.kts` aggiornati; il progetto compila con Kotlin abilitato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-fondamenta-permessi-e-avvio/01-CONTEXT.md
@.planning/phases/01-fondamenta-permessi-e-avvio/01-PATTERNS.md

<decisions_locked>
- Layout library: **ConstraintLayout** (scelta del planner, nessun precedente nel repo). Motivazione: e lo standard Android moderno, la UI a schermo intero della Fase 3 (numero centrato e massimizzato in ogni orientamento) lo user, e il layout di riferimento in PATTERNS.md lo usa gia. La dipendenza va aggiunta ora perche il layout placeholder del Plan 02 la richiede.
- Kotlin: plugin `org.jetbrains.kotlin.android`, gestito via version catalog (pattern "version-catalog-first" da PATTERNS.md e CONVENTIONS.md).
</decisions_locked>

<interfaces>
Stato attuale `gradle/libs.versions.toml` (da estendere, NON riscrivere):
```toml
[versions]
agp = "9.1.1"
junit = "4.13.2"
junitVersion = "1.1.5"
espressoCore = "3.5.1"
appcompat = "1.6.1"
material = "1.10.0"

[libraries]
appcompat = { group = "androidx.appcompat", name = "appcompat", version.ref = "appcompat" }
material = { group = "com.google.android.material", name = "material", version.ref = "material" }
# ... junit/ext-junit/espresso-core invariati ...

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
```

Stato attuale blocchi rilevanti di `app/build.gradle.kts`:
```kotlin
plugins {
    alias(libs.plugins.android.application)
}
// ...
    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
    }
// ...
dependencies {
    implementation(libs.appcompat)
    implementation(libs.material)
    // ...
}
```
Repository gia configurati in `settings.gradle.kts`: `google()`, `mavenCentral()`, `gradlePluginPortal()` (fonti attendibili di prima parte per Kotlin/AndroidX).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dichiarare Kotlin e ConstraintLayout nella version catalog</name>
  <read_first>
    - gradle/libs.versions.toml (stato corrente da estendere)
    - .planning/phases/01-fondamenta-permessi-e-avvio/01-PATTERNS.md (sezione "gradle/libs.versions.toml" e "Version-catalog-first dependency/plugin management")
  </read_first>
  <files>gradle/libs.versions.toml</files>
  <action>
    Estendere (non riscrivere) `gradle/libs.versions.toml` seguendo la struttura esatta gia usata per `agp`/`android-application`:
    - In `[versions]` aggiungere una chiave `kotlin` e una chiave `constraintlayout`. Per `kotlin` usare l'ultima versione stabile della serie 2.x compatibile con AGP 9.1.1: verificare contro la tabella di compatibilita AGP-Kotlin (via Context7 su "android gradle plugin kotlin compatibility" oppure developer.android.com/build/kotlin-support) prima di fissarla; non scendere sotto `2.0.21`. Per `constraintlayout` usare l'ultima stabile della serie 2.x (es. `2.2.x`), da confermare su maven.
    - In `[libraries]` aggiungere `constraintlayout = { group = "androidx.constraintlayout", name = "constraintlayout", version.ref = "constraintlayout" }` (stesso pattern group/name/version.ref delle voci esistenti).
    - In `[plugins]` aggiungere `kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }` (stesso pattern di `android-application`).
    L'alias plugin risultante referenziabile e `libs.plugins.kotlin.android`; l'alias libreria e `libs.constraintlayout`.
  </action>
  <verify>
    <automated>cd "C:/Users/fedes/AndroidStudioProjects/Tachimetro" && grep -q 'kotlin-android' gradle/libs.versions.toml && grep -q 'org.jetbrains.kotlin.android' gradle/libs.versions.toml && grep -q 'androidx.constraintlayout' gradle/libs.versions.toml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` contiene una voce `[versions] kotlin = "..."` con valore >= 2.0.21
    - `[plugins]` contiene `kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }`
    - `[libraries]` contiene `constraintlayout` con group `androidx.constraintlayout` e name `constraintlayout`
    - Le voci preesistenti (agp, appcompat, material, junit, ext-junit, espresso-core, android-application) restano invariate
  </acceptance_criteria>
  <done>Version catalog dichiara plugin Kotlin e libreria ConstraintLayout con alias corretti</done>
</task>

<task type="auto">
  <name>Task 2: Applicare il plugin Kotlin e allineare jvmTarget in app/build.gradle.kts</name>
  <read_first>
    - app/build.gradle.kts (stato corrente)
    - gradle/libs.versions.toml (alias creati nel Task 1)
    - .planning/phases/01-fondamenta-permessi-e-avvio/01-PATTERNS.md (nota su compileOptions/kotlinOptions e "No new dependency required for permission handling")
  </read_first>
  <files>app/build.gradle.kts</files>
  <action>
    Modificare `app/build.gradle.kts`:
    - Nel blocco `plugins { }` aggiungere `alias(libs.plugins.kotlin.android)` sotto la riga esistente `alias(libs.plugins.android.application)` (non rimuovere quella esistente).
    - Allineare il target bytecode Kotlin a Java 11 per coerenza con le `compileOptions` gia presenti (`sourceCompatibility`/`targetCompatibility = JavaVersion.VERSION_11`, righe 32-35). Con Kotlin 2.x preferire il DSL `compilerOptions`: aggiungere un blocco `kotlin { compilerOptions { jvmTarget = org.jetbrains.kotlin.gradle.dsl.JvmTarget.JVM_11 } }` a livello di modulo (fuori dal blocco `android { }`). Se la versione del plugin Kotlin applicata non espone quel DSL, usare in alternativa `kotlinOptions { jvmTarget = "11" }` dentro il blocco `android { }`. Il valore deve essere "11", mai un default diverso.
    - Nel blocco `dependencies { }` aggiungere `implementation(libs.constraintlayout)` accanto a `implementation(libs.appcompat)` / `implementation(libs.material)` (stesso pattern `implementation(libs.<alias>)`).
    - NON aggiungere `activity-ktx`, `core-ktx` o altre dipendenze: `AppCompatActivity` (via `libs.appcompat`) fornisce gia `registerForActivityResult` per il Plan 02 (confermato in PATTERNS.md).
  </action>
  <verify>
    <automated>cd "C:/Users/fedes/AndroidStudioProjects/Tachimetro" && grep -q 'libs.plugins.kotlin.android' app/build.gradle.kts && grep -q 'libs.constraintlayout' app/build.gradle.kts && grep -Eq 'JVM_11|jvmTarget = "11"' app/build.gradle.kts && ./gradlew.bat assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - Il blocco `plugins` contiene sia `alias(libs.plugins.android.application)` sia `alias(libs.plugins.kotlin.android)`
    - Il jvmTarget Kotlin e impostato a 11 (JvmTarget.JVM_11 oppure "11")
    - `dependencies` contiene `implementation(libs.constraintlayout)`
    - `./gradlew.bat assembleDebug` termina con BUILD SUCCESSFUL
    - Nessuna dipendenza extra (activity-ktx/core-ktx) aggiunta
  </acceptance_criteria>
  <done>Il progetto compila con Kotlin abilitato e ConstraintLayout disponibile come dipendenza</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build system to artifact repositories | Gradle risolve plugin/dipendenze da `google()`/`mavenCentral()`/`gradlePluginPortal()` (dichiarati in settings.gradle.kts) — input di build proveniente dall'esterno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Plugin/dipendenze Gradle (kotlin-android, constraintlayout) | mitigate | Usare solo artefatti di prima parte con coordinate esatte e note: `org.jetbrains.kotlin.android` (JetBrains) e `androidx.constraintlayout:constraintlayout` (Google), dichiarati via version catalog e risolti dai repository attendibili gia pinnati in settings.gradle.kts. Nessun repository di terze parti aggiunto. |
| T-01-SC | Tampering | Installazioni package manager | accept | I package Gradle/Maven sono fuori dallo scope del gate di legittimita npm/pip/cargo; coordinate note e di prima parte, rischio basso. Nessun checkpoint umano bloccante richiesto. |
</threat_model>

<verification>
- `./gradlew.bat assembleDebug` termina con BUILD SUCCESSFUL con Kotlin plugin applicato
- Gli alias `libs.plugins.kotlin.android` e `libs.constraintlayout` risolvono correttamente (nessun errore "unresolved reference" in fase di configurazione Gradle)
- Nessuna modifica a namespace, minSdk (30), targetSdk (36), compileOptions Java 11
</verification>

<success_criteria>
- Il plugin Kotlin Android e applicato e il progetto compila
- ConstraintLayout e disponibile come dipendenza per il layout del Plan 02
- jvmTarget Kotlin allineato a Java 11
</success_criteria>

<output>
Create `.planning/phases/01-fondamenta-permessi-e-avvio/01-01-SUMMARY.md` when done
</output>
