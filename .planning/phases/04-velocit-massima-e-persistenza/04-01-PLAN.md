---
phase: 04-velocit-massima-e-persistenza
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/strings.xml
  - app/src/main/res/layout/activity_main.xml
  - app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt
  - app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt
  - app/src/test/java/com/sed/tachimetro/maxspeed/MaxSpeedReducerTest.kt
  - app/src/main/java/com/sed/tachimetro/MainActivity.kt
autonomous: true
requirements: [MAX-01, MAX-02, MAX-03]

must_haves:
  truths:
    - "L'utente vede la velocità massima raggiunta dall'ultimo azzeramento in un'area secondaria in alto a sinistra, nel formato 'MAX 120' (etichetta + numero, D-01/D-02)"
    - "Il massimo si aggiorna in memoria e su disco immediatamente ogni volta che una lettura corrente supera il massimo corrente (D-07)"
    - "Il pulsante 'Azzera massimo' azzera il valore al tap senza dialog di conferma e scrive 0 su disco immediatamente (D-04/D-08)"
    - "Etichetta MAX e pulsante Azzera sono nascosti (GONE) quando il massimo è 0 — nessun 'MAX 0' mostrato all'avvio o dopo l'azzeramento (D-03/D-09)"
    - "All'avvio (onCreate) il valore massimo salvato viene letto da SharedPreferences prima dell'inizio della raccolta GPS, così l'area MAX appare senza flash di 'MAX 0' (D-09)"
    - "L'area MAX rispetta gli insets di status bar / display cutout sul lato top+left in entrambi gli orientamenti, come unitText sul lato top+right"
    - "Il pulsante 'Azzera massimo' e retryButton sono mutuamente esclusivi: l'area MAX è nascosta negli stati di permesso negato"
    - "Tutte le stringhe sono in italiano e passano da getString() (nessuna stringa hardcoded, UI-05)"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "Stringhe italiane max_speed_format ('MAX %1$d') e reset_max_button ('Azzera massimo')"
      contains: "max_speed_format"
    - path: "app/src/main/res/layout/activity_main.xml"
      provides: "TextView maxSpeedText (top-left, 22sp, GONE) + Button resetMaxButton (sotto, 48dp min, GONE)"
      contains: "@+id/maxSpeedText"
    - path: "app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt"
      provides: "Funzioni pure reduceMax(currentMax, reading) e sanitizePersistedMax(raw)"
      contains: "fun reduceMax"
    - path: "app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt"
      provides: "Wrapper SharedPreferences read()/write() per un singolo Int (D-06)"
      contains: "getSharedPreferences"
    - path: "app/src/test/java/com/sed/tachimetro/maxspeed/MaxSpeedReducerTest.kt"
      provides: "Test JVM che bloccano D-07 (crescita monotona) e la sanitizzazione del valore letto"
      contains: "reduceMax"
    - path: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      provides: "Wiring: lettura all'avvio, aggiornamento in updatePlaceholder, handler reset, insets, visibilità"
      contains: "MaxSpeedStore"
  key_links:
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt updatePlaceholder(Reading)"
      to: "reduceMax + MaxSpeedStore.write"
      via: "confronto state.kmh col massimo corrente e persistenza immediata (D-07)"
      pattern: "reduceMax\\("
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt resetMaxButton"
      to: "MaxSpeedStore.write(0) + updateMaxArea()"
      via: "setOnClickListener onResetMaxClicked (D-04/D-08)"
      pattern: "setOnClickListener"
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt onCreate"
      to: "MaxSpeedStore.read()"
      via: "lettura del massimo salvato prima della raccolta GPS (D-09)"
      pattern: "maxSpeedStore\\.read\\(\\)"
---

<objective>
Aggiungere il monitoraggio della velocità massima con persistenza su disco. Un nuovo elemento `maxSpeedText` in alto a sinistra (speculare a `unitText` in alto a destra) mostra "MAX <n>"; un `resetMaxButton` sotto di esso azzera il record al tap. Il massimo viene letto da SharedPreferences all'avvio, aggiornato e riscritto immediatamente ogni volta che la velocità corrente lo supera, e riscritto a 0 al tap del reset. L'intera area MAX resta nascosta finché il massimo è 0 (nessun "MAX 0").

Purpose: Soddisfa MAX-01 (area secondaria con il massimo), MAX-02 (pulsante di azzeramento) e MAX-03 (persistenza che sopravvive a chiusura app e riavvio telefono), rispettando le decisioni D-01..D-09 del CONTEXT e il contratto visivo 04-UI-SPEC.
Output: strings.xml + activity_main.xml aggiornati, nuovi file MaxSpeedReducer.kt/MaxSpeedStore.kt + test JVM, MainActivity.kt cablato. Verifica su device nel piano 04-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-velocit-massima-e-persistenza/04-CONTEXT.md
@.planning/phases/04-velocit-massima-e-persistenza/04-UI-SPEC.md

<interfaces>
<!-- Contratti che l'executor deve usare direttamente — nessuna esplorazione aggiuntiva del codice necessaria. -->

From app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt (read-only):
  sealed class SpeedState
    data object Searching : SpeedState()
    data class Reading(val kmh: Int) : SpeedState()   // <-- fonte del valore corrente da confrontare col massimo
    data object NoSignal : SpeedState()

From app/src/main/java/com/sed/tachimetro/MainActivity.kt (punti di integrazione esistenti):
  - onCreate(): setContentView(R.layout.activity_main); findViewById per messageText/unitText/retryButton;
    retryButton.setOnClickListener { onRetryClicked() }; applyUnitTextWindowInsets();
    poi crea gpsSpeedProvider e avvia la raccolta STARTED-scoped; infine checkAndRequestPermission().
  - updatePlaceholder(state: SpeedState): ramo `is SpeedState.Reading` imposta messageText = state.kmh.toString()
    (è QUI che va confrontato state.kmh col massimo).
  - showDenied(): mostra retryButton (stato permesso negato). retryButton e resetMaxButton NON compaiono mai insieme.
  - showReady(): stato "Pronto" (permesso concesso, prima del GPS).
  - applyUnitTextWindowInsets(): pattern di riferimento per gli insets, da specchiare sul lato start/left.

Nuova API interna esposta da questo piano (Task 2), consumata da MainActivity (Task 3):
  fun reduceMax(currentMax: Int, reading: Int): Int          // in com.sed.tachimetro.maxspeed
  fun sanitizePersistedMax(raw: Int): Int                    // in com.sed.tachimetro.maxspeed
  class MaxSpeedStore(context: Context) { fun read(): Int; fun write(value: Int) }  // com.sed.tachimetro.maxspeed
</interfaces>

<constraints>
- minSdk = 30 confermato in app/build.gradle.kts. SharedPreferences, ConstraintLayout, TextView, Button sono tutte API/widget già disponibili — NESSUNA nuova dipendenza Gradle (CONTEXT Established Patterns).
- AGP 9.1.1 con supporto Kotlin integrato: NON aggiungere il plugin classico `org.jetbrains.kotlin.android` (incompatibilità documentata).
- Root ConstraintLayout dichiara già `xmlns:app` e `xmlns:tools` — nessun namespace nuovo da aggiungere.
- Questa fase NON tocca lo screen-always-on (Fase 5). NON aggiungere FLAG_KEEP_SCREEN_ON né toggle.
- Nessuna animazione/transizione sullo show/hide dell'area MAX (UI-04): solo View.GONE / View.VISIBLE.
- Passare `applicationContext` a MaxSpeedStore (pattern WR-04: mai un riferimento all'Activity in oggetti a vita lunga).
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere stringhe italiane e i due elementi di layout (maxSpeedText + resetMaxButton)</name>
  <files>app/src/main/res/values/strings.xml, app/src/main/res/layout/activity_main.xml</files>
  <read_first>
    - app/src/main/res/values/strings.xml (stato attuale — aggiungere in coda, non riscrivere)
    - app/src/main/res/layout/activity_main.xml (stato attuale — evolvere in place; osservare come è definito unitText da specchiare)
    - .planning/phases/04-velocit-massima-e-persistenza/04-UI-SPEC.md (Layout Contract, Spacing Scale, Typography, Copywriting Contract)
  </read_first>
  <action>
    (A) In `app/src/main/res/values/strings.xml`, aggiungere ESATTAMENTE queste due stringhe (italiano, UI-05), prima del tag di chiusura `</resources>`:
      <string name="max_speed_format">MAX %1$d</string>
      <string name="reset_max_button">Azzera massimo</string>
    NON modificare o rinominare le stringhe esistenti (status_ready, unit_kmh, ecc.).

    (B) In `app/src/main/res/layout/activity_main.xml`, aggiungere DUE nuovi figli al ConstraintLayout root, DOPO `unitText` e PRIMA o DOPO `retryButton` (l'ordine tra fratelli non conta per ConstraintLayout). NON modificare messageText, unitText né retryButton (nessun cambio a id, vincoli, margini, autosize, visibilità di quegli elementi esistenti).

    Aggiungere maxSpeedText (specchio di unitText sul lato start, D-01; 22sp fisso Regular, singola riga, GONE di default — D-09):
      <TextView
          android:id="@+id/maxSpeedText"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_marginStart="16dp"
          android:layout_marginTop="16dp"
          android:maxLines="1"
          android:singleLine="true"
          android:textColor="@android:color/white"
          android:textSize="22sp"
          android:visibility="gone"
          app:layout_constraintStart_toStartOf="parent"
          app:layout_constraintTop_toTopOf="parent"
          tools:text="MAX 120"
          tools:visibility="visible" />

    Aggiungere resetMaxButton (sotto maxSpeedText, allineato a sinistra sotto l'etichetta che controlla — D-05; touch target 48dp; GONE di default — D-03/D-09; testo via string reset_max_button, NESSUN colore destructive — D-04):
      <Button
          android:id="@+id/resetMaxButton"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_marginStart="16dp"
          android:layout_marginTop="8dp"
          android:minHeight="48dp"
          android:text="@string/reset_max_button"
          android:visibility="gone"
          app:layout_constraintStart_toStartOf="parent"
          app:layout_constraintTop_toBottomOf="@id/maxSpeedText"
          tools:visibility="visible" />

    NOTE vincolanti da UI-SPEC (non ridiscutere): maxSpeedText NON usa autosize (elemento secondario a dimensione fissa 22sp); NON impostare `android:text` su maxSpeedText nel XML (verrà impostato a runtime via getString(R.string.max_speed_format, n)); il pulsante è allineato a start (NON centrato sull'intera larghezza come retryButton); nessuna icona sul pulsante (UI-04); nessun `android:textStyle`/`android:textFontWeight` su maxSpeedText (Regular/400 come unitText).
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:assembleDebug` termina con BUILD SUCCESSFUL.
    - `strings.xml` contiene `<string name="max_speed_format">MAX %1$d</string>` e `<string name="reset_max_button">Azzera massimo</string>`.
    - `activity_main.xml` contiene `@+id/maxSpeedText` con `android:textSize="22sp"`, `android:visibility="gone"`, `app:layout_constraintStart_toStartOf="parent"` e `app:layout_constraintTop_toTopOf="parent"`.
    - `activity_main.xml` contiene `@+id/resetMaxButton` con `android:minHeight="48dp"`, `android:text="@string/reset_max_button"`, `android:visibility="gone"`, `app:layout_constraintStart_toStartOf="parent"` e `app:layout_constraintTop_toBottomOf="@id/maxSpeedText"`.
    - `maxSpeedText` NON contiene `app:autoSizeTextType` né `android:text="..."` (testo impostato a runtime).
    - `maxSpeedText` NON contiene `android:textStyle` né `android:textFontWeight` (resta Regular).
    - Gli elementi esistenti `@+id/messageText`, `@+id/unitText`, `@+id/retryButton` sono ancora presenti e invariati.
  </acceptance_criteria>
  <done>Build debug verde; le due stringhe italiane esistono; maxSpeedText (top-left, 22sp, GONE) e resetMaxButton (sotto, 48dp min, GONE, testo "Azzera massimo") sono presenti nel layout con i vincoli speculari a unitText, senza toccare gli elementi esistenti.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Logica pura del massimo (reduceMax + sanitizePersistedMax) con test JVM, e store SharedPreferences</name>
  <files>app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt, app/src/test/java/com/sed/tachimetro/maxspeed/MaxSpeedReducerTest.kt, app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/gps/SpeedMapping.kt (pattern di funzione pura framework-free da imitare)
    - app/src/test/java/com/sed/tachimetro/gps/SpeedMappingTest.kt (pattern di test JUnit4 JVM da imitare: import org.junit, assertEquals)
    - .planning/phases/04-velocit-massima-e-persistenza/04-CONTEXT.md (D-06, D-07, D-08 sui tempi di persistenza)
  </read_first>
  <behavior>
    reduceMax(currentMax, reading):
    - reduceMax(0, 50) == 50        (primo record: cresce da 0)
    - reduceMax(120, 80) == 120     (D-07: una lettura più bassa NON abbassa il massimo)
    - reduceMax(120, 121) == 121    (una lettura più alta di 1 aggiorna il massimo)
    - reduceMax(100, 100) == 100    (uguale: resta invariato)
    - reduceMax(120, -5) == 120     (lettura negativa/anomala trattata come 0, non abbassa)
    sanitizePersistedMax(raw):
    - sanitizePersistedMax(150) == 150   (valore valido passa invariato)
    - sanitizePersistedMax(0) == 0       (zero valido)
    - sanitizePersistedMax(-1) == 0      (T-04-01: valore persistito negativo/manomesso azzerato)
  </behavior>
  <action>
    (A) Creare `app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt` con DUE funzioni pure framework-free (nessun import android.*), imitando lo stile di SpeedMapping.kt:

      package com.sed.tachimetro.maxspeed

      import kotlin.math.max

      /** D-07: il massimo di sessione cresce solo verso la lettura più alta vista; letture piu' basse o anomale non lo abbassano. */
      fun reduceMax(currentMax: Int, reading: Int): Int {
          val safeCurrent = if (currentMax < 0) 0 else currentMax
          val safeReading = if (reading < 0) 0 else reading
          return max(safeCurrent, safeReading)
      }

      /** T-04-01: un valore persistito manomesso/negativo viene riportato a 0 alla lettura. */
      fun sanitizePersistedMax(raw: Int): Int = if (raw < 0) 0 else raw

    (B) Creare `app/src/test/java/com/sed/tachimetro/maxspeed/MaxSpeedReducerTest.kt` con test JUnit4 JVM (import org.junit.Test, org.junit.Assert.assertEquals) che coprono TUTTI i casi elencati nel blocco <behavior> sopra (5 casi reduceMax + 3 casi sanitizePersistedMax). Un metodo di test per caso o raggruppati per funzione, nomi in stile subject_expectedBehavior.

    (C) Creare `app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt` — wrapper SharedPreferences per un singolo Int (D-06):

      package com.sed.tachimetro.maxspeed

      import android.content.Context

      /**
       * D-06/D-07/D-08: persistenza del massimo (un solo Int) via SharedPreferences app-private.
       * Niente Room/DataStore: un intero non giustifica una dipendenza (CONTEXT Established Patterns).
       */
      class MaxSpeedStore(context: Context) {
          // WR-04: il chiamante passa applicationContext, mai un'Activity.
          private val prefs = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)

          /** Legge il massimo persistito, azzerando qualsiasi valore malformato/negativo (T-04-01). */
          fun read(): Int = sanitizePersistedMax(prefs.getInt(KEY_MAX_SPEED, 0))

          /** D-07/D-08: scrittura immediata asincrona (apply(), fuori dal main thread) — un solo Int economico. */
          fun write(value: Int) {
              prefs.edit().putInt(KEY_MAX_SPEED, value).apply()
          }

          companion object {
              const val PREFS_NAME = "tachimetro_prefs"
              const val KEY_MAX_SPEED = "max_speed_kmh"
          }
      }

    NON usare `commit()` (bloccante) — usare `apply()`. NON introdurre coroutine/thread manuali: apply() è già off-main-thread.
  </action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.sed.tachimetro.maxspeed.MaxSpeedReducerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:testDebugUnitTest --tests "com.sed.tachimetro.maxspeed.MaxSpeedReducerTest"` termina con BUILD SUCCESSFUL e tutti i test verdi.
    - `MaxSpeedReducer.kt` contiene `fun reduceMax(currentMax: Int, reading: Int): Int` e `fun sanitizePersistedMax(raw: Int): Int` e NON contiene alcun import `android.` (funzioni pure).
    - `MaxSpeedReducerTest.kt` contiene almeno gli assert per: reduceMax(120, 80)==120, reduceMax(0,50)==50, reduceMax(120,-5)==120, sanitizePersistedMax(-1)==0.
    - `MaxSpeedStore.kt` contiene `getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)`, `putInt(KEY_MAX_SPEED`, `.apply()` e usa `sanitizePersistedMax(` nel metodo read().
    - `MaxSpeedStore.kt` NON contiene `.commit()`.
  </acceptance_criteria>
  <done>I test JVM su reduceMax/sanitizePersistedMax passano (D-07 e sanitizzazione bloccati); MaxSpeedStore espone read()/write() su SharedPreferences app-private con apply() e read() sanitizzato.</done>
</task>

<task type="auto">
  <name>Task 3: Cablare MainActivity — lettura all'avvio, aggiornamento massimo, reset, insets, visibilità</name>
  <files>app/src/main/java/com/sed/tachimetro/MainActivity.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt (stato attuale COMPLETO — modificare in place, osservare onCreate, updatePlaceholder, showReady, showDenied, applyUnitTextWindowInsets)
    - app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt (API read()/write() creata nel Task 2)
    - app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt (reduceMax creata nel Task 2)
    - .planning/phases/04-velocit-massima-e-persistenza/04-UI-SPEC.md (righe su Window insets, Startup read timing, visibilità mutuamente esclusiva con retryButton)
  </read_first>
  <action>
    Modificare `app/src/main/java/com/sed/tachimetro/MainActivity.kt`. Aggiungere gli import necessari: `import com.sed.tachimetro.maxspeed.MaxSpeedStore` e `import com.sed.tachimetro.maxspeed.reduceMax` (TextView, Button, ConstraintLayout, View, ViewCompat, WindowInsetsCompat sono già importati).

    (A) Aggiungere campi accanto agli esistenti (dopo `private lateinit var retryButton: Button`):
      private lateinit var maxSpeedText: TextView
      private lateinit var resetMaxButton: Button
      private lateinit var maxSpeedStore: MaxSpeedStore
      private var currentMax: Int = 0

    (B) In onCreate(), dopo `retryButton.setOnClickListener { onRetryClicked() }` e `applyUnitTextWindowInsets()`, aggiungere:
      maxSpeedText = findViewById(R.id.maxSpeedText)
      resetMaxButton = findViewById(R.id.resetMaxButton)
      resetMaxButton.setOnClickListener { onResetMaxClicked() }
      applyMaxAreaWindowInsets()
      // D-09: leggere il massimo salvato PRIMA di avviare la raccolta GPS, cosi' l'area MAX
      // appare gia' con lo stato corretto senza flash di "MAX 0".
      maxSpeedStore = MaxSpeedStore(applicationContext)
      currentMax = maxSpeedStore.read()
      updateMaxArea()
    Queste righe devono stare PRIMA della creazione di `gpsSpeedProvider` e dell'avvio della coroutine di raccolta.

    (C) Nel ramo `is SpeedState.Reading` di updatePlaceholder(), DOPO `messageText.text = state.kmh.toString()`, aggiungere l'aggiornamento del massimo (D-07):
      val newMax = reduceMax(currentMax, state.kmh)
      if (newMax != currentMax) {
          currentMax = newMax
          maxSpeedStore.write(currentMax)   // D-07: scrittura immediata su ogni nuovo record
      }
    Alla FINE di updatePlaceholder() (dopo il blocco `when`, valido per tutti gli stati GPS Searching/NoSignal/Reading), aggiungere una singola chiamata:
      updateMaxArea()

    (D) Aggiungere il metodo onResetMaxClicked() (D-04 immediato, D-08 persistenza immediata):
      private fun onResetMaxClicked() {
          currentMax = 0                 // D-04: azzeramento immediato, nessun dialog di conferma
          maxSpeedStore.write(0)         // D-08: scrivi 0 su disco subito
          updateMaxArea()                // D-09: area MAX torna GONE (massimo == 0)
      }

    (E) Aggiungere il metodo updateMaxArea() (D-03/D-09: visibile solo se massimo > 0):
      private fun updateMaxArea() {
          if (currentMax > 0) {
              maxSpeedText.text = getString(R.string.max_speed_format, currentMax)
              maxSpeedText.visibility = View.VISIBLE
              resetMaxButton.visibility = View.VISIBLE
          } else {
              maxSpeedText.visibility = View.GONE
              resetMaxButton.visibility = View.GONE
          }
      }

    (F) In showDenied(), aggiungere l'occultamento esplicito dell'area MAX (mutua esclusivita' con retryButton, per UI-SPEC):
      maxSpeedText.visibility = View.GONE
      resetMaxButton.visibility = View.GONE
    In showReady(), aggiungere `updateMaxArea()` alla fine, cosi' un massimo ripristinato dal disco resta visibile appena il permesso e' concesso (D-09).

    (G) Aggiungere il metodo applyMaxAreaWindowInsets() — specchio di applyUnitTextWindowInsets() sul lato start/left. Applica l'inset top+left a maxSpeedText e l'inset left a resetMaxButton (il pulsante e' vincolato sotto l'etichetta, quindi serve solo l'inset orizzontale):
      private fun applyMaxAreaWindowInsets() {
          val labelParams = maxSpeedText.layoutParams as ConstraintLayout.LayoutParams
          val labelBaseTop = labelParams.topMargin
          val labelBaseStart = labelParams.marginStart
          val buttonParams = resetMaxButton.layoutParams as ConstraintLayout.LayoutParams
          val buttonBaseStart = buttonParams.marginStart
          ViewCompat.setOnApplyWindowInsetsListener(maxSpeedText) { view, insets ->
              val systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars())
              val cutout = insets.getInsets(WindowInsetsCompat.Type.displayCutout())
              val extraTop = maxOf(systemBars.top, cutout.top)
              val extraStart = maxOf(systemBars.left, cutout.left)
              val lp = view.layoutParams as ConstraintLayout.LayoutParams
              lp.topMargin = labelBaseTop + extraTop
              lp.marginStart = labelBaseStart + extraStart
              view.layoutParams = lp
              val bp = resetMaxButton.layoutParams as ConstraintLayout.LayoutParams
              bp.marginStart = buttonBaseStart + extraStart
              resetMaxButton.layoutParams = bp
              insets
          }
      }
    NON riusare l'istanza di listener di unitText (gli inset differiscono per lato: start vs end). NON toccare applyUnitTextWindowInsets().

    NON aggiungere FLAG_KEEP_SCREEN_ON o qualsiasi cosa relativa allo schermo sempre acceso (Fase 5). NON modificare la logica GPS/permessi esistente oltre alle aggiunte descritte.
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:assembleDebug` termina con BUILD SUCCESSFUL.
    - `MainActivity.kt` contiene `import com.sed.tachimetro.maxspeed.MaxSpeedStore` e `import com.sed.tachimetro.maxspeed.reduceMax`.
    - `MainActivity.kt` contiene i campi `maxSpeedText`, `resetMaxButton`, `maxSpeedStore` e `private var currentMax`.
    - onCreate contiene `maxSpeedStore = MaxSpeedStore(applicationContext)`, `currentMax = maxSpeedStore.read()` e la chiamata a `applyMaxAreaWindowInsets()`.
    - updatePlaceholder contiene `reduceMax(currentMax, state.kmh)` e una chiamata a `maxSpeedStore.write(currentMax)` dentro il ramo Reading.
    - Esistono i metodi `onResetMaxClicked()`, `updateMaxArea()` e `applyMaxAreaWindowInsets()`; `resetMaxButton.setOnClickListener { onResetMaxClicked() }` è presente in onCreate.
    - `onResetMaxClicked` contiene `maxSpeedStore.write(0)` e `updateMaxArea()`.
    - `showDenied` imposta `maxSpeedText.visibility = View.GONE` e `resetMaxButton.visibility = View.GONE`.
    - `MainActivity.kt` NON contiene `KEEP_SCREEN_ON`.
  </acceptance_criteria>
  <done>Build debug verde; MainActivity legge il massimo all'avvio, lo aggiorna e persiste in updatePlaceholder(Reading) via reduceMax+write, azzera+persiste al tap del reset, mostra/nasconde l'area MAX in base a currentMax>0, applica gli insets top+left e nasconde l'area negli stati di permesso negato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPS → memoria app | Il valore corrente arriva come `SpeedState.Reading(kmh: Int)` — un intero già filtrato/validato dal motore GPS (Fase 2), non testo utente arbitrario. Nessuna injection possibile su un Int. |
| Memoria app → SharedPreferences | Scrittura di un singolo Int in file app-private (`MODE_PRIVATE`, `tachimetro_prefs`). Non leggibile/scrivibile da altre app su device non-root. |
| SharedPreferences → memoria app (lettura all'avvio) | Il valore letto potrebbe essere stato manomesso solo su device rooted / dalla stessa app. `getInt` restituisce sempre un Int tipizzato (nessun parsing di stringa, nessun crash da tipo errato). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | Valore persistito in SharedPreferences letto in `MaxSpeedStore.read()` | mitigate | `sanitizePersistedMax(raw)` azzera qualsiasi valore negativo/manomesso alla lettura (clamp `< 0` → 0). `reduceMax` applica lo stesso clamp anche in memoria. Il typography no-wrap del layout (maxLines=1) evita che un valore anomalo enorme rompa il layout. |
| T-04-02 | Information Disclosure | Massimo salvato in `tachimetro_prefs` | accept | Il dato è la sola velocità massima di sessione (un Int), non è PII né informazione sensibile; storage è app-private. Nessuna mitigazione necessaria. |
| T-04-03 | Denial of Service | Scrittura su ogni nuovo record (D-07) | accept | Scrittura di un solo Int via `apply()` (asincrona, off-main-thread) al più ~1 volta/sec e solo quando il massimo cresce; costo trascurabile, nessun rischio di ANR o esaurimento I/O. |
| T-04-04 | Tampering (UX, non-sicurezza) | `resetMaxButton` senza dialog di conferma (D-04) | accept | Decisione utente esplicita (D-04): azzerare un contatore di sessione non è perdita di dati critici; non è un confine di sicurezza. Documentato come scelta UX intenzionale, non come vulnerabilità. |
| T-04-SC | Tampering | install npm/pip/cargo | accept | Nessun package manager invocato in questa fase (solo Kotlin/XML nativi, SharedPreferences è API di piattaforma). Nessun pacchetto da auditare; gate di legittimità non applicabile. |

Nessuna minaccia ad alta severità in questa fase: nessuna rete, nessuna autenticazione, nessun PII, nessun input testuale non fidato. L'unica mitigazione attiva (T-04-01) è la sanitizzazione del valore persistito alla lettura, già inclusa in `sanitizePersistedMax`/`reduceMax`. Nessun install di package manager → sezione Package Legitimacy Audit non richiesta.
</threat_model>

<verification>
- `./gradlew.bat :app:assembleDebug` → BUILD SUCCESSFUL (Task 1 e Task 3).
- `./gradlew.bat :app:testDebugUnitTest --tests "com.sed.tachimetro.maxspeed.MaxSpeedReducerTest"` → test verdi (Task 2, blocca D-07 e sanitizzazione T-04-01).
- Asserzioni sorgente (grep) su strings.xml, activity_main.xml, MaxSpeedReducer.kt, MaxSpeedStore.kt, MainActivity.kt — vedi acceptance_criteria di ciascun task.
- La verifica comportamentale su device (display, reset, persistenza dopo chiusura app E riavvio telefono) avviene nel piano 04-02 (checkpoint human-verify).
</verification>

<success_criteria>
- MAX-01: l'utente vede la velocità massima ("MAX <n>") in alto a sinistra, area secondaria, quando il massimo > 0.
- MAX-02: il pulsante "Azzera massimo" azzera il valore al tap (immediato, senza dialog — D-04) e l'area MAX torna nascosta.
- MAX-03: il massimo è scritto su SharedPreferences immediatamente ad ogni nuovo record (D-07) e all'azzeramento (D-08) — base per la persistenza verificata su device nel piano 04-02.
- Nessun "MAX 0" mostrato (D-03/D-09); nessuna nuova dipendenza Gradle; nessuna animazione (UI-04); tutte le stringhe in italiano (UI-05).
</success_criteria>

<output>
Create `.planning/phases/04-velocit-massima-e-persistenza/04-01-SUMMARY.md` when done
</output>
