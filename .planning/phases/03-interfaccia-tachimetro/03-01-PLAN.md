---
phase: 03-interfaccia-tachimetro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/layout/activity_main.xml
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05]

must_haves:
  truths:
    - "Il numero della velocità si ridimensiona automaticamente (D-01) per riempire lo schermo (grande per 1 cifra, per 2 cifre e per 3 cifre)"
    - "Il numero è centrato orizzontalmente e verticalmente sullo schermo nero"
    - "Ruotando in landscape il numero resta centrato e massimizzato senza tagli, tramite un unico layout adattivo senza res/layout-land separato (D-02)"
    - "Il testo è bianco in grassetto pesante (Black/900) ad alto contrasto su sfondo nero"
    - "I messaggi di stato ('Ricerca segnale GPS...', permesso negato/permanente) riusano lo stesso TextView messageText (D-03) e si ridimensionano automaticamente più piccoli del numero"
    - "Il pulsante Riprova/Apri impostazioni mantiene lo stesso posizionamento attuale, ricentrato nel nuovo layout (D-04)"
    - "Non sono presenti menu, animazioni o elementi grafici oltre a messageText e retryButton; nessuno spazio riservato per la velocità massima della Fase 4 (D-05)"
    - "Tutti i testi restano in italiano (nessuna stringa hardcoded, invariati rispetto alle Fasi 1-2)"
  artifacts:
    - path: "app/src/main/res/layout/activity_main.xml"
      provides: "Layout a schermo intero con messageText auto-dimensionante e retryButton ricentrato"
      contains: "autoSizeTextType"
  key_links:
    - from: "app/src/main/res/layout/activity_main.xml"
      to: "MainActivity.kt findViewById(R.id.messageText) / R.id.retryButton"
      via: "ID di vista invariati"
      pattern: "@\\+id/(messageText|retryButton)"
    - from: "app/src/main/res/layout/activity_main.xml messageText"
      to: "autosize uniform con bounding box 0dp/0dp"
      via: "TextView auto-size"
      pattern: "autoSizeTextType=\"uniform\""
---

<objective>
Evolvere il layout `activity_main.xml` esistente (baseline Fasi 1-2) affinché il numero della velocità diventi l'elemento dominante dello schermo: sempre centrato, il più grande possibile in portrait e landscape, tramite auto-dimensionamento uniforme del `TextView messageText`. Lo sfondo resta nero, il testo bianco in grassetto pesante. Lo stesso `messageText` mostra anche i messaggi di stato, che con l'auto-size si riducono naturalmente rispetto al numero (nessun secondo elemento di testo).

Purpose: Soddisfa i requisiti UI-01..UI-05 della Fase 3 — massima leggibilità a colpo d'occhio, in ogni orientamento e condizione di luce, interamente in italiano.
Output: `app/src/main/res/layout/activity_main.xml` aggiornato, verificato su device reale/emulatore in portrait e landscape con velocità a 1, 2 e 3 cifre.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interfaccia-tachimetro/03-CONTEXT.md
@.planning/phases/03-interfaccia-tachimetro/03-UI-SPEC.md
@.planning/phases/03-interfaccia-tachimetro/03-PATTERNS.md

<interfaces>
<!-- Contratto consumatore: MainActivity.kt (NON modificato in questa fase). -->
<!-- Gli ID di vista qui sotto DEVONO sopravvivere alle modifiche di layout. -->

From app/src/main/java/com/sed/tachimetro/MainActivity.kt (righe 53-54, read-only):
messageText = findViewById(R.id.messageText)
retryButton = findViewById(R.id.retryButton)

Il testo viene sempre impostato via getString() (showReady/showDenied/updatePlaceholder) — le modifiche di attributo (size/width/height/gravity/fontWeight) NON richiedono modifiche Kotlin.
</interfaces>

<constraints>
- minSdk = 30 (API 30) confermato in app/build.gradle.kts — `android:textFontWeight="900"` (richiede API 28) è sicuro senza guardie.
- Root ConstraintLayout dichiara già `xmlns:app="http://schemas.android.com/apk/res-auto"` — gli attributi autosize usano il prefisso `app:`, nessun namespace nuovo.
- Nessuna nuova stringa: tutte le copy (status_ready, searching_gps_signal, speed_kmh_format, permission_denied, permission_denied_permanent, retry, open_settings) esistono già ed è già italiano (UI-05).
- Nessun file `res/layout-land/` separato (D-02): un unico layout adattivo.
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Applicare auto-size e stile dominante a messageText in activity_main.xml</name>
  <files>app/src/main/res/layout/activity_main.xml</files>
  <read_first>
    - app/src/main/res/layout/activity_main.xml (stato attuale da evolvere in place, NON riscrivere da zero)
    - .planning/phases/03-interfaccia-tachimetro/03-PATTERNS.md (sezione "What must change on messageText" punti 1-10 e "What must be preserved as-is")
    - .planning/phases/03-interfaccia-tachimetro/03-UI-SPEC.md (Layout Contract, Typography table, Spacing Scale, Color table)
  </read_first>
  <action>
    Modificare SOLO l'elemento `<TextView android:id="@+id/messageText">`; NON toccare il root ConstraintLayout (mantiene `match_parent`/`match_parent` + `android:background="@android:color/black"`) né il `<Button android:id="@+id/retryButton">` (mantiene `wrap_content`/`wrap_content`, `layout_marginTop="16dp"`, `visibility="gone"`, la catena di vincoli esistente e gli attributi `tools:`).

    Su `messageText` applicare esattamente questi cambiamenti (valori mandatori da UI-SPEC, non ridiscutere):
    - Cambiare `android:layout_width="wrap_content"` in `android:layout_width="0dp"`.
    - Cambiare `android:layout_height="wrap_content"` in `android:layout_height="0dp"`.
    - Aggiungere `android:layout_marginStart="16dp"` e `android:layout_marginEnd="16dp"` (token `md` = 16dp, margine orizzontale bordi schermo).
    - Rimuovere `android:textSize="20sp"` (incompatibile con autosize; il triad autosize lo sostituisce).
    - Aggiungere `app:autoSizeTextType="uniform"`.
    - Aggiungere `app:autoSizeMinTextSize="12sp"`, `app:autoSizeMaxTextSize="300sp"`, `app:autoSizeStepGranularity="4sp"`.
    - Aggiungere `android:gravity="center"` e `android:textAlignment="center"`.
    - Aggiungere `android:textFontWeight="900"` mantenendo l'esistente `android:textStyle="bold"` come fallback.
    - Aggiungere `android:fontFamily="sans-serif"`.
    - Mantenere invariati: `android:id="@+id/messageText"`, `android:textColor="@android:color/white"`, tutti i quattro vincoli constraint esistenti (Top_toTopOf parent, Bottom_toTopOf retryButton, Start_toStartOf parent, End_toEndOf parent), `app:layout_constraintVertical_chainStyle="packed"`, `tools:text`.

    NON aggiungere `android:singleLine`, `android:maxLines`, `app:autoSizePresetSizes`, né alcun animatore/transizione. NON creare `res/layout-land/`. NON modificare MainActivity.kt, themes.xml, colors.xml o strings.xml.
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:assembleDebug` termina con BUILD SUCCESSFUL (il layout XML compila senza errori aapt).
    - `activity_main.xml` contiene la stringa `app:autoSizeTextType="uniform"`.
    - `activity_main.xml` contiene `app:autoSizeMinTextSize="12sp"`, `app:autoSizeMaxTextSize="300sp"`, `app:autoSizeStepGranularity="4sp"`.
    - Sull'elemento `messageText`: `android:layout_width="0dp"` e `android:layout_height="0dp"` presenti; la stringa `android:textSize="20sp"` NON è più presente nel file.
    - `activity_main.xml` contiene `android:gravity="center"`, `android:textAlignment="center"`, `android:textFontWeight="900"`, `android:textStyle="bold"`, `android:fontFamily="sans-serif"`, `android:layout_marginStart="16dp"`, `android:layout_marginEnd="16dp"`.
    - Gli ID `@+id/messageText` e `@+id/retryButton` sono ancora presenti (nessun rename).
    - Il file NON contiene `android:singleLine`, `app:autoSizePresetSizes`, né `res/layout-land`.
    - `retryButton` conserva `android:layout_width="wrap_content"` e `android:layout_height="wrap_content"` (non entra nello scope autosize).
  </acceptance_criteria>
  <done>Il build debug passa e activity_main.xml presenta messageText con auto-size uniform (12/300/4sp), box 0dp/0dp, centratura, font weight 900 e margini 16dp, con ID e vincoli invariati.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifica visiva su device — portrait + landscape, velocità 1/2/3 cifre</name>
  <read_first>
    - app/src/main/res/layout/activity_main.xml (stato post-Task 1)
    - .planning/phases/03-interfaccia-tachimetro/03-UI-SPEC.md (Layout Contract, Typography table)
  </read_first>
  <what-built>
    Layout a schermo intero con numero velocità auto-dimensionante (uniform, 12-300sp) su sfondo nero, testo bianco Black/900, centrato, adattivo portrait/landscape. Lo stesso TextView mostra i messaggi di stato che si riducono automaticamente rispetto al numero.
  </what-built>
  <how-to-verify>
    Precondizione: installare la build debug su device reale o emulatore con Google Play Services. Comando: `./gradlew.bat :app:installDebug` poi avviare l'app. Usare Extended Controls > Location > Routes (o un valore di velocità simulato) sull'emulatore per generare velocità diverse; in alternativa provare su device reale in movimento.

    1. PORTRAIT — velocità 1 cifra (es. "5 km/h"): il numero deve essere molto grande e centrato orizzontalmente e verticalmente, sfondo nero, testo bianco spesso.
    2. PORTRAIT — velocità 2 cifre (es. "60 km/h"): il numero riempie ancora la larghezza disponibile, resta centrato, nessun taglio ai bordi (margini 16dp rispettati).
    3. PORTRAIT — velocità 3 cifre (es. "180 km/h"): il numero è il più grande possibile senza uscire dai bordi, centrato.
    4. LANDSCAPE — ruotare il device: ripetere con 1, 2 e 3 cifre. Il numero deve restare centrato e massimizzato adattandosi alla nuova forma dello schermo, senza tagli.
    5. MESSAGGIO DI STATO — con GPS senza segnale deve comparire "Ricerca segnale GPS..." nello stesso TextView, visibilmente più piccolo del numero (gerarchia visiva automatica), leggibile e centrato.
    6. MESSAGGIO PERMESSO — negare il permesso GPS: verificare che "Permesso GPS negato. Aprire le impostazioni per abilitarlo" venga mostrato interamente (può andare a capo su 2 righe) senza rimpicciolirsi in modo illeggibile, con il pulsante "Apri impostazioni" centrato sotto.
    7. Confermare assenza di menu, animazioni o elementi grafici extra; tutti i testi in italiano.

    Esito atteso: in ogni combinazione orientamento × numero-cifre, il numero è dominante, centrato e massimizzato; i messaggi si adattano correttamente.
  </how-to-verify>
  <resume-signal>Scrivere "approvato" se tutti i casi passano, oppure descrivere i problemi riscontrati (orientamento, numero cifre, comportamento osservato).</resume-signal>
  <acceptance_criteria>
    - Confermato visivamente: numero dominante e centrato con 1, 2 e 3 cifre in PORTRAIT.
    - Confermato visivamente: numero dominante e centrato con 1, 2 e 3 cifre in LANDSCAPE, senza tagli.
    - Confermato: "Ricerca segnale GPS..." appare più piccolo del numero nello stesso TextView.
    - Confermato: stringa permesso negato permanente visibile per intero (wrap ammesso) e leggibile, con pulsante centrato.
    - Confermato: nessun menu/animazione/elemento extra; tutto in italiano.
  </acceptance_criteria>
  <done>L'utente conferma "approvato" dopo aver verificato tutte le combinazioni portrait/landscape × 1/2/3 cifre e i due stati di messaggio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Nessuna nuova | Questa fase modifica solo una risorsa di layout statica (`activity_main.xml`). Non introduce nuovi input utente, nuove permission, nuove sorgenti dati, né superfici di rete/IPC. Nessun nuovo confine di fiducia rispetto alle Fasi 1-2. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | `messageText` (auto-size, contenuti già esistenti) | accept | Il TextView mostra solo velocità e messaggi di stato già presenti in Fasi 1-2; nessun nuovo dato sensibile o PII esposto. Rischio nullo. |
| T-03-02 | Tampering | `activity_main.xml` risorsa statica | accept | Risorsa di layout compilata nell'APK, non modificabile a runtime; nessun input esterno la altera. Nessuna dipendenza/pacchetto aggiunto (zero install di package manager in questa fase). |
| T-03-03 | Denial of Service | Autosize su box 0dp/0dp | accept | Autosize uniforme con min/max/step fissi (12/300/4sp) è una funzionalità di piattaforma Android collaudata; non causa loop di misura né esaurimento risorse. Rischio trascurabile. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Nessun install di package manager in questa fase (solo modifica XML). Nessun pacchetto ASSUMED/SUS/SLOP da verificare; checkpoint di legittimità non applicabile. |

Nessun pacchetto viene installato in questa fase — la sezione Package Legitimacy Audit di RESEARCH.md non è richiesta (nessun task di install). Tutti i widget usati (TextView, Button, ConstraintLayout AndroidX) sono già dipendenze del progetto dalla Fase 1. Nessuna disposition "mitigate" necessaria: fase puramente visiva a rischio basso, come previsto dalla policy di sicurezza.
</threat_model>

<verification>
- `./gradlew.bat :app:assembleDebug` → BUILD SUCCESSFUL (Task 1).
- Asserzioni sorgente su `activity_main.xml` (attributi autosize, 0dp, centratura, font weight, margini; assenza di textSize fisso/singleLine/presetSizes; ID invariati) — vedi acceptance_criteria Task 1.
- Verifica umana su device: portrait+landscape × 1/2/3 cifre + stati messaggio — vedi acceptance_criteria Task 2.
</verification>

<success_criteria>
- Il numero della velocità è l'elemento dominante, centrato e massimizzato in entrambi gli orientamenti (UI-01, UI-03).
- Sfondo nero, testo bianco Black/900 ad alto contrasto (UI-02).
- Nessun menu/animazione/elemento grafico non necessario (UI-04).
- Tutti i testi restano in italiano, nessuna stringa nuova o hardcoded (UI-05).
- MainActivity.kt continua a funzionare senza modifiche (ID di vista preservati).
</success_criteria>

<output>
Create `.planning/phases/03-interfaccia-tachimetro/03-01-SUMMARY.md` when done
</output>
