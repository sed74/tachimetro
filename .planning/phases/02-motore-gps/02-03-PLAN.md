---
phase: 02-motore-gps
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - app/src/main/res/values/strings.xml
  - app/src/main/java/com/sed/tachimetro/MainActivity.kt
autonomous: false
requirements: [GPS-01, GPS-02]
must_haves:
  truths:
    - "All'avvio (permesso concesso) l'utente vede 'Ricerca segnale GPS...' finché non arriva un fix valido"
    - "Quando il GPS legge una velocità, il placeholder mostra il numero intero seguito da ' km/h' (es. '42 km/h')"
    - "Se il segnale si perde per >5s, il placeholder torna a 'Ricerca segnale GPS...'"
    - "Gli aggiornamenti GPS partono quando l'app diventa visibile (onStart) e si fermano quando non è più visibile (onStop)"
    - "Il flusso permesso della Fase 1 (grant/deny/permanent-deny) continua a funzionare invariato"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "Italian no-signal string"
      contains: "searching_gps_signal"
    - path: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      provides: "GpsSpeedProvider wiring + updatePlaceholder + lifecycle-scoped collector"
      contains: "repeatOnLifecycle"
  key_links:
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "GpsSpeedProvider.state"
      via: "lifecycleScope.launch { repeatOnLifecycle(STARTED) { ...collect } }"
      pattern: "repeatOnLifecycle\\(Lifecycle\\.State\\.STARTED\\)"
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "R.string.searching_gps_signal"
      via: "updatePlaceholder sets messageText.text"
      pattern: "searching_gps_signal"
---

<objective>
Wire the GPS engine into the existing `MainActivity` so the Phase-1 black "Pronto" placeholder is temporarily replaced by the live speed number (or the "Ricerca segnale GPS..." message), and verify the whole speed-reading path on a moving GPS trace using the emulator Route Playback (D-10).

Purpose: Make Phase 2 verifiable on-device before Phase 3 builds the real UI (D-09), and confirm the 1 Hz cadence, no-signal states, noise-floor, and accuracy filtering behave correctly with genuinely varying speed input — not just a static 0.
Output: One new Italian string, an updated `MainActivity`, and a human-confirmed Route Playback verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-motore-gps/02-RESEARCH.md
@.planning/phases/02-motore-gps/02-PATTERNS.md

<interfaces>
<!-- Contracts CONSUMED from Plan 02 (already built). Use directly — no exploration needed. -->

com.sed.tachimetro.gps.SpeedState (sealed):
- SpeedState.Searching          (data object)
- SpeedState.Reading(val kmh: Int) (data class)
- SpeedState.NoSignal           (data object)

com.sed.tachimetro.gps.GpsSpeedProvider(context: Context):
- val state: StateFlow<SpeedState>   // initial value SpeedState.Searching

From MainActivity.kt (existing, Phase 1) — reuse, do not rewrite:
- private lateinit var messageText: TextView   // the placeholder to update
- private lateinit var retryButton: Button
- private fun showReady() / showDenied()        // Phase-1 permission-state UI (keep working)
- ContextCompat.checkSelfPermission(this, Manifest.permission.ACCESS_FINE_LOCATION) == PERMISSION_GRANTED  // grant check
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add no-signal string + wire GpsSpeedProvider collector into MainActivity</name>
  <files>app/src/main/res/values/strings.xml, app/src/main/java/com/sed/tachimetro/MainActivity.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt (FULL current file — imports two-group convention, onCreate wiring lines ~31-40, onResume precedent lines ~42-59, showReady/showDenied display-method style lines ~93-112)
    - app/src/main/res/values/strings.xml (existing 6 Italian entries, snake_case naming)
    - app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt (the sealed cases to `when` over)
    - app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt (the `state: StateFlow<SpeedState>` surface to collect)
    - .planning/phases/02-motore-gps/02-PATTERNS.md ("MainActivity.kt" section — imports, field decl, onCreate wiring, updatePlaceholder style, D-07 warning against manual onStart/onStop overrides)
    - .planning/phases/02-motore-gps/02-RESEARCH.md (Pattern 2 "repeatOnLifecycle" lines ~227-243; Architecture Diagram MainActivity block)
  </read_first>
  <action>
    In strings.xml, add one entry following the existing snake_case, Italian-only convention: `<string name="searching_gps_signal">Ricerca segnale GPS...</string>`. Do not alter existing strings.
    In MainActivity.kt, append new imports after the existing `androidx.*` group as a third group: `androidx.lifecycle.Lifecycle`, `androidx.lifecycle.lifecycleScope`, `androidx.lifecycle.repeatOnLifecycle`, `kotlinx.coroutines.launch`, `com.sed.tachimetro.gps.GpsSpeedProvider`, `com.sed.tachimetro.gps.SpeedState`.
    Add a field `private lateinit var gpsSpeedProvider: GpsSpeedProvider` beside `messageText`/`retryButton`. In `onCreate()` (after `retryButton.setOnClickListener { onRetryClicked() }`, before/after `checkAndRequestPermission()`), initialize `gpsSpeedProvider = GpsSpeedProvider(this)` and register the collector ONCE: `lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { ... } }`. This implements D-07 (start on onStart, auto-cancel on onStop) — do NOT add manual `onStart()`/`onStop()` overrides that call collect/cancel (RESEARCH.md Anti-Pattern).
    Inside the `repeatOnLifecycle(STARTED)` block, gate on permission: if `ContextCompat.checkSelfPermission(this, Manifest.permission.ACCESS_FINE_LOCATION) == PackageManager.PERMISSION_GRANTED`, then `gpsSpeedProvider.state.collect { state -> updatePlaceholder(state) }`; otherwise do nothing (the existing showDenied() path owns the denied UI). This keeps the Phase-1 permission flow the single source of truth and starts GPS only when allowed.
    Add `private fun updatePlaceholder(state: SpeedState)` in the style of showReady()/showDenied(): a `when (state)` — `SpeedState.Searching` and `SpeedState.NoSignal` both set `messageText.text = getString(R.string.searching_gps_signal)`; `is SpeedState.Reading` sets `messageText.text = "${state.kmh} km/h"` (numeric interpolation is the one allowed non-getString case, per D-09 — no decimals). Also set `retryButton.visibility = View.GONE` when showing a speed/searching state so the placeholder stays clean.
    Leave `showReady()` in place (still called by the Phase-1 permission-grant callbacks). It briefly shows "Pronto" on grant; the collector's first `Searching`/`Reading` emission then overwrites `messageText`. Do not delete Phase-1 permission logic; do not modify AndroidManifest.xml (no permission changes this phase).
  </action>
  <verify>
    <automated>./gradlew.bat assembleDebug lintDebug</automated>
  </verify>
  <acceptance_criteria>
    - `strings.xml` contains `<string name="searching_gps_signal">Ricerca segnale GPS...</string>`
    - MainActivity.kt collects `gpsSpeedProvider.state` inside `lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { ... } }` and contains NO manually-added `onStart()`/`onStop()` override calling collect/cancel
    - `updatePlaceholder` maps `SpeedState.Reading` to `"<kmh> km/h"` (grep shows `km/h` interpolation) and both `Searching` and `NoSignal` to `getString(R.string.searching_gps_signal)`
    - The GPS collector body is guarded by a `checkSelfPermission(... ACCESS_FINE_LOCATION) == PERMISSION_GRANTED` check
    - AndroidManifest.xml is unchanged (no new permissions); existing `checkAndRequestPermission`/`showReady`/`showDenied`/`onResume` logic still present
    - `./gradlew.bat assembleDebug lintDebug` exits 0
  </acceptance_criteria>
  <done>MainActivity instantiates GpsSpeedProvider, collects its StateFlow only while visible and only when permission is granted, and renders Searching/NoSignal as the Italian message and Reading as "<N> km/h"; app compiles and lints clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify live speed on emulator Route Playback (D-10)</name>
  <what-built>
    GpsSpeedProvider (Plan 02) is now wired into MainActivity: the black placeholder shows "Ricerca segnale GPS..." until a valid fix, then the live speed as "<N> km/h" (whole numbers, ~1 update/sec), reverting to the search message if the signal is lost for >5s. Updates run only while the app is visible.
  </what-built>
  <how-to-verify>
    Static testing shows only "0 km/h"/"Ricerca segnale GPS..." and does NOT exercise the speed path (RESEARCH.md Pitfall 3) — a moving trace is required (D-10).
    1. Launch an emulator using a **Play Store** system image (ships current Google Play Services), API 30+.
    2. Install/run the app: `./gradlew.bat installDebug` then open Tachimetro (or run from Android Studio). Grant the ACCESS_FINE_LOCATION permission when prompted.
    3. Confirm at startup, before any location is set, the screen shows "Ricerca segnale GPS...".
    4. Open Extended Controls (the "..." button on the emulator toolbar) → Location → **Routes** tab. Either draw a route on the map or import a GPX/KML file, then click **Play Route**.
    5. Expected: within a few seconds the placeholder switches from "Ricerca segnale GPS..." to a numeric "<N> km/h" that changes roughly once per second and tracks the route's simulated speed (accelerating/decelerating along the path). Values are whole numbers (no decimals).
    6. Expected (stationary/noise): when the route is paused or the simulated vehicle is stopped, the display reads "0 km/h" (not a small noise value like "1 km/h").
    7. Expected (signal loss): stop route playback and clear the location / leave it idle for >5 seconds — the display returns to "Ricerca segnale GPS...".
    8. Expected (lifecycle, D-07): send the app to the background (Home) while a route plays — the emulator's GPS indicator should stop being driven by the app; return to the app and confirm it resumes reading. (Optional but recommended.)
  </how-to-verify>
  <resume-signal>Type "approved" if the speed tracks the route (varying whole-number km/h ~1/sec, 0 when stopped, search message on loss), or describe what you saw (e.g. stuck at 0, no decimals wrong, jumpy spikes → may need accuracy threshold tuning from 50m toward 30m per RESEARCH.md A1).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GpsSpeedProvider → MainActivity (UI) | Only `SpeedState` (Int km/h) crosses into the Activity; raw coordinates must not reach the UI layer |
| MainActivity → screen/logcat | User-visible text + any logs; must not expose precise location |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-ID | Information Disclosure | MainActivity display + any logging | mitigate | MainActivity consumes only `SpeedState` (Int km/h) — it never receives lat/lng. `updatePlaceholder` renders only the derived number / the Italian search string. No `Log.*` of location added. Verified by "only Int km/h crosses the engine boundary" (Plan 02) + this plan adding no Location access. |
| T-02-EP | Elevation of Privilege | AndroidManifest permission scope | accept | This plan adds no permissions; AndroidManifest.xml is explicitly unchanged (acceptance criterion). Only `ACCESS_FINE_LOCATION` from Phase 1 is used — no coarse/background scope. |
| T-02-D | Denial of Service (self, battery) | Lifecycle-scoped collection | mitigate | Collection is bound to `repeatOnLifecycle(STARTED)` — GPS updates (and the callbackFlow's `awaitClose` removeLocationUpdates) stop when the app leaves the foreground, preventing background GPS/battery drain. Verified indirectly by checkpoint step 8. |
</threat_model>

<verification>
- `assembleDebug lintDebug` green.
- Human Route Playback checkpoint confirms: varying whole-number km/h ~1/sec on a moving trace, 0 when stopped, "Ricerca segnale GPS..." at startup and after >5s loss, and lifecycle-bound updates.
</verification>

<success_criteria>
- GPS-01: live km/h visible, updating ~1/sec, whole numbers.
- GPS-02: "Ricerca segnale GPS..." shown at startup and on signal loss.
- Phase-1 permission behavior preserved; no new permissions; no location data leaked to UI/logs.
- Phase is verifiable end-to-end via the required emulator Route Playback method (D-10).
</success_criteria>

<output>
Create `.planning/phases/02-motore-gps/02-03-SUMMARY.md` when done.
</output>
