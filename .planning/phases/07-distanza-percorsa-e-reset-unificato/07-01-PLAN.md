---
phase: 07-distanza-percorsa-e-reset-unificato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/sed/tachimetro/distance/DistanceReducer.kt
  - app/src/main/java/com/sed/tachimetro/distance/DistanceStore.kt
  - app/src/main/java/com/sed/tachimetro/distance/DistanceFormat.kt
  - app/src/test/java/com/sed/tachimetro/distance/DistanceReducerTest.kt
  - app/src/test/java/com/sed/tachimetro/distance/DistanceFormatTest.kt
autonomous: true
requirements: [DIST-01, DIST-03]

must_haves:
  truths:
    - "Un delta di distanza viene sommato al totale solo quando la lettura di velocità è pari o superiore alla soglia di rumore di 2 km/h"
    - "Un valore di distanza persistito negativo o corrotto viene riportato a 0 al momento della lettura"
    - "Il totale della distanza è scritto su SharedPreferences app-private e sopravvive alla morte del processo"
    - "Sotto i 1000 m il valore è espresso in metri interi, da 1000 m in su in chilometri con una decimale"
  artifacts:
    - path: "app/src/main/java/com/sed/tachimetro/distance/DistanceReducer.kt"
      provides: "Funzioni pure reduceDistance (gate soglia rumore D-04) e sanitizePersistedDistance"
      exports: ["reduceDistance", "sanitizePersistedDistance"]
    - path: "app/src/main/java/com/sed/tachimetro/distance/DistanceStore.kt"
      provides: "Persistenza SharedPreferences della distanza in metri (Float)"
      contains: "class DistanceStore"
    - path: "app/src/main/java/com/sed/tachimetro/distance/DistanceFormat.kt"
      provides: "Modello sealed DistanceDisplay e funzione pura formatDistanceDisplay (D-01)"
      exports: ["DistanceDisplay", "formatDistanceDisplay"]
    - path: "app/src/test/java/com/sed/tachimetro/distance/DistanceReducerTest.kt"
      provides: "Test JVM del gate D-04 e della sanitizzazione"
      min_lines: 40
    - path: "app/src/test/java/com/sed/tachimetro/distance/DistanceFormatTest.kt"
      provides: "Test JVM della soglia adattiva m/km (D-01)"
      min_lines: 30
  key_links:
    - from: "app/src/main/java/com/sed/tachimetro/distance/DistanceStore.kt"
      to: "sanitizePersistedDistance"
      via: "chiamata dentro read()"
      pattern: "sanitizePersistedDistance"
    - from: "app/src/main/java/com/sed/tachimetro/distance/DistanceStore.kt"
      to: "SharedPreferences \"tachimetro_prefs\""
      via: "context.getSharedPreferences + putFloat/apply"
      pattern: "tachimetro_prefs"
---

<objective>
Creare il package `com.sed.tachimetro.distance` con la logica di dominio della distanza: la funzione pura di accumulo `reduceDistance()` con il gate della soglia di rumore (D-04), il sanitizzatore `sanitizePersistedDistance()`, la persistenza `DistanceStore` (mirror esatto di `MaxSpeedStore`) e la funzione pura di formattazione adattiva metri/chilometri `formatDistanceDisplay()` (D-01), con i relativi test JVM.

Purpose: isolare tutta la logica testabile della distanza in funzioni pure framework-free, prima che qualsiasi codice di UI o di GPS la consumi. Nessun file esistente viene toccato da questo piano — è additivo al 100%, quindi può girare in parallelo al Piano 02.
Output: 3 file Kotlin di produzione + 2 file di test, tutti nuovi.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distanza-percorsa-e-reset-unificato/07-CONTEXT.md
@.planning/phases/07-distanza-percorsa-e-reset-unificato/07-PATTERNS.md
@app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt
@app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt
@app/src/test/java/com/sed/tachimetro/maxspeed/MaxSpeedReducerTest.kt
@app/src/main/java/com/sed/tachimetro/gps/SpeedMapping.kt
</context>

<interfaces>
<!-- Contratti prodotti da questo piano e consumati dal Piano 03. Nomi e firme VINCOLANTI. -->

package `com.sed.tachimetro.distance`

- `fun reduceDistance(currentTotalMeters: Float, deltaMeters: Float, kmh: Int, noiseFloorKmh: Double = 2.0): Float`
- `fun sanitizePersistedDistance(raw: Float): Float`
- `class DistanceStore(context: Context)` con `fun read(): Float` e `fun write(value: Float)`
  - `companion object { const val PREFS_NAME = "tachimetro_prefs"; const val KEY_DISTANCE_METERS = "distance_meters" }`
- `sealed class DistanceDisplay` con `data class Meters(val value: Int) : DistanceDisplay()` e `data class Kilometers(val value: Float) : DistanceDisplay()`
- `fun formatDistanceDisplay(meters: Float): DistanceDisplay`

Tutte le funzioni sono top-level (fuori da qualsiasi classe), come `reduceMax`/`sanitizePersistedMax` in `MaxSpeedReducer.kt` e `mapSpeedToKmh` in `SpeedMapping.kt`.
</interfaces>

<constraints>
- Nessuna nuova dipendenza Gradle: `gradle/libs.versions.toml` e `app/build.gradle.kts` restano invariati.
- `DistanceReducer.kt` e `DistanceFormat.kt` devono restare framework-free: nessun import `android.*` né `com.google.android.gms.*` (sono unit-testabili su JVM puro, senza Robolectric).
- La distanza persiste come `Float` in metri grezzi, MAI troncata a `Int`: troncare a ogni fix accumulerebbe errore di arrotondamento su percorsi lunghi (RESEARCH.md "Don't Hand-Roll").
- `noiseFloorKmh` ha default `2.0` per allinearsi al default identico di `mapSpeedToKmh` (`SpeedMapping.kt:18`) — non introdurre una costante duplicata con un valore diverso.
- Convenzione commenti del progetto (CLAUDE.md): KDoc su ogni funzione/classe pubblica, con il tag decisionale (`D-01`, `D-04`, `D-06`, `DIST-03`) citato nella prima frase.
- Nessun file esistente va modificato in questo piano.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dominio distanza — reduceDistance, sanitizePersistedDistance e DistanceStore (test-first)</name>
  <files>app/src/test/java/com/sed/tachimetro/distance/DistanceReducerTest.kt, app/src/main/java/com/sed/tachimetro/distance/DistanceReducer.kt, app/src/main/java/com/sed/tachimetro/distance/DistanceStore.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt (analogo esatto: funzioni top-level, clamp difensivo dei negativi, KDoc con tag decisionale in prima frase)
    - app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt (analogo esatto: costruttore `(context: Context)`, `getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)`, `read()` che sanitizza, `write()` con `apply()`, costanti nel `companion object`, commento WR-04)
    - app/src/test/java/com/sed/tachimetro/maxspeed/MaxSpeedReducerTest.kt (convenzione nomi test `[condizione]_[esito]`, KDoc di classe, `assertEquals`, nessun mock/Robolectric)
    - app/src/main/java/com/sed/tachimetro/gps/SpeedMapping.kt (valore e semantica di `noiseFloorKmh: Double = 2.0` e del confronto stretto `kmh < noiseFloorKmh`)
  </read_first>
  <behavior>
    - `reduceDistance(100f, 14.2f, kmh = 20)` → `114.2f` (sopra soglia: il delta si somma)
    - `reduceDistance(100f, 3.5f, kmh = 1)` → `100f` (D-04: sotto soglia, delta scartato)
    - `reduceDistance(100f, 3.5f, kmh = 0)` → `100f` (fermo: `mapSpeedToKmh` restituisce esattamente 0 sotto la soglia)
    - `reduceDistance(100f, 5f, kmh = 2)` → `105f` (confronto stretto: 2 NON è sotto la soglia 2.0)
    - `reduceDistance(-5f, 10f, kmh = 20)` → `10f` (totale corrente negativo trattato come 0)
    - `reduceDistance(100f, -3f, kmh = 20)` → `100f` (delta negativo trattato come 0, difensivo)
    - `sanitizePersistedDistance(123.4f)` → `123.4f`
    - `sanitizePersistedDistance(0f)` → `0f`
    - `sanitizePersistedDistance(-1f)` → `0f`
  </behavior>
  <action>
    Creare PRIMA `app/src/test/java/com/sed/tachimetro/distance/DistanceReducerTest.kt`, package `com.sed.tachimetro.distance`, classe `DistanceReducerTest`, con un `@Test` per ciascuna riga del blocco `<behavior>` e i nomi: `aboveNoiseFloor_addsDeltaToTotal`, `belowNoiseFloor_doesNotAddDelta`, `zeroKmh_doesNotAddDelta`, `exactlyAtNoiseFloorBoundary_addsDelta`, `negativeCurrentTotal_treatedAsZero`, `negativeDelta_treatedAsZero`, `sanitizePersistedDistance_validValue_passesThrough`, `sanitizePersistedDistance_zero_isValid`, `sanitizePersistedDistance_negativeValue_isClampedToZero`. Usare l'overload `assertEquals(expected, actual, delta)` con delta `0.001f` (obbligatorio per i `Float`: l'overload a due argomenti è deprecato per i float). Import consentiti: solo `org.junit.Assert.assertEquals` e `org.junit.Test`. Eseguire il comando di verifica e confermare che il test FALLISCE in compilazione (RED, riferimenti non risolti) prima di scrivere l'implementazione.

    Creare `app/src/main/java/com/sed/tachimetro/distance/DistanceReducer.kt`, package `com.sed.tachimetro.distance`, con due funzioni top-level e nessuna classe:
    - `fun reduceDistance(currentTotalMeters: Float, deltaMeters: Float, kmh: Int, noiseFloorKmh: Double = 2.0): Float` — clampa `currentTotalMeters` negativo a `0f`, poi (D-04) restituisce il totale invariato se `kmh < noiseFloorKmh`, altrimenti somma `deltaMeters` clampato a `0f` se negativo. KDoc: citare D-04 (nessun accumulo sotto la soglia di rumore, evita la deriva a veicolo fermo) e D-06 (il `deltaMeters` arriva già calcolato con `Location.distanceTo()` dal chiamante, questa funzione non conosce `Location`), e annotare che il default `2.0` rispecchia `mapSpeedToKmh`.
    - `fun sanitizePersistedDistance(raw: Float): Float = if (raw < 0f) 0f else raw` — KDoc nello stile di `sanitizePersistedMax`, citando che un valore persistito manomesso/negativo viene riportato a 0 alla lettura.
    Nessun import `android.*`.

    Creare `app/src/main/java/com/sed/tachimetro/distance/DistanceStore.kt`, mirror 1:1 di `MaxSpeedStore.kt` con `Float` al posto di `Int`: `class DistanceStore(context: Context)` con `private val prefs = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)` (mantenere il commento WR-04 sul fatto che il chiamante passa `applicationContext`), `fun read(): Float = sanitizePersistedDistance(prefs.getFloat(KEY_DISTANCE_METERS, 0f))`, `fun write(value: Float) { prefs.edit().putFloat(KEY_DISTANCE_METERS, value).apply() }` e `companion object` con `const val PREFS_NAME = "tachimetro_prefs"` (stesso file di preferenze degli altri store, la costante viene ridichiarata qui e NON importata da `MaxSpeedStore`) e `const val KEY_DISTANCE_METERS = "distance_meters"`. Unico import: `android.content.Context`. KDoc di classe citando DIST-03 e il rifiuto esplicito di Room/DataStore per un singolo scalare.
  </action>
  <verify>
    <automated>./gradlew.bat :app:testDebugUnitTest --tests "com.sed.tachimetro.distance.DistanceReducerTest"</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:testDebugUnitTest --tests "com.sed.tachimetro.distance.DistanceReducerTest"` termina con `BUILD SUCCESSFUL` e il report `app/build/reports/tests/testDebugUnitTest/index.html` riporta 9 test eseguiti, 0 falliti
    - `DistanceReducer.kt` contiene una dichiarazione `fun reduceDistance(` a livello top-level (colonna 0, non indentata dentro una classe) e una `fun sanitizePersistedDistance(`
    - La firma di `reduceDistance` contiene esattamente i parametri `currentTotalMeters: Float`, `deltaMeters: Float`, `kmh: Int`, `noiseFloorKmh: Double = 2.0`
    - `grep -v '^\s*[*/]' app/src/main/java/com/sed/tachimetro/distance/DistanceReducer.kt | grep -c '^import android'` restituisce `0`
    - `DistanceStore.kt` contiene le stringhe `class DistanceStore(context: Context)`, `"tachimetro_prefs"`, `"distance_meters"`, `putFloat`, `getFloat`, `.apply()` e `sanitizePersistedDistance`
    - `DistanceStore.kt` NON contiene `commit()`
    - `git diff --name-only` non elenca `app/src/main/java/com/sed/tachimetro/MainActivity.kt` né alcun file sotto `app/src/main/java/com/sed/tachimetro/gps/`
  </acceptance_criteria>
  <done>I 9 test passano; `reduceDistance`, `sanitizePersistedDistance` e `DistanceStore` esistono, sono puri/framework-free (tranne il `Context` dello store) e rispecchiano il package `maxspeed`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Formattazione adattiva metri/chilometri (D-01, test-first)</name>
  <files>app/src/test/java/com/sed/tachimetro/distance/DistanceFormatTest.kt, app/src/main/java/com/sed/tachimetro/distance/DistanceFormat.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/gps/SpeedMapping.kt (analogo strutturale: funzione pura top-level, primitivi in ingresso, KDoc con tag decisionale, `roundToInt()` da `kotlin.math`)
    - app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt (shape del sealed model da replicare per `DistanceDisplay`: KDoc di classe + commento per variante)
    - app/src/test/java/com/sed/tachimetro/maxspeed/MaxSpeedReducerTest.kt (convenzione nomi test e assert)
    - .planning/phases/07-distanza-percorsa-e-reset-unificato/07-UI-SPEC.md sezione "Adaptive format logic (D-01)" (contratto vincolante sul ramo scelto e sull'edge case a 999.6 m)
  </read_first>
  <behavior>
    - `formatDistanceDisplay(0f)` → `DistanceDisplay.Meters(0)`
    - `formatDistanceDisplay(850f)` → `DistanceDisplay.Meters(850)`
    - `formatDistanceDisplay(849.6f)` → `DistanceDisplay.Meters(850)` (arrotondamento al metro dentro il ramo metri)
    - `formatDistanceDisplay(999.6f)` → `DistanceDisplay.Meters(1000)` (Pitfall 5: il ramo si decide sul valore grezzo non arrotondato — comportamento accettato e bloccato dal test)
    - `formatDistanceDisplay(1000f)` → `DistanceDisplay.Kilometers(1.0f)` (soglia inclusiva: 1000 m è già km)
    - `formatDistanceDisplay(1234f)` → `DistanceDisplay.Kilometers(1.234f)` (valore km grezzo, l'arrotondamento a una decimale avviene solo a display via `%1$.1f`)
  </behavior>
  <action>
    Creare PRIMA `app/src/test/java/com/sed/tachimetro/distance/DistanceFormatTest.kt`, package `com.sed.tachimetro.distance`, classe `DistanceFormatTest`, un `@Test` per riga del blocco `<behavior>` con i nomi: `zeroMeters_returnsMetersZero`, `belowOneKilometer_returnsWholeMeters`, `fractionalMeters_areRoundedToNearestMeter`, `justBelowOneKilometer_staysInMetersBranch`, `exactlyOneKilometer_returnsKilometers`, `aboveOneKilometer_returnsRawKilometersValue`. Per il ramo `Meters` usare `assertEquals(DistanceDisplay.Meters(850), formatDistanceDisplay(850f))` (data class, uguaglianza strutturale su `Int`). Per il ramo `Kilometers` estrarre il valore e confrontarlo con delta, es. verificare che il risultato sia un'istanza di `DistanceDisplay.Kilometers` e poi `assertEquals(1.234f, (result as DistanceDisplay.Kilometers).value, 0.0001f)` — NON usare `assertEquals` strutturale su una data class con campo `Float`. Import consentiti: `org.junit.Assert.assertEquals`, `org.junit.Assert.assertTrue`, `org.junit.Test`. Eseguire il comando di verifica e confermare il FALLIMENTO (RED) prima di implementare.

    Creare `app/src/main/java/com/sed/tachimetro/distance/DistanceFormat.kt`, package `com.sed.tachimetro.distance`:
    - `sealed class DistanceDisplay` con `data class Meters(val value: Int) : DistanceDisplay()` e `data class Kilometers(val value: Float) : DistanceDisplay()`, KDoc di classe che cita D-01 e chiarisce che il modello porta solo il NUMERO: l'unità di misura vive in una view separata (D-02), quindi qui non compaiono stringhe "m"/"km".
    - `fun formatDistanceDisplay(meters: Float): DistanceDisplay` — se `meters < 1000f` restituisce `DistanceDisplay.Meters(meters.roundToInt())`, altrimenti `DistanceDisplay.Kilometers(meters / 1000f)`. Import `kotlin.math.roundToInt`. KDoc: citare D-01 (metri interi sotto 1 km, km con una decimale sopra), e documentare esplicitamente in un commento che la decisione del ramo usa il valore GREZZO non arrotondato, quindi 999.6 m rende "1000" nel ramo metri anziché "1,0" nel ramo km (edge case ~1 metro, accettato in 07-UI-SPEC.md — non è un difetto da correggere).
    Nessun import `android.*`, nessuna chiamata a `getString`/`String.format` dentro questo file: la resa localizzata è responsabilità di `MainActivity` (Piano 03).
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug :app:testDebugUnitTest</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:assembleDebug :app:testDebugUnitTest` termina con `BUILD SUCCESSFUL`
    - Il report `app/build/reports/tests/testDebugUnitTest/index.html` elenca `DistanceFormatTest` con 6 test eseguiti, 0 falliti, e `DistanceReducerTest` con 9 test eseguiti, 0 falliti
    - `DistanceFormat.kt` contiene `sealed class DistanceDisplay`, `data class Meters(val value: Int)`, `data class Kilometers(val value: Float)` e `fun formatDistanceDisplay(meters: Float): DistanceDisplay`
    - `DistanceFormat.kt` contiene la soglia letterale `1000f`
    - `grep -v '^\s*[*/]' app/src/main/java/com/sed/tachimetro/distance/DistanceFormat.kt | grep -c '^import android'` restituisce `0`
    - `DistanceFormat.kt` non contiene `getString` né `String.format`
    - `git diff --name-only` elenca solo file sotto `app/src/main/java/com/sed/tachimetro/distance/` e `app/src/test/java/com/sed/tachimetro/distance/`
  </acceptance_criteria>
  <done>I 6 test di formattazione passano insieme ai 9 del Task 1; il package `distance` espone il contratto completo consumato dal Piano 03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disco (SharedPreferences) → app | Il valore `distance_meters` letto da `tachimetro_prefs` è dato persistito, alterabile su dispositivo rooted o da un ripristino di backup malformato |
| app → disco | `DistanceStore.write()` scrive nella sandbox privata dell'app (`MODE_PRIVATE`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-T | Tampering | `DistanceStore.read()` | mitigate | `sanitizePersistedDistance()` clampa a `0f` qualunque valore negativo, esattamente come `sanitizePersistedMax()` per `MaxSpeedStore`; test dedicato `sanitizePersistedDistance_negativeValue_isClampedToZero` |
| T-07-01-T2 | Tampering | `reduceDistance()` | mitigate | Clamp difensivo di `currentTotalMeters` e `deltaMeters` negativi prima della somma: un totale corrotto non può far decrescere né esplodere l'accumulo; test `negativeCurrentTotal_treatedAsZero` e `negativeDelta_treatedAsZero` |
| T-07-01-I | Information disclosure | distanza persistita | accept | La distanza percorsa dall'ultimo azzeramento è un singolo scalare non sensibile, senza coordinate né timestamp; resta nella sandbox `MODE_PRIVATE`, non viene loggata e non lascia il dispositivo. Nessuna posizione GPS grezza viene mai persistita — coerente con il precedente non cifrato di `MaxSpeedStore` |
| T-07-01-D | Denial of service | scritture SharedPreferences | accept | `apply()` è asincrona e non blocca il main thread; la cadenza è limitata a 1 fix/secondo da `LocationRequest` (nessuna amplificazione possibile lato app) |
| T-07-01-E | Elevation of privilege | permessi | mitigate | Nessun nuovo permesso: `AndroidManifest.xml` resta invariato, questo piano non tocca né GPS né API di sistema |
| T-07-01-SC | Tampering | supply chain (Gradle) | accept | Nessun pacchetto installato: solo API di piattaforma e JUnit già presente. `gradle/libs.versions.toml` e `app/build.gradle.kts` restano invariati (verificato in acceptance criteria via `git diff --name-only`) |
</threat_model>

<verification>
- `./gradlew.bat :app:testDebugUnitTest --tests "com.sed.tachimetro.distance.*"` → BUILD SUCCESSFUL, 15 test verdi
- `./gradlew.bat :app:assembleDebug` → BUILD SUCCESSFUL
- `git diff --name-only` limitato ai 5 file dichiarati in `files_modified`
</verification>

<success_criteria>
- `reduceDistance` applica il gate D-04 (nessun accumulo sotto 2 km/h) ed è coperto da test JVM
- `sanitizePersistedDistance` riporta a 0 qualunque valore persistito negativo
- `DistanceStore` persiste un `Float` in metri su `tachimetro_prefs` con chiave `distance_meters`, senza troncamenti a `Int`
- `formatDistanceDisplay` implementa la soglia D-01 a 1000 m ed è coperto da test JVM
- Nessun file esistente modificato, nessuna nuova dipendenza
</success_criteria>

<output>
Create `.planning/phases/07-distanza-percorsa-e-reset-unificato/07-01-SUMMARY.md` when done
</output>
