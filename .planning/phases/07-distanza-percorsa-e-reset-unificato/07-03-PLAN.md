---
phase: 07-distanza-percorsa-e-reset-unificato
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - app/src/main/res/values/strings.xml
  - app/src/main/res/layout/activity_main.xml
  - app/src/main/java/com/sed/tachimetro/MainActivity.kt
autonomous: true
requirements: [DIST-01, DIST-02, DIST-03, MAX-04]

must_haves:
  truths:
    - "L'utente vede in basso a destra la distanza percorsa dall'ultimo azzeramento, con il numero a 32sp, visibilmente più grande dei 22sp dell'area MAX (D-03)"
    - "L'unità di misura è una view separata accanto al numero, come per unitText, e passa da 'm' a 'km' superati i 1000 metri (D-02)"
    - "La distanza aumenta a ogni fix GPS accettato con velocità sopra la soglia di rumore, e viene scritta su disco a ogni incremento"
    - "Riaprendo l'app la distanza precedentemente accumulata è già a schermo prima che arrivi il primo fix GPS"
    - "Un solo tocco sul pulsante, ora etichettato 'Azzera' invece di 'Azzera massimo', porta a zero sia la velocità massima sia la distanza (D-08)"
    - "L'area distanza è nascosta quando il permesso GPS è negato e torna visibile quando viene concesso"
    - "L'area distanza e il numero della velocità non introducono nuovi colori né animazioni"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "Formati distanza, unità e nuova etichetta del pulsante di reset"
      contains: "distance_km_format"
    - path: "app/src/main/res/layout/activity_main.xml"
      provides: "Coppia distanceText (32sp) + distanceUnitText (16sp) ancorata in basso a destra"
      contains: "@+id/distanceText"
    - path: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      provides: "Lettura persistita all'avvio, accumulo per fix, rendering adattivo, insets bottom-right, reset unificato"
      exports: ["MainActivity"]
  key_links:
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "com.sed.tachimetro.distance.reduceDistance"
      via: "chiamata nel ramo SpeedState.Reading di updatePlaceholder"
      pattern: "reduceDistance\\(currentDistanceMeters"
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "com.sed.tachimetro.distance.DistanceStore"
      via: "read() in onCreate e write() a ogni incremento e sul reset"
      pattern: "distanceStore\\.(read|write)"
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "com.sed.tachimetro.gps.SpeedState.Reading.deltaMeters"
      via: "consumo del delta esposto dal Piano 02"
      pattern: "state\\.deltaMeters"
    - from: "app/src/main/res/layout/activity_main.xml"
      to: "distanceUnitText"
      via: "unico elemento del gruppo ancorato a parent, riceve il listener degli insets"
      pattern: "layout_constraintEnd_toStartOf=\"@id/distanceUnitText\""
---

<objective>
Rendere visibile e viva la distanza: risorse stringa, coppia di view in basso a destra nel layout, e tutto il cablaggio in `MainActivity` — lettura del valore persistito prima dell'avvio del GPS, accumulo a ogni fix accettato, rendering adattivo m/km, gestione degli insets per il nuovo angolo, e unificazione del reset sul pulsante esistente rinominato "Azzera".

Purpose: chiude DIST-01 (area visibile in basso a destra, font più grande dell'area MAX), DIST-02 (accumulo solo in foreground, garantito dal `repeatOnLifecycle(STARTED)` già esistente), DIST-03 (persistenza a ogni incremento) e MAX-04 (un solo pulsante azzera entrambe le metriche).
Output: 3 file modificati, nessun file nuovo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-distanza-percorsa-e-reset-unificato/07-CONTEXT.md
@.planning/phases/07-distanza-percorsa-e-reset-unificato/07-UI-SPEC.md
@.planning/phases/07-distanza-percorsa-e-reset-unificato/07-PATTERNS.md
@.planning/phases/07-distanza-percorsa-e-reset-unificato/07-01-SUMMARY.md
@.planning/phases/07-distanza-percorsa-e-reset-unificato/07-02-SUMMARY.md
@app/src/main/java/com/sed/tachimetro/MainActivity.kt
@app/src/main/res/layout/activity_main.xml
@app/src/main/res/values/strings.xml
</context>

<interfaces>
<!-- Contratti già prodotti dai Piani 01 e 02. Usarli così come sono, senza esplorare il codice. -->

package `com.sed.tachimetro.distance` (Piano 01)
- `fun reduceDistance(currentTotalMeters: Float, deltaMeters: Float, kmh: Int, noiseFloorKmh: Double = 2.0): Float`
- `fun sanitizePersistedDistance(raw: Float): Float`
- `class DistanceStore(context: Context)` → `fun read(): Float`, `fun write(value: Float)`
- `sealed class DistanceDisplay` → `data class Meters(val value: Int)`, `data class Kilometers(val value: Float)`
- `fun formatDistanceDisplay(meters: Float): DistanceDisplay`

package `com.sed.tachimetro.gps` (Piano 02)
- `data class SpeedState.Reading(val kmh: Int, val deltaMeters: Float)`

Nuovi id di view introdotti da questo piano: `R.id.distanceText`, `R.id.distanceUnitText`.
Nuove stringhe introdotte da questo piano: `R.string.distance_meters_format`, `R.string.distance_km_format`, `R.string.unit_meters`, `R.string.unit_km`.
</interfaces>

<constraints>
- **Zero nuovi colori e zero animazioni** (PROJECT.md, vincolo v1.1): `distanceText`/`distanceUnitText` sono `@android:color/white` su sfondo nero, identiche nel trattamento a `maxSpeedText`/`unitText`. Il lime `lime_charging_accent` resta riservato esclusivamente all'icona di ricarica. Ogni cambio di visibilità è `View.VISIBLE` ↔ `View.GONE` istantaneo, mai una dissolvenza.
- **D-02:** l'unità di misura vive in una view separata (`distanceUnitText`), MAI concatenata dentro `distanceText` come fa `max_speed_format` con "MAX %1$d".
- **D-03:** `distanceText` è a `32sp` fissi, non autosize (l'unica view autosize dell'app resta `messageText`). `distanceUnitText` è a `16sp` fissi (scelta bloccata in 07-UI-SPEC.md Typography).
- **Pitfall 3:** per il valore in km usare SEMPRE `getString(R.string.distance_km_format, value)`, MAI `String.format(...)` nudo — `getString` formatta con la locale di configurazione corrente e rende "1,2" con la virgola su dispositivo it-IT.
- **Divergenza deliberata da `updateMaxArea()`:** l'area distanza resta SEMPRE visibile quando il permesso è concesso, anche a valore 0 (mostra "0 m" dopo un azzeramento). Non replicare il ramo "nascondi a zero" di `updateMaxArea()`: "0 m" dopo un reset è accurato, mentre "MAX 0" prima di qualsiasi lettura sarebbe fuorviante. Documentare la divergenza con un commento inline, così non venga scambiata per una svista in revisione.
- **MAX-04:** un solo punto di reset. Non aggiungere un secondo pulsante, non aggiungere un dialog di conferma, non batchare le scritture.
- Nessuna nuova dipendenza Gradle, nessun nuovo permesso, nessuna modifica ad `AndroidManifest.xml`.
- Non modificare i file dei Piani 01 e 02 (`distance/`, `gps/`): se qualcosa non torna, va segnalato nel SUMMARY, non aggirato riscrivendo il dominio.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Risorse stringa e coppia di view in basso a destra nel layout</name>
  <files>app/src/main/res/values/strings.xml, app/src/main/res/layout/activity_main.xml</files>
  <read_first>
    - app/src/main/res/values/strings.xml (contenuto attuale: convenzione dei formati `speed_kmh_format` `%1$d km/h`, `max_speed_format` `MAX %1$d`, unità nuda `unit_kmh`, e il valore attuale di `reset_max_button`)
    - app/src/main/res/layout/activity_main.xml (contenuto attuale: blocco `unitText` righe 33-45 come modello di piccola view di unità ancorata a un angolo, blocco `maxSpeedText` righe 60-74 come modello di etichetta a testo singolo, e conferma che nessuna view usa oggi la combinazione `constraintBottom_toBottomOf="parent"` + `constraintEnd_toEndOf="parent"`)
    - .planning/phases/07-distanza-percorsa-e-reset-unificato/07-UI-SPEC.md sezioni "Copywriting Contract", "Layout / positioning (bottom-right corner, DIST-01)" e "Typography" (contratto visivo vincolante)
  </read_first>
  <action>
    In `app/src/main/res/values/strings.xml`:
    - Cambiare il VALORE di `reset_max_button` da `Azzera massimo` a `Azzera` (D-08). NON rinominare la chiave: sia `activity_main.xml` sia `MainActivity.kt` la referenziano per chiave.
    - Aggiungere quattro nuove stringhe, in italiano come tutte le esistenti, subito dopo `reset_max_button`: `distance_meters_format` con valore `%1$d`, `distance_km_format` con valore `%1$.1f`, `unit_meters` con valore `m`, `unit_km` con valore `km`. I due formati contengono SOLO il numero: l'unità sta nelle due stringhe separate, coerentemente con D-02 e con il modo in cui `unit_kmh` è separata da `messageText`.

    In `app/src/main/res/layout/activity_main.xml`, aggiungere due `TextView` dentro il `ConstraintLayout` radice, DOPO il blocco `resetMaxButton` e PRIMA del blocco `chargingIcon`, dichiarando `distanceUnitText` per PRIMA e `distanceText` per seconda (`distanceText` referenzia `@id/distanceUnitText`, che deve quindi essere già dichiarata):
    - `distanceUnitText`: `@+id/distanceUnitText`, larghezza e altezza `wrap_content`, `android:layout_marginEnd="16dp"`, `android:layout_marginBottom="16dp"`, `android:text="@string/unit_meters"`, `android:textColor="@android:color/white"`, `android:textSize="16sp"`, `app:layout_constraintBottom_toBottomOf="parent"`, `app:layout_constraintEnd_toEndOf="parent"`, `tools:text="m"`. È l'UNICA view del gruppo ancorata a `parent`: per questo sarà l'unica a ricevere il listener degli insets nel Task 2.
    - `distanceText`: `@+id/distanceText`, larghezza e altezza `wrap_content`, `android:layout_marginEnd="4dp"`, `android:maxLines="1"`, `android:singleLine="true"`, `android:textColor="@android:color/white"`, `android:textSize="32sp"`, `app:layout_constraintEnd_toStartOf="@id/distanceUnitText"`, `app:layout_constraintBaseline_toBaselineOf="@id/distanceUnitText"`, `tools:text="850"`. Nessun vincolo verticale oltre alla baseline: la posizione verticale è interamente derivata da `distanceUnitText`.
    - NESSUN attributo `android:visibility` su queste due view (a differenza di `maxSpeedText`/`unitText`/`resetMaxButton` che partono `gone`): partono visibili perché `MainActivity.onCreate()` ne imposta il testo dal valore persistito prima che arrivi qualsiasi stato GPS, e "0 m" è un valore accurato da mostrare (divergenza documentata in 07-UI-SPEC.md).
    - Nessun attributo di colore diverso da `@android:color/white`, nessun `autoSize*`, nessun `textStyle`/`textFontWeight`.
    Non toccare i blocchi `messageText`, `unitText`, `retryButton`, `maxSpeedText`, `resetMaxButton`, `chargingIcon`, `keepScreenOnSwitch`.
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL` (un vincolo circolare o un `@id` non risolto farebbe fallire la compilazione delle risorse)
    - `strings.xml` contiene `<string name="reset_max_button">Azzera</string>` e non contiene più la stringa `Azzera massimo`
    - `strings.xml` contiene le quattro chiavi `distance_meters_format`, `distance_km_format`, `unit_meters`, `unit_km` con i valori `%1$d`, `%1$.1f`, `m`, `km`
    - `activity_main.xml` contiene `@+id/distanceUnitText` e `@+id/distanceText`, e la riga `@+id/distanceUnitText` compare PRIMA della riga `@+id/distanceText` nel file
    - `activity_main.xml` contiene `android:textSize="32sp"` e `android:textSize="16sp"`
    - `activity_main.xml` contiene `app:layout_constraintEnd_toStartOf="@id/distanceUnitText"` e `app:layout_constraintBaseline_toBaselineOf="@id/distanceUnitText"`
    - `grep -c 'lime' app/src/main/res/layout/activity_main.xml` restituisce `0`
    - `git diff --name-only` elenca solo `app/src/main/res/values/strings.xml` e `app/src/main/res/layout/activity_main.xml`
  </acceptance_criteria>
  <done>Le risorse esistono, il layout compila e l'angolo in basso a destra ospita la coppia numero (32sp) + unità (16sp) allineate sulla baseline.</done>
</task>

<task type="auto">
  <name>Task 2: MainActivity — binding, lettura persistita all'avvio, rendering adattivo e insets bottom-right</name>
  <files>app/src/main/java/com/sed/tachimetro/MainActivity.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt (contenuto attuale integrale: blocco import righe 40-46, campi righe 76-89, blocco MAX in `onCreate` righe 118-126 con il commento D-09, `showReady()` righe 280-286, `showDenied()` righe 288-306, `updateMaxArea()` righe 345-356, `applyUnitTextWindowInsets()` righe 495-510, `applyMaxAreaWindowInsets()` righe 512-537, `applyBottomLeftWindowInsets()` righe 560-589)
    - .planning/phases/07-distanza-percorsa-e-reset-unificato/07-UI-SPEC.md sezione "States" (tabella vincolante di quali stati mostrano o nascondono l'area distanza) e "Window insets"
    - .planning/phases/07-distanza-percorsa-e-reset-unificato/07-01-SUMMARY.md (firme effettive di `DistanceStore`, `formatDistanceDisplay`, `DistanceDisplay`, in caso di scostamenti dal piano)
  </read_first>
  <action>
    Aggiungere nel gruppo import `com.sed.tachimetro.*` (righe 40-46, ordine alfabetico: il package `distance` precede `gps`): `com.sed.tachimetro.distance.DistanceDisplay`, `com.sed.tachimetro.distance.DistanceStore`, `com.sed.tachimetro.distance.formatDistanceDisplay`.

    Aggiungere accanto ai campi dell'area MAX (righe 79-82): `private lateinit var distanceText: TextView`, `private lateinit var distanceUnitText: TextView`, `private lateinit var distanceStore: DistanceStore`, `private var currentDistanceMeters: Float = 0f`.

    In `onCreate()`, inserire un blocco distanza SUBITO DOPO `applyMaxAreaWindowInsets()` e SUBITO PRIMA di `maxSpeedStore = MaxSpeedStore(applicationContext)`: `distanceText = findViewById(R.id.distanceText)`, `distanceUnitText = findViewById(R.id.distanceUnitText)`, `applyDistanceAreaWindowInsets()`, `distanceStore = DistanceStore(applicationContext)` (WR-04: `applicationContext`, mai l'Activity), `currentDistanceMeters = distanceStore.read()`, `updateDistanceArea()`. Commentare l'ordine con due motivazioni esplicite: (a) D-09, leggere il valore persistito PRIMA di costruire `gpsSpeedProvider` evita il flash di "0 m" all'avvio; (b) `currentDistanceMeters` deve essere valorizzato PRIMA della prima chiamata a `updateMaxArea()`, perché nel Task 3 la visibilità di `resetMaxButton` dipenderà anche da questo campo.

    Aggiungere il metodo `private fun updateDistanceArea()` subito dopo `updateMaxArea()`: un `when (val display = formatDistanceDisplay(currentDistanceMeters))` esaustivo sui due rami di `DistanceDisplay` — ramo `Meters` → `distanceText.text = getString(R.string.distance_meters_format, display.value)` e `distanceUnitText.text = getString(R.string.unit_meters)`; ramo `Kilometers` → `distanceText.text = getString(R.string.distance_km_format, display.value)` e `distanceUnitText.text = getString(R.string.unit_km)`. Dopo il `when`, impostare incondizionatamente `distanceText.visibility = View.VISIBLE` e `distanceUnitText.visibility = View.VISIBLE`. KDoc/commento obbligatorio: citare D-01 per la soglia adattiva, D-02 per l'unità in view separata, Pitfall 3 per il motivo per cui si usa `getString` e non `String.format` (localizzazione della virgola decimale), e la divergenza deliberata da `updateMaxArea()` sul non nascondere a zero.

    In `showDenied()`, accanto alle righe che già nascondono `maxSpeedText` e `resetMaxButton`, aggiungere `distanceText.visibility = View.GONE` e `distanceUnitText.visibility = View.GONE` (07-UI-SPEC.md "States": con permesso negato tutta la UI delle metriche secondarie sparisce, resta solo il flusso di richiesta permesso).

    In `showReady()`, aggiungere `updateDistanceArea()` subito dopo la chiamata esistente a `updateMaxArea()`, così che dopo una concessione del permesso l'area torni visibile.

    Aggiungere il metodo `private fun applyDistanceAreaWindowInsets()` accanto agli altri `applyXWindowInsets()` (dopo `applyBottomLeftWindowInsets()`), modellato su `applyUnitTextWindowInsets()` ma con gli assi bottom+end: catturare PRIMA della registrazione i margini base dichiarati nell'XML leggendo `distanceUnitText.layoutParams as ConstraintLayout.LayoutParams` (`bottomMargin` ed `marginEnd`); dentro `ViewCompat.setOnApplyWindowInsetsListener(distanceUnitText) { view, insets -> ... }` calcolare `extraBottom = maxOf(systemBars.bottom, cutout.bottom)` ed `extraEnd = maxOf(systemBars.right, cutout.right)`, sommarli ai margini base e riassegnare `view.layoutParams`, restituendo `insets` non consumati. Registrare il listener SOLO su `distanceUnitText`: commentare che `distanceText` è agganciata a `distanceUnitText` su entrambi gli assi (end + baseline) e quindi si sposta di conseguenza senza bisogno di un secondo listener — a differenza di `applyMaxAreaWindowInsets()`, dove entrambe le view sono ancorate a `parent`.

    Non toccare in questo task `updatePlaceholder()`, `onResetMaxClicked()` né la logica di `updateMaxArea()` (sono del Task 3).
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug :app:testDebugUnitTest</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:assembleDebug :app:testDebugUnitTest` termina con `BUILD SUCCESSFUL`
    - `MainActivity.kt` contiene `private lateinit var distanceStore: DistanceStore` e `private var currentDistanceMeters: Float = 0f`
    - `MainActivity.kt` contiene `currentDistanceMeters = distanceStore.read()` e questa riga compare PRIMA della riga `currentMax = maxSpeedStore.read()` nel file
    - `MainActivity.kt` contiene `private fun updateDistanceArea()` e `private fun applyDistanceAreaWindowInsets()`
    - `updateDistanceArea()` contiene sia `getString(R.string.distance_meters_format` sia `getString(R.string.distance_km_format`
    - `MainActivity.kt` non contiene `String.format`
    - `grep -c 'setOnApplyWindowInsetsListener(distanceUnitText)' app/src/main/java/com/sed/tachimetro/MainActivity.kt` restituisce `1`
    - `MainActivity.kt` non contiene `setOnApplyWindowInsetsListener(distanceText)`
    - `showDenied()` contiene `distanceText.visibility = View.GONE` e `distanceUnitText.visibility = View.GONE`
    - `git diff --name-only` elenca solo `app/src/main/java/com/sed/tachimetro/MainActivity.kt` (più i due file del Task 1 già committati)
  </acceptance_criteria>
  <done>All'avvio l'app mostra subito la distanza persistita nell'angolo in basso a destra, correttamente formattata e mai coperta da barre di sistema o cutout; con permesso negato l'area sparisce.</done>
</task>

<task type="auto">
  <name>Task 3: Accumulo per fix accettato e reset unificato sul pulsante "Azzera"</name>
  <files>app/src/main/java/com/sed/tachimetro/MainActivity.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt (stato corrente dopo il Task 2: ramo `is SpeedState.Reading` di `updatePlaceholder()` righe ~316-332 con il blocco `reduceMax`/`maxSpeedStore.write` e il suo commento D-07, `onResetMaxClicked()` righe ~339-343, `updateMaxArea()` righe ~347-356, e la registrazione del listener `resetMaxButton.setOnClickListener { ... }` in `onCreate()`)
    - .planning/phases/07-distanza-percorsa-e-reset-unificato/07-02-SUMMARY.md (conferma che `SpeedState.Reading` espone `deltaMeters` e con quale semantica)
    - .planning/phases/07-distanza-percorsa-e-reset-unificato/07-UI-SPEC.md sezione "Reset Button — existing component, behavior extended (MAX-04)" (regola vincolante di visibilità del pulsante)
  </read_first>
  <action>
    Aggiungere l'import `com.sed.tachimetro.distance.reduceDistance` nel gruppo `com.sed.tachimetro.*`.

    In `updatePlaceholder()`, ramo `is SpeedState.Reading ->`, subito DOPO il blocco esistente che aggiorna e persiste il massimo: aggiungere `val newDistance = reduceDistance(currentDistanceMeters, state.deltaMeters, state.kmh)` e, se `newDistance != currentDistanceMeters`, assegnare `currentDistanceMeters = newDistance` e chiamare `distanceStore.write(currentDistanceMeters)`. Commento obbligatorio: citare DIST-03 (scrittura immediata a ogni incremento, nessun batching su `onPause()`/`onStop()`, così un kill del processo non perde gli ultimi metri) e D-04 (il gate della soglia di rumore vive dentro `reduceDistance`, non qui: a veicolo fermo `state.kmh` vale 0 e la funzione restituisce il totale invariato, quindi non c'è nemmeno una scrittura su disco). Non toccare il blocco `reduceMax` esistente.

    Alla fine di `updatePlaceholder()`, subito dopo la chiamata esistente a `updateMaxArea()`, aggiungere `updateDistanceArea()` — chiamata incondizionatamente a ogni emissione, esattamente come `updateMaxArea()`, così l'area resta visibile e coerente anche nei rami `Searching`/`NoSignal` (dove mostra congelato l'ultimo totale, senza testo di errore né indicatore di pausa: DIST-02, 07-UI-SPEC.md "States").

    Rinominare `onResetMaxClicked()` in `onResetClicked()` (il pulsante ora è generico, "Azzera") e aggiornare di conseguenza il `resetMaxButton.setOnClickListener { onResetClicked() }` in `onCreate()`. Estendere il corpo del metodo, DOPO le righe esistenti che azzerano e persistono il massimo, con: `currentDistanceMeters = 0f`, `distanceStore.write(0f)`, `updateDistanceArea()`. Aggiornare il commento del metodo citando MAX-04: unico punto di reset dell'app, un solo tocco azzera entrambe le metriche, nessun dialog di conferma, entrambe le scritture immediate. NON aggiungere un secondo pulsante né un secondo handler.

    In `updateMaxArea()`, separare la visibilità del pulsante da quella dell'etichetta: `maxSpeedText` mantiene esattamente il comportamento attuale (testo + `VISIBLE` se `currentMax > 0`, altrimenti `GONE`), mentre `resetMaxButton.visibility` diventa `View.VISIBLE` quando `currentMax > 0 || currentDistanceMeters > 0f`, altrimenti `View.GONE`. Commento obbligatorio: MAX-04/07-UI-SPEC.md — dato che l'area distanza è sempre visibile e indipendente dallo stato dell'area MAX, il pulsante unico deve restare raggiungibile finché almeno una delle due metriche ha qualcosa da azzerare. Non toccare `showDenied()`, che continua a forzare `GONE` su entrambe le view dell'area MAX.
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug :app:testDebugUnitTest</automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew.bat :app:assembleDebug :app:testDebugUnitTest` termina con `BUILD SUCCESSFUL` e nessun test unitario preesistente regredisce (`DistanceReducerTest` 9, `DistanceFormatTest` 6, `GpsSpeedProviderStateTest` 7, `MaxSpeedReducerTest` invariato — tutti 0 falliti)
    - `MainActivity.kt` contiene `reduceDistance(currentDistanceMeters, state.deltaMeters, state.kmh)`
    - `MainActivity.kt` contiene `distanceStore.write(currentDistanceMeters)` e `distanceStore.write(0f)`
    - `MainActivity.kt` contiene `private fun onResetClicked()` e non contiene più alcuna occorrenza di `onResetMaxClicked`
    - `grep -c 'setOnClickListener { onResetClicked() }' app/src/main/java/com/sed/tachimetro/MainActivity.kt` restituisce `1` (un solo punto di reset, nessun secondo pulsante)
    - `MainActivity.kt` contiene `currentMax > 0 || currentDistanceMeters > 0f`
    - `grep -v '^\s*//' app/src/main/java/com/sed/tachimetro/MainActivity.kt | grep -c 'updateDistanceArea()'` restituisce almeno `5` (dichiarazione + chiamate in `onCreate`, `showReady`, `updatePlaceholder`, `onResetClicked`; il filtro esclude le righe di commento)
    - `MainActivity.kt` non contiene `AlertDialog` né alcun testo di conferma del reset
    - `git diff --name-only` elenca solo i 3 file dichiarati in `files_modified`
  </acceptance_criteria>
  <done>La distanza cresce a ogni fix accettato sopra soglia e viene persistita subito; il pulsante "Azzera" porta a zero massimo e distanza nello stesso tocco e resta disponibile finché una delle due metriche è diversa da zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| disco (SharedPreferences) → UI | Il valore letto da `distanceStore.read()` in `onCreate()` viene mostrato direttamente all'utente |
| utente → app | Il tocco su `resetMaxButton` è l'unico input utente che modifica in modo distruttivo lo stato persistito |
| sistema Android → app | Gli insets di system bars/display cutout arrivano dal sistema e pilotano i margini delle nuove view |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-T | Tampering | `distanceStore.read()` in `onCreate()` | mitigate | La sanitizzazione avviene dentro `DistanceStore.read()` (Piano 01, `sanitizePersistedDistance`): `MainActivity` non legge mai `prefs` direttamente e non può bypassare il clamp. Verificato: `MainActivity.kt` non contiene `getSharedPreferences` per la distanza |
| T-07-03-R | Repudiation | reset distruttivo senza conferma | accept | Il tocco su "Azzera" è irreversibile e senza dialog di conferma: è la UX già spedita in Fase 4 per il massimo, coerente con la filosofia "immediato, nessun dialog" del prodotto. Dato non critico (una metrica personale), nessun requisito di audit trail |
| T-07-03-I | Information disclosure | area distanza a schermo | accept | La distanza percorsa è visibile su uno schermo montato in auto: è esattamente l'informazione che l'utente ha chiesto di vedere. Nessuna coordinata, nessun percorso, nessuna cronologia viene mostrata o memorizzata (tracciamento percorso è esplicitamente Out of Scope in REQUIREMENTS.md) |
| T-07-03-D | Denial of service | scritture su `updatePlaceholder` | mitigate | La scrittura avviene solo quando `newDistance != currentDistanceMeters`, quindi mai a veicolo fermo (D-04 azzera il delta), e comunque al massimo 1 volta/secondo per la cadenza di `LocationRequest`; `apply()` è asincrona e non blocca il main thread |
| T-07-03-D2 | Denial of service | tracking in background | mitigate | Nessun servizio in foreground, nessun `WorkManager`, nessun wake lock aggiuntivo: l'accumulo avviene solo dentro il collettore già racchiuso in `repeatOnLifecycle(STARTED)`, che smette in `onStop()` e via `WhileSubscribed()` interrompe anche gli aggiornamenti di posizione a monte (DIST-02) |
| T-07-03-E | Elevation of privilege | permessi | mitigate | Nessun permesso nuovo, `AndroidManifest.xml` invariato: la distanza riusa lo stesso `ACCESS_FINE_LOCATION` già concesso per la velocità |
| T-07-03-SC | Tampering | supply chain (Gradle) | accept | Nessun pacchetto installato: solo risorse XML e API di piattaforma già in uso. `gradle/libs.versions.toml` e `app/build.gradle.kts` invariati |
</threat_model>

<verification>
- `./gradlew.bat :app:assembleDebug :app:testDebugUnitTest` → BUILD SUCCESSFUL, tutti i test unitari verdi
- `git diff --name-only` limitato ai 3 file dichiarati in `files_modified`
- `grep -rn 'lime' app/src/main/res/layout/activity_main.xml` → nessun risultato (nessun colore accento introdotto dall'area distanza)
- `grep -c 'ValueAnimator\|ObjectAnimator' app/src/main/java/com/sed/tachimetro/MainActivity.kt` invariato rispetto a prima della fase (nessuna nuova animazione)
</verification>

<success_criteria>
- L'area in basso a destra mostra numero (32sp) + unità (16sp) con formato adattivo m/km e resta libera da barre di sistema e cutout
- La distanza si accumula a ogni fix accettato sopra soglia e viene scritta su disco immediatamente
- Il valore persistito è già a schermo prima del primo fix GPS dopo una riapertura
- Il pulsante "Azzera" azzera massimo e distanza in un solo tocco e resta visibile finché una delle due metriche è > 0
- Nessun nuovo colore, nessuna nuova animazione, nessun nuovo permesso, nessuna nuova dipendenza
</success_criteria>

<output>
Create `.planning/phases/07-distanza-percorsa-e-reset-unificato/07-03-SUMMARY.md` when done
</output>
