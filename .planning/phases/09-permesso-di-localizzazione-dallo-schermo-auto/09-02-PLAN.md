---
phase: 09-permesso-di-localizzazione-dallo-schermo-auto
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt
autonomous: true
requirements: [AA-04]
user_setup: []

must_haves:
  truths:
    - "Al primo collegamento Android Auto senza permesso concesso, lo schermo auto avvia da solo la richiesta di sistema e mostra 'Controlla il telefono', senza nessuna azione preliminare sull'auto (D-05, D-01)"
    - "Concedendo il permesso dal dialogo mostrato sul telefono, lo schermo auto passa da solo alla velocita' (o a 'Ricerca segnale...') senza riavviare l'app ne' il collegamento (SC2)"
    - "Negando il permesso, lo schermo auto mostra un messaggio esplicito e un'azione 'Riprova' che rilancia la richiesta senza uscire dall'app auto (D-02, D-03)"
    - "Dopo un secondo rifiuto il messaggio cambia e l'azione diventa 'Apri impostazioni', che apre la scheda dell'app nelle impostazioni del telefono invece di ritentare a vuoto (D-04)"
    - "Il dialogo non viene mai rilanciato automaticamente in loop dopo un rifiuto gia' ricevuto, ne' due volte in parallelo mentre una richiesta e' ancora pendente (D-06, Pitfall 2)"
    - "Lo schermo auto non colleziona mai GpsSpeedProvider senza permesso concesso: la garanzia difensiva T-08-08 resta intatta"
  artifacts:
    - path: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      provides: "Macchina a stati del permesso lato auto, richiesta automatica, retry/impostazioni e rendering di tutti gli stati nel PaneTemplate"
      contains: "resolveCarPermissionState("
      min_lines: 150
  key_links:
    - from: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      to: "CarContext.requestPermissions"
      via: "richiesta esplicita del permesso dallo schermo auto (AA-04)"
      pattern: "requestPermissions\\("
    - from: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      to: "com.sed.tachimetro.car.CarPermissionDenialStore"
      via: "lettura e incremento del contatore dei rifiuti"
      pattern: "CarPermissionDenialStore\\("
    - from: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      to: "androidx.car.app.model.ParkedOnlyOnClickListener"
      via: "Action di retry/impostazioni eseguibile solo a veicolo fermo"
      pattern: "ParkedOnlyOnClickListener\\.create"
    - from: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      to: "android.provider.Settings.ACTION_APPLICATION_DETAILS_SETTINGS"
      via: "apertura delle impostazioni dell'app sul telefono dal rifiuto permanente"
      pattern: "ACTION_APPLICATION_DETAILS_SETTINGS"
    - from: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      to: "androidx.car.app.model.Pane.Builder.addAction"
      via: "Action di retry/impostazioni nel PaneTemplate"
      pattern: "addAction\\("
---

<objective>
Sostituire in `SpeedScreen` il gate difensivo passivo T-08-08 (che lascia lo schermo auto bloccato per sempre su "Ricerca segnale..." quando il permesso manca) con il flusso completo di richiesta del permesso dallo schermo auto: richiesta automatica al primo collegamento, attesa "Controlla il telefono", transizione automatica alla velocita' alla concessione, messaggio + azione di retry al rifiuto, messaggio + "Apri impostazioni" al rifiuto permanente.

Purpose: e' il deliverable della fase (AA-04) — l'unico punto in cui l'utente puo' sbloccare l'app senza mai aprirla sul telefono.
Output: `SpeedScreen.kt` con macchina a stati del permesso reattiva, `CarContext.requestPermissions()` cablato, e tutti gli stati resi nel `PaneTemplate` gia' lockato in Fase 8.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-permesso-di-localizzazione-dallo-schermo-auto/09-CONTEXT.md
@.planning/phases/09-permesso-di-localizzazione-dallo-schermo-auto/09-RESEARCH.md
@.planning/phases/09-permesso-di-localizzazione-dallo-schermo-auto/09-PATTERNS.md
@.planning/phases/09-permesso-di-localizzazione-dallo-schermo-auto/09-01-SUMMARY.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-UI-SPEC.md

<interfaces>
<!-- Contratti prodotti dal Piano 01 e gia' esistenti. L'esecutore li usa direttamente. -->

Da app/src/main/java/com/sed/tachimetro/car/CarPermissionState.kt (Piano 01):
- `sealed class CarPermissionState`
- `data object CarPermissionState.Granted`
- `data object CarPermissionState.NotRequested`
- `data object CarPermissionState.Waiting`
- `data class CarPermissionState.Denied(val permanent: Boolean)`
- `fun resolveCarPermissionState(granted: Boolean, denialCount: Int): CarPermissionState` — non produce mai `Waiting`

Da app/src/main/java/com/sed/tachimetro/car/CarPermissionDenialStore.kt (Piano 01):
- `class CarPermissionDenialStore(context: Context)`
- `fun denialCount(): Int` (gia' sanitizzato)
- `fun recordDenial()`

Da app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt (INVARIATO, non modificare):
- `fun carSpeedContent(state: SpeedState): CarSpeedContent` -> `Speed(kmh)` | `Searching`

Da app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt:
- `val gpsSpeedProvider: GpsSpeedProvider`, con `val state: StateFlow<SpeedState>` (ticker interno a 1 Hz)

Risorse stringa disponibili (Piano 01 + esistenti):
- `R.string.car_check_your_phone` = "Controlla il telefono"
- `R.string.car_permission_denied` = "Permesso GPS necessario"
- `R.string.car_permission_denied_permanent` = "Permesso negato. Apri le impostazioni sul telefono"
- `R.string.retry` = "Riprova" — titolo Action per il rifiuto NON permanente
- `R.string.open_settings` = "Apri impostazioni" — titolo Action per il rifiuto permanente
- `R.string.car_searching_gps_signal` = "Ricerca segnale..." / `R.string.unit_kmh` = "km/h"

API Car App Library 1.7.0 usate (tutte disponibili a CarAppApiLevel 1, verificate a sorgente in 09-RESEARCH.md):
- `CarContext.requestPermissions(permissions: List<String>, executor: Executor, listener: OnRequestPermissionsListener)` — `listener` e' un'interfaccia SAM `(approved: List<String>, rejected: List<String>) -> Unit`, quindi utilizzabile come lambda Kotlin
- `CarContext.startActivity(intent: Intent)` — `ContextWrapper.startActivity` non sovrascritto: lancia una normale Activity sul telefono, richiede `FLAG_ACTIVITY_NEW_TASK`
- `androidx.car.app.model.ParkedOnlyOnClickListener.create { ... }` — l'host esegue il blocco solo a veicolo fermo e mostra da solo il messaggio "solo da fermi" altrimenti
- `androidx.car.app.model.Pane.Builder.addAction(Action)` — max 2 azioni, nessuna icona obbligatoria
- `androidx.car.app.model.Action.Builder().setTitle(CharSequence).setOnClickListener(OnClickListener).build()`
- `androidx.core.content.ContextCompat.getMainExecutor(context)` per l'Executor del callback
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Macchina a stati del permesso e richiesta automatica in SpeedScreen</name>
  <files>app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt (stato attuale: il blocco `init` righe 54-77 con il gate T-08-08 da SOSTITUIRE, il campo `provider` riga 43, `latestState` riga 48, `templateBuildCount` riga 52)
    - app/src/main/java/com/sed/tachimetro/car/CarPermissionState.kt (Piano 01: i quattro stati e la semantica del resolver)
    - app/src/main/java/com/sed/tachimetro/car/CarPermissionDenialStore.kt (Piano 01: `denialCount()` / `recordDenial()`)
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt righe 100-113 (`permissionGranted: MutableStateFlow<Boolean>` + callback del launcher che richiama `refreshPermissionState()`), righe 217-230 (`setupGpsCollection()`: `repeatOnLifecycle(STARTED)` + `collectLatest`), righe 305-341 (`refreshPermissionState()`, `checkAndRequestPermission()`, `onRetryClicked()`, `openAppSettings()`)
    - .planning/phases/09-permesso-di-localizzazione-dallo-schermo-auto/09-RESEARCH.md sezioni "Pattern 3" e "Common Pitfalls" (Pitfall 1 richiesta silenziosamente ignorata alla guida, Pitfall 2 doppia richiesta concorrente)
    - .planning/phases/09-permesso-di-localizzazione-dallo-schermo-auto/09-PATTERNS.md sezione "Reactive permission-state flow" (la forma `collectLatest` da rispecchiare)
  </read_first>
  <action>
Riscrivere il blocco `init` di `app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt` sostituendo il gate T-08-08 con una macchina a stati reattiva. Nessun altro file va toccato.

1. Nuovi campi privati della classe:
   - `private val denialStore = CarPermissionDenialStore(carContext.applicationContext)` — WR-04: si passa `applicationContext`, mai il `CarContext`
   - `private val permissionState = MutableStateFlow<CarPermissionState>(CarPermissionState.NotRequested)` — sorgente di verita' reattiva dello stato del permesso, mirror esatto di `MainActivity.permissionGranted`
   - `private var requestInFlight = false` — guardia in memoria contro una seconda `requestPermissions()` lanciata mentre la prima non ha ancora risposto (Pitfall 2: `repeatOnLifecycle` riparte a ogni rientro in STARTED e il contatore persistito da solo non copre questo caso, perche' nessun rifiuto e' ancora stato registrato)

2. `private fun refreshPermissionState()`: se `requestInFlight` e' true NON fa nulla (una richiesta e' pendente, il callback e' l'autorita'); altrimenti calcola
   `permissionState.value = resolveCarPermissionState(granted = ContextCompat.checkSelfPermission(carContext, Manifest.permission.ACCESS_FINE_LOCATION) == PackageManager.PERMISSION_GRANTED, denialCount = denialStore.denialCount())`.
   Commento CR-01-equivalente: unica funzione che spinge lo stato corrente dentro `permissionState`, esattamente come `MainActivity.refreshPermissionState()`.

3. Nuovo blocco `init`, che sostituisce interamente quello attuale, mantenendo la forma `lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { ... } }` gia' usata (nessun secondo meccanismo di collection parallelo):
   - come prima cosa dentro `repeatOnLifecycle`, chiamare `refreshPermissionState()` — cosi' un permesso concesso nel frattempo dalle impostazioni del telefono viene raccolto al rientro in STARTED
   - poi `permissionState.collectLatest { state -> ... }`. `collectLatest` (non `collect`) e' obbligatorio: quando lo stato cambia da `Granted` a qualcos'altro la raccolta GPS in corso deve essere cancellata, e quando cambia da `Waiting` a `Granted` la raccolta deve partire senza attendere un nuovo ciclo di lifecycle — e' questo che soddisfa SC2 (transizione automatica senza riavvio di app o collegamento)
   - dentro la lambda: chiamare `invalidate()` per riflettere subito il nuovo stato nel template, poi un `when (state)` esaustivo senza `else`:
     - `CarPermissionState.NotRequested` -> se `!requestInFlight`, chiamare `requestLocationPermission()` (D-05: richiesta automatica, nessuna azione preliminare richiesta all'utente sull'auto)
     - `CarPermissionState.Granted` -> `provider?.gpsSpeedProvider?.state?.collect { latestState = it; invalidate() }` (identico al comportamento attuale, cadenza 1 Hz ereditata dal ticker del provider, nessun timer lato auto)
     - `CarPermissionState.Waiting` -> nessuna azione: il dialogo e' aperto sul telefono, il template mostra gia' "Controlla il telefono"
     - `is CarPermissionState.Denied` -> nessuna azione (D-06: dopo un rifiuto gia' ricevuto il dialogo NON viene mai rilanciato automaticamente; si riparte solo dal tocco esplicito sull'Action)

4. `private fun requestLocationPermission()`, nell'ordine esatto:
   - `requestInFlight = true`
   - `carContext.requestPermissions(listOf(Manifest.permission.ACCESS_FINE_LOCATION), ContextCompat.getMainExecutor(carContext)) { _, _ -> ... }` — il callback usa la forma lambda (SAM conversion, nessun `object :`)
   - `permissionState.value = CarPermissionState.Waiting` (dopo la chiamata, cosi' che la cancellazione di `collectLatest` non possa mai precedere la richiesta)
   Nel callback, nell'ordine:
   - `requestInFlight = false`
   - rileggere la verita' dal sistema invece di fidarsi delle liste: `val granted = ContextCompat.checkSelfPermission(carContext, Manifest.permission.ACCESS_FINE_LOCATION) == PackageManager.PERMISSION_GRANTED` (stessa disciplina di `MainActivity`, il cui callback chiama `refreshPermissionState()`)
   - se `granted`: `permissionState.value = CarPermissionState.Granted`
   - altrimenti: catturare `val wasAlreadyDenied = denialStore.denialCount() > 0` PRIMA di incrementare, poi `denialStore.recordDenial()`, poi `permissionState.value = CarPermissionState.Denied(permanent = wasAlreadyDenied)`. L'ordine e' vincolante: incrementare prima renderebbe permanente gia' il primo rifiuto.
   `recordDenial()` va chiamato SOLO qui: nessun altro punto del file deve incrementare il contatore.

5. `private fun onRetryOrSettingsClicked()` — invocata dall'Action del Task 2 (finche' il Task 2 non la collega, il compilatore emettera' solo un warning di funzione non usata, non un errore):
   - se `requestInFlight` e' true, non fare nulla (evita doppie richieste da tocchi ripetuti)
   - `val current = permissionState.value`; se `current is CarPermissionState.Denied && current.permanent` -> `openAppSettingsFromCar()`, altrimenti -> `requestLocationPermission()`
   E' il mirror esatto di `MainActivity.onRetryClicked()`, con `current.permanent` al posto di `shouldShowRequestPermissionRationale()` (D-04: quell'API richiede un'Activity, che uno `Screen` non ha).

6. `private fun openAppSettingsFromCar()`: costruire `Intent(Settings.ACTION_APPLICATION_DETAILS_SETTINGS)` con `data = Uri.fromParts("package", carContext.packageName, null)` e `addFlags(Intent.FLAG_ACTIVITY_NEW_TASK)`, poi `carContext.startActivity(intent)`. Il flag e' obbligatorio perche' il chiamante non e' un'Activity. Il `package` proviene sempre da `carContext.packageName` (l'app stessa), mai da un dato ricevuto dall'host.

7. Commenti inline obbligatori, greppabili:
   - sopra il gate/macchina a stati: il commento T-08-08 attuale va AGGIORNATO, non cancellato — deve dire che il gate difensivo resta valido (nessuna collect senza permesso concesso, `GpsSpeedProvider` e' `@Suppress("MissingPermission")` e solleverebbe `SecurityException`) ma non e' piu' passivo: ora la Fase 9 lo accompagna con la richiesta esplicita
   - sopra `requestLocationPermission()`: un commento contenente la stringa letterale `Pitfall 1` che documenta il limite di piattaforma noto — se Android Auto si collega mentre il veicolo e' gia' in movimento, l'host puo' ignorare silenziosamente la richiesta (Javadoc di `requestPermissions()`), e non e' documentato se il callback venga comunque invocato; in quel caso lo schermo puo' restare su "Controlla il telefono" finche' il veicolo non e' fermo e un nuovo ingresso in STARTED non rivaluta lo stato. Limite accettato e documentato per questa milestone, non un bug da correggere qui
   - sopra `requestInFlight`: riferimento a `Pitfall 2`
   - tag decisione (D-01, D-04, D-05, D-06, SC2, AA-04) nei punti corrispondenti, come da convenzione del progetto

8. Nuovi import espliciti da aggiungere: `android.content.Intent`, `android.net.Uri`, `android.provider.Settings`, `kotlinx.coroutines.flow.MutableStateFlow`, `kotlinx.coroutines.flow.collectLatest`. Mai wildcard. `onGetTemplate()` NON viene toccato in questo task (e' il Task 2).

9. Vincoli invariati dalla Fase 8, da non violare: nessun `delay()`/throttle lato auto; nessun accesso a `MaxSpeedStore`, `DistanceStore`, `reduceMax`, `reduceDistance`, `ScreenOnPreferenceStore`; nessuna modifica a `CarSpeedContent.kt`; il cast al provider resta `as?` (mai forzato).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && ./gradlew.bat :app:testDebugUnitTest --console=plain -q</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt && C=$(grep -v '^\s*[/*]' $F) && echo "$C" | grep -c 'carContext.requestPermissions(' && echo "$C" | grep -c 'CarPermissionDenialStore(carContext.applicationContext)' && echo "$C" | grep -c 'resolveCarPermissionState(' && echo "$C" | grep -c 'collectLatest' && echo "$C" | grep -c 'recordDenial()' && echo "$C" | grep -c 'ACTION_APPLICATION_DETAILS_SETTINGS' && echo "$C" | grep -c 'FLAG_ACTIVITY_NEW_TASK' && test "$(echo "$C" | grep -c 'recordDenial()')" -eq 1 && test "$(echo "$C" | grep -c 'delay(\|MaxSpeedStore\|DistanceStore\|ScreenOnPreferenceStore\|shouldShowRequestPermissionRationale')" -eq 0 && test "$(echo "$C" | grep -c 'applicationContext as TachimetroApplication')" -eq 0 && grep -c 'Pitfall 1' $F && grep -c 'Pitfall 2' $F</automated>
  </verify>
  <acceptance_criteria>
    - `SpeedScreen.kt` dichiara `private val permissionState = MutableStateFlow<CarPermissionState>(...)`, `private val denialStore = CarPermissionDenialStore(carContext.applicationContext)` e `private var requestInFlight = false`
    - Il blocco `init` mantiene `lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { ... } }` e al suo interno chiama `refreshPermissionState()` prima di `permissionState.collectLatest { ... }`
    - Il `when` dentro `collectLatest` copre tutti e quattro gli stati senza `else`, e la `collect` di `provider?.gpsSpeedProvider?.state` avviene esclusivamente nel ramo `Granted`
    - `refreshPermissionState()` non modifica lo stato quando `requestInFlight` e' true
    - `requestLocationPermission()` imposta `requestInFlight = true` prima di chiamare `carContext.requestPermissions(...)` e imposta `CarPermissionState.Waiting` dopo la chiamata
    - Nel callback la concessione e' determinata da `ContextCompat.checkSelfPermission(...) == PackageManager.PERMISSION_GRANTED`, non dalle liste `approved`/`rejected`
    - `wasAlreadyDenied` e' letto PRIMA di `recordDenial()`, e `Denied(permanent = wasAlreadyDenied)` ne usa il valore
    - `recordDenial()` compare esattamente una volta nel file (fuori dai commenti)
    - `onRetryOrSettingsClicked()` apre le impostazioni solo quando lo stato e' `Denied(permanent = true)`, altrimenti rilancia `requestLocationPermission()`, e non fa nulla se `requestInFlight` e' true
    - `openAppSettingsFromCar()` usa `Settings.ACTION_APPLICATION_DETAILS_SETTINGS`, `Uri.fromParts("package", carContext.packageName, null)` e `Intent.FLAG_ACTIVITY_NEW_TASK`
    - Il file contiene un commento con la stringa letterale `Pitfall 1` che descrive il limite "richiesta ignorata alla guida" e uno con `Pitfall 2` sopra `requestInFlight`
    - Il commento `T-08-08` e' ancora presente e aggiornato (non rimosso)
    - `grep -c 'delay(\|MaxSpeedStore\|DistanceStore\|ScreenOnPreferenceStore\|shouldShowRequestPermissionRationale'` sul file, escludendo i commenti, restituisce 0
    - `git diff --name-only` contiene solo `app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt`
    - `./gradlew.bat :app:assembleDebug` e `./gradlew.bat :app:testDebugUnitTest` terminano entrambi con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>Lo schermo auto richiede da solo il permesso al primo collegamento, registra i rifiuti, e passa reattivamente alla raccolta GPS nell'istante in cui il permesso viene concesso, senza riavvii.</done>
</task>

<task type="auto">
  <name>Task 2: Rendering degli stati di permesso nel PaneTemplate con Action di retry/impostazioni</name>
  <files>app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt (stato dopo il Task 1: `onGetTemplate()` righe ~79-109 ancora nella forma della Fase 8, con `templateBuildCount`, il log `BuildConfig.DEBUG` e la costruzione della Row da `carSpeedContent`)
    - app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt (contratto puro da riusare NON MODIFICATO nel solo ramo Granted)
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt righe 360-375 (`showDenied()`: lo switch a due stati messaggio+etichetta pulsante su `permanentlyDenied`, da rispecchiare identico sull'auto)
    - app/src/main/res/values/strings.xml (le tre stringhe auto create dal Piano 01)
    - .planning/phases/09-permesso-di-localizzazione-dallo-schermo-auto/09-RESEARCH.md sezione "Pattern 4" (vincoli di `Pane.addAction` e semantica di `ParkedOnlyOnClickListener`)
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-UI-SPEC.md sezioni "Content Contract" e "States" (regole invariate: nessun titolo/branding nel template, nessuna icona nella Row)
    - scripts/dhu-quota-check.ps1 (conta le righe logcat che contengono `onGetTemplate #`: il formato del prefisso NON puo' cambiare)
  </read_first>
  <action>
Estendere `onGetTemplate()` in `app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt` perche' renda tutti gli stati del permesso, e collegare l'Action di retry/impostazioni.

1. Estrarre la costruzione del template in una funzione pubblica usata come seam di test:
   `fun buildTemplate(permission: CarPermissionState, speed: SpeedState): PaneTemplate`.
   `onGetTemplate(): Template` resta l'override e si limita a: incrementare `templateBuildCount`, emettere il log diagnostico, e restituire `buildTemplate(permissionState.value, latestState)`.
   KDoc su `buildTemplate` che dichiara perche' e' pubblica: e' il seam che permette al test strumentato del Piano 03 di verificare la forma del template per ogni stato del permesso senza dipendere dallo stato reale del permesso sul dispositivo di test. Non ha effetti collaterali e non legge campi mutabili.

2. `buildTemplate` costruisce un `Pane.Builder()` e ramifica con un `when (permission)` esaustivo senza `else`:
   - `CarPermissionState.Granted` -> ramo invariato dalla Fase 8: `carSpeedContent(speed)` e le due `Row` gia' esistenti (`Speed` -> `setTitle(kmh)` + `addText(R.string.unit_kmh)`; `Searching` -> `setTitle(R.string.car_searching_gps_signal)` senza `addText`). Nessuna Action.
   - `CarPermissionState.NotRequested` e `CarPermissionState.Waiting` -> una sola `Row` con `setTitle(carContext.getString(R.string.car_check_your_phone))`, nessun `addText`, nessuna Action (D-01: il dialogo e' sul telefono, sull'auto non c'e' nulla da toccare; D-06: nessun retry offerto prima che un rifiuto sia arrivato)
   - `is CarPermissionState.Denied` -> una `Row` con `setTitle(...)` di `R.string.car_permission_denied_permanent` se `permission.permanent`, altrimenti `R.string.car_permission_denied`; PIU' una sola Action aggiunta con `pane.addAction(...)`, il cui titolo e' `R.string.open_settings` se `permission.permanent` e `R.string.retry` altrimenti — mirror esatto dello switch a due stati di `MainActivity.showDenied()` (D-02/D-03/D-04)
   L'Action si costruisce con `Action.Builder().setTitle(...).setOnClickListener(ParkedOnlyOnClickListener.create { onRetryOrSettingsClicked() }).build()`. `ParkedOnlyOnClickListener` non e' opzionale: e' quanto prescrive la Javadoc di `requestPermissions()` per le azioni che rimandano l'utente al telefono, e fa mostrare all'host il messaggio "solo da fermi" senza codice aggiuntivo. Nessuna icona sull'Action, nessuna seconda Action (il limite del `Pane` e' 2, ne serve 1).

3. Il template restituito resta `PaneTemplate.Builder(pane.build()).setHeaderAction(Action.APP_ICON).build()` in TUTTI i rami: nessun `setTitle` sul `PaneTemplate.Builder`, nessuna `setActionStrip` (D-03 Fase 8, D-07 Fase 9 — nessun branding, nessuna personalizzazione).

4. Log diagnostico: mantenere il formato del prefisso ESATTAMENTE come oggi, `onGetTemplate #<n> content=<etichetta>`, perche' `scripts/dhu-quota-check.ps1` conta quelle righe. Estendere solo l'insieme delle etichette a: `Speed`, `Searching`, `PermissionNotRequested`, `PermissionWaiting`, `PermissionDenied`, `PermissionDeniedPermanent`. Il log resta dentro `if (BuildConfig.DEBUG)` con tag `TachimetroCar`. Nessun valore di velocita' ne' dato di posizione nel messaggio oltre a quanto gia' presente.

5. NON introdurre gli stati di permesso dentro `CarSpeedContent`: i due modelli restano ortogonali (09-RESEARCH.md Pattern 3 — `CarSpeedContent` mappa `SpeedState`, `CarPermissionState` decide se quella mappatura viene mostrata). `CarSpeedContent.kt` non va toccato.

6. Nessuna dichiarazione `androidx.car.app.theme` ne' `carPermissionActivityLayout` va aggiunta in nessun file (D-07: il dialogo di sistema resta di default).

7. Import esplicito aggiuntivo: `androidx.car.app.model.ParkedOnlyOnClickListener`. Mai wildcard.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && ./gradlew.bat :app:testDebugUnitTest --console=plain -q</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt && C=$(grep -v '^\s*[/*]' $F) && echo "$C" | grep -c 'fun buildTemplate(permission: CarPermissionState, speed: SpeedState): PaneTemplate' && echo "$C" | grep -c 'ParkedOnlyOnClickListener.create' && echo "$C" | grep -c 'addAction(' && echo "$C" | grep -c 'car_check_your_phone' && echo "$C" | grep -c 'car_permission_denied_permanent' && echo "$C" | grep -c 'R.string.open_settings' && echo "$C" | grep -c 'R.string.retry' && echo "$C" | grep -c 'setHeaderAction(Action.APP_ICON)' && echo "$C" | grep -c 'onGetTemplate #' && test "$(echo "$C" | grep -c 'setActionStrip\|carPermissionActivityLayout\|androidx.car.app.theme')" -eq 0 && test "$(git diff --name-only -- app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt | wc -l)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `SpeedScreen.kt` dichiara `fun buildTemplate(permission: CarPermissionState, speed: SpeedState): PaneTemplate` e `onGetTemplate()` la invoca con `permissionState.value` e `latestState`
    - `buildTemplate` ramifica con un `when` esaustivo senza `else` su tutti e quattro gli stati di `CarPermissionState`
    - Nel ramo `Granted` il contenuto della Row proviene da `carSpeedContent(speed)` e nessuna Action viene aggiunta al `Pane`
    - Nei rami `NotRequested` e `Waiting` la Row ha titolo `R.string.car_check_your_phone`, nessun testo secondario e nessuna Action
    - Nel ramo `Denied` la Row ha titolo `R.string.car_permission_denied_permanent` quando `permanent` e' true e `R.string.car_permission_denied` altrimenti, e il `Pane` ha esattamente una Action con titolo `R.string.open_settings` / `R.string.retry` secondo lo stesso booleano
    - L'unica Action e' costruita con `ParkedOnlyOnClickListener.create { onRetryOrSettingsClicked() }`
    - Tutti i rami restituiscono un `PaneTemplate` con `setHeaderAction(Action.APP_ICON)`; il file non contiene `setActionStrip`, `androidx.car.app.theme` o `carPermissionActivityLayout`
    - Il messaggio di log conserva il prefisso `onGetTemplate #` ed e' racchiuso in `if (BuildConfig.DEBUG)` con tag `TachimetroCar`; le etichette possibili sono `Speed`, `Searching`, `PermissionNotRequested`, `PermissionWaiting`, `PermissionDenied`, `PermissionDeniedPermanent`
    - `app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt` non risulta modificato (`git diff --name-only` non lo elenca)
    - `./gradlew.bat :app:assembleDebug` e `./gradlew.bat :app:testDebugUnitTest` terminano con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>Lo schermo auto mostra il testo corretto per ognuno dei quattro stati del permesso, e nei due stati di rifiuto offre un'unica azione che riprova o apre le impostazioni sul telefono, eseguibile solo a veicolo fermo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Processo host Android Auto <-> `SpeedScreen` (Binder IPC) | L'host invoca `onGetTemplate()` e recapita i tocchi sull'Action; contenuto e callback attraversano un confine di processo. |
| `SpeedScreen` <-> `CarAppPermissionActivity` (Activity di sistema lanciata dalla libreria sul telefono) | Il dialogo di permesso non e' reso dall'app: l'app puo' solo innescarlo e osservarne l'esito. |
| `SpeedScreen` <-> app Impostazioni di sistema (Intent esplicito) | Il rifiuto permanente lancia una Activity esterna dal contesto auto. |
| `SpeedScreen` <-> `GpsSpeedProvider` (in-process) | Confine oltre il quale si accede a dati di localizzazione reali: attraversabile solo con permesso concesso. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Elevation of Privilege | raccolta di `GpsSpeedProvider.state` senza `ACCESS_FINE_LOCATION` | mitigate | La `collect` esiste esclusivamente nel ramo `CarPermissionState.Granted` del `when`, e `Granted` puo' derivare solo da `ContextCompat.checkSelfPermission(...) == PERMISSION_GRANTED` (mai dal contatore persistito). La garanzia T-08-08 della Fase 8 resta quindi intatta: senza permesso `requestLocationUpdates` (annotato `@Suppress("MissingPermission")`) non viene mai raggiunto e nessuna `SecurityException` puo' far crashare lo schermo auto. Verificabile: `collect` di `gpsSpeedProvider.state` presente una sola volta e solo nel ramo `Granted`. |
| T-09-06 | Denial of Service | rilancio ripetuto di `CarContext.requestPermissions()` | mitigate | Due guardie indipendenti: `requestInFlight` (in memoria) impedisce una seconda richiesta mentre la prima e' pendente, incluso il caso di ciclo STARTED->STOPPED->STARTED (Pitfall 2); il contatore persistito porta lo stato a `Denied` dopo il primo rifiuto, e il ramo `Denied` non lancia mai una richiesta automatica (D-06). Verificabile: `requestPermissions(` invocato solo da `requestLocationPermission()`, chiamata solo dal ramo `NotRequested` e da `onRetryOrSettingsClicked()`. |
| T-09-07 | Tampering | Intent verso le Impostazioni costruito dallo schermo auto | mitigate | Intent con action di sistema costante `Settings.ACTION_APPLICATION_DETAILS_SETTINGS` e `Uri.fromParts("package", carContext.packageName, null)`: il package e' sempre quello dell'app stessa, nessun dato proveniente dall'host viene interpolato nell'Intent. `FLAG_ACTIVITY_NEW_TASK` e' necessario perche' il chiamante non e' un'Activity, e non allarga la superficie. |
| T-09-08 | Elevation of Privilege | esecuzione dell'Action mentre il veicolo e' in movimento | mitigate | L'unica Action e' avvolta in `ParkedOnlyOnClickListener.create { ... }`: e' l'host a decidere se eseguire il blocco, e a mostrare da solo il messaggio "solo a veicolo fermo". Nessuna logica di driving-state fatta in casa. |
| T-09-09 | Information Disclosure | log diagnostico dei refresh | mitigate | Le nuove etichette (`PermissionNotRequested`, `PermissionWaiting`, `PermissionDenied`, `PermissionDeniedPermanent`) non contengono dati di posizione, identificatori o valori di velocita' aggiuntivi, e l'intero log resta dentro `if (BuildConfig.DEBUG)` (T-08-07, invariato). |
| T-09-10 | Spoofing | `<service exported="true">` con `HostValidator.ALLOW_ALL_HOSTS_VALIDATOR` | accept | Rischio ereditato invariato dalla Fase 8 (T-08-05): questa fase non modifica il manifest ne' il validatore. La sostituzione con un `HostValidator` reale resta scope esplicito della Fase 11 (ROADMAP.md Phase 11 SC1). Nota: un host non legittimo puo' innescare il dialogo di permesso, ma non puo' concederlo — la concessione richiede comunque un'interazione dell'utente con il dialogo di sistema sul telefono. |
| T-09-11 | Tampering | personalizzazione del dialogo di permesso | accept | D-07: nessuna dichiarazione `androidx.car.app.theme`/`carPermissionActivityLayout` viene aggiunta, quindi il dialogo resta quello di sistema con aspetto di default e nessun layout dell'app viene iniettato nell'Activity di permesso della libreria. Verificabile con il grep negativo nell'acceptance del Task 2. |
| T-09-SC | Tampering | installazioni da package manager | n/a | Nessun nuovo pacchetto: tutte le API usate sono in `androidx.car.app:app:1.7.0`, gia' presente e valutato in Fase 8. |
</threat_model>

<verification>
1. `./gradlew.bat :app:assembleDebug --console=plain` -> BUILD SUCCESSFUL
2. `./gradlew.bat :app:testDebugUnitTest --console=plain` -> BUILD SUCCESSFUL, nessuna regressione (`CarPermissionStateTest`, `CarSpeedContentTest` e gli altri test JVM esistenti)
3. `grep -c '<uses-permission' app/src/main/AndroidManifest.xml` -> 1 (nessun nuovo permesso introdotto da questa fase)
4. `git diff --name-only` -> solo `app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt`
5. `grep -c 'androidx.car.app.theme\|carPermissionActivityLayout' app/src/main/AndroidManifest.xml` -> 0 (D-07)
6. Verifica manuale rapida sul telefono (la verifica formale su DHU e' il Piano 03): `./gradlew.bat :app:installDebug`, l'app sul telefono si avvia, chiede il permesso e mostra velocita'/MAX/distanza come in v1.1 — nessuna regressione lato telefono
</verification>

<success_criteria>
- `SpeedScreen` richiede automaticamente `ACCESS_FINE_LOCATION` via `CarContext.requestPermissions()` quando lo stato risolto e' `NotRequested`, e mai in altre circostanze automatiche
- Alla concessione, la raccolta di `GpsSpeedProvider.state` parte senza riavviare app o collegamento (SC2), grazie a `permissionState` + `collectLatest`
- Al primo rifiuto lo schermo mostra "Permesso GPS necessario" con Action "Riprova"; al secondo mostra "Permesso negato. Apri le impostazioni sul telefono" con Action "Apri impostazioni" che apre la scheda dell'app sul telefono
- Nessuna richiesta automatica dopo un rifiuto (D-06), nessuna richiesta concorrente (Pitfall 2), nessuna raccolta GPS senza permesso (T-08-08 preservato)
- Nessun nuovo permesso, nessuna modifica al manifest, nessun branding del dialogo (D-07), `CarSpeedContent` invariato
</success_criteria>

<output>
Create `.planning/phases/09-permesso-di-localizzazione-dallo-schermo-auto/09-02-SUMMARY.md` when done
</output>
