---
phase: 06-indicatore-di-ricarica
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/colors.xml
  - app/src/main/res/values/strings.xml
  - app/src/main/res/drawable/ic_charging_flash.xml
  - app/src/main/res/drawable/ic_charging_flash_lime.xml
  - app/src/main/res/drawable/charging_flash_fill.xml
  - app/src/main/res/layout/activity_main.xml
autonomous: true
requirements: [CHRG-01, CHRG-02]

must_haves:
  truths:
    - "Il layout contiene un ImageView chargingIcon di 24dp a sinistra di keepScreenOnSwitch, nascosto di default"
    - "Esiste un drawable a due strati che può essere riempito progressivamente dal basso verso l'alto di lime"
    - "Il colore lime esiste una sola volta in colors.xml ed è usato solo da questo drawable"
  artifacts:
    - path: "app/src/main/res/drawable/ic_charging_flash.xml"
      provides: "Sagoma bianca del fulmine Material flash_on 24x24dp"
      contains: "pathData"
    - path: "app/src/main/res/drawable/ic_charging_flash_lime.xml"
      provides: "Copia lime della stessa sagoma, usata come livello di riempimento"
      contains: "@color/lime_charging_accent"
    - path: "app/src/main/res/drawable/charging_flash_fill.xml"
      provides: "layer-list: base bianca + ClipDrawable lime pilotabile via level 0..10000"
      contains: "clipOrientation"
    - path: "app/src/main/res/values/colors.xml"
      provides: "lime_charging_accent"
      contains: "lime_charging_accent"
    - path: "app/src/main/res/layout/activity_main.xml"
      provides: "chargingIcon ImageView + re-chaining di keepScreenOnSwitch"
      contains: "chargingIcon"
  key_links:
    - from: "app/src/main/res/layout/activity_main.xml"
      to: "app/src/main/res/drawable/charging_flash_fill.xml"
      via: "android:src"
      pattern: "android:src=\"@drawable/charging_flash_fill\""
    - from: "app/src/main/res/drawable/charging_flash_fill.xml"
      to: "app/src/main/res/drawable/ic_charging_flash_lime.xml"
      via: "clip item drawable"
      pattern: "ic_charging_flash_lime"
    - from: "app/src/main/res/layout/activity_main.xml"
      to: "chargingIcon"
      via: "constraint chain di keepScreenOnSwitch"
      pattern: "layout_constraintStart_toEndOf=\"@id/chargingIcon\""
---

<objective>
Creare tutte le risorse dichiarative (colore accento lime, stringa di accessibilità, i tre drawable del fulmine riempibile) e inserire l'`ImageView` `chargingIcon` nel layout esistente, a sinistra di `keepScreenOnSwitch`.

Purpose: separare le risorse visive dal wiring Kotlin, così il Piano 03 (MainActivity) trova già `R.id.chargingIcon`, `R.drawable.charging_flash_fill` e `R.id.chargingIconFill` pronti da referenziare.
Output: 3 nuovi drawable, 1 nuovo colore, 1 nuova stringa, layout modificato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-indicatore-di-ricarica/06-CONTEXT.md
@.planning/phases/06-indicatore-di-ricarica/06-UI-SPEC.md
@.planning/phases/06-indicatore-di-ricarica/06-PATTERNS.md
</context>

<constraints>
- D-04 / PROJECT.md: il lime è l'UNICO colore accento ammesso nell'intera app ed è riservato ESCLUSIVAMENTE a questa icona. Non applicarlo a testo, switch, pulsanti, background.
- D-05: glifo Material "flash on" classico pieno (zigzag), non un outline custom.
- UI-SPEC Spacing: l'icona è non interattiva — NON applicare `minHeight="48dp"` né `clickable="true"`.
- Attenzione: `06-PATTERNS.md` mostra `android:src="@drawable/ic_charging_flash.xml"` — è errato (suffisso `.xml` e drawable sbagliato). Il valore corretto è `@drawable/charging_flash_fill`.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Aggiungere colore lime, stringa di accessibilità e i tre drawable del fulmine</name>
  <files>app/src/main/res/values/colors.xml, app/src/main/res/values/strings.xml, app/src/main/res/drawable/ic_charging_flash.xml, app/src/main/res/drawable/ic_charging_flash_lime.xml, app/src/main/res/drawable/charging_flash_fill.xml</files>
  <read_first>
    - app/src/main/res/values/colors.xml (formato piatto esistente `#AARRGGBB`)
    - app/src/main/res/values/strings.xml (lista piatta, nessun raggruppamento/commento)
    - app/src/main/res/drawable/ic_launcher_foreground.xml (unica `<vector>` esistente: shape degli attributi `android:width`/`height`/`viewportWidth`/`viewportHeight`/`<path android:fillColor .../>`)
    - .planning/phases/06-indicatore-di-ricarica/06-UI-SPEC.md sezioni "Color" e "Animation spec (fill loop)"
  </read_first>
  <action>
    In `colors.xml`, aggiungere prima di `</resources>` la entry `<color name="lime_charging_accent">#FFAEEA00</color>` (Material Lime A700, alfa esplicita a 8 cifre come `black`/`white`). Non toccare le entry `purple_*`/`teal_*` esistenti.

    In `strings.xml`, aggiungere in coda alla lista piatta `<string name="charging_indicator_description">In carica</string>` (UI-SPEC Copywriting Contract: content-description adottata).

    Creare `app/src/main/res/drawable/ic_charging_flash.xml`: `<vector>` con `android:width="24dp"`, `android:height="24dp"`, `android:viewportWidth="24"`, `android:viewportHeight="24"`, contenente un solo `<path>` con `android:fillColor="#FFFFFFFF"` e `android:pathData="M7,2v11h3v9l7,-12h-4l4,-8z"` — è il pathData standard del glifo Material `flash_on` filled (D-05: non inventare geometria custom, non usare uno stile outline).

    Creare `app/src/main/res/drawable/ic_charging_flash_lime.xml`: identico al precedente per viewport/pathData, ma con `android:fillColor="@color/lime_charging_accent"`. Due file vettoriali distinti anziché un tint runtime, per evitare la condivisione di `ConstantState` fra i due livelli dello stesso layer-list.

    Creare `app/src/main/res/drawable/charging_flash_fill.xml`: root `<layer-list>` con due `<item>`:
    - primo item `android:id="@+id/chargingIconBase"` con `android:drawable="@drawable/ic_charging_flash"` (base bianca sempre visibile);
    - secondo item `android:id="@+id/chargingIconFill"` contenente un elemento figlio `<clip>` con `android:drawable="@drawable/ic_charging_flash_lime"`, `android:clipOrientation="vertical"` e `android:gravity="bottom"`. Con gravity bottom il livello lime si scopre dal basso verso l'alto al crescere del `level` (0 = nulla, 10000 = pieno), che è esattamente il riempimento "liquido" richiesto da CHRG-02.

    Gli id `chargingIconBase`/`chargingIconFill` sono contrattuali: il Piano 03 li usa via `LayerDrawable.findDrawableByLayerId(R.id.chargingIconFill)`. Non rinominarli.
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'lime_charging_accent' app/src/main/res/values/colors.xml` restituisce 1 e la riga contiene esattamente `#FFAEEA00`
    - `grep -c 'charging_indicator_description' app/src/main/res/values/strings.xml` restituisce 1 e il valore è `In carica`
    - I file `app/src/main/res/drawable/ic_charging_flash.xml`, `ic_charging_flash_lime.xml`, `charging_flash_fill.xml` esistono
    - `ic_charging_flash.xml` e `ic_charging_flash_lime.xml` contengono entrambi la stringa `M7,2v11h3v9l7,-12h-4l4,-8z` e dichiarano `android:viewportWidth="24"` e `android:viewportHeight="24"`
    - `ic_charging_flash.xml` contiene `android:fillColor="#FFFFFFFF"`; `ic_charging_flash_lime.xml` contiene `android:fillColor="@color/lime_charging_accent"`
    - `charging_flash_fill.xml` contiene `@+id/chargingIconBase`, `@+id/chargingIconFill`, `android:clipOrientation="vertical"` e `android:gravity="bottom"`
    - `grep -rn 'lime_charging_accent' app/src/main/res/` restituisce solo `colors.xml` e `ic_charging_flash_lime.xml` (D-04: nessun altro uso del lime)
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>Colore, stringa e i tre drawable esistono, compilano e il lime non compare in nessun'altra risorsa.</done>
</task>

<task type="auto">
  <name>Task 2: Inserire chargingIcon nel layout e ri-agganciare keepScreenOnSwitch</name>
  <files>app/src/main/res/layout/activity_main.xml</files>
  <read_first>
    - app/src/main/res/layout/activity_main.xml (in particolare il blocco `keepScreenOnSwitch`, righe 89-105, e la convenzione `android:visibility="gone"` + `tools:visibility="visible"` usata da `unitText`/`maxSpeedText`/`resetMaxButton`)
    - .planning/phases/06-indicatore-di-ricarica/06-UI-SPEC.md sezione "Layout / positioning"
  </read_first>
  <action>
    Inserire un nuovo `<ImageView>` nel `ConstraintLayout` immediatamente PRIMA del blocco `androidx.appcompat.widget.SwitchCompat` esistente, con questi attributi esatti: `android:id="@+id/chargingIcon"`, `android:layout_width="24dp"`, `android:layout_height="24dp"`, `android:layout_marginStart="16dp"`, `android:layout_marginBottom="16dp"`, `android:src="@drawable/charging_flash_fill"`, `android:contentDescription="@string/charging_indicator_description"`, `android:clickable="false"`, `android:focusable="false"`, `android:visibility="gone"`, `app:layout_constraintStart_toStartOf="parent"`, `app:layout_constraintBottom_toBottomOf="parent"`, `tools:visibility="visible"`.

    Nota sui vincoli verticali: NON ancorare top/bottom a `@id/keepScreenOnSwitch` (creerebbe una dipendenza circolare, dato che lo switch ora si aggancia orizzontalmente all'icona). Ancorare `chargingIcon` a `parent` in basso con lo stesso `layout_marginBottom="16dp"` dello switch e centrare lo switch verticalmente sull'icona (passo successivo).

    Nel blocco `keepScreenOnSwitch` esistente:
    - sostituire `app:layout_constraintStart_toStartOf="parent"` con `app:layout_constraintStart_toEndOf="@id/chargingIcon"`;
    - cambiare `android:layout_marginStart="16dp"` in `android:layout_marginStart="8dp"` (token `sm` di UI-SPEC: gap fra icona e switch — il margine base di 16dp dal bordo schermo è ora portato da `chargingIcon`);
    - lasciare invariati `android:layout_marginBottom="16dp"`, `app:layout_constraintBottom_toBottomOf="parent"`, `minHeight`, tint, testo e dimensione testo.

    Non introdurre alcuna variante `layout-land` (UI-SPEC: layout adattivo unico). Non applicare `minHeight="48dp"` a `chargingIcon` (indicatore passivo, non interattivo).
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `activity_main.xml` contiene un `<ImageView>` con `android:id="@+id/chargingIcon"`
    - Il blocco `chargingIcon` contiene `android:layout_width="24dp"`, `android:layout_height="24dp"`, `android:src="@drawable/charging_flash_fill"`, `android:visibility="gone"`, `android:clickable="false"`, `android:focusable="false"`
    - `grep -c 'minHeight' ` sul solo blocco `chargingIcon` restituisce 0 (l'icona non ha touch target minimo)
    - `grep -c 'layout_constraintStart_toEndOf="@id/chargingIcon"' app/src/main/res/layout/activity_main.xml` restituisce 1
    - `grep -c 'layout_constraintStart_toStartOf="parent"' app/src/main/res/layout/activity_main.xml` non include più il blocco `keepScreenOnSwitch` (lo switch non è più agganciato a parent sul lato start)
    - Il blocco `keepScreenOnSwitch` contiene `android:layout_marginStart="8dp"`
    - Non esiste alcuna directory `app/src/main/res/layout-land/`
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>Il layout compila, `chargingIcon` è dichiarato nell'angolo in basso a sinistra con visibilità `gone` di default e lo switch si aggancia alla sua destra con gap 8dp.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nessuno | Questo piano introduce solo risorse dichiarative (colori, stringhe, drawable vettoriali, layout XML). Nessun input esterno, nessuna rete, nessuna persistenza, nessun permesso, nessuna nuova dipendenza. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-I | Information disclosure | `charging_indicator_description` string | accept | La stringa "In carica" non espone dati sensibili; è un'etichetta di accessibilità già visibile a schermo come icona |
| T-06-01-SC | Tampering | supply chain | accept | Nessun `implementation`/`testImplementation` aggiunto, nessun pacchetto installato; `gradle/libs.versions.toml` e `app/build.gradle.kts` restano invariati (verificabile con `git diff --name-only`) |
</threat_model>

<verification>
- `./gradlew.bat :app:assembleDebug` → BUILD SUCCESSFUL
- `git diff --name-only` NON elenca `app/build.gradle.kts` né `gradle/libs.versions.toml` né `app/src/main/AndroidManifest.xml`
- `grep -rn 'AEEA00\|lime_charging_accent' app/src/` limita il lime a `colors.xml` + `ic_charging_flash_lime.xml`
</verification>

<success_criteria>
- 3 nuovi drawable, 1 nuovo colore, 1 nuova stringa creati
- `chargingIcon` presente nel layout, `gone` di default, 24dp, non interattivo
- `keepScreenOnSwitch` agganciato a `@id/chargingIcon` con margine start 8dp
- Il progetto compila senza errori né nuove dipendenze
</success_criteria>

<output>
Create `.planning/phases/06-indicatore-di-ricarica/06-01-SUMMARY.md` when done
</output>
