---
phase: 10-comportamento-del-telefono-alla-connessione-android-auto
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - app/src/main/java/com/sed/tachimetro/MainActivity.kt
autonomous: true
requirements: [CONN-01, CONN-02]
user_setup: []

must_haves:
  truths:
    - "Quando Android Auto si connette, il telefono mostra 'Connesso ad Android Auto' al posto del numero della velocita'"
    - "Quando Android Auto si connette, il telefono rilascia FLAG_KEEP_SCREEN_ON anche se lo switch 'Sempre acceso' era attivo"
    - "Alla disconnessione, il telefono riapplica esattamente la preferenza salvata e torna a mostrare la velocita' senza richiedere riavvio dell'app"
    - "Nessuna transizione di connessione scrive sulla preferenza persistita: lo switch resta sul valore scelto dall'utente"
    - "Con Android Auto NON connesso, lo switch 'Sempre acceso' si comporta esattamente come in v1.0/v1.1"
    - "Mentre Android Auto e' connesso, MAX velocita' e distanza continuano ad accumularsi e a essere persistiti come prima"
    - "Se il permesso di localizzazione e' negato, il messaggio di permesso e il pulsante Riprova restano visibili anche con Android Auto connesso (non vengono sostituiti dal messaggio neutro)"
  artifacts:
    - path: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      provides: "Osservazione di CarConnection, stato neutro lato telefono, derivazione del flag schermo-sempre-acceso"
      contains: "setupCarConnectionObserver"
      min_lines: 780
  key_links:
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "androidx.car.app.connection.CarConnection"
      via: "osservazione del LiveData del tipo di connessione con il lifecycle dell'Activity"
      pattern: "CarConnection\\(applicationContext\\)"
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "com.sed.tachimetro.car.resolveCarLinkState"
      via: "mappatura del tipo di connessione grezzo nel modello CarLinkState"
      pattern: "resolveCarLinkState\\("
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "com.sed.tachimetro.car.resolveEffectiveKeepScreenOn"
      via: "unico modo di calcolare l'argomento di applyKeepScreenOn()"
      pattern: "applyKeepScreenOn\\(resolveEffectiveKeepScreenOn\\("
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "R.string.android_auto_connected"
      via: "testo dello stato neutro in messageText"
      pattern: "R\\.string\\.android_auto_connected"
---

<objective>
Cablare in `MainActivity` il comportamento del telefono alla connessione Android Auto: osservare `CarConnection`, mostrare lo stato neutro al posto del numero della velocita' (CONN-01), rilasciare `FLAG_KEEP_SCREEN_ON` durante la proiezione e riapplicare esattamente la preferenza salvata alla disconnessione (CONN-02), senza alcuna regressione del toggle esistente quando Android Auto non e' connesso.

Purpose: e' l'unico piano che tocca l'interfaccia del telefono. Il rischio principale non e' aggiungere il nuovo comportamento, ma romperne uno gia' spedito: il rendering di `messageText` e' condiviso da cifre della velocita', messaggi di stato e messaggi di permesso, e la stessa `TextView` e' riusata con due configurazioni di autosize diverse. Per questo il piano separa un refactor puro (Task 1, nessun cambiamento di comportamento a Android Auto disconnesso) dal cablaggio effettivo (Task 2).
Output: `MainActivity.kt` con `renderSpeedArea()`, `setupCarConnectionObserver()`, `onCarLinkChanged()` e il listener dello switch riscritto sulla derivazione pura.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-comportamento-del-telefono-alla-connessione-android-auto/10-01-SUMMARY.md

<interfaces>
<!-- Contratti consumati da questo piano. Non serve esplorare il codebase per trovarli. -->

Dal Piano 01, app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt:
- `sealed class CarLinkState` con `data object Disconnected` e `data object Connected`
- `fun resolveCarLinkState(connectionType: Int?): CarLinkState` — `Connected` solo per `CarConnection.CONNECTION_TYPE_PROJECTION`, tutto il resto `Disconnected`
- `fun resolveEffectiveKeepScreenOn(savedPreference: Boolean, link: CarLinkState): Boolean` — `false` se `Connected`, altrimenti `savedPreference`

Dal Piano 01, app/src/main/res/values/strings.xml:
- `R.string.android_auto_connected` = "Connesso ad Android Auto" (mostrata SUL TELEFONO)

Da androidx.car.app:app:1.7.0 (dipendenza gia' presente, nessuna modifica ai file di build):
- `androidx.car.app.connection.CarConnection(context: Context)` — annotato `@MainThread`
- `CarConnection.getType(): LiveData<Integer>` — in Kotlin la proprieta' `type`
- `LiveData.observe(owner: LifecycleOwner, observer: Observer<in T>)` da `androidx.lifecycle:lifecycle-livedata-core:2.11.0`, gia' su `debugCompileClasspath` per via transitiva (verificato con `./gradlew :app:dependencies --configuration debugCompileClasspath`) — NON aggiungere alcuna dipendenza
- Il `LiveData` restituito registra il proprio BroadcastReceiver in `onActive()` e lo deregistra in `onInactive()`: osservandolo con il lifecycle dell'Activity, la registrazione segue automaticamente START/STOP e l'observer viene rimosso a DESTROY. Nessuna pulizia manuale in `onDestroy()`.

Da app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt:
- `val state: StateFlow<SpeedState>` — essendo uno `StateFlow`, espone `state.value` con l'ultimo valore emesso, leggibile senza collect
- `const val NOISE_FLOOR_KMH = 2.0`

Da app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt:
- `sealed class SpeedState` con `data object Searching`, `data class Reading(val kmh: Int, val deltaMeters: Float)`, `data object NoSignal`

Da app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt (INVARIATO):
- `fun read(): Boolean?` (null = primo avvio) / `fun write(keepOn: Boolean)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estrarre renderSpeedArea() e introdurre il ramo dello stato neutro</name>
  <files>app/src/main/java/com/sed/tachimetro/MainActivity.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt (file intero; in particolare `updatePlaceholder()`, `showReady()`, `showDenied()`, `applyMessageAutosize()`, `applySpeedAutosize()`, `updateMaxArea()`, `updateDistanceArea()` — la stessa `messageText` e' riusata da tutti con configurazioni di autosize diverse, ed e' esattamente qui che un refactor sbagliato regredisce)
    - app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt (creato dal Piano 01: il modello da introdurre come campo)
    - app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt (i tre stati su cui il `when` deve restare esaustivo)
    - app/src/main/res/values/strings.xml (la nuova `android_auto_connected` e le esistenti `status_ready`, `searching_gps_signal`)
  </read_first>
  <action>
Modificare SOLO `app/src/main/java/com/sed/tachimetro/MainActivity.kt`. Questo task e' un refactor a comportamento invariato quando Android Auto non e' connesso: nessun observer viene ancora registrato (Task 2), quindi il campo `carLink` resta su `Disconnected` per tutta la durata di questo task e ogni percorso deve produrre esattamente lo stesso output di oggi.

1. Nuovi import espliciti (mai wildcard): `com.sed.tachimetro.car.CarLinkState`.

2. Nuovo campo privato, dichiarato accanto agli altri campi di stato in memoria (vicino a `currentMax`/`currentDistanceMeters`):
   `private var carLink: CarLinkState = CarLinkState.Disconnected`
   Commento CONN-01: e' l'unico punto in memoria che rappresenta "Android Auto sta proiettando"; il valore iniziale `Disconnected` garantisce che qualunque percorso eseguito prima della prima emissione dell'observer si comporti come in v1.1.

3. Estrarre da `updatePlaceholder(state: SpeedState)` la sola parte di RENDERING in una nuova funzione privata `private fun renderSpeedArea(state: SpeedState)`, con questa struttura esatta:
   - `retryButton.visibility = View.GONE` (prima riga, come oggi in `updatePlaceholder()`)
   - subito dopo, il ramo CONN-01: se `carLink is CarLinkState.Connected`, allora `unitText.visibility = View.GONE`, `applyMessageAutosize()`, `messageText.text = getString(R.string.android_auto_connected)` e `return`. Il commento deve spiegare perche' `applyMessageAutosize()` e' obbligatorio qui: `messageText` puo' arrivare da `applySpeedAutosize()`, che lascia `maxLines = 1` e un tetto di 300sp: senza il ripristino il messaggio verrebbe compresso su una riga sola invece di andare a capo (stesso motivo gia' documentato nel commento di `applyMessageAutosize()`)
   - dopo il ramo, il `when (state)` esistente, spostato senza modifiche: `is SpeedState.Searching, is SpeedState.NoSignal` -> `unitText` GONE + `applyMessageAutosize()` + `searching_gps_signal`; `is SpeedState.Reading` -> `unitText` VISIBLE + `applySpeedAutosize()` + `messageText.text = state.kmh.toString()`
   - il `when` NON deve piu' contenere gli aggiornamenti di massimo e distanza: quelli restano in `updatePlaceholder()` (punto 4)

4. Riscrivere `updatePlaceholder(state: SpeedState)` in questa forma, mantenendo integralmente i commenti esistenti su D-07, DIST-03, D-04, WR-03, DIST-02:
   - prima riga: `renderSpeedArea(state)`
   - poi l'accumulo, spostato dal ramo `Reading` del `when` a una guardia `if (state is SpeedState.Reading) { ... }` che contiene, INVARIATI: il blocco `reduceMax(currentMax, state.kmh)` con la scrittura su `maxSpeedStore` solo quando il valore cambia, e il blocco `reduceDistance(currentDistanceMeters, state.deltaMeters, state.kmh, noiseFloorKmh = GpsSpeedProvider.NOISE_FLOOR_KMH)` con la scrittura su `distanceStore` solo quando il valore cambia
   - infine `updateMaxArea()` e `updateDistanceArea()`, incondizionate come oggi
   Aggiungere un commento CONN-01 che dichiara la scelta deliberata: l'accumulo di MAX e distanza avviene FUORI dal ramo di rendering, quindi continua a funzionare identico mentre Android Auto e' connesso — lo stato neutro sostituisce solo il numero della velocita', non sospende la registrazione del viaggio. Per lo stesso motivo `updateMaxArea()`/`updateDistanceArea()` NON vengono saltate nello stato neutro: le aree MAX e distanza restano visibili e aggiornate.

5. Modificare `showReady()` cambiando SOLO l'assegnazione di `messageText.text` in una espressione condizionale: `getString(R.string.android_auto_connected)` quando `carLink is CarLinkState.Connected`, altrimenti `getString(R.string.status_ready)` come oggi. Tutto il resto della funzione (visibilita' di `retryButton`/`unitText`, `applyMessageAutosize()`, `updateMaxArea()`, `updateDistanceArea()`) resta identico. Commento: `showReady()` viene invocata anche da `onResume()`, quindi senza questa condizione il ritorno in foreground con Android Auto gia' connesso mostrerebbe "Pronto" al posto del messaggio neutro.

6. NON modificare `showDenied()`. E' una precedenza deliberata da documentare con un commento in `showDenied()`: quando il permesso di localizzazione manca, il messaggio di permesso e il pulsante Riprova hanno la precedenza sullo stato neutro CONN-01, perche' sono l'unica via per rendere di nuovo utilizzabile l'app; sostituirli con "Connesso ad Android Auto" toglierebbe all'utente l'unica azione disponibile.

7. NON aggiungere ancora alcun observer, alcun campo `savedKeepOn` e nessuna modifica a `applyKeepScreenOn()`/`setupScreenOnSwitch()`: sono del Task 2.

8. Nessun'altra modifica di comportamento, nessun rinominare di campi o funzioni esistenti, nessuna modifica al layout XML.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && F=app/src/main/java/com/sed/tachimetro/MainActivity.kt && C=$(grep -v '^\s*//' $F) && echo "$C" | grep -c 'private fun renderSpeedArea(state: SpeedState)' && echo "$C" | grep -c 'renderSpeedArea(state)' && echo "$C" | grep -c 'carLink is CarLinkState.Connected' && echo "$C" | grep -c 'R.string.android_auto_connected' && echo "$C" | grep -c 'if (state is SpeedState.Reading)' && test "$(echo "$C" | grep -c 'R.string.android_auto_connected')" -eq 2</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:testDebugUnitTest --console=plain 2>&1 | tail -15</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=app/src/main/java/com/sed/tachimetro/MainActivity.kt && test "$(grep -c 'screenOnStore.write(' $F)" -eq 2 && test "$(grep -c 'maxSpeedStore.write(' $F)" -eq 3 && test "$(grep -c 'reduceDistance(' $F)" -eq 1 && test "$(grep -c 'import .*\*$' $F)" -eq 0 && test "$(git diff --name-only)" = "app/src/main/java/com/sed/tachimetro/MainActivity.kt"</automated>
  </verify>
  <acceptance_criteria>
    - `MainActivity.kt` dichiara `private var carLink: CarLinkState = CarLinkState.Disconnected` e importa `com.sed.tachimetro.car.CarLinkState`
    - Esiste `private fun renderSpeedArea(state: SpeedState)` contenente il ramo `carLink is CarLinkState.Connected` con `unitText.visibility = View.GONE`, `applyMessageAutosize()`, `messageText.text = getString(R.string.android_auto_connected)` e un `return` anticipato
    - `updatePlaceholder()` chiama `renderSpeedArea(state)` come prima istruzione e non contiene piu' alcun `messageText.text =`
    - L'accumulo di massimo e distanza vive in `updatePlaceholder()` sotto una guardia `if (state is SpeedState.Reading)`, non dentro il `when` di `renderSpeedArea()`; `reduceMax(` e `reduceDistance(` ricorrono ciascuna esattamente una volta nel file
    - `updateMaxArea()` e `updateDistanceArea()` continuano a essere chiamate incondizionatamente alla fine di `updatePlaceholder()`
    - `showReady()` sceglie fra `R.string.android_auto_connected` e `R.string.status_ready` in base a `carLink`; `R.string.android_auto_connected` ricorre esattamente 2 volte nel file
    - `showDenied()` e' invariata nel comportamento e porta un commento che dichiara la precedenza del messaggio di permesso sullo stato neutro CONN-01
    - `screenOnStore.write(` ricorre esattamente 2 volte (nessuna nuova scrittura introdotta da questo task)
    - `./gradlew.bat :app:assembleDebug` e `./gradlew.bat :app:testDebugUnitTest` terminano entrambi con `BUILD SUCCESSFUL`
    - `git diff --name-only` elenca esclusivamente `app/src/main/java/com/sed/tachimetro/MainActivity.kt`
  </acceptance_criteria>
  <done>Il rendering dell'area velocita' ha un unico punto di decisione che conosce lo stato del collegamento auto, e con `carLink` su `Disconnected` il telefono si comporta esattamente come in v1.1.</done>
</task>

<task type="auto">
  <name>Task 2: Osservare CarConnection e derivare il flag schermo-sempre-acceso (CONN-01, CONN-02)</name>
  <files>app/src/main/java/com/sed/tachimetro/MainActivity.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt (stato dopo il Task 1; in particolare `onCreate()`, `setupScreenOnSwitch()`, `setupGpsCollection()`, `setupChargingIndicator()`, `applyKeepScreenOn()`, `onDestroy()` e il commento WR-04 sul passaggio di `applicationContext`)
    - app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt (le due funzioni pure del Piano 01 e il contratto dichiarato nel loro KDoc)
    - app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt (precedente esatto del log diagnostico gated su `BuildConfig.DEBUG` con un `LOG_TAG` nel companion object — T-08-07)
    - app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt (semantica di `read()` che restituisce null al primo avvio, da preservare integralmente)
    - .planning/ROADMAP.md sezione "Phase 10" (i tre Success Criteria che questo task deve rendere veri)
  </read_first>
  <action>
Modificare SOLO `app/src/main/java/com/sed/tachimetro/MainActivity.kt`.

1. Nuovi import espliciti: `android.util.Log`, `androidx.car.app.connection.CarConnection`, `com.sed.tachimetro.car.resolveCarLinkState`, `com.sed.tachimetro.car.resolveEffectiveKeepScreenOn`. `BuildConfig` NON va importato: `MainActivity` e' gia' nel package `com.sed.tachimetro`.

2. Nel `companion object` aggiungere `private const val LOG_TAG = "TachimetroPhone"` — tag distinto da `"TachimetroCar"` usato da `SpeedScreen`, cosi' i due lati sono filtrabili separatamente in logcat durante la verifica DHU del Piano 03.

3. Nuovi campi privati:
   - `private lateinit var carConnection: CarConnection`
   - `private var savedKeepOn: Boolean = false` — copia in memoria della PREFERENZA PERSISTITA (non del flag effettivo). Commento CONN-02: e' il valore che verra' riapplicato tale e quale alla disconnessione; viene aggiornato solo in due punti, la lettura iniziale in `setupScreenOnSwitch()` e il listener dello switch, mai da una transizione di connessione.

4. Riscrivere il corpo di `setupScreenOnSwitch()` mantenendo intatta la logica D-04/D-05 del default derivato dallo stato di ricarica:
   - conservare `val savedPreference = screenOnStore.read()`, `val keepOn = savedPreference ?: isDeviceCharging()` e la scrittura una-tantum `if (savedPreference == null) screenOnStore.write(keepOn)`
   - aggiungere `savedKeepOn = keepOn` subito dopo
   - `keepScreenOnSwitch.isChecked = keepOn` resta invariato (lo switch riflette la preferenza, non il flag effettivo) e resta impostato PRIMA del listener, come oggi
   - sostituire `applyKeepScreenOn(keepOn)` con `applyKeepScreenOn(resolveEffectiveKeepScreenOn(savedKeepOn, carLink))`
   - riscrivere il listener come, nell'ordine: `savedKeepOn = isChecked`; `screenOnStore.write(isChecked)`; `applyKeepScreenOn(resolveEffectiveKeepScreenOn(savedKeepOn, carLink))`
   - commento CONN-02 sul listener: la persistenza della preferenza resta immediata e incondizionata anche mentre Android Auto e' connesso — l'utente puo' cambiare idea durante la proiezione e la scelta verra' onorata alla disconnessione; e' solo l'APPLICAZIONE del flag a essere derivata

5. Nuova funzione privata `setupCarConnectionObserver()`, sullo stesso modello delle altre `setupXxx()` (commento WR-02 di appartenenza a `onCreate()`):
   - `carConnection = CarConnection(applicationContext)` — WR-04: `applicationContext`, mai l'Activity; il costruttore e' annotato `@MainThread` e `onCreate()` soddisfa il vincolo
   - `carConnection.type.observe(this) { connectionType -> onCarLinkChanged(resolveCarLinkState(connectionType)) }`
   - commento che documenta il ciclo di vita: il `LiveData` registra il proprio BroadcastReceiver in `onActive()` e lo deregistra in `onInactive()`, quindi osservandolo con il lifecycle dell'Activity la registrazione segue automaticamente START/STOP e l'observer viene rimosso a DESTROY. Per questo `onDestroy()` NON va toccata: nessuna deregistrazione manuale, nessun rischio di leak, coerente con `repeatOnLifecycle(STARTED)` usato dagli altri collector.

6. Nuova funzione privata `onCarLinkChanged(link: CarLinkState)`:
   - `val changed = link != carLink`
   - `carLink = link`
   - log diagnostico gated: `if (BuildConfig.DEBUG) { Log.d(LOG_TAG, "carLink=$link savedKeepOn=$savedKeepOn effectiveKeepOn=${resolveEffectiveKeepScreenOn(savedKeepOn, carLink)}") }` — solo stato del collegamento e booleani di preferenza, MAI velocita' ne' dati di posizione (T-10-05)
   - `applyKeepScreenOn(resolveEffectiveKeepScreenOn(savedKeepOn, carLink))` — chiamata incondizionata: e' idempotente (`addFlags`/`clearFlags`) e cosi' si auto-ripara anche se il `LiveData` riemette lo stesso valore
   - `if (changed && permissionGranted.value) { renderSpeedArea(gpsSpeedProvider.state.value) }` — ridisegno immediato senza aspettare il prossimo tick del GPS. Due guardie, entrambe obbligatorie e da commentare: (a) `changed` evita che la prima emissione dell'observer sovrascriva il "Pronto" iniziale con "Ricerca segnale GPS..."; (b) `permissionGranted.value` protegge la precedenza del messaggio di permesso, che `showDenied()` ha gia' scritto in `messageText`
   - commento CONN-02 esplicito: questa funzione NON chiama `screenOnStore.write()` in nessun ramo. La preferenza persistita e' immutata dalle transizioni di connessione; e' il requisito "senza alterare la preferenza memorizzata"
   - NON chiamare `updatePlaceholder()` qui: quella funzione accumula la distanza da `state.deltaMeters`, e invocarla con il valore gia' consumato di `gpsSpeedProvider.state.value` conterebbe due volte gli stessi metri. `renderSpeedArea()` e' privo di effetti collaterali ed e' l'unica chiamata corretta in questo punto.

7. In `onCreate()`, aggiungere `setupCarConnectionObserver()` come ULTIMA delle chiamate `setupXxx()`, subito dopo `setupChargingIndicator()` e prima di `checkAndRequestPermission()`. L'ordine e' vincolante e va commentato: deve venire dopo `setupScreenOnSwitch()` (che valorizza `savedKeepOn`) e dopo `setupGpsCollection()` (che valorizza `gpsSpeedProvider`), entrambi letti da `onCarLinkChanged()`.

8. NON modificare `onDestroy()`, `onStop()`, `onResume()`, `onWindowFocusChanged()`, `applyKeepScreenOn()`, `isDeviceCharging()`, ne' alcuna funzione di window insets. NON toccare `AndroidManifest.xml`, `app/build.gradle.kts` o `gradle/libs.versions.toml`: nessun permesso, nessun elemento `<queries>` e nessuna dipendenza vanno aggiunti (il `<queries>` per il provider `androidx.car.app.connection` e' gia' nel manifest della libreria e viene unito automaticamente).

9. Import espliciti, mai wildcard.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && F=app/src/main/java/com/sed/tachimetro/MainActivity.kt && C=$(grep -v '^\s*//' $F) && echo "$C" | grep -c 'CarConnection(applicationContext)' && echo "$C" | grep -c 'carConnection.type.observe(this)' && echo "$C" | grep -c 'resolveCarLinkState(connectionType)' && echo "$C" | grep -c 'private fun onCarLinkChanged(link: CarLinkState)' && echo "$C" | grep -c 'setupCarConnectionObserver()' && test "$(echo "$C" | grep -c 'applyKeepScreenOn(resolveEffectiveKeepScreenOn(savedKeepOn, carLink))')" -eq 3 && test "$(echo "$C" | grep -c 'applyKeepScreenOn(')" -eq 4</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=app/src/main/java/com/sed/tachimetro/MainActivity.kt && test "$(grep -c 'screenOnStore.write(' $F)" -eq 2 && test "$(grep -c 'updatePlaceholder(' $F)" -eq 2 && test "$(grep -c 'Log.d(' $F)" -eq 1 && test "$(grep -c 'BuildConfig.DEBUG' $F)" -eq 1 && test "$(grep -c 'import .*\*$' $F)" -eq 0 && git diff --name-only --exit-code app/src/main/AndroidManifest.xml app/build.gradle.kts gradle/libs.versions.toml</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:testDebugUnitTest --console=plain 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `MainActivity.kt` costruisce `CarConnection(applicationContext)` (mai con l'Activity come Context) e lo osserva con `carConnection.type.observe(this) { ... }`
    - Esiste `private fun onCarLinkChanged(link: CarLinkState)` che aggiorna `carLink`, chiama `applyKeepScreenOn(resolveEffectiveKeepScreenOn(savedKeepOn, carLink))` e ridisegna con `renderSpeedArea(gpsSpeedProvider.state.value)` sotto la doppia guardia `changed && permissionGranted.value`
    - `onCarLinkChanged()` non contiene alcuna chiamata a `screenOnStore.write(`, `updatePlaceholder(`, `maxSpeedStore.write(` o `distanceStore.write(`
    - Ogni chiamata a `applyKeepScreenOn(` nel file passa `resolveEffectiveKeepScreenOn(savedKeepOn, carLink)`: `grep -c 'applyKeepScreenOn(resolveEffectiveKeepScreenOn(savedKeepOn, carLink))'` restituisce 3 e `grep -c 'applyKeepScreenOn('` restituisce 4 (le 3 chiamate + la dichiarazione della funzione)
    - `screenOnStore.write(` ricorre ancora esattamente 2 volte: la scrittura una-tantum del default al primo avvio e il listener dello switch
    - `keepScreenOnSwitch.isChecked` continua a essere impostato dalla preferenza salvata prima della registrazione del listener, e il listener persiste `isChecked` in modo incondizionato
    - `setupCarConnectionObserver()` e' chiamata in `onCreate()` dopo `setupChargingIndicator()` e prima di `checkAndRequestPermission()`
    - Il log diagnostico e' l'unico `Log.d(` del file, e' racchiuso in `if (BuildConfig.DEBUG)` e non stampa alcun valore di velocita', distanza o posizione
    - `onDestroy()` non contiene alcuna rimozione manuale dell'observer ne' riferimenti a `carConnection`
    - `git diff --name-only --exit-code app/src/main/AndroidManifest.xml app/build.gradle.kts gradle/libs.versions.toml` esce con codice 0 (nessuno dei tre file modificato)
    - `./gradlew.bat :app:assembleDebug` e `./gradlew.bat :app:testDebugUnitTest` terminano con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>Il telefono reagisce dal vivo a connessione e disconnessione di Android Auto: stato neutro e flag rilasciato alla connessione, preferenza salvata riapplicata tale e quale alla disconnessione, con la preferenza persistita mai toccata dalle transizioni.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider `androidx.car.app.connection` (app Android Auto) <-> `MainActivity` | Il tipo di connessione arriva da un componente esterno via ContentProvider. Nessun valore ricevuto viene usato come autorita' su permessi o dati: influenza solo `FLAG_KEEP_SCREEN_ON` e il testo di `messageText`. |
| Broadcast `androidx.car.app.connection.action.CAR_CONNECTION_UPDATED` <-> receiver interno del LiveData | Il receiver e' registrato dalla libreria come esportato su API 33+: qualunque app installata puo' inviare il broadcast. |
| Transizioni di connessione <-> preferenza persistita `keep_screen_on` | Confine che NON deve essere attraversato (CONN-02). Verificato staticamente: nessuna `screenOnStore.write(` fuori dalla lettura iniziale e dal listener dello switch. |
| `MainActivity` <-> gate del permesso di localizzazione | Le transizioni di connessione non devono poter modificare `permissionGranted` ne' avviare/fermare la raccolta GPS. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Spoofing | broadcast `CAR_CONNECTION_UPDATED` ricevuto dal LiveData della libreria | accept | Il broadcast non trasporta lo stato: il receiver della libreria reagisce rieseguendo la query verso il ContentProvider `androidx.car.app.connection`, che resta l'unica sorgente del valore. Un'app ostile puo' al massimo forzare query aggiuntive. L'autorita' del provider e' unica per dispositivo, quindi non e' impersonabile da un'app di terze parti. Impatto massimo: una query in piu'; nessuna esposizione di dati e nessun cambio di stato non autorizzato. |
| T-10-07 | Tampering | valore del tipo di connessione | mitigate | Il valore grezzo non viene mai usato direttamente: passa sempre da `resolveCarLinkState()` (Piano 01), che riconosce solo `CONNECTION_TYPE_PROJECTION` e degrada tutto il resto a `Disconnected`. Verificabile: `grep -c 'resolveCarLinkState(connectionType)'` >= 1 e nessun confronto diretto su `CONNECTION_TYPE_` dentro `MainActivity.kt`. |
| T-10-08 | Tampering | preferenza persistita `keep_screen_on` (CONN-02) | mitigate | `onCarLinkChanged()` non contiene alcuna scrittura; il conteggio globale di `screenOnStore.write(` nel file resta bloccato a 2 (default al primo avvio + listener dello switch), verificato da un gate automatico in `<verify>`. La preferenza non puo' quindi essere alterata da una sequenza di connessioni/disconnessioni, nemmeno ripetuta. |
| T-10-09 | Elevation of Privilege | gate del permesso di localizzazione | mitigate | `onCarLinkChanged()` non tocca `permissionGranted`, non lancia `requestPermissionLauncher` e non avvia/ferma la raccolta di `gpsSpeedProvider.state`: legge solo `permissionGranted.value` come guardia di sola lettura e `gpsSpeedProvider.state.value` come ultimo valore gia' emesso. Nessun nuovo percorso puo' far partire il GPS senza permesso. |
| T-10-10 | Information Disclosure | log diagnostico | mitigate | Unico `Log.d(` del file, racchiuso in `if (BuildConfig.DEBUG)`, con un payload limitato a stato del collegamento e due booleani di preferenza. Nessuna velocita', distanza, coordinata o identificatore. Nelle build release la riga non viene eseguita. Verificabile: `grep -c 'Log.d('` == 1 e `grep -c 'BuildConfig.DEBUG'` == 1. |
| T-10-11 | Denial of Service | leak di receiver/observer | mitigate | `CarConnection` e' costruito con `applicationContext` (WR-04) e osservato con il lifecycle dell'Activity: il `LiveData` registra il receiver in `onActive()` e lo deregistra in `onInactive()`, e l'observer viene rimosso automaticamente a DESTROY. Nessuna registrazione manuale sopravvive alla distruzione dell'Activity; `onDestroy()` resta invariata (criterio di accettazione esplicito). |
| T-10-12 | Denial of Service | interfaccia del telefono bloccata sullo stato neutro | mitigate | Lo stato neutro e' ricalcolato a ogni emissione GPS (1 Hz) dentro `renderSpeedArea()` a partire da `carLink`: appena il collegamento torna `Disconnected` il numero riappare al tick successivo, senza dipendere dalla singola callback dell'observer. Nessuno stato "appiccicoso" persistito su disco. |
| T-10-SC | Tampering | installazioni da package manager | n/a | Nessun nuovo pacchetto e nessuna coordinata Maven aggiunta: `androidx.car.app:app:1.7.0` e `androidx.lifecycle:lifecycle-livedata-core:2.11.0` (transitiva) sono gia' su `debugCompileClasspath`. Un gate in `<verify>` fallisce se `app/build.gradle.kts` o `gradle/libs.versions.toml` risultano modificati. |
</threat_model>

<verification>
1. `./gradlew.bat :app:assembleDebug --console=plain` -> BUILD SUCCESSFUL
2. `./gradlew.bat :app:testDebugUnitTest --console=plain` -> BUILD SUCCESSFUL, nessuna regressione nei test JVM esistenti (incluso `CarLinkStateTest` del Piano 01)
3. `git diff --name-only` -> esclusivamente `app/src/main/java/com/sed/tachimetro/MainActivity.kt`
4. `git diff app/src/main/java/com/sed/tachimetro/car/` -> vuoto (il Piano 02 consuma le funzioni pure, non le modifica)
5. `git diff app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt` -> vuoto
6. `grep -c 'CONNECTION_TYPE_' app/src/main/java/com/sed/tachimetro/MainActivity.kt` -> 0 (la mappatura vive solo in `CarLinkState.kt`)
</verification>

<success_criteria>
- Alla connessione Android Auto il telefono mostra "Connesso ad Android Auto" al posto del numero e rilascia `FLAG_KEEP_SCREEN_ON` anche se lo switch era attivo (Success Criterion 1 della roadmap)
- Alla disconnessione il telefono riapplica esattamente la preferenza salvata e torna al rendering normale, senza aver mai riscritto la preferenza (Success Criterion 2)
- Con Android Auto non connesso, `resolveEffectiveKeepScreenOn(pref, Disconnected) == pref` rende il toggle identico a v1.0/v1.1 (Success Criterion 3)
- MAX e distanza continuano ad accumularsi e a persistere anche durante la proiezione
- Nessuna modifica a manifest, file di build o file del package `car/` e `screen/`
</success_criteria>

<output>
Create `.planning/phases/10-comportamento-del-telefono-alla-connessione-android-auto/10-02-SUMMARY.md` when done
</output>
