---
phase: 10-comportamento-del-telefono-alla-connessione-android-auto
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - app/src/test/java/com/sed/tachimetro/car/CarLinkSequenceTest.kt
autonomous: false
requirements: [CONN-01, CONN-02]
user_setup: []

must_haves:
  truths:
    - "Una sequenza di connessioni e disconnessioni ripetute non altera mai il valore che verra' riapplicato alla disconnessione"
    - "Il flag effettivo dipende solo dallo stato corrente del collegamento e dalla preferenza, mai dalla storia delle transizioni"
    - "Una persona ha visto dal vivo il telefono passare allo stato neutro alla connessione Android Auto e tornare al comportamento precedente alla disconnessione"
    - "Una persona ha confermato che lo switch 'Sempre acceso' funziona come in v1.1 quando Android Auto non e' connesso"
    - "Una persona ha confermato che MAX velocita' e distanza non vengono azzerati ne' congelati da una connessione Android Auto"
  artifacts:
    - path: "app/src/test/java/com/sed/tachimetro/car/CarLinkSequenceTest.kt"
      provides: "Test JVM che locka l'assenza di stato e l'idempotenza della derivazione su sequenze di transizioni ripetute"
      contains: "resolveEffectiveKeepScreenOn"
  key_links:
    - from: "app/src/test/java/com/sed/tachimetro/car/CarLinkSequenceTest.kt"
      to: "com.sed.tachimetro.car.resolveEffectiveKeepScreenOn"
      via: "iterazione su una sequenza di CarLinkState con la stessa preferenza"
      pattern: "resolveEffectiveKeepScreenOn\\("
---

<objective>
Chiudere la Fase 10 con i due livelli di verifica che il comportamento richiede: un gate automatico che locka l'assenza di stato della derivazione sotto connessioni/disconnessioni ripetute, e una sessione DHU dal vivo in cui una persona conferma punto per punto i tre Success Criteria di roadmap.

Purpose: i Success Criteria di questa fase descrivono transizioni osservabili solo con un host Android Auto reale (o DHU) — nessun test automatico puo' far comparire una connessione di proiezione. Il test di sequenza copre pero' la parte che PUO' essere lockata a costo zero e che e' anche la piu' insidiosa: il rischio che una serie di transizioni produca una deriva nella preferenza applicata (il "reset indesiderato" che CONN-02 vieta, e il ciclo rapido connect/disconnect che la Fase 11 verifichera' su strada).
Output: `CarLinkSequenceTest.kt` e un checkpoint umano di sessione DHU con esito registrato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-comportamento-del-telefono-alla-connessione-android-auto/10-01-SUMMARY.md
@.planning/phases/10-comportamento-del-telefono-alla-connessione-android-auto/10-02-SUMMARY.md

<interfaces>
<!-- Contratti consumati da questo piano. -->

Dai Piani 01-02:
- `sealed class CarLinkState` con `data object Disconnected` / `data object Connected` (`com.sed.tachimetro.car`)
- `fun resolveCarLinkState(connectionType: Int?): CarLinkState`
- `fun resolveEffectiveKeepScreenOn(savedPreference: Boolean, link: CarLinkState): Boolean`
- `R.string.android_auto_connected` = "Connesso ad Android Auto"
- `MainActivity`: `setupCarConnectionObserver()`, `onCarLinkChanged()`, `renderSpeedArea()`, `savedKeepOn`, `carLink`, `LOG_TAG = "TachimetroPhone"`

Stile dei test JVM del package (analogo diretto):
- `app/src/test/java/com/sed/tachimetro/car/CarPermissionStateTest.kt` — solo `org.junit.Assert.assertEquals` + `org.junit.Test`, nomi snake_case condizione_risultato, commento inline sopra ogni assert

Runbook DHU gia' esistente:
- `docs/dhu-quota-verification.md` — preparazione AVD/DHU, avvio dell'head unit, uso di adb (parte di setup identica a questa fase)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test di sequenza — nessuna deriva su connessioni/disconnessioni ripetute</name>
  <files>app/src/test/java/com/sed/tachimetro/car/CarLinkSequenceTest.kt</files>
  <read_first>
    - app/src/test/java/com/sed/tachimetro/car/CarLinkStateTest.kt (Piano 01: casi gia' coperti, da NON duplicare qui — questo test aggiunge solo la dimensione "sequenza")
    - app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt (le due funzioni pure sotto test e il contratto dichiarato nel loro KDoc)
    - app/src/test/java/com/sed/tachimetro/car/CarPermissionStateTest.kt (stile esatto dei test JVM del package)
    - .planning/ROADMAP.md sezione "Phase 10" Success Criterion 2 ("senza reset indesiderati") e sezione "Phase 11" Success Criterion 3 (cicli rapidi connect/disconnect)
  </read_first>
  <behavior>
    - Data `pref = true`, applicando la sequenza `Disconnected, Connected, Disconnected, Connected, Connected, Disconnected`, i valori effettivi prodotti sono nell'ordine `true, false, true, false, false, true`
    - Data `pref = false`, la stessa sequenza produce `false, false, false, false, false, false`
    - Per entrambe le preferenze, dopo 20 alternanze `Connected`/`Disconnected` il valore effettivo su `Disconnected` e' ancora esattamente `pref` (nessuna deriva accumulata)
    - Due invocazioni consecutive con lo stesso `link` producono lo stesso risultato (idempotenza: la funzione non ha memoria)
    - `resolveEffectiveKeepScreenOn(pref, resolveCarLinkState(2))` == `false` per entrambe le preferenze, e `resolveEffectiveKeepScreenOn(pref, resolveCarLinkState(0))` == `pref` — la catena completa "tipo di connessione grezzo -> flag effettivo" non introduce alcun passaggio che possa alterare `pref`
  </behavior>
  <action>
Creare `app/src/test/java/com/sed/tachimetro/car/CarLinkSequenceTest.kt`, package `com.sed.tachimetro.car`. Test JVM puro: solo `org.junit.Assert.assertEquals`, `org.junit.Assert.assertTrue` se serve, e `org.junit.Test`. Nessun mocking, nessun Robolectric, nessun import Android salvo eventualmente `androidx.car.app.connection.CarConnection` per le costanti simboliche.

1. KDoc di classe che dichiara lo scopo distinto rispetto a `CarLinkStateTest`: quello copre la tabella di verita' punto per punto; questo copre la dimensione temporale, cioe' la proprieta' che CONN-02 chiama "senza reset indesiderati" — il valore riapplicato alla disconnessione deve dipendere SOLO dalla preferenza e dallo stato corrente, mai dal numero o dall'ordine delle transizioni precedenti. Citare che e' anche la copertura a costo zero del Success Criterion 3 della Fase 11 (cicli rapidi di connessione/disconnessione) sul livello logico.

2. Un `@Test` per ciascuna riga di `<behavior>`, nomi snake_case condizione_risultato, per esempio `alternatingSequence_withPreferenceOn_restoresPreferenceOnEveryDisconnect`, `twentyCycles_leavePreferenceUnchanged`, `sameLinkTwice_producesSameResult`, `rawConnectionType_toEffectiveFlag_neverAltersPreference`.

3. Per le sequenze, costruire una `listOf(CarLinkState.Disconnected, CarLinkState.Connected, ...)` e mappare con `resolveEffectiveKeepScreenOn(pref, link)`, poi confrontare l'intera lista risultante con la lista attesa in un solo `assertEquals` — cosi' un fallimento mostra la sequenza completa invece del solo primo elemento.

4. Per il test delle 20 alternanze, generare la sequenza con un ciclo o `List(40) { if (it % 2 == 0) CarLinkState.Connected else CarLinkState.Disconnected }`, ripiegare la sequenza e asserire che l'ultimo valore calcolato su `Disconnected` e' `pref`. Aggiungere un commento che chiarisce cosa dimostra il test: la funzione non ha stato, quindi nessuna sequenza di transizioni puo' produrre un valore diverso da quello che produrrebbe la stessa transizione isolata.

5. Il test NON deve istanziare `MainActivity`, `CarConnection` o alcun componente Android: e' un test di proprieta' sulle funzioni pure. Il fatto che `MainActivity` chiami effettivamente queste funzioni e' lockato dai grep di `<verify>` del Piano 02, non da qui — dichiararlo in un commento cosi' che nessuno tenti in seguito di trasformare questo file in un test strumentato.

6. Nessuna modifica a file di `app/src/main`. Import espliciti, mai wildcard.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:testDebugUnitTest --tests 'com.sed.tachimetro.car.CarLinkSequenceTest' --console=plain 2>&1 | tail -20</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && T=app/src/test/java/com/sed/tachimetro/car/CarLinkSequenceTest.kt && grep -c 'resolveEffectiveKeepScreenOn(' $T && test "$(grep -c '@Test' $T)" -ge 5 && test "$(grep -c 'MainActivity\|CarConnection(' $T)" -eq 0 && test "$(grep -c 'import .*\*$' $T)" -eq 0 && test "$(git diff --name-only -- app/src/main | wc -l)" -eq 0</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:testDebugUnitTest --console=plain 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `CarLinkSequenceTest.kt` contiene almeno 5 `@Test`, uno per ciascuna riga di `<behavior>`
    - Almeno un test confronta un'INTERA lista di valori effettivi con la lista attesa in un solo `assertEquals` (non un assert per elemento)
    - Esiste un test che applica almeno 20 alternanze `Connected`/`Disconnected` e asserisce che il valore su `Disconnected` e' ancora esattamente la preferenza iniziale, per entrambi i valori di preferenza
    - Esiste un test che passa per la catena completa `resolveCarLinkState(...)` -> `resolveEffectiveKeepScreenOn(...)`
    - Il file non istanzia `MainActivity` ne' `CarConnection` (verificabile: `grep -c 'MainActivity\|CarConnection('` restituisce 0)
    - Il KDoc di classe dichiara la differenza di scopo rispetto a `CarLinkStateTest` e cita CONN-02
    - `./gradlew.bat :app:testDebugUnitTest` termina con `BUILD SUCCESSFUL` e 0 test falliti su TUTTA la suite
    - `git diff --name-only -- app/src/main` e' vuoto
  </acceptance_criteria>
  <done>Nessuna sequenza di connessioni e disconnessioni, per quanto lunga o rapida, puo' produrre un valore di "schermo sempre acceso" diverso dalla preferenza salvata alla disconnessione.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verifica DHU dal vivo del comportamento del telefono (SC1/SC2/SC3 di roadmap)</name>
  <files>(nessun file modificato -- checkpoint di verifica)</files>
  <read_first>
    - docs/dhu-quota-verification.md (runbook della Fase 8: preparazione AVD/DHU, avvio dell'head unit, comandi adb — la parte di setup e' identica qui)
    - .planning/ROADMAP.md sezione "Phase 10" (i tre Success Criteria da confermare punto per punto)
    - .planning/REQUIREMENTS.md sezione "Connessione Android Auto" e la riga "Spegnimento/blocco forzato dello schermo del telefono" della tabella Out of Scope (il limite da NON confondere con un fallimento)
    - .planning/phases/10-comportamento-del-telefono-alla-connessione-android-auto/10-02-SUMMARY.md (cosa e' stato effettivamente cablato e dove)
  </read_first>
  <action>
Fermare l'esecuzione e consegnare alla persona la procedura descritta in `<how-to-verify>`. E' il gate di chiusura della fase: i tre Success Criteria descrivono transizioni che nessun test automatico puo' osservare, perche' richiedono una connessione di proiezione reale.

Prima di presentare il checkpoint, l'esecutore prepara il terreno:
1. Eseguire `./gradlew.bat :app:installDebug` e riportare l'esito.
2. Eseguire `adb devices` e riportare l'elenco, confermando almeno un dispositivo/emulatore in stato `device`.
3. Stampare il comando per osservare le transizioni durante la sessione: `adb logcat -s TachimetroPhone` — ogni riga `carLink=... savedKeepOn=... effectiveKeepOn=...` documenta una transizione osservata dal telefono. Precisare che `adb logcat -s TachimetroCar` mostra invece il lato schermo auto (Fasi 8-9) e che i due tag possono essere seguiti insieme con `adb logcat -s TachimetroPhone TachimetroCar`.
4. Stampare il comando per leggere la preferenza persistita e dimostrare CONN-02 su disco:
   `adb shell run-as com.sed.tachimetro cat /data/data/com.sed.tachimetro/shared_prefs/tachimetro_prefs.xml`
   (funziona sulle build debug; se `run-as` non e' disponibile sul dispositivo in uso, l'esecutore lo segnala e la verifica di CONN-02 si basa sui soli punti osservabili dall'interfaccia).
5. Presentare la checklist di `<how-to-verify>` e attendere la risposta.

L'esecutore NON deve dichiarare superato il gate sulla base del solo logcat: il comportamento dello schermo del telefono e la reazione dell'host non sono verificabili da codice. Se uno scenario fallisce, l'esecutore riporta il sintomo osservato e attende istruzioni; NON introduce di propria iniziativa meccanismi non previsti (per esempio disabilitare lo switch durante la proiezione, o forzare lo spegnimento dello schermo — quest'ultimo e' esplicitamente Out of Scope in REQUIREMENTS.md).
  </action>
  <what-built>
    - `CarLinkState` + `resolveCarLinkState()`: modello e mappatura fail-safe del tipo di connessione riportato da `CarConnection`; solo `CONNECTION_TYPE_PROJECTION` conta come connesso (Piano 01)
    - `resolveEffectiveKeepScreenOn()`: funzione pura che rilascia `FLAG_KEEP_SCREEN_ON` durante la proiezione e restituisce esattamente la preferenza salvata alla disconnessione, senza alcun accesso al file di preferenze (Piano 01)
    - Stringa italiana `android_auto_connected` = "Connesso ad Android Auto", mostrata sul telefono (Piano 01)
    - `MainActivity`: osservazione di `CarConnection` con il lifecycle dell'Activity, `renderSpeedArea()` con il ramo dello stato neutro, listener dello switch riscritto sulla derivazione pura, log diagnostico `TachimetroPhone` gated su `BuildConfig.DEBUG` (Piano 02)
    - Test JVM: `CarLinkStateTest` (tabella di verita' completa) e `CarLinkSequenceTest` (nessuna deriva su transizioni ripetute)
  </what-built>
  <how-to-verify>
    Setup iniziale (identico alla Fase 8, vedi `docs/dhu-quota-verification.md`): telefono/emulatore con Google Play e Android Auto, Developer Mode attivo, Desktop Head Unit pronto, build debug installata con `./gradlew.bat :app:installDebug`, permesso di localizzazione GIA' concesso. Tenere aperta una finestra con `adb logcat -s TachimetroPhone TachimetroCar`.

    **Scenario A — connessione con "Sempre acceso" ATTIVO (SC1 di roadmap, CONN-01)**
    1. Aprire Tachimetro sul telefono, mettere lo switch "Sempre acceso" su ATTIVO, verificare che il numero della velocita' (o "Ricerca segnale GPS...") sia visibile.
    2. Avviare il DHU / collegare Android Auto, tenendo il telefono acceso e visibile.
    - [ ] A1 — sul TELEFONO compare **"Connesso ad Android Auto"** al posto del numero della velocita'
    - [ ] A2 — l'etichetta "km/h" sparisce insieme al numero (nessuna unita' orfana sullo schermo)
    - [ ] A3 — lo schermo del telefono torna a spegnersi da solo secondo il timeout di sistema, invece di restare acceso indefinitamente (attendere il timeout impostato nelle impostazioni del telefono; se il timeout e' lungo, abbassarlo temporaneamente a 15-30 secondi prima della prova)
    - [ ] A4 — lo switch "Sempre acceso" e' ancora visibile e ancora su ATTIVO: la scelta dell'utente non e' stata cambiata sotto i suoi occhi
    - [ ] A5 — sullo schermo AUTO la velocita' continua a comportarsi come nelle Fasi 8-9 (nessuna regressione lato auto)
    - [ ] A6 — logcat mostra una riga `carLink=Connected savedKeepOn=true effectiveKeepOn=false`

    **Scenario B — disconnessione con ripristino esatto (SC2 di roadmap, CONN-02)**
    3. Scollegare Android Auto / chiudere il DHU, con il telefono in primo piano sull'app.
    - [ ] B1 — sul telefono ricompare il numero della velocita' (o "Ricerca segnale GPS...") entro pochi secondi, **senza** riaprire l'app
    - [ ] B2 — lo schermo torna a restare sempre acceso, cioe' la preferenza ATTIVA e' stata riapplicata
    - [ ] B3 — lo switch "Sempre acceso" e' ancora su ATTIVO
    - [ ] B4 — logcat mostra `carLink=Disconnected savedKeepOn=true effectiveKeepOn=true`
    - [ ] B5 — `adb shell run-as com.sed.tachimetro cat /data/data/com.sed.tachimetro/shared_prefs/tachimetro_prefs.xml` mostra `keep_screen_on` con lo stesso valore di prima del collegamento (nessuna riscrittura durante il ciclo)

    **Scenario C — connessione con "Sempre acceso" DISATTIVO (CONN-02, ramo speculare)**
    4. Mettere lo switch su DISATTIVO, collegare Android Auto, poi scollegare.
    - [ ] C1 — alla connessione compare comunque "Connesso ad Android Auto" sul telefono
    - [ ] C2 — alla disconnessione lo switch e' ancora DISATTIVO e lo schermo continua a spegnersi da solo: la preferenza "automatica" non e' stata promossa ad "attiva" da sola
    - [ ] C3 — logcat mostra `savedKeepOn=false` sia sulla riga `Connected` sia su quella `Disconnected`

    **Scenario D — nessuna regressione del toggle a Android Auto NON connesso (SC3 di roadmap)**
    5. Con Android Auto scollegato, azionare lo switch avanti e indietro alcune volte.
    - [ ] D1 — attivandolo, lo schermo resta acceso immediatamente, senza riavviare l'app (comportamento v1.0/v1.1)
    - [ ] D2 — disattivandolo, lo schermo torna al timeout di sistema immediatamente
    - [ ] D3 — chiudendo e riaprendo l'app, lo switch riapre sull'ultimo valore scelto
    - [ ] D4 — al primo avvio dopo `adb shell pm clear com.sed.tachimetro`, con il telefono in carica, lo switch parte su ATTIVO come in v1.0 (default derivato dalla ricarica, D-04/D-05 della Fase 5)

    **Scenario E — MAX, distanza e ricarica durante la proiezione**
    6. Con una posizione simulata in movimento (o durante un breve tragitto reale a telefono montato), collegare Android Auto mentre l'app registra velocita'.
    - [ ] E1 — durante la proiezione, le aree MAX e distanza sul telefono restano visibili e continuano ad aggiornarsi: lo stato neutro sostituisce solo il numero grande, non congela il viaggio
    - [ ] E2 — alla disconnessione, MAX e distanza NON sono azzerati ne' arretrati
    - [ ] E3 — l'icona di ricarica (se il telefono e' collegato all'alimentazione) si comporta come in v1.1

    **Scenario F — cicli rapidi e robustezza**
    7. Collegare e scollegare Android Auto 3-4 volte in rapida successione.
    - [ ] F1 — l'app non va in crash e non resta bloccata sul messaggio neutro dopo l'ultima disconnessione
    - [ ] F2 — lo switch e la preferenza persistita sono ancora sul valore scelto all'inizio del ciclo
    - [ ] F3 — `adb logcat -b crash -d` non mostra crash di `com.sed.tachimetro` durante la sessione

    **Scenario G — limite gia' documentato, da NON scambiare per un fallimento**
    `REQUIREMENTS.md` (tabella Out of Scope) stabilisce che l'app non puo' e non deve spegnere o bloccare forzatamente lo schermo del telefono: non esiste API pubblica senza permessi Device Admin sproporzionati. Rilasciare `FLAG_KEEP_SCREEN_ON` restituisce il controllo al timeout di sistema, ed e' il massimo ottenibile. Se lo schermo del telefono resta acceso perche' il timeout di sistema e' lungo, o perche' l'utente lo tocca, NON e' un fallimento di A3.
    - [ ] G1 — la persona conferma di aver valutato A3 rispetto al timeout di sistema e non rispetto a uno spegnimento immediato
  </how-to-verify>
  <resume-signal>Rispondere "approvato" solo se A1-A6, B1-B5, C1-C3, D1-D4, E1-E3, F1-F3 e G1 sono tutti confermati. Altrimenti indicare quale punto e' fallito e con quale sintomo osservato.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:installDebug --console=plain -q && adb devices | grep -v 'List of devices' | grep -c 'device$'</automated>
    <human-check>Sessione DHU con ciclo completo connessione/disconnessione nei due rami di preferenza (attiva e disattiva), conferma punto per punto di A1-G1.</human-check>
  </verify>
  <acceptance_criteria>
    - L'esecutore ha eseguito `./gradlew.bat :app:installDebug` con esito `BUILD SUCCESSFUL` e ha riportato l'output di `adb devices` con almeno una riga in stato `device`
    - L'esecutore ha stampato i comandi esatti `adb logcat -s TachimetroPhone TachimetroCar`, `adb shell run-as com.sed.tachimetro cat /data/data/com.sed.tachimetro/shared_prefs/tachimetro_prefs.xml` e `adb shell pm clear com.sed.tachimetro`
    - La persona ha risposto ai singoli punti A1-A6, B1-B5, C1-C3, D1-D4, E1-E3, F1-F3, G1 — non con un'approvazione generica
    - In caso di esito approvato: `git diff --name-only` non elenca alcun file sotto `app/src`
    - In caso di fallimento di uno scenario: il sintomo osservato e' riportato testualmente e nessuna correzione e' stata applicata di iniziativa dall'esecutore
    - Se `run-as` non e' disponibile, la limitazione e' riportata esplicitamente e B5 e' marcato come non verificabile invece che dichiarato superato
  </acceptance_criteria>
  <done>Una persona ha percorso dal vivo su DHU il ciclo completo connessione/disconnessione nei due rami di preferenza e ha confermato i tre Success Criteria di roadmap, l'assenza di regressioni sul toggle e su MAX/distanza, e la robustezza ai cicli rapidi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Persona che esegue il checkpoint <-> esito dichiarato della fase | Un'approvazione generica non verificata chiuderebbe la fase su un comportamento non osservato. Il gate richiede risposte punto per punto. |
| Dispositivo di test (adb/DHU) <-> workstation | La sessione richiede `adb` e l'installazione di una build debug su un dispositivo fisico o emulatore. |
| File `tachimetro_prefs.xml` letto via `run-as` <-> verifica di CONN-02 | La lettura diretta del file di preferenze e' una prova su disco che le transizioni non hanno riscritto la preferenza. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-13 | Repudiation | esito del checkpoint umano | mitigate | La checklist e' articolata in punti identificati (A1..G1) e il criterio di accettazione richiede una risposta per ciascuno; un "approvato" generico non soddisfa il gate. L'esito viene registrato nel SUMMARY della fase, come gia' fatto per le Fasi 8 e 9. |
| T-10-14 | Information Disclosure | `run-as` + `cat` del file di preferenze | accept | Il comando funziona solo su build debug e solo per l'app stessa; il file contiene esclusivamente max velocita', distanza, contatore dei rifiuti e il booleano schermo-sempre-acceso — nessun dato personale, nessuna coordinata. Il valore diagnostico (prova su disco di CONN-02) supera il rischio nullo. |
| T-10-15 | Information Disclosure | logcat durante la sessione | mitigate | Il tag `TachimetroPhone` stampa solo stato del collegamento e booleani di preferenza (T-10-10, Piano 02) e la riga esiste solo nelle build debug. Nessun dato di posizione compare in logcat. |
| T-10-16 | Tampering | correzioni improvvisate durante il checkpoint | mitigate | L'azione vieta esplicitamente all'esecutore di applicare correzioni di propria iniziativa in caso di fallimento, e un criterio di accettazione richiede che `git diff --name-only` non elenchi file sotto `app/src` in caso di esito approvato. |
| T-10-SC | Tampering | installazioni da package manager | n/a | Nessun nuovo pacchetto: il piano aggiunge un solo file di test che usa JUnit 4, gia' presente come `testImplementation(libs.junit)`. |
</threat_model>

<verification>
1. `./gradlew.bat :app:testDebugUnitTest --console=plain` -> BUILD SUCCESSFUL su tutta la suite (`CarLinkStateTest`, `CarLinkSequenceTest`, `CarPermissionStateTest`, `CarSpeedContentTest`, `MaxSpeedReducerTest`, `SpeedMappingTest`, `GpsSpeedProviderStateTest`, `DistanceReducerTest`, `DistanceFormatTest`, `ChargingStateProviderStateTest`)
2. `./gradlew.bat :app:installDebug --console=plain` -> BUILD SUCCESSFUL
3. `git diff --name-only` -> esclusivamente `app/src/test/java/com/sed/tachimetro/car/CarLinkSequenceTest.kt`
4. Checkpoint umano: tutti i punti A1-G1 confermati esplicitamente
</verification>

<success_criteria>
- Il test di sequenza dimostra che nessuna serie di transizioni, per quanto lunga o rapida, altera il valore riapplicato alla disconnessione
- Una persona ha confermato dal vivo su DHU i tre Success Criteria della Fase 10 nella roadmap
- Nessuna regressione osservata su toggle "Sempre acceso", MAX, distanza, indicatore di ricarica e schermo auto
- Nessun file di `app/src/main` modificato da questo piano
</success_criteria>

<output>
Create `.planning/phases/10-comportamento-del-telefono-alla-connessione-android-auto/10-03-SUMMARY.md` when done
</output>
