---
phase: 10-comportamento-del-telefono-alla-connessione-android-auto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt
  - app/src/test/java/com/sed/tachimetro/car/CarLinkStateTest.kt
  - app/src/main/res/values/strings.xml
autonomous: true
requirements: [CONN-01, CONN-02]
user_setup: []

must_haves:
  truths:
    - "Esiste un modello di stato che distingue 'Android Auto sta proiettando sul display auto' da 'nessuna proiezione in corso'"
    - "Un valore di tipo di connessione sconosciuto, negativo o nullo proveniente dal provider di Android Auto non viene MAI interpretato come 'connesso' (fail-safe: il telefono non si oscura mai per un dato spurio)"
    - "L'esecuzione su Android Automotive OS (CONNECTION_TYPE_NATIVE) non viene interpretata come proiezione"
    - "Esiste una funzione pura che, dati la preferenza salvata e lo stato del collegamento, produce il valore effettivo di 'schermo sempre acceso': alla connessione il flag e' sempre rilasciato, alla disconnessione torna esattamente la preferenza salvata"
    - "La funzione pura non puo' in alcun modo alterare la preferenza salvata: la riceve come parametro e restituisce un Boolean derivato"
    - "Esiste la stringa italiana dello stato neutro mostrato SUL TELEFONO quando Android Auto e' connesso"
  artifacts:
    - path: "app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt"
      provides: "Modello sealed CarLinkState + resolveCarLinkState() + resolveEffectiveKeepScreenOn()"
      contains: "sealed class CarLinkState"
      min_lines: 45
    - path: "app/src/test/java/com/sed/tachimetro/car/CarLinkStateTest.kt"
      provides: "Test JVM puri che lockano la mappatura dei tipi di connessione e la tabella di verita' del flag effettivo"
      contains: "resolveEffectiveKeepScreenOn"
    - path: "app/src/main/res/values/strings.xml"
      provides: "Copy italiana dello stato neutro lato telefono"
      contains: "android_auto_connected"
  key_links:
    - from: "app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt"
      to: "androidx.car.app.connection.CarConnection.CONNECTION_TYPE_PROJECTION"
      via: "confronto sul valore costante della libreria, mai un letterale numerico duplicato"
      pattern: "CarConnection\\.CONNECTION_TYPE_PROJECTION"
    - from: "app/src/test/java/com/sed/tachimetro/car/CarLinkStateTest.kt"
      to: "com.sed.tachimetro.car.resolveCarLinkState"
      via: "assertEquals su ogni valore di tipo di connessione"
      pattern: "resolveCarLinkState\\("
    - from: "app/src/test/java/com/sed/tachimetro/car/CarLinkStateTest.kt"
      to: "com.sed.tachimetro.car.resolveEffectiveKeepScreenOn"
      via: "assertEquals sui quattro casi della tabella di verita'"
      pattern: "resolveEffectiveKeepScreenOn\\("
---

<objective>
Costruire le fondamenta pure e testabili del comportamento del telefono alla connessione Android Auto: il modello `CarLinkState`, la mappatura fail-safe del tipo di connessione riportato dal provider di Android Auto, la funzione pura che deriva il valore effettivo di "schermo sempre acceso" (CONN-01 + CONN-02 in un solo punto) e la stringa italiana dello stato neutro mostrato sul telefono.

Purpose: CONN-02 richiede che alla disconnessione torni ESATTAMENTE la preferenza salvata "senza alterare la preferenza memorizzata". Il modo piu' sicuro di garantirlo e' non avere affatto due variabili di stato che possano divergere: la preferenza persistita resta l'unica sorgente di verita' e il flag effettivo e' un valore DERIVATO da una funzione pura, testata a livello JVM, che non ha accesso al file di preferenze. Il cablaggio in `MainActivity` (Piano 02) diventa cosi' una sola riga di chiamata invece di una macchina a stati sparsa fra listener e callback.
Output: `CarLinkState.kt` (sealed + due funzioni pure), `CarLinkStateTest.kt`, una nuova voce in `strings.xml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Contratti gia' esistenti nel codebase o nella libreria, consumati o rispecchiati da questo piano. -->

Da androidx.car.app:app:1.7.0 (gia' dipendenza del progetto, `libs.car.app` in app/build.gradle.kts riga 86) —
`androidx.car.app.connection.CarConnection` (verificato leggendo il sorgente dell'artifact):
- `public CarConnection(@NonNull Context context)` — annotato `@MainThread`
- `public @NonNull LiveData<Integer> getType()` — in Kotlin si legge come proprieta' `type`
- `public static final int CONNECTION_TYPE_NOT_CONNECTED = 0`
- `public static final int CONNECTION_TYPE_NATIVE = 1`  // in esecuzione NATIVA su Android Automotive OS
- `public static final int CONNECTION_TYPE_PROJECTION = 2`  // proiezione Android Auto: il caso di questa fase
Le tre costanti sono `static final int` inizializzate da espressioni costanti: il compilatore Kotlin le INLINE nel bytecode del chiamante, quindi referenziarle da un file compilato in `src/main` non impedisce ai test JVM puri di `src/test` di girare senza runtime Android.

Da app/src/main/java/com/sed/tachimetro/car/CarPermissionState.kt (analogo strutturale ESATTO di questo piano — stesso package, stessa forma "sealed + funzioni pure nello stesso file"):
- `sealed class CarPermissionState` con `data object Granted`, `data object NotRequested`, `data object Waiting`, `data class Denied(val permanent: Boolean)`
- `fun resolveCarPermissionState(granted: Boolean, denialCount: Int): CarPermissionState`
- `fun sanitizeDenialCount(raw: Int): Int = if (raw < 0) 0 else raw`

Da app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt (NON modificato da questa fase — resta l'unica sorgente di verita' della preferenza):
- `class ScreenOnPreferenceStore(context: Context)`
- `fun read(): Boolean?` — null = nessuna preferenza ancora salvata (primo avvio, D-05 della Fase 5)
- `fun write(keepOn: Boolean)`
- `companion object { const val PREFS_NAME = "tachimetro_prefs"; const val KEY_KEEP_SCREEN_ON = "keep_screen_on" }`

Da app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt (forma della funzione pura top-level da copiare):
- `fun reduceMax(currentMax: Int, reading: Int): Int`
- `fun sanitizePersistedMax(raw: Int): Int = if (raw < 0) 0 else raw`

Stringhe gia' presenti in app/src/main/res/values/strings.xml (riferimento di tono, NON da modificare):
- `status_ready` = "Pronto"
- `searching_gps_signal` = "Ricerca segnale GPS..."
- `car_searching_gps_signal` = "Ricerca segnale..." (schermo AUTO — le stringhe con prefisso `car_` sono quelle rese dall'host Android Auto)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Modello CarLinkState e le due funzioni pure (CONN-01, CONN-02)</name>
  <files>app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt, app/src/test/java/com/sed/tachimetro/car/CarLinkStateTest.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/car/CarPermissionState.kt (analogo esatto: KDoc di file che motiva il meccanismo, `sealed class` con `data object` per gli stati senza payload, funzioni pure top-level nello stesso file, `when` esaustivo, KDoc per caso con il tag del requisito)
    - app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt (forma minimale del sealed model del progetto)
    - app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedReducer.kt (forma della funzione pura top-level: nessuna classe contenitore, KDoc con il tag della decisione)
    - app/src/test/java/com/sed/tachimetro/car/CarPermissionStateTest.kt (stile dei test JVM del package `car/`: solo `org.junit.Assert.assertEquals` + `org.junit.Test`, nomi snake_case condizione_risultato, commento inline sopra ogni assert con la chiamata letterale e il valore atteso)
    - app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt (la preferenza persistita che questa funzione NON deve mai toccare)
  </read_first>
  <behavior>
    - `resolveCarLinkState(CarConnection.CONNECTION_TYPE_PROJECTION)` restituisce `CarLinkState.Connected`
    - `resolveCarLinkState(CarConnection.CONNECTION_TYPE_NOT_CONNECTED)` restituisce `CarLinkState.Disconnected`
    - `resolveCarLinkState(CarConnection.CONNECTION_TYPE_NATIVE)` restituisce `CarLinkState.Disconnected` (esecuzione nativa su Automotive OS: non c'e' un secondo schermo telefono da neutralizzare)
    - `resolveCarLinkState(null)` restituisce `CarLinkState.Disconnected` (nessun valore ancora emesso dal provider)
    - `resolveCarLinkState(-1)` restituisce `CarLinkState.Disconnected`
    - `resolveCarLinkState(99)` restituisce `CarLinkState.Disconnected` (valore futuro/sconosciuto: fail-safe verso "non connesso")
    - `resolveEffectiveKeepScreenOn(savedPreference = true, link = CarLinkState.Disconnected)` restituisce `true`
    - `resolveEffectiveKeepScreenOn(savedPreference = false, link = CarLinkState.Disconnected)` restituisce `false`
    - `resolveEffectiveKeepScreenOn(savedPreference = true, link = CarLinkState.Connected)` restituisce `false` (CONN-01: il flag viene rilasciato anche se la preferenza era attiva)
    - `resolveEffectiveKeepScreenOn(savedPreference = false, link = CarLinkState.Connected)` restituisce `false`
    - Round-trip CONN-02: per ogni `pref` in `listOf(true, false)`, `resolveEffectiveKeepScreenOn(pref, CarLinkState.Disconnected)` dopo un passaggio da `Connected` restituisce di nuovo esattamente `pref` (la funzione e' senza stato: lo stesso input produce sempre lo stesso output)
  </behavior>
  <action>
Creare `app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt` (package `com.sed.tachimetro.car`) e il test JVM `app/src/test/java/com/sed/tachimetro/car/CarLinkStateTest.kt` (stesso package). Scrivere PRIMA il test con tutti i casi elencati in `<behavior>`, poi l'implementazione minima che li soddisfa.

1. `sealed class CarLinkState` con esattamente due sottotipi `data object`, nella forma di `CarPermissionState.kt`:
   - `data object Disconnected` — nessuna proiezione Android Auto in corso; il telefono si comporta esattamente come in v1.0/v1.1
   - `data object Connected` — Android Auto sta proiettando su un display auto (`CONNECTION_TYPE_PROJECTION`); il telefono passa allo stato neutro e rilascia `FLAG_KEEP_SCREEN_ON` (CONN-01)
   Una riga di KDoc per ciascun caso, citando il requisito (CONN-01 / CONN-02) come fa `CarPermissionState.kt` con i tag D-XX.

2. Funzione pura top-level `fun resolveCarLinkState(connectionType: Int?): CarLinkState`:
   - il parametro e' NULLABLE di proposito: `CarConnection.getType()` e' un `LiveData<Integer>` che in Kotlin arriva come piattaforma-type e puo' non avere ancora emesso alcun valore; assorbire il null qui evita un `?:` con valore magico nel chiamante (Piano 02)
   - unico ramo che produce `Connected`: `connectionType == CarConnection.CONNECTION_TYPE_PROJECTION`. Ogni altro valore — `CONNECTION_TYPE_NOT_CONNECTED`, `CONNECTION_TYPE_NATIVE`, null, negativi, valori futuri sconosciuti — produce `Disconnected`
   - importare `androidx.car.app.connection.CarConnection` SOLO per la costante. NON scrivere il letterale `2`: la costante e' un `static final int` compile-time, quindi il compilatore Kotlin la inlinea e il file resta eseguibile da un test JVM puro senza runtime Android
   - il KDoc deve motivare esplicitamente le due scelte non ovvie: (a) perche' `CONNECTION_TYPE_NATIVE` NON e' "connesso" ai fini di questa fase (su Android Automotive OS l'app gira nativamente sull'head unit, non esiste un secondo schermo telefono da mettere in stato neutro: trattarlo come connesso oscurerebbe l'unica interfaccia esistente); (b) perche' il default e' `Disconnected` (fail-safe: un valore spurio o manomesso non deve MAI poter sostituire il tachimetro con un messaggio neutro sul telefono — T-10-02)

3. Funzione pura top-level `fun resolveEffectiveKeepScreenOn(savedPreference: Boolean, link: CarLinkState): Boolean`, con un `when` esaustivo su `link` e nessun `else`:
   - `CarLinkState.Connected` -> `false` (CONN-01: il flag viene sempre rilasciato durante la proiezione, qualunque sia la preferenza)
   - `CarLinkState.Disconnected` -> `savedPreference` (CONN-02: alla disconnessione torna ESATTAMENTE la preferenza salvata, attiva se era attiva, automatica se era automatica)
   - il KDoc deve dichiarare il contratto architetturale su cui poggia CONN-02: questa funzione non ha alcun accesso a `ScreenOnPreferenceStore`; riceve la preferenza come parametro e restituisce un valore DERIVATO. Non esiste quindi alcun percorso in cui una transizione di connessione possa scrivere la preferenza persistita. Il chiamante (Piano 02) deve chiamare `screenOnStore.write()` SOLO dal listener dello switch, mai da qui.
   - documentare che lo switch dell'interfaccia continua a riflettere la PREFERENZA SALVATA e non il flag effettivo: mentre Android Auto e' connesso lo switch puo' restare su "acceso" mentre il flag e' rilasciato, ed e' il comportamento voluto (CONN-02: "senza alterare la preferenza memorizzata")

4. KDoc di file che inquadra il meccanismo: la connessione Android Auto viene osservata lato telefono con `androidx.car.app.connection.CarConnection`, indipendente da `CarAppService`/`Session`/`SpeedScreen` (nessuna comunicazione fra i due processi/superfici e' necessaria). Citare che il provider consultato da `CarConnection` e' esposto dall'app Android Auto e che l'elemento `<queries>` richiesto dal package visibility di Android 11+ e' gia' dichiarato nel manifest della libreria `androidx.car.app:app` e viene unito automaticamente: NON aggiungere alcun `<queries>` ne' alcun permesso al manifest dell'app.

5. Il test `CarLinkStateTest` copre un `@Test` per ogni riga di `<behavior>`, con il commento inline sopra ogni `assertEquals` nello stile di `CarPermissionStateTest.kt`. Solo `org.junit.Assert.assertEquals` e `org.junit.Test`; nessun mocking, nessun Robolectric. Il test importa `androidx.car.app.connection.CarConnection` per usare le costanti simboliche invece dei numeri nudi. Se — e solo se — l'esecuzione del test fallisse con un errore di caricamento classi su `CarConnection`, sostituire nel TEST le costanti con i letterali `0`, `1`, `2` accompagnati da un commento che cita il nome della costante corrispondente, lasciando il file di `src/main` invariato; riportare la deviazione nel SUMMARY.

6. Import espliciti, mai wildcard. Nessun riferimento a `CarPermissionState`, `CarSpeedContent` o `SpeedState`: i modelli restano ortogonali.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:testDebugUnitTest --tests 'com.sed.tachimetro.car.CarLinkStateTest' --console=plain 2>&1 | tail -20</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && S=app/src/main/java/com/sed/tachimetro/car/CarLinkState.kt && T=app/src/test/java/com/sed/tachimetro/car/CarLinkStateTest.kt && C=$(grep -v '^\s*[/*]' $S) && echo "$C" | grep -c 'sealed class CarLinkState' && echo "$C" | grep -c 'CarConnection.CONNECTION_TYPE_PROJECTION' && echo "$C" | grep -c 'fun resolveEffectiveKeepScreenOn(' && test "$(echo "$C" | grep -c 'ScreenOnPreferenceStore\|getSharedPreferences\|\.write(')" -eq 0 && test "$(echo "$C" | grep -c '^import android\.')" -eq 0 && test "$(grep -c 'assertEquals' $T)" -ge 11 && test "$(grep -c 'import .*\*$' $S $T | grep -cv ':0$')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `CarLinkState.kt` dichiara `sealed class CarLinkState` con esattamente due sottotipi, entrambi `data object`: `Disconnected` e `Connected`
    - `resolveCarLinkState` e `resolveEffectiveKeepScreenOn` sono funzioni top-level (nessuna classe/object contenitore), come in `MaxSpeedReducer.kt` e `CarPermissionState.kt`
    - `resolveCarLinkState` accetta `Int?` e produce `Connected` esclusivamente per `CarConnection.CONNECTION_TYPE_PROJECTION`; il confronto usa la costante simbolica, non il letterale `2` (verificabile: `grep -c 'CarConnection.CONNECTION_TYPE_PROJECTION'` >= 1)
    - `CarLinkState.kt` non contiene alcun import `android.*` e nessun riferimento a `ScreenOnPreferenceStore`, `getSharedPreferences` o a un metodo `.write(` (verificabile con i grep negativi del blocco verify)
    - `resolveEffectiveKeepScreenOn` usa un `when` esaustivo su `CarLinkState` senza `else`, restituisce `false` per `Connected` e `savedPreference` per `Disconnected`
    - Il KDoc di `resolveCarLinkState` spiega perche' `CONNECTION_TYPE_NATIVE` non e' trattato come connesso e perche' il default e' `Disconnected` (fail-safe)
    - Il KDoc di `resolveEffectiveKeepScreenOn` dichiara che la funzione non ha accesso alla preferenza persistita e che `screenOnStore.write()` va chiamato solo dal listener dello switch
    - `CarLinkStateTest.kt` contiene almeno 11 `assertEquals`, uno per ciascun caso elencato in `<behavior>`, con nomi di metodo snake_case condizione_risultato
    - `./gradlew.bat :app:testDebugUnitTest --tests 'com.sed.tachimetro.car.CarLinkStateTest'` termina con `BUILD SUCCESSFUL` e 0 test falliti
    - Nessun import wildcard in nessuno dei due file
  </acceptance_criteria>
  <done>CONN-01 e CONN-02 sono espressi come un'unica funzione pura senza stato, testata su tutta la tabella di verita', e nessun valore di connessione spurio puo' produrre lo stato "connesso".</done>
</task>

<task type="auto">
  <name>Task 2: Stringa italiana dello stato neutro lato telefono (CONN-01)</name>
  <files>app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/src/main/res/values/strings.xml (stato attuale: `status_ready`, `searching_gps_signal`, il blocco di stringhe con prefisso `car_` e i commenti XML che citano il tag della decisione)
    - .planning/REQUIREMENTS.md sezione "Connessione Android Auto" (testo esatto richiesto da CONN-01)
    - .planning/ROADMAP.md sezione "Phase 10" (Success Criteria 1: la copy esatta attesa)
  </read_first>
  <action>
Aggiungere a `app/src/main/res/values/strings.xml` esattamente UNA nuova stringa, inserita subito dopo `searching_gps_signal` (cioe' PRIMA del blocco delle stringhe con prefisso `car_`), preceduta da un commento XML nello stile gia' usato nel file.

1. Nome della risorsa: `android_auto_connected`. Valore: `Connesso ad Android Auto` — testo esatto di CONN-01 e del Success Criterion 1 della roadmap, senza punteggiatura finale.

2. Il commento XML deve citare `CONN-01` e chiarire il punto che altrimenti confonde chi legge il file: questa stringa e' mostrata SUL TELEFONO, non sullo schermo auto. Le stringhe con prefisso `car_` sono quelle rese dall'host Android Auto (Fasi 8-9); questa e' l'unica del file che parla di Android Auto ma vive nell'interfaccia del telefono, ed e' per questo che NON usa il prefisso `car_`.

3. NON modificare, rinominare o riordinare alcuna stringa esistente. In particolare `status_ready` ("Pronto") resta invariata: e' lo stato mostrato quando il permesso e' concesso ma Android Auto NON e' connesso.

4. NON creare varianti in `values-night/` o altri qualificatori: e' una stringa, non una risorsa dipendente dal tema.

5. NON introdurre segnaposto di formato (`%1$s` e simili): la stringa e' statica e non interpola alcun dato esterno (T-10-05).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && F=app/src/main/res/values/strings.xml && grep -c 'name="android_auto_connected">Connesso ad Android Auto<' $F && grep -c 'name="status_ready">Pronto<' $F && grep -c 'name="searching_gps_signal">Ricerca segnale GPS...<' $F && test "$(grep -c 'name="car_connected\|name="car_android_auto' $F)" -eq 0 && test "$(git diff --numstat -- $F | awk '{print $2}')" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `strings.xml` contiene `<string name="android_auto_connected">Connesso ad Android Auto</string>`, carattere per carattere come in CONN-01
    - La nuova stringa e' preceduta da un commento XML che contiene la sigla `CONN-01` e dichiara esplicitamente che la stringa e' mostrata sul telefono e non sullo schermo auto
    - Il nome della risorsa NON usa il prefisso `car_` (verificabile: `grep -c 'name="car_connected\|name="car_android_auto'` restituisce 0)
    - `git diff --numstat app/src/main/res/values/strings.xml` mostra 0 righe rimosse: nessuna stringa esistente e' stata modificata o riordinata
    - La stringa non contiene segnaposto di formato
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>La copy esatta richiesta dal Success Criterion 1 della roadmap esiste come risorsa e il Piano 02 puo' risolverla con `getString(R.string.android_auto_connected)`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider `androidx.car.app.connection` (app Android Auto) <-> processo dell'app | Il tipo di connessione e' un `Int` prodotto da un componente esterno all'app. Va trattato come input non fidato: nessun valore, per quanto inatteso, deve produrre uno stato incoerente. |
| Funzioni pure di `CarLinkState.kt` <-> preferenza persistita `keep_screen_on` | Confine deliberatamente NON attraversato in questo piano: le funzioni pure non hanno alcun accesso a `SharedPreferences`. Il vincolo e' verificabile staticamente (grep negativo su `getSharedPreferences`/`ScreenOnPreferenceStore`). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `resolveCarLinkState(connectionType)` | mitigate | Allow-list a valore singolo: solo `CarConnection.CONNECTION_TYPE_PROJECTION` produce `Connected`; null, negativi, `CONNECTION_TYPE_NATIVE` e qualunque valore futuro sconosciuto ricadono su `Disconnected`. Lockato dai test `resolveCarLinkState(null/-1/99) == Disconnected`. Fail-safe verso il comportamento v1.1 gia' approvato. |
| T-10-02 | Denial of Service | interfaccia del telefono | mitigate | La direzione fail-safe e' scelta di proposito: un dato spurio non puo' sostituire il tachimetro con il messaggio neutro (che sarebbe una negazione della funzione principale dell'app), puo' al massimo lasciare il telefono nel comportamento v1.1. Il caso opposto (spurio -> `Connected`) e' impossibile senza il valore esatto `CONNECTION_TYPE_PROJECTION`. |
| T-10-03 | Tampering | preferenza persistita `keep_screen_on` | mitigate | CONN-02 richiede che la preferenza non venga alterata dalle transizioni di connessione. Garantito per costruzione: `resolveEffectiveKeepScreenOn` e' una funzione pura che riceve la preferenza per parametro e non importa ne' `ScreenOnPreferenceStore` ne' `android.content.Context`. Verificabile: `grep -c 'ScreenOnPreferenceStore\|getSharedPreferences\|\.write('` su `CarLinkState.kt` restituisce 0. |
| T-10-04 | Elevation of Privilege | superficie dei permessi | mitigate | Questo piano non introduce alcun permesso ne' alcun elemento `<queries>`: il `<queries><provider android:authorities="androidx.car.app.connection"/></queries>` necessario e' gia' dichiarato nel manifest di `androidx.car.app:app:1.7.0` (verificato leggendo l'AndroidManifest dell'AAR) e viene unito automaticamente. `AndroidManifest.xml` non compare in `files_modified`; una sua modifica e' un fallimento del piano. |
| T-10-05 | Information Disclosure | risorsa stringa | accept | La nuova stringa e' statica, senza segnaposto di formato, e non interpola alcun dato: non puo' veicolare posizione, velocita' o identificatori. Il file `strings.xml` e' gia' compilato nell'APK e coperto dalla firma. |
| T-10-SC | Tampering | installazioni da package manager | n/a | Nessun nuovo pacchetto e nessuna nuova coordinata Maven. `androidx.car.app:app:1.7.0` (che contiene `CarConnection`) e' gia' in `libs.versions.toml`/`app/build.gradle.kts` dalla Fase 8; `androidx.lifecycle:lifecycle-livedata-core:2.11.0`, necessario per `LiveData.observe()` nel Piano 02, e' gia' presente su `debugCompileClasspath` per via transitiva (verificato con `./gradlew :app:dependencies --configuration debugCompileClasspath`). `libs.versions.toml` e `app/build.gradle.kts` NON compaiono in `files_modified`. |
</threat_model>

<verification>
1. `./gradlew.bat :app:testDebugUnitTest --console=plain` -> BUILD SUCCESSFUL, nessuna regressione nei test JVM esistenti (`CarPermissionStateTest`, `CarSpeedContentTest`, `MaxSpeedReducerTest`, `SpeedMappingTest`, `GpsSpeedProviderStateTest`, `DistanceReducerTest`, `DistanceFormatTest`, `ChargingStateProviderStateTest`)
2. `./gradlew.bat :app:assembleDebug --console=plain` -> BUILD SUCCESSFUL
3. `git diff --name-only` -> esattamente i tre file dichiarati in `files_modified`; in particolare NON devono comparire `app/src/main/AndroidManifest.xml`, `app/build.gradle.kts`, `gradle/libs.versions.toml`
4. `git diff app/src/main/java/com/sed/tachimetro/MainActivity.kt` -> vuoto (il cablaggio e' del Piano 02)
5. `git diff app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt` -> vuoto (lo store resta invariato: questa fase lo ESTENDE per uso, non per codice)
</verification>

<success_criteria>
- `CarLinkState` distingue `Connected` e `Disconnected`, e `resolveCarLinkState()` produce `Connected` solo per `CONNECTION_TYPE_PROJECTION`, con null/negativi/sconosciuti/`NATIVE` lockati su `Disconnected` da test JVM
- `resolveEffectiveKeepScreenOn()` copre l'intera tabella di verita' CONN-01/CONN-02 in una funzione pura senza alcun accesso alla preferenza persistita
- La stringa `android_auto_connected` esiste con il testo esatto "Connesso ad Android Auto"
- Nessuna modifica a `MainActivity.kt`, `ScreenOnPreferenceStore.kt`, `AndroidManifest.xml` o ai file di build
</success_criteria>

<output>
Create `.planning/phases/10-comportamento-del-telefono-alla-connessione-android-auto/10-01-SUMMARY.md` when done
</output>
