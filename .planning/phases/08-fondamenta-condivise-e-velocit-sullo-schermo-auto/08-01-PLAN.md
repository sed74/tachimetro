---
phase: 08-fondamenta-condivise-e-velocit-sullo-schermo-auto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app/build.gradle.kts
  - app/src/main/res/values/strings.xml
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt
  - app/src/main/java/com/sed/tachimetro/MainActivity.kt
  - app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt
  - app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt
  - app/src/test/java/com/sed/tachimetro/car/CarSpeedContentTest.kt
autonomous: true
requirements: [AA-01, AA-02]
user_setup: []

must_haves:
  truths:
    - "Una sola istanza di GpsSpeedProvider esiste per processo, posseduta da TachimetroApplication"
    - "Il telefono si comporta esattamente come in v1.1: velocita', MAX, distanza, icona ricarica, switch schermo sempre acceso"
    - "Esiste una stringa dedicata all'auto 'Ricerca segnale...' distinta da searching_gps_signal del telefono"
    - "Una funzione pura mappa SpeedState nel contenuto della Row auto, verificabile senza runtime Android"
  artifacts:
    - path: "app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt"
      provides: "Application subclass che possiede il GpsSpeedProvider condiviso"
      contains: "class TachimetroApplication : Application()"
    - path: "app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt"
      provides: "Contratto di contenuto puro SpeedState -> (cifre | testo ricerca)"
      exports: ["CarSpeedContent", "carSpeedContent"]
    - path: "app/src/test/java/com/sed/tachimetro/car/CarSpeedContentTest.kt"
      provides: "Test JUnit puri del contratto di contenuto (D-01/D-02)"
      contains: "class CarSpeedContentTest"
    - path: "gradle/libs.versions.toml"
      provides: "Versione pinnata androidx.car.app 1.7.0"
      contains: "carApp"
  key_links:
    - from: "app/src/main/AndroidManifest.xml"
      to: "com.sed.tachimetro.TachimetroApplication"
      via: "attributo android:name sul tag <application>"
      pattern: "android:name=\"\\.TachimetroApplication\""
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      to: "TachimetroApplication.gpsSpeedProvider"
      via: "cast di application, non piu' costruzione diretta"
      pattern: "application as TachimetroApplication"
---

<objective>
Preparare le fondamenta condivise della milestone v2.0: promuovere `GpsSpeedProvider` da Activity-scoped ad Application-scoped (D-00b), aggiungere la dipendenza Car App Library, la stringa di stato dedicata all'auto (D-02) e il contratto di contenuto puro `SpeedState -> Row` (D-01) che lo schermo auto consumera' nel Piano 02.

Purpose: senza una sorgente GPS unica a livello di processo, il futuro `SpeedScreen` aprirebbe una seconda sottoscrizione `FusedLocationProviderClient` in parallelo a `MainActivity` (RESEARCH PITFALLS.md Pitfall 5) — batteria doppia e valori potenzialmente disallineati tra i due schermi, violando SC3 e SC5 del goal di fase.
Output: `TachimetroApplication`, `car/CarSpeedContent.kt` + test, dipendenza `androidx.car.app:app:1.7.0` pinnata, stringa `car_searching_gps_signal`, `MainActivity` che legge il provider condiviso invece di costruirlo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-CONTEXT.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-PATTERNS.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-UI-SPEC.md

<interfaces>
<!-- Contratti gia' esistenti nel codebase che l'esecutore deve rispettare senza esplorazione. -->

Da app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt:
- `sealed class SpeedState`
- `data object SpeedState.Searching`
- `data class SpeedState.Reading(val kmh: Int, val deltaMeters: Float)`
- `data object SpeedState.NoSignal`
- NOTA CRITICA gia' documentata nel file: `Reading` DEVE restare una data class con esattamente questi due campi (dedup StateFlow). Non modificarla.

Da app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt:
- `class GpsSpeedProvider(context: Context)` — usa gia' `context.applicationContext` internamente (riga 53)
- `val state: StateFlow<SpeedState>` — costruito con `SharingStarted.WhileSubscribed()` (righe 138-147), gia' ref-counted su piu' collector indipendenti
- `fun close()` — cancella lo scope interno
- `companion object { const val NOISE_FLOOR_KMH = 2.0 }`

Precedente di funzione pura + sealed result da mirrorare (stessa forma, stesso stile di KDoc):
- `app/src/main/java/com/sed/tachimetro/distance/DistanceFormat.kt` — `sealed class DistanceDisplay` + `fun formatDistanceDisplay(meters: Float): DistanceDisplay`
- `app/src/test/java/com/sed/tachimetro/distance/DistanceFormatTest.kt` — plain JUnit, nessun runtime Android
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dipendenza Car App Library, buildConfig e stringa di stato auto</name>
  <files>gradle/libs.versions.toml, app/build.gradle.kts, app/src/main/res/values/strings.xml</files>
  <read_first>
    - gradle/libs.versions.toml (stato attuale di [versions] e [libraries]; nota che NON esiste un plugin Kotlin separato — AGP 9.x usa il Kotlin built-in, vedi il commento a fine file)
    - app/build.gradle.kts (blocco `android {}` e blocco `dependencies {}` alle righe 71-82)
    - app/src/main/res/values/strings.xml (voce esistente `searching_gps_signal` alla riga 8 e `unit_kmh` alla riga 10)
  </read_first>
  <action>
Aggiungere la Car App Library al version catalog e al modulo app, abilitare `BuildConfig`, e aggiungere la stringa di stato dedicata allo schermo auto.

1. In `gradle/libs.versions.toml`, sezione `[versions]`: aggiungere `carApp = "1.7.0"` seguendo lo stile camelCase delle voci esistenti (`playServicesLocation`, `lifecycleRuntimeKtx`). La versione e' pinnata esatta, mai un range dinamico: 1.7.0 e' la prima stable che contiene il fix per CVE-2024-10382 (vedi `<threat_model>` T-08-SC).

2. In `gradle/libs.versions.toml`, sezione `[libraries]`: aggiungere due voci con lo stesso formato delle esistenti (`group`/`name`/`version.ref`):
   - alias `car-app` -> group `androidx.car.app`, name `app`, version.ref `carApp`
   - alias `car-app-testing` -> group `androidx.car.app`, name `app-testing`, version.ref `carApp`

3. In `app/build.gradle.kts`, blocco `dependencies {}`: aggiungere `implementation(libs.car.app)` insieme alle altre `implementation`, e `androidTestImplementation(libs.car.app.testing)` insieme alle altre `androidTestImplementation`. `app-testing` va in `androidTestImplementation` e NON in `testImplementation`: le sue classi (`TestCarContext`, `ScreenController`) richiedono un runtime Android e il progetto non usa Robolectric — il Piano 03 le userera' in un test strumentato.
   NON aggiungere `androidx.car.app:app-automotive` ne' `app-projected`: `app-automotive` porterebbe `CarAppActivity` (Android Automotive OS nativo, fuori scope esplicito della milestone, PITFALLS.md Pitfall 3); `app-projected` e' documentato come opzionale per Android Auto.

4. In `app/build.gradle.kts`, dentro il blocco `android {}`: aggiungere un blocco `buildFeatures { buildConfig = true }`. Motivo concreto: AGP 8+ non genera piu' `BuildConfig` di default, e il Piano 02 deve poter gating-are il log di conteggio dei refresh su `BuildConfig.DEBUG` cosi' nessun valore di velocita' finisce in logcat nelle build di release (T-08-03). Aggiungere un commento che spiega esattamente questo motivo.

5. In `app/src/main/res/values/strings.xml`: aggiungere una nuova voce con name `car_searching_gps_signal` e valore letterale esatto `Ricerca segnale...` (tre punti ASCII, nessun carattere ellissi unicode). NON modificare ne' riusare `searching_gps_signal` ("Ricerca segnale GPS...") — D-02 richiede una risorsa separata e piu' corta per l'auto. Aggiungere un commento XML che rimanda a D-02. `unit_kmh` esiste gia' e va riusata as-is per lo slot unita' (D-01): non creare una nuova stringa per "km/h".
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && grep -c 'carApp = "1.7.0"' gradle/libs.versions.toml && grep -c 'implementation(libs.car.app)' app/build.gradle.kts && grep -c 'buildConfig = true' app/build.gradle.kts && grep -c '<string name="car_searching_gps_signal">Ricerca segnale\.\.\.</string>' app/src/main/res/values/strings.xml</automated>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` contiene esattamente `carApp = "1.7.0"` in `[versions]` e gli alias `car-app` e `car-app-testing` in `[libraries]`, entrambi con `version.ref = "carApp"`
    - `app/build.gradle.kts` contiene `implementation(libs.car.app)` e `androidTestImplementation(libs.car.app.testing)`
    - `app/build.gradle.kts` NON contiene alcun riferimento a `app-automotive` o `app-projected`: `grep -c 'app-automotive\|app-projected' app/build.gradle.kts gradle/libs.versions.toml` restituisce 0 per entrambi i file
    - `app/build.gradle.kts` contiene `buildConfig = true` dentro un blocco `buildFeatures` nel blocco `android {}`
    - `app/src/main/res/values/strings.xml` contiene la riga `<string name="car_searching_gps_signal">Ricerca segnale...</string>` e la riga preesistente `<string name="searching_gps_signal">Ricerca segnale GPS...</string>` e' invariata
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>Il progetto compila con androidx.car.app 1.7.0 risolto dal version catalog, BuildConfig generato, e la stringa car_searching_gps_signal disponibile come R.string.car_searching_gps_signal.</done>
</task>

<task type="auto">
  <name>Task 2: Promuovere GpsSpeedProvider a scope Application</name>
  <files>app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt, app/src/main/AndroidManifest.xml, app/src/main/java/com/sed/tachimetro/MainActivity.kt, app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt (in particolare `setupGpsCollection()` righe 207-228 e `onDestroy()` righe 288-299)
    - app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt (commento su `scope` righe 65-67, costruzione `state` righe 138-147, KDoc di `close()` righe 149-157)
    - app/src/main/AndroidManifest.xml (tag `<application>` alla riga 13)
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-PATTERNS.md (sezione "TachimetroApplication.kt" e sezione "MainActivity.kt (modified, exact self-analog)")
  </read_first>
  <action>
Creare l'`Application` subclass che possiede il `GpsSpeedProvider` condiviso (D-00b) e adattare i due punti di `MainActivity` che oggi assumono la proprieta' esclusiva del provider.

1. Creare `app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt`, package `com.sed.tachimetro`:
   - `class TachimetroApplication : Application()`
   - una sola proprieta' pubblica: `val gpsSpeedProvider: GpsSpeedProvider by lazy { GpsSpeedProvider(applicationContext) }`
   - `by lazy`, non un framework DI (D-00b esplicito): l'inizializzazione pigra evita di costruire il client di localizzazione a ogni avvio del processo anche quando nessuna superficie e' attiva
   - KDoc di classe che spiega: unica sorgente GPS per processo condivisa tra `MainActivity` (telefono) e il futuro `SpeedScreen` (auto); nessuna raccolta avviata qui — `SharingStarted.WhileSubscribed()` in `GpsSpeedProvider.state` decide da solo quando accendere/spegnere l'upstream in base ai collector attivi
   - commento inline che riprende il tag WR-04 gia' usato nel progetto: `applicationContext` e' esattamente cio' che il costruttore di `GpsSpeedProvider` si aspetta, nessuna `Activity` viene mai passata
   - NON aggiungere `onCreate()` con raccolta eager di `gpsSpeedProvider.state`: un collector permanente a livello di Application terrebbe il GPS acceso per sempre (T-08-01)

2. In `app/src/main/AndroidManifest.xml`: aggiungere l'attributo `android:name=".TachimetroApplication"` al tag `<application>` esistente, mantenendo tutti gli attributi gia' presenti (`allowBackup`, `dataExtractionRules`, `fullBackupContent`, `icon`, `label`, `roundIcon`, `supportsRtl`, `theme`).

3. In `app/src/main/java/com/sed/tachimetro/MainActivity.kt`, funzione `setupGpsCollection()`: sostituire la costruzione `gpsSpeedProvider = GpsSpeedProvider(applicationContext)` con la lettura dell'istanza condivisa `gpsSpeedProvider = (application as TachimetroApplication).gpsSpeedProvider`. Mantenere invariato tutto il resto della funzione (il blocco `lifecycleScope.launch { repeatOnLifecycle(STARTED) { permissionGranted.collectLatest { ... } } }`). Aggiornare il commento WR-04 sopra la riga: il motivo per cui non si passa l'Activity resta valido ma ora la costruzione avviene in `TachimetroApplication`.

4. In `app/src/main/java/com/sed/tachimetro/MainActivity.kt`, `onDestroy()`: RIMUOVERE la chiamata `gpsSpeedProvider.close()` e il suo commento WR-04. MANTENERE `chargingStateProvider.close()` invariato — `ChargingStateProvider` NON viene promosso a scope Application in questa fase e resta di proprieta' dell'Activity. Sostituire il commento rimosso con uno nuovo che spiega perche' la chiamata non e' piu' corretta: il provider ora sopravvive all'Activity, e l'host Android Auto puo' tenere vivo `SpeedScreen` come unico collector anche quando `MainActivity` viene distrutta; cancellare lo scope condiviso da qui ucciderebbe gli aggiornamenti GPS dello schermo auto. `SharingStarted.WhileSubscribed()` resta il meccanismo primario di stop dell'upstream.

5. In `app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt`: aggiornare DUE commenti diventati obsoleti, senza toccare una sola riga di logica:
   - il commento sopra `private val scope` (righe 65-67) che dice "scoped to this provider's own lifetime (mirrors the Activity that owns it — no ViewModel/DI layer in this project)": ora lo scope e' quello del processo, posseduto da `TachimetroApplication`
   - il KDoc di `close()` (righe 149-157) che istruisce "Call from MainActivity.onDestroy()": ora `close()` NON deve piu' essere chiamato da `MainActivity`; documentare che resta disponibile come teardown esplicito ma che nel ciclo di vita normale nessuno lo chiama, perche' lo scope vive quanto il processo
   Verificare che nessun altro file chiami `gpsSpeedProvider.close()`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && grep -c 'android:name="\.TachimetroApplication"' app/src/main/AndroidManifest.xml && grep -c 'application as TachimetroApplication' app/src/main/java/com/sed/tachimetro/MainActivity.kt && test "$(grep -rn 'gpsSpeedProvider.close()' app/src/main/java | wc -l)" -eq 0 && grep -c 'chargingStateProvider.close()' app/src/main/java/com/sed/tachimetro/MainActivity.kt</automated>
  </verify>
  <acceptance_criteria>
    - Esiste `app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt` con `class TachimetroApplication : Application()` e la proprieta' `val gpsSpeedProvider: GpsSpeedProvider by lazy { GpsSpeedProvider(applicationContext) }`
    - `TachimetroApplication.kt` NON contiene `onCreate` con una `collect` su `gpsSpeedProvider.state`: `grep -c 'state.collect' app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt` restituisce 0
    - `app/src/main/AndroidManifest.xml` contiene `android:name=".TachimetroApplication"` sul tag `<application>` e tutti gli 8 attributi preesistenti restano presenti
    - `MainActivity.setupGpsCollection()` contiene `(application as TachimetroApplication).gpsSpeedProvider` e NON contiene piu' `GpsSpeedProvider(applicationContext)`
    - `grep -rn 'gpsSpeedProvider.close()' app/src/main/java` non restituisce nessuna riga
    - `MainActivity.onDestroy()` contiene ancora `chargingStateProvider.close()`
    - `GpsSpeedProvider.kt` non ha differenze di logica: `git diff -- app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt` mostra solo righe che iniziano (dopo indentazione) con `//` o `*` o `/**`
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>Il processo ha una sola istanza di GpsSpeedProvider, creata pigramente da TachimetroApplication; MainActivity la legge invece di costruirla e non la chiude piu'; il comportamento del telefono e' invariato.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Contratto di contenuto puro per la Row dello schermo auto</name>
  <files>app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt, app/src/test/java/com/sed/tachimetro/car/CarSpeedContentTest.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/distance/DistanceFormat.kt (precedente esatto: sealed result + funzione pura top-level con KDoc)
    - app/src/test/java/com/sed/tachimetro/distance/DistanceFormatTest.kt (precedente esatto di test: plain JUnit, nessun runtime Android, nome metodo snake_case condizione_risultato)
    - app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt (i tre sottotipi da mappare)
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt righe 371-386 (il `when` su SpeedState che unifica Searching e NoSignal sotto un unico messaggio — la forma da rispecchiare)
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-UI-SPEC.md (sezioni "Content Contract" e "States")
  </read_first>
  <behavior>
    - `carSpeedContent(SpeedState.Reading(87, 12f))` -> `CarSpeedContent.Speed(87)`
    - `carSpeedContent(SpeedState.Reading(0, 0f))` -> `CarSpeedContent.Speed(0)` — 0 km/h e' una lettura valida, NON deve degradare a Searching
    - `carSpeedContent(SpeedState.Searching)` -> `CarSpeedContent.Searching`
    - `carSpeedContent(SpeedState.NoSignal)` -> `CarSpeedContent.Searching` — AA-02: unificato con Searching (stessa copia, D-02), lo schermo auto non resta mai su un valore vecchio
    - `deltaMeters` non influenza il risultato: `carSpeedContent(SpeedState.Reading(50, 0f))` == `carSpeedContent(SpeedState.Reading(50, 999f))` — distanza e massimo restano solo sul telefono (fuori scope milestone)
  </behavior>
  <action>
Estrarre il contratto di contenuto dello schermo auto in una funzione pura testabile senza runtime Android, secondo la convenzione "Function Design" di CLAUDE.md e il precedente `formatDistanceDisplay`/`DistanceDisplay`.

1. Creare il package `app/src/main/java/com/sed/tachimetro/car/` e il file `CarSpeedContent.kt`, package `com.sed.tachimetro.car`.

2. Definire `sealed class CarSpeedContent` con due sottotipi:
   - `data class Speed(val kmh: Int) : CarSpeedContent()` — D-01: le sole cifre, l'unita' "km/h" vive in uno slot separato deciso dal chiamante, mai concatenata qui
   - `data object Searching : CarSpeedContent()` — D-02/AA-02: stato unificato Searching + NoSignal
   Nessun sottotipo per il caso "permesso non concesso": UI-SPEC.md lo rimanda esplicitamente alla Fase 9 e questa fase non inventa copy per quello stato.

3. Definire la funzione pura top-level `fun carSpeedContent(state: SpeedState): CarSpeedContent` con un `when` esaustivo (senza `else`, cosi' il compilatore obbliga a gestire ogni ramo di `SpeedState`), che mappa i tre sottotipi come descritto in `<behavior>`. Nessun import Android, nessun accesso a risorse: la risoluzione di `R.string.unit_kmh` e `R.string.car_searching_gps_signal` avverra' nel `SpeedScreen` del Piano 02.

4. KDoc sulla funzione e sulla sealed class che documenta il "perche'": rimanda a D-01 (cifre e unita' separate, rispecchia la separazione `messageText`/`unitText` di MainActivity), D-02 (copy piu' corta per l'auto), AA-02 (mai bloccati su un valore vecchio) e nota che il ramo `Reading` NON propaga `deltaMeters` perche' distanza e velocita' massima restano funzioni solo del telefono (REQUIREMENTS.md "Out of Scope").

5. Creare `app/src/test/java/com/sed/tachimetro/car/CarSpeedContentTest.kt`, package `com.sed.tachimetro.car`, con un test per ciascuna riga di `<behavior>`. Usare plain JUnit (`org.junit.Test`, `org.junit.Assert.assertEquals`), nessun runtime Android, nessuna dipendenza coroutines-test — esattamente come `DistanceFormatTest`. Nomi dei metodi in snake_case condizione_risultato, es. `noSignal_returnsSearching`, `readingZeroKmh_returnsSpeedZero`, `deltaMeters_doesNotAffectContent`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:testDebugUnitTest --tests 'com.sed.tachimetro.car.CarSpeedContentTest' --console=plain && test "$(grep -c 'import android' app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - Esiste `app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt` che esporta `sealed class CarSpeedContent`, `CarSpeedContent.Speed(kmh: Int)`, `CarSpeedContent.Searching` e `fun carSpeedContent(state: SpeedState): CarSpeedContent`
    - `CarSpeedContent.kt` non importa nulla dal framework Android: `grep -c 'import android' app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt` restituisce 0
    - `CarSpeedContent.kt` non contiene alcun riferimento a `R.string`: `grep -c 'R\.string' app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt` restituisce 0
    - Il `when` in `carSpeedContent` non ha ramo `else`: `grep -c 'else ->' app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt` restituisce 0
    - `CarSpeedContentTest.kt` contiene almeno 5 metodi annotati `@Test`, uno per ciascuna riga di `<behavior>`
    - `./gradlew.bat :app:testDebugUnitTest --tests 'com.sed.tachimetro.car.CarSpeedContentTest'` termina con `BUILD SUCCESSFUL` e 0 test falliti
  </acceptance_criteria>
  <done>La mappatura SpeedState -> contenuto della Row auto e' isolata in una funzione pura, coperta da test JUnit che lockano D-01, D-02 e AA-02 senza bisogno di un emulatore.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Processo app <-> servizi Google Play (FusedLocationProviderClient) | La sorgente di localizzazione cambia proprietario: da Activity a processo. Il confine dati non cambia, ma cambia chi decide quando la sottoscrizione e' attiva. |
| Repository <-> Google Maven | Nuovo artifact di terze parti (`androidx.car.app`) entra nel classpath di produzione. |
| Processo app <-> logcat di sistema | `BuildConfig` viene abilitato in questo piano per permettere al Piano 02 di gating-are il logging di diagnostica. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | `TachimetroApplication.gpsSpeedProvider` | mitigate | Nessun collector eager a livello di Application: `TachimetroApplication` espone solo la proprieta' `by lazy` e non chiama mai `state.collect`. L'upstream `requestLocationUpdates` resta acceso solo finche' esiste almeno un collector, per costruzione di `SharingStarted.WhileSubscribed()` (GpsSpeedProvider.kt:145). Criterio verificabile in Task 2: `grep -c 'state.collect' TachimetroApplication.kt` == 0. |
| T-08-02 | Denial of Service | `MainActivity.onDestroy()` | mitigate | Rimozione di `gpsSpeedProvider.close()`: con lo scope condiviso, quella chiamata cancellerebbe il `SupervisorJob` del provider e lascerebbe lo schermo auto permanentemente senza dati (fallimento silenzioso di AA-01/AA-03). Criterio verificabile: `grep -rn 'gpsSpeedProvider.close()' app/src/main/java` vuoto. |
| T-08-03 | Information Disclosure | logging di diagnostica in build di release | mitigate | `buildFeatures { buildConfig = true }` viene abilitato qui esclusivamente perche' il conteggio dei refresh del Piano 02 sia racchiuso in `if (BuildConfig.DEBUG)`; nessun valore di velocita' finisce in logcat nelle build di release. |
| T-08-04 | Spoofing | superficie dei permessi dell'app | accept | Nessun nuovo `<uses-permission>` viene aggiunto: il percorso POI/template non richiede `NAVIGATION_TEMPLATES` ne' `ACCESS_SURFACE` (UI-SPEC.md "Surface/Canvas access: Not used"). La superficie di permessi resta identica alla v1.1 (solo `ACCESS_FINE_LOCATION`). Rischio residuo nullo — annotato per tracciabilita'. |
| T-08-SC | Tampering | dipendenza `androidx.car.app:app` / `app-testing` | mitigate | Artifact first-party AndroidX pubblicato su Google Maven, gia' presente in `settings.gradle.kts` — non un pacchetto di community da un registry aperto, quindi il protocollo Package Legitimacy Gate (npm/pip/cargo) non si applica. Versione pinnata esatta `1.7.0`, mai un range dinamico; 1.7.0 e' la prima stable che include il fix per CVE-2024-10382 (versioni < 1.7.0-beta03 sono vulnerabili). `app-automotive` e `app-projected` sono esplicitamente esclusi per non allargare la superficie. |
</threat_model>

<verification>
1. `./gradlew.bat :app:assembleDebug` -> BUILD SUCCESSFUL con la nuova dipendenza risolta
2. `./gradlew.bat :app:testDebugUnitTest` -> tutti i test JVM esistenti (SpeedMapping, GpsSpeedProviderState, MaxSpeedReducer, DistanceFormat, DistanceReducer, ChargingStateProviderState) continuano a passare, piu' i nuovi CarSpeedContentTest
3. `grep -rn 'gpsSpeedProvider.close()' app/src/main/java` -> nessun risultato
4. `grep -c 'android:name="\.TachimetroApplication"' app/src/main/AndroidManifest.xml` -> 1
5. Nessun nuovo `<uses-permission>` nel manifest: `grep -c 'uses-permission' app/src/main/AndroidManifest.xml` -> 1 (solo ACCESS_FINE_LOCATION)
</verification>

<success_criteria>
- Il progetto compila e tutti i test unitari JVM passano
- `GpsSpeedProvider` viene costruito in un solo punto del codebase (`TachimetroApplication`) e mai piu' in `MainActivity`
- `R.string.car_searching_gps_signal` = "Ricerca segnale..." esiste ed e' distinta da `R.string.searching_gps_signal`
- `carSpeedContent()` mappa i tre stati secondo D-01/D-02/AA-02 con copertura di test JUnit
- Nessuna modifica di logica in `GpsSpeedProvider.kt` (solo commenti aggiornati), nessuna modifica al layout o alle altre feature del telefono
</success_criteria>

<output>
Create `.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-01-SUMMARY.md` when done
</output>
