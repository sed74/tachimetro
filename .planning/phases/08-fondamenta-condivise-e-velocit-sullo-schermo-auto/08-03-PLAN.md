---
phase: 08-fondamenta-condivise-e-velocit-sullo-schermo-auto
plan: 03
type: execute
wave: 3
depends_on: [08-02]
files_modified:
  - app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt
  - scripts/dhu-quota-check.ps1
  - docs/dhu-quota-verification.md
  - .gitignore
autonomous: false
requirements: [AA-01, AA-02, AA-03]
user_setup: []

must_haves:
  truths:
    - "La forma del template prodotto da SpeedScreen e' verificata automaticamente per lo stato Reading e per lo stato Searching/NoSignal"
    - "Esiste una procedura riproducibile e in gran parte automatizzata per misurare la cadenza reale dei refresh e rilevare una chiusura dell'app da parte dell'host"
    - "Una sessione continua di 5-10 minuti a 1 Hz su DHU non fa chiudere l'app dall'host per superamento della quota di refresh dei template"
    - "Il telefono si comporta come in v1.1 durante e dopo la sessione Android Auto"
  artifacts:
    - path: "app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt"
      provides: "Test strumentato della forma del PaneTemplate per entrambi gli stati"
      contains: "PaneTemplate"
    - path: "scripts/dhu-quota-check.ps1"
      provides: "Automazione della misura dei refresh e del rilevamento di chiusura dell'app durante la sessione DHU"
      contains: "onGetTemplate #"
    - path: "docs/dhu-quota-verification.md"
      provides: "Runbook riproducibile della verifica empirica (D-08/D-09/D-10) e protocollo di contingenza D-07"
      contains: "D-07"
  key_links:
    - from: "scripts/dhu-quota-check.ps1"
      to: "adb logcat -s TachimetroCar"
      via: "conteggio delle righe di refresh emesse da SpeedScreen"
      pattern: "TachimetroCar"
    - from: "docs/dhu-quota-verification.md"
      to: ".planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-CONTEXT.md"
      via: "riferimenti espliciti alle decisioni D-05..D-10"
      pattern: "D-0[5-9]|D-10"
---

<objective>
Chiudere il gate SC4 della fase: dimostrare empiricamente che un refresh continuo a 1 Hz del `PaneTemplate` non fa chiudere l'app dall'host Android Auto, e confermare che il telefono non ha regressioni (SC5).

Purpose: la ricerca di progetto (`.planning/research/STACK.md`, `PITFALLS.md` Pitfall 2) sostiene che un valore che cambia ogni secondo dovrebbe esaurire quasi subito la quota di 5 step dei template non-Surface, mentre la documentazione Google non chiarisce se un refresh dello stesso template con solo il contenuto cambiato sia esente. D-06 rende questa verifica un gate esplicito, non un dettaglio da scoprire in produzione: se il rischio e' reale, va scoperto ora, non alla Fase 11.
Output: test strumentato sulla forma del template, script di misura riproducibile, runbook della procedura e del piano di contingenza D-07, e la conferma umana finale della sessione DHU.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-CONTEXT.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-UI-SPEC.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-02-SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Prodotti dai Piani 01 e 02, consumati qui. -->

Da app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt (Piano 02):
- `class SpeedScreen(carContext: CarContext) : Screen(carContext)`
- `override fun onGetTemplate(): Template` -> `PaneTemplate` con un `Pane` di una sola `Row` e `headerAction = Action.APP_ICON`
- emette in debug una riga logcat con tag `TachimetroCar` nel formato `onGetTemplate #<n> content=<Speed|Searching>`

Da app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt (Piano 01):
- `fun carSpeedContent(state: SpeedState): CarSpeedContent`

Da app/src/main/java/com/sed/tachimetro/gps/SpeedState.kt:
- `SpeedState.Reading(kmh: Int, deltaMeters: Float)`, `SpeedState.Searching`, `SpeedState.NoSignal`

Risorse: `R.string.unit_kmh` = "km/h", `R.string.car_searching_gps_signal` = "Ricerca segnale..."

Dipendenza di test disponibile (aggiunta dal Piano 01): `androidTestImplementation(libs.car.app.testing)` -> `androidx.car.app:app-testing:1.7.0`
- `androidx.car.app.testing.TestCarContext.create(context)` — crea un `CarContext` utilizzabile senza host reale
- `androidx.car.app.testing.ScreenController` — pilota il ciclo di vita di uno `Screen` e permette di ottenere il template corrente
Convenzione di test strumentato gia' presente nel progetto: runner `androidx.test.runner.AndroidJUnitRunner`, `androidx.test.ext.junit.runners.AndroidJUnit4`, `androidx.test.platform.app.InstrumentationRegistry`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test strumentato della forma del template</name>
  <files>app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt (la classe sotto test: costruttore, forma del template, come ottiene il provider)
    - app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt (il contratto di contenuto gia' testato a livello puro nel Piano 01 — qui si verifica la traduzione in oggetti template, non di nuovo la mappatura)
    - app/src/test/java/com/sed/tachimetro/car/CarSpeedContentTest.kt (stile e nomenclatura dei test del progetto: snake_case condizione_risultato)
    - app/src/androidTest/java/com/sed/tachimetro/ExampleInstrumentedTest.java (unico test strumentato esistente: mostra runner e uso di InstrumentationRegistry nel progetto)
    - app/build.gradle.kts (conferma che `androidTestImplementation(libs.car.app.testing)` sia presente dal Piano 01)
  </read_first>
  <action>
Creare `app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt`, package `com.sed.tachimetro.car`, annotato `@RunWith(AndroidJUnit4::class)`.

Obiettivo: bloccare con asserzioni la forma del template che il Piano 02 produce — cioe' la parte di AA-01/AA-02 che i test JVM puri non possono coprire (il contratto `PaneTemplate`/`Pane`/`Row`/`headerAction`).

1. Setup: ottenere il context con `InstrumentationRegistry.getInstrumentation().targetContext` e costruire un `TestCarContext` con `TestCarContext.create(context)`. Costruire lo `Screen` con `SpeedScreen(testCarContext)`.

2. Per pilotare lo stato mostrato senza un GPS reale, usare `ScreenController` di `androidx.car.app:app-testing` per portare lo `Screen` almeno a `Lifecycle.State.CREATED`/`STARTED` e poi ottenere il template corrente. Se l'API di `ScreenController` disponibile in 1.7.0 non espone direttamente il template, invocare `screen.onGetTemplate()` e castarlo a `PaneTemplate`. Documentare in un commento quale delle due strade e' stata usata e perche'.

3. Casi da asserire (usare nomi snake_case condizione_risultato):
   - `initialState_showsSearchingRowWithoutUnit`: senza permesso concesso e senza fix, il template e' un `PaneTemplate`, il suo `Pane` ha esattamente 1 `Row`, il titolo della Row e' uguale a `context.getString(R.string.car_searching_gps_signal)` (cioe' "Ricerca segnale...") e la lista dei testi secondari della Row e' vuota. Questo copre AA-02 e la garanzia difensiva "non va in crash senza permesso" (UI-SPEC.md).
   - `template_hasAppIconHeaderActionAndNoTitle`: l'header action del `PaneTemplate` e' `Action.APP_ICON` e il titolo del template e' null/vuoto — D-03, nessun branding dentro lo schermo.
   - `template_hasNoActionStripAndRowHasNoImage`: il template non espone una action strip e la Row non ha immagine — REQUIREMENTS.md "Out of Scope" (nessun pulsante interattivo) e UI-SPEC.md Content Contract ("Icon inside the Row: None").
   - `repeatedInvalidate_keepsProducingValidTemplate`: chiamare `onGetTemplate()` 60 volte di seguito e asserire che ogni chiamata restituisce un `PaneTemplate` non null con esattamente 1 Row. Questo NON verifica la quota dell'host (che vive nel processo host, non testabile qui) ma esclude che sia l'app stessa a degradare o accumulare stato dopo molti rebuild — la quota vera e' verificata nel Task 3.

4. Se `TestCarContext` o `ScreenController` non permettono di iniettare uno `SpeedState` arbitrario (il provider viene letto da `TachimetroApplication`, non iniettato), NON introdurre dependency injection in `SpeedScreen` solo per il test: asserire i casi ottenibili (stato iniziale `Searching`, forma del template, ripetibilita') e documentare in un commento che la copertura del ramo `Reading` vive nel test JVM puro `CarSpeedContentTest` piu' la verifica visiva DHU del Task 3. Il ramo `Reading` e' comunque coperto end-to-end dal checkpoint umano.

5. Import espliciti, mai wildcard.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:compileDebugAndroidTestKotlin --console=plain -q && grep -c 'PaneTemplate' app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt && grep -c 'Action.APP_ICON' app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt && grep -c 'car_searching_gps_signal' app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:connectedDebugAndroidTest --console=plain 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - Esiste `app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt` con `@RunWith(AndroidJUnit4::class)` e almeno 4 metodi `@Test`
    - Il test costruisce lo screen tramite `TestCarContext` (non un `CarContext` reale ottenuto da un host)
    - Almeno un test asserisce che il titolo della Row nello stato iniziale e' uguale a `getString(R.string.car_searching_gps_signal)`
    - Almeno un test asserisce `Action.APP_ICON` come header action e l'assenza di titolo sul template
    - Almeno un test chiama `onGetTemplate()` in un ciclo di 60 iterazioni e asserisce un `PaneTemplate` valido con 1 Row a ogni iterazione
    - `grep -c 'import .*\*$' app/src/androidTest/java/com/sed/tachimetro/car/SpeedScreenTemplateTest.kt` restituisce 0
    - `./gradlew.bat :app:compileDebugAndroidTestKotlin` termina con `BUILD SUCCESSFUL`
    - Con un emulatore/dispositivo connesso, `./gradlew.bat :app:connectedDebugAndroidTest` termina con `BUILD SUCCESSFUL` e 0 test falliti
  </acceptance_criteria>
  <done>La forma del PaneTemplate e la copy dello stato "Ricerca segnale..." sono lockate da asserzioni automatiche, e ripetuti rebuild del template non degradano lo stato interno dello Screen.</done>
</task>

<task type="auto">
  <name>Task 2: Automazione e runbook della verifica quota su DHU</name>
  <files>scripts/dhu-quota-check.ps1, docs/dhu-quota-verification.md, .gitignore</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt (il formato esatto della riga di log da contare: tag `TachimetroCar`, messaggio `onGetTemplate #<n> content=<...>`)
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-CONTEXT.md (decisioni D-05, D-06, D-07, D-08, D-09, D-10 — vincoli esatti della procedura)
    - .planning/research/PITFALLS.md (Pitfall 2 sulla quota di refresh, Pitfall 7 sul DHU su Windows e sull'assunzione "telefono reale" da cui D-09 devia consapevolmente)
    - .gitignore (stato attuale, per capire dove aggiungere l'esclusione degli artefatti di log)
    - app/build.gradle.kts (applicationId `com.sed.tachimetro`, usato dallo script per interrogare il processo)
  </read_first>
  <action>
Rendere la verifica empirica riproducibile e in gran parte automatizzata (D-10), lasciando alla persona solo cio' che richiede occhi su uno schermo.

1. Creare `scripts/dhu-quota-check.ps1`, script PowerShell (l'ambiente di sviluppo del progetto e' Windows) con un parametro `-DurationSeconds` di default `600` (10 minuti, estremo alto della finestra 5-10 minuti di D-08) e un parametro `-OutputDir` di default `build/dhu-quota`. Lo script deve:
   - verificare che `adb` sia raggiungibile e che `adb devices` elenchi almeno un dispositivo/emulatore in stato `device`; uscire con errore chiaro altrimenti
   - eseguire `adb forward tcp:5277 tcp:5277` (porta standard richiesta dal Desktop Head Unit) e riportare l'esito
   - svuotare il buffer con `adb logcat -c`, poi avviare in background la cattura filtrata `adb logcat -s TachimetroCar:D` su un file dentro `-OutputDir`
   - stampare a video l'istruzione di avviare il Desktop Head Unit e di selezionare l'app Tachimetro nella lista app dell'head unit, poi attendere INVIO prima di far partire il cronometro (l'avvio del DHU e la selezione dell'app non sono automatizzabili da script)
   - durante `-DurationSeconds`, campionare ogni 10 secondi `adb shell pidof com.sed.tachimetro`, registrando l'orario di ogni campione e il PID; se il PID sparisce o cambia, registrarlo come evento di terminazione/riavvio del processo e continuare a campionare fino alla fine
   - al termine, fermare la cattura e produrre un riepilogo stampato a video e salvato in un file di riepilogo dentro `-OutputDir`, contenente almeno: durata effettiva, numero di righe `onGetTemplate #` catturate, il valore piu' alto del contatore `#<n>` osservato, la cadenza media in refresh/secondo, il PID iniziale e finale, e l'elenco degli eventi di terminazione/cambio PID
   - stampare un esito euristico `PASS` / `FAIL` / `INCONCLUSIVO` secondo queste soglie esplicite: `FAIL` se il PID e' sparito o cambiato durante la sessione, oppure se il conteggio dei refresh si e' fermato per piu' di 30 secondi consecutivi prima della fine; `PASS` se il processo e' rimasto vivo per tutta la durata e la cadenza media e' compresa tra 0,8 e 1,2 refresh/secondo; `INCONCLUSIVO` in ogni altro caso. Lo script deve dichiarare a video che l'esito euristico NON sostituisce la conferma umana del Task 3.
   - non installare nulla e non scaricare nulla: usa solo `adb` dall'Android SDK locale e il binario DHU gia' presente nell'SDK

2. Creare `docs/dhu-quota-verification.md`, runbook della procedura, con queste sezioni:
   - **Perche' questa verifica esiste**: rischio quota template non-Surface (5 step per task) contro un valore che cambia ogni secondo; la documentazione Google non chiarisce se un refresh con solo il contenuto cambiato sia esente (`.planning/research/PITFALLS.md` Pitfall 2). D-06 lo rende un gate.
   - **Prerequisiti**: AVD di Android Studio con Google Play, API 30+ (D-09: un telefono fisico NON serve per questa verifica — serve invece alla Fase 11 per il comportamento background-location); Desktop Head Unit installato dall'SDK Manager (`extras/google/auto`); Android Auto installato sull'AVD; Developer Mode di Android Auto attivo con l'overlay di debug abilitato; permesso di localizzazione gia' concesso all'app; posizione simulata in movimento sull'AVD (Extended Controls -> Location -> route/playback) cosi' che `SpeedState` sia `Reading` e non `Searching` per la maggior parte della sessione
   - **Procedura passo-passo**: comandi esatti per installare la build debug, avviare lo script con la durata scelta, avviare il DHU, selezionare Tachimetro, e cosa osservare a schermo
   - **Cosa osservare a occhio durante la sessione** (input del checkpoint del Task 3): il numero sullo schermo auto cambia visibilmente; corrisponde al numero mostrato sul telefono; nei momenti di perdita segnale appare "Ricerca segnale..." e non un valore congelato; nessun ritorno improvviso alla lista app dell'head unit; nessun messaggio di errore dell'host
   - **Criteri di esito**: le stesse soglie PASS/FAIL/INCONCLUSIVO dello script, piu' i criteri visivi
   - **Contingenza se FAIL (D-07)**: la mitigazione e' un throttle del refresh SOLO lato auto (esempio: ricostruire il template ogni 2-3 secondi invece che ogni secondo), lasciando il telefono a 1 Hz invariato. Va implementato in `SpeedScreen`, mai in `GpsSpeedProvider`. Questo comporta la rinegoziazione ESPLICITA di SC3 ("stessa cadenza del telefono, senza salti ne' disallineamenti") con l'utente, non un compromesso silenzioso. Scrivere esplicitamente che l'opzione di ripiegare sul percorso Surface/NAVIGATION e' stata scartata e NON va riaperta.
   - **Cosa e' fuori scope di questa verifica**: comportamento a telefono bloccato/in background durante una guida reale (Fase 11), `HostValidator` reale (Fase 11)

3. In `.gitignore`: aggiungere l'esclusione della directory di output dei log della verifica (`build/dhu-quota/` e' gia' sotto `build/` se `build/` e' ignorato — verificare lo stato attuale e, se `build/` NON e' ignorato o se si sceglie un percorso diverso, aggiungere una riga esplicita). I file catturati contengono output logcat del dispositivo e non devono finire nel repository (vedi `<threat_model>` T-08-12).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && powershell -NoProfile -Command "\$ErrorActionPreference='Stop'; \$null = [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'scripts/dhu-quota-check.ps1').Path, [ref]\$null, [ref]\$null); Write-Output 'PARSE_OK'" && grep -c 'onGetTemplate #' scripts/dhu-quota-check.ps1 && grep -c 'adb forward tcp:5277 tcp:5277' scripts/dhu-quota-check.ps1 && grep -c 'pidof com.sed.tachimetro' scripts/dhu-quota-check.ps1 && grep -c 'DurationSeconds' scripts/dhu-quota-check.ps1 && grep -c 'D-07' docs/dhu-quota-verification.md && grep -c 'D-09' docs/dhu-quota-verification.md && grep -c 'Ricerca segnale' docs/dhu-quota-verification.md && test "$(git check-ignore -q build/dhu-quota && echo ok || echo no)" = "ok"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/dhu-quota-check.ps1` esiste, viene parsato senza errori da PowerShell, e accetta i parametri `-DurationSeconds` (default 600) e `-OutputDir`
    - Lo script contiene `adb forward tcp:5277 tcp:5277`, `adb logcat -c`, una cattura filtrata sul tag `TachimetroCar`, e un campionamento periodico di `pidof com.sed.tachimetro`
    - Lo script conta le occorrenze di `onGetTemplate #` e calcola una cadenza media in refresh/secondo
    - Lo script stampa un esito `PASS`/`FAIL`/`INCONCLUSIVO` con le soglie esatte descritte nell'action, e dichiara che l'esito euristico non sostituisce la conferma umana
    - Lo script non contiene comandi di installazione/download di pacchetti: `grep -ci 'Install-Module\|Invoke-WebRequest\|choco \|winget \|npm i\|pip install' scripts/dhu-quota-check.ps1` restituisce 0
    - `docs/dhu-quota-verification.md` contiene sezioni per prerequisiti, procedura, criteri di esito e contingenza, e cita esplicitamente `D-06`, `D-07`, `D-08`, `D-09`
    - Il runbook afferma esplicitamente che il throttle di contingenza va applicato solo lato auto in `SpeedScreen` e mai in `GpsSpeedProvider`, e che il percorso Surface/NAVIGATION resta scartato
    - Il runbook afferma esplicitamente che un telefono fisico non serve per questa verifica (D-09) ma serve alla Fase 11
    - `git check-ignore -q build/dhu-quota` restituisce esito 0 (la directory di output e' ignorata da git)
  </acceptance_criteria>
  <done>Chiunque puo' rieseguire la verifica di quota con un comando e ottenere una misura oggettiva della cadenza e della sopravvivenza del processo, con criteri di esito e piano di contingenza scritti in anticipo.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Gate SC4 — sessione DHU 5-10 minuti e conferma assenza di regressioni sul telefono</name>
  <read_first>
    - docs/dhu-quota-verification.md (il runbook prodotto dal Task 2 — la persona lo segue passo-passo)
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-CONTEXT.md (D-06 gate, D-07 contingenza, D-08 durata, D-09 setup, D-10 conferma umana)
  </read_first>
  <files>(nessun file modificato -- checkpoint di verifica)</files>
  <action>
Fermare l'esecuzione e consegnare alla persona la procedura di verifica descritta in `<how-to-verify>`. Questo e' il gate bloccante D-06 della fase: nessun lavoro successivo (e nessun avanzamento alla Fase 9) parte prima che la sessione DHU sia stata eseguita e il suo esito confermato da una persona.

Prima di presentare il checkpoint, l'esecutore prepara il terreno per rendere la sessione immediata:
1. Eseguire `./gradlew.bat :app:installDebug` e riportare l'esito.
2. Eseguire `adb devices` e riportare l'elenco, confermando che almeno un emulatore/dispositivo e' in stato `device`.
3. Stampare il comando esatto, gia' compilato con la durata scelta, che la persona dovra' lanciare: `powershell -File scripts/dhu-quota-check.ps1 -DurationSeconds 600`.
4. Presentare la checklist SC1-SC5 di `<how-to-verify>` e attendere la risposta.

L'esecutore NON deve dichiarare superato il gate sulla base del solo esito euristico dello script (D-10): il rendering visivo e il comportamento dell'host non sono verificabili da codice. Se l'esito e' FAIL, l'esecutore NON implementa il throttle di contingenza di propria iniziativa -- riporta il fallimento e attende la decisione dell'utente secondo il protocollo D-07 (rinegoziazione esplicita di SC3).
  </action>
  <what-built>
    - `GpsSpeedProvider` promosso a scope Application: una sola sottoscrizione GPS condivisa tra telefono e auto (Piano 01)
    - `TachimetroCarAppService` / `TachimetroCarSession` / `SpeedScreen` con `PaneTemplate` di categoria POI (Piano 02)
    - Contenuto: cifre della velocita' con "km/h" separato quando c'e' un fix, "Ricerca segnale..." altrimenti, nessun titolo ne' branding nello schermo
    - Refresh a piena cadenza 1 Hz, ritmato dal ticker gia' esistente del provider, senza timer ne' throttle lato auto (D-05)
    - Test automatici: `CarSpeedContentTest` (JVM) sul contratto di contenuto, `SpeedScreenTemplateTest` (strumentato) sulla forma del template
    - `scripts/dhu-quota-check.ps1` + `docs/dhu-quota-verification.md` per la misura riproducibile
  </what-built>
  <how-to-verify>
    Seguire `docs/dhu-quota-verification.md`. In sintesi:

    1. Preparare l'AVD: emulatore Android Studio con Google Play, API 30+, Android Auto installato, Developer Mode di Android Auto attivo con overlay di debug.
    2. Installare la build debug: `./gradlew.bat :app:installDebug`
    3. Aprire l'app sul telefono/emulatore e concedere il permesso di localizzazione.
    4. Attivare una posizione simulata in movimento (Extended Controls -> Location -> riproduzione di un percorso), cosi' che la velocita' sia una lettura reale e non "Ricerca segnale..." per tutta la sessione.
    5. Avviare la misura: `powershell -File scripts/dhu-quota-check.ps1 -DurationSeconds 600`
    6. Quando lo script lo chiede, avviare il Desktop Head Unit e selezionare Tachimetro nella lista app dell'head unit, poi premere INVIO nello script.
    7. Lasciare girare la sessione per l'intera durata (5-10 minuti, D-08) OSSERVANDO lo schermo DHU.

    Confermare punto per punto:
    - [ ] SC1 — lo schermo auto mostra la velocita' attuale come testo grande e leggibile, e il valore corrisponde a quello mostrato sul telefono
    - [ ] SC2 — quando il segnale GPS manca (fermare la riproduzione del percorso o disattivare la posizione simulata per >5 secondi) lo schermo auto passa a "Ricerca segnale..." e NON resta bloccato sull'ultimo valore
    - [ ] SC3 — il valore sullo schermo auto cambia una volta al secondo, in sincronia con il telefono, senza salti visibili
    - [ ] SC4 — per l'intera durata della sessione l'host NON ha chiuso l'app: nessun ritorno improvviso alla lista app dell'head unit, nessun messaggio di errore dell'host, e lo script riporta `PASS` (processo vivo per tutta la durata, cadenza media tra 0,8 e 1,2 refresh/s)
    - [ ] SC5 — sul telefono, durante e dopo la sessione: la velocita' e' mostrata come in v1.1, l'area MAX si aggiorna, la distanza si accumula, l'icona di ricarica si comporta come prima, lo switch "Sempre acceso" funziona; chiudendo il DHU e riaprendo l'app tutto resta coerente
    - [ ] Nessun crash: `adb logcat -b crash -d` non mostra crash di `com.sed.tachimetro` durante la sessione

    Allegare alla risposta il riepilogo prodotto dallo script (durata, numero di refresh, cadenza media, PID iniziale/finale, eventi di terminazione).

    **Se SC4 FALLISCE** (l'host chiude l'app o interrompe i refresh): NON procedere e NON applicare autonomamente il throttle. Riportare il risultato: scatta il protocollo D-07, che richiede una decisione esplicita dell'utente per rinegoziare SC3 prima di implementare un throttle a 2-3 secondi SOLO lato auto in `SpeedScreen`. Il ripiego sul percorso Surface/NAVIGATION resta un'opzione scartata e non va riaperta.
  </how-to-verify>
  <resume-signal>Rispondere "approvato" se tutti i punti SC1-SC5 sono confermati, allegando il riepilogo dello script; altrimenti descrivere quale punto e' fallito e con quale sintomo osservato.</resume-signal>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:installDebug --console=plain -q && adb devices | grep -v 'List of devices' | grep -c 'device'</automated>
    <human-check>Sessione DHU continua di 5-10 minuti a 1 Hz con conferma punto per punto di SC1, SC2, SC3, SC4, SC5 e assenza di crash, con il riepilogo di scripts/dhu-quota-check.ps1 allegato.</human-check>
  </verify>
  <acceptance_criteria>
    - L'esecutore ha eseguito `./gradlew.bat :app:installDebug` con esito `BUILD SUCCESSFUL` e ha riportato l'output di `adb devices` con almeno una riga in stato `device`
    - La persona ha risposto esplicitamente ai 6 punti della checklist (SC1, SC2, SC3, SC4, SC5, assenza di crash), non con un'approvazione generica
    - Il riepilogo prodotto da `scripts/dhu-quota-check.ps1` e' allegato alla risposta e riporta: durata effettiva >= 300 secondi (D-08), numero di righe `onGetTemplate #`, cadenza media in refresh/secondo, PID iniziale e finale, elenco degli eventi di terminazione
    - In caso di esito approvato: il PID rilevato all'inizio e alla fine della sessione e' lo stesso e non ci sono eventi di terminazione registrati
    - In caso di esito FAIL: nessuna modifica a `SpeedScreen.kt` e' stata effettuata dall'esecutore (`git diff --name-only` non contiene `SpeedScreen.kt`), e il fallimento e' stato riportato all'utente citando il protocollo D-07
  </acceptance_criteria>
  <done>Il gate SC4 e' chiuso: una persona ha eseguito una sessione DHU continua di almeno 5 minuti a 1 Hz e ha confermato punto per punto SC1-SC5, oppure ha registrato un fallimento che attiva il protocollo di rinegoziazione D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host Android Auto (DHU) <-> `TachimetroCarAppService` | Il confine IPC gia' introdotto dal Piano 02 viene qui esercitato sotto carico continuo per la prima volta. |
| Macchina di sviluppo <-> emulatore via adb | Lo script apre un forward di porta e cattura logcat dal dispositivo. |
| Artefatti di verifica <-> repository git | I log catturati sono file su disco che potrebbero essere committati per errore. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-11 | Denial of Service | quota di refresh dei template dell'host contro il refresh a 1 Hz di `SpeedScreen` | mitigate | E' la minaccia centrale della fase e il motivo per cui questo piano esiste. Mitigazione: gate empirico bloccante (D-06) di 5-10 minuti su DHU (D-08/D-09) con misura oggettiva della sopravvivenza del processo e della cadenza; contingenza scritta in anticipo (D-07: throttle solo lato auto in `SpeedScreen`, telefono invariato, con rinegoziazione esplicita di SC3). Nessun rilascio dipende da questo prima che il gate sia superato. |
| T-08-12 | Information Disclosure | file di cattura logcat prodotti da `dhu-quota-check.ps1` | mitigate | La cattura e' filtrata sul solo tag `TachimetroCar` (`adb logcat -s TachimetroCar:D`), quindi non raccoglie logcat di sistema o di altre app; l'output finisce in una directory ignorata da git (verificata con `git check-ignore`), quindi non entra nel repository. |
| T-08-13 | Tampering | catena di strumenti della verifica | mitigate | Lo script usa esclusivamente `adb` dall'Android SDK gia' installato e il binario DHU distribuito dall'SDK Manager; non scarica ne' installa nulla. Verificabile con il grep negativo su `Install-Module`/`Invoke-WebRequest`/`choco`/`winget`/`npm`/`pip` nell'acceptance del Task 2. |
| T-08-14 | Spoofing | `adb forward tcp:5277` esposto sulla macchina di sviluppo | accept | Il forward e' il meccanismo documentato e obbligatorio del Desktop Head Unit, e' loopback-local, dura solo la sessione di test e non e' presente in nessuna build distribuita. Nessuna mitigazione aggiuntiva proporzionata. |
| T-08-SC | Tampering | installazioni da package manager | n/a | Nessuna installazione da npm/pip/cargo in questo piano; `androidx.car.app:app-testing` e' stato pinnato e valutato nel Piano 01 (T-08-SC). |
</threat_model>

<verification>
1. `./gradlew.bat :app:compileDebugAndroidTestKotlin` -> BUILD SUCCESSFUL
2. `./gradlew.bat :app:connectedDebugAndroidTest` con emulatore connesso -> BUILD SUCCESSFUL, 0 test falliti
3. `./gradlew.bat :app:testDebugUnitTest` -> nessuna regressione nei test JVM
4. `powershell -File scripts/dhu-quota-check.ps1 -DurationSeconds 600` -> produce un riepilogo con conteggio refresh, cadenza media, tracciamento PID ed esito euristico
5. `git check-ignore -q build/dhu-quota` -> esito 0
6. Checkpoint umano SC1-SC5 approvato con riepilogo dello script allegato
</verification>

<success_criteria>
- La forma del `PaneTemplate` e la copy dello stato "Ricerca segnale..." sono coperte da asserzioni automatiche eseguibili su emulatore
- La verifica di quota e' riproducibile con un comando e produce una misura oggettiva, non un'impressione
- Il runbook documenta prerequisiti, criteri di esito e il protocollo di contingenza D-07 PRIMA che serva
- Il gate SC4 e' esplicitamente superato (o esplicitamente fallito e scalato all'utente secondo D-07), e SC1/SC2/SC3/SC5 sono confermati a occhio sul DHU e sul telefono
</success_criteria>

<output>
Create `.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-03-SUMMARY.md` when done
</output>
