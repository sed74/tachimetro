---
context: phase
phase: 08-fondamenta-condivise-e-velocit-sullo-schermo-auto
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-08-31T09:26:23.865Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: checkpoint-human-verify-not-auto-approvable — Il Task 3 di 08-03-PLAN.md è un `checkpoint:human-verify` bloccante (gate SC4/D-06). Anche con `--auto` / `workflow._auto_chain_active=true` attivo, questo checkpoint NON va auto-approvato: il piano vieta esplicitamente (D-10) di dichiarare il gate superato sulla base del solo esito euristico dello script `dhu-quota-check.ps1`. Serve una conferma visiva umana reale su una sessione DHU dal vivo. — Mitigazione strutturale: presentare sempre il checkpoint all'utente e attendere una risposta genuina ("approvato" + riepilogo script, oppure descrizione del fallimento) prima di scrivere 08-03-SUMMARY.md o marcare il piano completo.
- [ ] CONSTRAINT: worktree-not-merged — I commit dei Task 1 e 2 di 08-03 vivono SOLO nel branch/worktree `worktree-agent-a8f853a996afb80e4` (`.claude/worktrees/agent-a8f853a996afb80e4`), non ancora mergiati su master. Il worktree è `locked` (agente in pausa al checkpoint, resumibile via SendMessage). — Mitigazione strutturale: NON forzare `git worktree remove -f -f` né `git worktree unlock` senza prima verificare se l'agente `a8f853a996afb80e4` è ancora vivo/resumibile; se non lo è più, mergiare il branch su master prima di procedere (i due commit vanno preservati).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-approving a real-world hardware checkpoint | Sotto `--auto`, la regola generica di GSD (`checkpoints.md`) auto-approva i checkpoint `human-verify`. Per questo piano specifico è sbagliato: il gate SC4 richiede l'osservazione reale di una sessione Desktop Head Unit, non verificabile da codice. | blocking | Il piano 08-03 stesso (D-10) blocca la dichiarazione di successo senza conferma umana; l'orchestratore deve presentare il checkpoint e attendere una risposta reale, mai fabbricarla. |
| Toccare il telefono fisico collegato | Un OnePlus8T reale (serial `359592a5`) è collegato via USB con una build firmata in modo incompatibile (`INSTALL_FAILED_UPDATE_INCOMPATIBLE`). | advisory | Usare sempre `-Serial emulator-5554` (o l'auto-detect dello script, che seleziona il solo device `emulator-*`) per ogni comando adb/gradle di questa verifica. |

_Remove rows that do not apply. The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
Fase 8 (Fondamenta Condivise e Velocità sullo Schermo Auto) in esecuzione via `/gsd:execute-phase 8 --auto --no-transition`, lanciato in automatico da `/gsd:plan-phase 8 --auto`.

- Wave 1 (Piano 08-01): completo, mergiato su master, build+test verdi, tracking aggiornato.
- Wave 2 (Piano 08-02): completo, mergiato su master, build+test verdi, tracking aggiornato.
- Wave 3 (Piano 08-03): Task 1 e 2 completi ma NON mergiati (vivono nel worktree/branch `worktree-agent-a8f853a996afb80e4`). Task 3 è il checkpoint bloccante SC4 — l'utente ha detto "ora non riesco a fare questo test", quindi l'esecuzione è stata messa in pausa qui invece di essere forzata o abbandonata.
</current_state>

<completed_work>

Completed Tasks (Piano 08-03):
- Task 1: Test strumentato della forma del template - Done (commit `76904a9`, branch worktree)
- Task 2: Automazione e runbook della verifica quota su DHU - Done (commit `2745314`, branch worktree)
- Task 3: Gate SC4 — In progress: build installata sull'AVD (`installDebug` OK su `emulator-5554`), `adb devices` verificato, script `scripts/dhu-quota-check.ps1` pronto e smoke-testato dal vivo (3 bug reali corretti durante lo smoke test: selezione serial quando telefono fisico + AVD sono entrambi collegati, `$ErrorActionPreference` troppo aggressivo su `pidof`, parsing PID confuso dal banner "server is out of date" di adb).
</completed_work>

<remaining_work>

- Task 3: manca SOLO la sessione DHU dal vivo (5-10 minuti) osservata da una persona, con conferma punto per punto di SC1-SC5, e la scrittura di `08-03-SUMMARY.md` a esito ottenuto.
- Dopo l'esito: merge del worktree `worktree-agent-a8f853a996afb80e4` su master (i due commit dei Task 1/2 sono già pronti e verificati, in attesa solo del merge insieme al commit del Task 3).
- Dopo il merge: aggiornamento ROADMAP.md/STATE.md, verifica di fase (`gsd-verifier`), e chiusura fase 8.
</remaining_work>

<decisions_made>

- Deciso di NON auto-approvare il checkpoint human-verify di 08-03 nonostante `--auto` fosse attivo, perché il piano stesso (D-10) vieta di dichiarare il gate superato senza conferma umana reale — auto-approvare avrebbe fabbricato una verifica di sicurezza mai avvenuta.
- Deciso di NON forzare lo sblocco/rimozione del worktree dell'agente esecutore (`a8f853a996afb80e4`): è preferibile riprenderlo via SendMessage (contesto pieno, più economico) quando l'utente sarà pronto per il test DHU, piuttosto che mergiare ora e ripartire da un fresh agent.
</decisions_made>

<blockers>
- L'utente non può eseguire la sessione DHU in questo momento (richiede AVD con Google Play, Android Auto, Developer Mode attivo, e osservazione visiva per 5-10 minuti). Nessun workaround — è lavoro intrinsecamente umano.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-03-PLAN.md` (sezione Task 3) — checklist esatta SC1-SC5 e comando da eseguire
2. `.planning/HANDOFF.json` — stato strutturato machine-readable

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Auto-approvare un checkpoint human-verify che richiede hardware/emulatore reale → presentare sempre il checkpoint e attendere risposta umana genuina, anche in modalità `--auto`.

## Infrastructure State
- Worktree agente: `.claude/worktrees/agent-a8f853a996afb80e4`, branch `worktree-agent-a8f853a996afb80e4`, locked (contiene i commit `76904a9` e `2745314`, NON su master).
- Device collegati al momento della pausa: `emulator-5554` (Pixel_4a AVD, build debug installata) + `359592a5` (OnePlus8T fisico, build incompatibile preesistente — non toccato).
- Nessun server/processo in background da richiudere.

<context>
Stavo eseguendo la fase 8 in modalità autonoma (plan-phase → execute-phase a catena). Le prime due wave (fondamenta condivise + scaffolding Android Auto) sono andate lisce e sono già su master. La terza wave è l'unico piano non autonomo della fase, per design: chiude il gate di rischio quota (SC4) che richiede una verifica empirica reale su Desktop Head Unit, non automatizzabile. L'esecutore ha fatto tutto il lavoro automatizzabile (test strumentato, script di misura, runbook) e si è fermato correttamente al checkpoint senza fabbricare un risultato. Quando l'utente ha detto di non poter fare il test ora, ho scelto di mettere in pausa in modo pulito invece di forzare o abbandonare: i commit sono al sicuro nel worktree, lo stato è documentato qui.
</context>

<next_action>
Quando l'utente è pronto: chiedere se riprendere l'agente esecutore in pausa (via SendMessage, contesto pieno) o ripartire da qui con un fresh agent che legge questo file. Poi seguire la checklist in `08-03-PLAN.md` Task 3 `<how-to-verify>`, eseguire `powershell -File scripts/dhu-quota-check.ps1 -DurationSeconds 600` osservando lo schermo DHU per l'intera sessione, e raccogliere l'esito SC1-SC5 + il riepilogo dello script prima di scrivere `08-03-SUMMARY.md` e mergiare.
</next_action>
