---
context: phase
phase: 08-fondamenta-condivise-e-velocit-sullo-schermo-auto
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-09-02T07:14:16.177Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: checkpoint-human-verify-not-auto-approvable — Il Task 3 di 08-03-PLAN.md è un `checkpoint:human-verify` bloccante (gate SC4/D-06). Anche con `--auto` attivo, questo checkpoint NON va auto-approvato: il piano vieta esplicitamente (D-10) di dichiarare il gate superato sulla base del solo esito euristico dello script `dhu-quota-check.ps1`. Serve una conferma visiva umana reale su una sessione DHU dal vivo. — Mitigazione: presentare sempre il checkpoint e attendere una risposta genuina prima di scrivere 08-03-SUMMARY.md.
- [ ] CONSTRAINT: emulatore-muore-da-solo-causa-ignota — In questa sessione l'emulatore Pixel_4a è stato lanciato DUE volte dentro chiamate Bash tool, con due modalità diverse: (1) detached con `emulator.exe ... &` + `disown`, (2) come comando DIRETTO di `run_in_background: true` (senza `&`/`disown`, sotto l'ipotesi che questo lo mantenesse vivo). **ENTRAMBE le volte l'emulatore ha completato il boot correttamente e poi è terminato da solo (exit code 0, "removeAll"/"Deleting snapshot" nel log) pochi minuti dopo**, senza che nessun comando esplicito lo terminasse. La seconda modalità NON ha risolto il problema come inizialmente ipotizzato — quindi **non è (solo) un problema di job object legato a `&`/`disown`**: la causa radice reale resta ignota. — Mitigazione: non fidarsi di un emulatore lanciato da un task Bash di questa sessione come "vivo e stabile" — verificare SEMPRE con `adb devices`+`sys.boot_completed` immediatamente prima di ogni passo che ne dipende, e considerare di chiedere all'utente di avviare l'AVD lui stesso (Android Studio Device Manager, o un proprio terminale esterno a Claude Code) se il problema si ripresenta una terza volta, così il processo non dipende dal ciclo di vita di un task di questa sessione.
- [ ] CONSTRAINT: worktree-agent-a8f853a996afb80e4-e-gia-mergiato — Il precedente `.continue-here.md`/`HANDOFF.json` (scritti alle 09:26 UTC del 2026-08-31) dichiaravano i commit di 08-03 Task 1/2 (`76904a9`, `2745314`) come NON mergiati, vivi solo nel worktree `worktree-agent-a8f853a996afb80e4`. Questo era stale: un commit successivo nella stessa sessione precedente (`7fa94a5 chore: merge executor worktree`, ore 11:16 CEST) li aveva già uniti a master prima ancora che il file di pausa venisse scritto l'ultima volta. Verificato in questa sessione con `git merge-base --is-ancestor worktree-agent-a8f853a996afb80e4 master` → true. — Mitigazione: non fidarsi ciecamente di un HANDOFF/.continue-here vecchio su fatti verificabili con git; ri-controllare sempre lo stato reale (`git log`, `git merge-base --is-ancestor`) prima di agire di conseguenza (es. prima di rimergiare o toccare il worktree).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Auto-approving a real-world hardware checkpoint | Sotto `--auto`, la regola generica di GSD auto-approva i checkpoint `human-verify`. Per questo piano è sbagliato: il gate SC4 richiede l'osservazione reale di una sessione DHU, non verificabile da codice. | blocking | Il piano 08-03 stesso (D-10) blocca la dichiarazione di successo senza conferma umana; presentare sempre il checkpoint e attendere una risposta reale. |
| Emulatore muore da solo entro pochi minuti | Lanciato due volte con modalità diverse (detached con `&`/`disown`, e come comando diretto non detached): entrambe le volte è terminato da solo dopo il boot, senza comando esplicito di kill. Causa radice non identificata. | blocking | Verificare sempre `adb devices`+`sys.boot_completed` subito prima di ogni passo dipendente; se si ripresenta, chiedere all'utente di avviare l'AVD lui stesso fuori da Claude Code. |
| Fidarsi di un handoff stale su fatti verificabili con git | Un HANDOFF.json precedente dichiarava un worktree non mergiato; era vero al momento della scrittura ma superato da un commit successivo nella stessa sessione. | advisory | Verificare sempre con `git log`/`git merge-base --is-ancestor` prima di agire su affermazioni di stato provenienti da file di handoff. |
| Toccare il telefono fisico collegato | Un OnePlus8T reale (serial `359592a5`) è collegato via USB con una build firmata in modo incompatibile. | advisory | Usare sempre `-s emulator-5554` / `ANDROID_SERIAL=emulator-5554` per ogni comando adb/gradle di questa verifica. |
| Churn della versione del server adb | Il binario adb bundlato con Gradle (v32) e quello di platform-tools su PATH (v41) si uccidono e riavviano a vicenda il server condiviso sulla porta 5037 quando invocati in sequenza da tool diversi; questo ha causato un fallimento "device offline" di installDebug. | advisory | Dopo un messaggio "adb server is out of date", ricontrollare `adb devices` prima di procedere; ritentare una volta il comando fallito se sembra una race transitoria. |

_Remove rows that do not apply. The discuss-phase and execute-phase workflows parse this table and enforce a mandatory understanding check for any `blocking` rows._

<current_state>
Fase 8 (Fondamenta Condivise e Velocità sullo Schermo Auto), Piano 08-03, Task 3 di 3 (checkpoint bloccante SC4). L'utente ha confermato in questa sessione di essere pronto per il test DHU ("Sì, procedo ora"), poi ha invocato `/gsd:pause-work` durante la preparazione dell'ambiente, prima che il checkpoint potesse essere presentato.

- Wave 1 (Piano 08-01) e Wave 2 (Piano 08-02): complete, mergiate su master, build+test verdi.
- Wave 3 (Piano 08-03) Task 1 e 2: completi E già mergiati su master (commit `7fa94a5`, verificato con git — correggendo un'informazione stale del precedente handoff).
- Wave 3 Task 3: in corso, NON pronto. Emulatore Pixel_4a lanciato due volte in questa sessione (con e senza detach), boot completato entrambe le volte, ma è morto da solo entro pochi minuti entrambe le volte — nessun emulatore vivo al momento della pausa. Un tentativo di `installDebug` (tra i due lanci) è fallito per `device offline`.
</current_state>

<completed_work>

Completed Tasks (Piano 08-03):
- Task 1: Test strumentato della forma del template - Done (commit `76904a9`, ora su master)
- Task 2: Automazione e runbook della verifica quota su DHU - Done (commit `2745314`, ora su master)
- Task 3: Gate SC4 — In progress, BLOCCATO su un problema infrastrutturale non risolto: l'emulatore `emulator-5554` (Pixel_4a) ha completato il boot due volte in questa sessione ma è morto da solo entrambe le volte pochi minuti dopo (causa ignota — non è il problema `&`/`disown` inizialmente ipotizzato, vedi CONSTRAINT sopra). Un tentativo di `ANDROID_SERIAL=emulator-5554 ./gradlew.bat :app:installDebug` tra i due lanci è FALLITO con `device offline`.
</completed_work>

<remaining_work>

- Task 3: nessun emulatore vivo — va rilanciato da capo. Dato che due tentativi via task Bash di questa sessione sono entrambi morti da soli, considerare di chiedere all'utente di avviarlo lui stesso (Android Studio o un proprio terminale) prima di ritentare un terzo lancio automatico identico. Poi verificare `adb devices` + `sys.boot_completed=1`.
- Ripetere `ANDROID_SERIAL=emulator-5554 ./gradlew.bat :app:installDebug --console=plain -q` fino a `BUILD SUCCESSFUL`.
- Presentare all'utente il comando esatto `powershell -File scripts/dhu-quota-check.ps1 -DurationSeconds 600` e la checklist SC1-SC5 di `08-03-PLAN.md` Task 3 `<how-to-verify>`.
- Guidare/attendere la sessione DHU dal vivo (5-10 minuti), raccogliere la conferma punto per punto SC1-SC5 + il riepilogo dello script.
- Se PASS: scrivere `08-03-SUMMARY.md`, poi chiusura fase (aggiornamento ROADMAP.md/STATE.md, `gsd-verifier`, transizione a Fase 9).
- Se FAIL: NON implementare il throttle di contingenza autonomamente — riportare il fallimento citando il protocollo D-07.
</remaining_work>

<decisions_made>

- Corretto un'informazione stale nel precedente handoff: i commit di 08-03 Task 1/2 sono già su master (merge `7fa94a5`), non bloccati in un worktree non mergiato come dichiarato in precedenza.
- Non forzato lo sblocco/rimozione del worktree `worktree-agent-a8f853a996afb80e4` benché ora ridondante — nessun rischio a lasciarlo com'è.
- Ipotesi iniziale (poi FALSIFICATA): rilanciare l'emulatore come comando diretto di un task `run_in_background` invece che con `&`/`disown` ne avrebbe garantito la sopravvivenza. Non è stato così — anche questo secondo lancio è morto da solo. Non ripetere questa spiegazione come se fosse risolutiva.
</decisions_made>

<blockers>
- La sessione DHU dal vivo (5-10 minuti, osservazione visiva umana) resta l'unico blocco reale: nessun workaround, è verifica intrinsecamente umana.
</blockers>

## Required Reading (in order)
1. `.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-03-PLAN.md` (sezione Task 3) — checklist esatta SC1-SC5 e comando da eseguire
2. `.planning/HANDOFF.json` — stato strutturato machine-readable

## Critical Anti-Patterns (do NOT repeat these)
- [ANTI-PATTERN]: Dare per scontato che l'emulatore lanciato da un task Bash di questa sessione resti vivo → verificato 2/2 che muore da solo entro pochi minuti dal boot, con causa ignota. Verificare sempre con `adb devices` prima di ogni passo dipendente; se si ripresenta una terza volta, chiedere all'utente di avviarlo lui stesso fuori da Claude Code.
- [ANTI-PATTERN]: Fidarsi di un handoff precedente su fatti verificabili con git (es. stato di merge di un worktree) senza ricontrollare → verificare sempre con `git log`/`git merge-base --is-ancestor`.
- [ANTI-PATTERN]: Auto-approvare un checkpoint human-verify che richiede hardware/emulatore reale → presentare sempre il checkpoint e attendere risposta umana genuina.

## Infrastructure State
- Emulatore: NESSUNO vivo al momento della pausa. `emulator-5554` (Pixel_4a AVD) è stato lanciato due volte in questa sessione, ha completato il boot entrambe le volte, ma è morto da solo (exit code 0) pochi minuti dopo entrambe le volte — causa non identificata. Non riavviato una terza volta in questa sessione.
- Device fisico `359592a5` (OnePlus8T): collegato via USB, build incompatibile preesistente — NON toccare, usare sempre `-s emulator-5554` / `ANDROID_SERIAL=emulator-5554`.
- Worktree agente: `.claude/worktrees/agent-a8f853a996afb80e4`, branch `worktree-agent-a8f853a996afb80e4`, ancora presente e `locked`, ma i suoi commit sono già ancestor di master (verificato) — ridondante, non bloccante, lasciato intatto.
- Ultimo tentativo di `installDebug` (tra i due lanci dell'emulatore): FALLITO con `device offline` — non ancora ripetuto con successo.

<context>
Ripresa via `/gsd:resume-work`: l'utente ha confermato di essere pronto ora per il checkpoint DHU. Durante la preparazione dell'ambiente sono emerse due cose: (1) il precedente handoff conteneva un'informazione stale sul merge del worktree, già superata da un commit successivo nella sessione precedente — corretta; (2) l'emulatore muore da solo pochi minuti dopo il boot, riprodotto 2 volte con modalità di lancio diverse (detached e non-detached) — la prima ipotesi di causa (`&`/`disown`/job object Windows) è stata FALSIFICATA dal secondo tentativo, la causa reale resta ignota. L'utente ha invocato `/gsd:pause-work` durante questi tentativi — nessuna modifica al codice è stata fatta in questa sessione, solo preparazione (non riuscita) dell'ambiente di verifica.
</context>

<next_action>
Alla ripresa: il problema dell'emulatore che muore da solo NON è risolto. Prima di ritentare un terzo lancio automatico identico (probabile stesso esito), valutare con l'utente se avviare l'AVD Pixel_4a lui stesso (Android Studio Device Manager o un proprio terminale esterno a Claude Code) — un processo avviato fuori dal ciclo di vita dei task Bash di questa sessione non sarebbe soggetto allo stesso problema. Poi verificare `adb devices` + `sys.boot_completed=1`, ripetere `ANDROID_SERIAL=emulator-5554 ./gradlew.bat :app:installDebug --console=plain -q` fino a `BUILD SUCCESSFUL`, presentare il comando `powershell -File scripts/dhu-quota-check.ps1 -DurationSeconds 600` e la checklist SC1-SC5 di `08-03-PLAN.md` Task 3, e attendere la sessione DHU dal vivo.
</next_action>
