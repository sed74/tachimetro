---
phase: 08-fondamenta-condivise-e-velocit-sullo-schermo-auto
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - app/src/main/res/xml/automotive_app_desc.xml
  - app/src/main/AndroidManifest.xml
  - app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt
  - app/src/main/java/com/sed/tachimetro/car/TachimetroCarSession.kt
  - app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt
autonomous: true
requirements: [AA-01, AA-02, AA-03]
user_setup: []

must_haves:
  truths:
    - "L'host Android Auto puo' scoprire e bindare l'app come app di categoria POI con esperienza a template"
    - "Lo schermo auto mostra la velocita' corrente come cifre nella Row principale, con 'km/h' in uno slot separato, senza titolo o branding nel template (D-03)"
    - "Quando il GPS manca o non ha ancora un fix, lo schermo auto mostra 'Ricerca segnale...' invece di un valore vecchio"
    - "Il template viene ricostruito una volta al secondo, ritmato dallo stesso ticker che alimenta il telefono, senza timer separato lato auto"
    - "L'app compare nella lista app di Android Auto riusando l'icona di lancio esistente, senza asset grafico dedicato per l'auto (D-04)"
    - "Lo schermo auto non va in crash se raggiunto prima che ACCESS_FINE_LOCATION sia concesso"
  artifacts:
    - path: "app/src/main/res/xml/automotive_app_desc.xml"
      provides: "Descrittore che dichiara l'esperienza a template all'host Android Auto"
      contains: "<uses name=\"template\""
    - path: "app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt"
      provides: "Entry point CarAppService bindato dall'host"
      contains: "class TachimetroCarAppService : CarAppService()"
    - path: "app/src/main/java/com/sed/tachimetro/car/TachimetroCarSession.kt"
      provides: "Session che crea SpeedScreen come schermo iniziale"
      contains: "class TachimetroCarSession : Session()"
    - path: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      provides: "Screen che collega lo StateFlow condiviso e costruisce il PaneTemplate"
      contains: "override fun onGetTemplate()"
      min_lines: 60
  key_links:
    - from: "app/src/main/AndroidManifest.xml"
      to: "androidx.car.app.category.POI"
      via: "intent-filter del <service> TachimetroCarAppService"
      pattern: "androidx\\.car\\.app\\.category\\.POI"
    - from: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      to: "TachimetroApplication.gpsSpeedProvider"
      via: "carContext.applicationContext castato a TachimetroApplication"
      pattern: "applicationContext as\\?? TachimetroApplication"
    - from: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      to: "Screen.invalidate()"
      via: "collect su gpsSpeedProvider.state dentro repeatOnLifecycle(STARTED)"
      pattern: "invalidate\\(\\)"
    - from: "app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt"
      to: "com.sed.tachimetro.car.carSpeedContent"
      via: "mappatura pura SpeedState -> contenuto Row"
      pattern: "carSpeedContent\\("
---

<objective>
Rendere l'app scopribile e utilizzabile dall'host Android Auto come app di categoria POI e mostrare sullo schermo auto la velocita' corrente (AA-01) e lo stato "Ricerca segnale..." (AA-02), aggiornati a 1 Hz dalla stessa sorgente GPS del telefono (AA-03).

Purpose: e' il deliverable visibile della fase — il primo momento in cui il valore della velocita' esce dal telefono e appare sullo schermo dell'auto.
Output: scaffolding manifest completo (descrittore + meta-data + `<service>` POI), `TachimetroCarAppService`, `TachimetroCarSession`, `SpeedScreen` con `PaneTemplate`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-CONTEXT.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-PATTERNS.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-UI-SPEC.md
@.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-01-SUMMARY.md

<interfaces>
<!-- Contratti prodotti dal Piano 01 e gia' esistenti. L'esecutore li usa direttamente. -->

Da app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt (Piano 01):
- `class TachimetroApplication : Application()`
- `val gpsSpeedProvider: GpsSpeedProvider` (by lazy)

Da app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt (Piano 01):
- `sealed class CarSpeedContent`
- `data class CarSpeedContent.Speed(val kmh: Int)`
- `data object CarSpeedContent.Searching`
- `fun carSpeedContent(state: SpeedState): CarSpeedContent`

Da app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt:
- `val state: StateFlow<SpeedState>` (SharingStarted.WhileSubscribed, ticker interno a 1 Hz)

Risorse stringa disponibili (app/src/main/res/values/strings.xml):
- `R.string.app_name` = "Tachimetro"
- `R.string.unit_kmh` = "km/h"
- `R.string.car_searching_gps_signal` = "Ricerca segnale..." (creata dal Piano 01)

API Car App Library 1.7.0 usate in questo piano (tutte CarAppApiLevel 1):
- `androidx.car.app.CarAppService` — `abstract fun createHostValidator(): HostValidator`, `abstract fun onCreateSession(): Session`
- `androidx.car.app.Session` — `abstract fun onCreateScreen(intent: Intent): Screen`, espone `carContext`
- `androidx.car.app.Screen(carContext: CarContext)` — implementa `LifecycleOwner`, `abstract fun onGetTemplate(): Template`, `fun invalidate()`
- `androidx.car.app.validation.HostValidator.ALLOW_ALL_HOSTS_VALIDATOR`
- `androidx.car.app.model.PaneTemplate.Builder(pane: Pane)` — richiede almeno UNO tra `setTitle(...)` e `setHeaderAction(...)`
- `androidx.car.app.model.Pane.Builder().addRow(row).build()`
- `androidx.car.app.model.Row.Builder().setTitle(CharSequence).addText(CharSequence).build()`
- `androidx.car.app.model.Action.APP_ICON`
- `lifecycleScope` / `repeatOnLifecycle` sono estensioni di `androidx.lifecycle:lifecycle-runtime-ktx` (gia' dipendenza del progetto) valide su qualsiasi `LifecycleOwner`, quindi anche su `Screen`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffolding manifest Android Auto (categoria POI)</name>
  <files>app/src/main/res/xml/automotive_app_desc.xml, app/src/main/AndroidManifest.xml</files>
  <read_first>
    - app/src/main/AndroidManifest.xml (stato dopo il Piano 01: `android:name=".TachimetroApplication"` deve gia' esserci sul tag `<application>`; il blocco `<activity>` alle righe ~23-32 e' il modello strutturale da rispecchiare)
    - app/src/main/res/xml/ (contiene gia' backup_rules.xml e data_extraction_rules.xml — stessa cartella di destinazione)
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-PATTERNS.md (sezione "TachimetroCarAppService.kt" -> "Manifest declaration" e "Required <application>-level meta-data")
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-UI-SPEC.md (tabella "Car App Library manifest/category constraints")
  </read_first>
  <action>
Dichiarare l'esperienza a template e il servizio Car App nella categoria POI.

1. Creare `app/src/main/res/xml/automotive_app_desc.xml` con radice `<automotiveApp>` contenente un solo elemento figlio `<uses name="template" />`. Il nome del file e' esattamente `automotive_app_desc.xml` (non `car-app-desc.xml`, che non esiste). NON aggiungere `<uses name="media" />` ne' altri usi.

2. In `app/src/main/AndroidManifest.xml`, dentro il tag `<application>`, aggiungere due `<meta-data>`:
   - name `com.google.android.gms.car.application`, attributo `android:resource="@xml/automotive_app_desc"`
   - name `androidx.car.app.minCarApiLevel`, attributo `android:value="1"` — il livello 1 massimizza la compatibilita' con head unit vecchi; tutte le API usate in questo piano (`PaneTemplate`, `Pane`, `Row`, `Action.APP_ICON`) sono disponibili da CarAppApiLevel 1

3. In `app/src/main/AndroidManifest.xml`, dopo il blocco `<activity>` esistente e ancora dentro `<application>`, aggiungere il blocco `<service>` per `TachimetroCarAppService`:
   - `android:name=".car.TachimetroCarAppService"`
   - `android:exported="true"` — obbligatorio perche' l'host possa bindare il servizio (vedi `<threat_model>` T-08-05 per la disposizione del rischio associato)
   - `android:label="@string/app_name"` e `android:icon="@mipmap/ic_launcher"` — D-04: l'app riusa l'icona di lancio esistente nella lista app di Android Auto, nessun asset grafico dedicato
   - un `<intent-filter>` con `<action android:name="androidx.car.app.CarAppService" />` e `<category android:name="androidx.car.app.category.POI" />`
   La categoria e' POI, NON `androidx.car.app.category.NAVIGATION`: D-00a e' lockata prima della fase. `.planning/research/STACK.md` mostra un esempio con NAVIGATION — quell'esempio e' superato per questo progetto e non va copiato.

4. NON aggiungere alcun nuovo `<uses-permission>`: il percorso POI/template non richiede ne' `androidx.car.app.NAVIGATION_TEMPLATES` ne' `androidx.car.app.ACCESS_SURFACE` (UI-SPEC.md: "Surface/Canvas access: Not used"). NON aggiungere `<uses-feature android:name="android.hardware.type.automotive" />`: farebbe apparire l'app come app Android Automotive OS nativa, fuori scope esplicito della milestone (PITFALLS.md Pitfall 3).

5. Aggiungere commenti XML che rimandano a D-00a (categoria POI e perche' non NAVIGATION), D-04 (riuso icona) e alla Fase 11 (validazione host).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && grep -c 'uses name="template"' app/src/main/res/xml/automotive_app_desc.xml && grep -c 'androidx.car.app.category.POI' app/src/main/AndroidManifest.xml && grep -c 'androidx.car.app.minCarApiLevel' app/src/main/AndroidManifest.xml && grep -c 'com.google.android.gms.car.application' app/src/main/AndroidManifest.xml && test "$(grep -c 'NAVIGATION_TEMPLATES\|ACCESS_SURFACE\|category.NAVIGATION\|hardware.type.automotive' app/src/main/AndroidManifest.xml)" -eq 0 && test "$(grep -c '<uses-permission' app/src/main/AndroidManifest.xml)" -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/main/res/xml/automotive_app_desc.xml` esiste, ha radice `<automotiveApp>` e contiene esattamente un elemento `<uses name="template" />`
    - `app/src/main/AndroidManifest.xml` contiene un `<service android:name=".car.TachimetroCarAppService"` con `android:exported="true"`, `android:label="@string/app_name"` e `android:icon="@mipmap/ic_launcher"`
    - Il suo `<intent-filter>` contiene sia l'action `androidx.car.app.CarAppService` sia la category `androidx.car.app.category.POI`
    - Il manifest contiene i due `<meta-data>`: `com.google.android.gms.car.application` -> `@xml/automotive_app_desc`, e `androidx.car.app.minCarApiLevel` -> `1`
    - `grep -c 'NAVIGATION_TEMPLATES\|ACCESS_SURFACE\|category.NAVIGATION\|hardware.type.automotive' app/src/main/AndroidManifest.xml` restituisce 0
    - `grep -c '<uses-permission' app/src/main/AndroidManifest.xml` restituisce 1 (solo ACCESS_FINE_LOCATION, invariato dalla v1.1)
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL` (il manifest merger accetta la dichiarazione anche prima che la classe esista, ma il Task 2 la crea nello stesso piano)
  </acceptance_criteria>
  <done>L'APK dichiara un'esperienza a template Android Auto di categoria POI, senza nuovi permessi e senza esporre l'app come Automotive OS nativa.</done>
</task>

<task type="auto">
  <name>Task 2: CarAppService e Session</name>
  <files>app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt, app/src/main/java/com/sed/tachimetro/car/TachimetroCarSession.kt</files>
  <read_first>
    - app/src/main/AndroidManifest.xml (la dichiarazione `<service>` creata nel Task 1: il nome della classe deve corrispondere esattamente a `.car.TachimetroCarAppService`)
    - app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt (package `com.sed.tachimetro.car` gia' creato dal Piano 01 — stessa cartella)
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-PATTERNS.md (sezioni "TachimetroCarAppService.kt" e "TachimetroCarSession.kt", "Core pattern")
    - .planning/ROADMAP.md sezione "Phase 11" (per capire cosa NON anticipare qui)
  </read_first>
  <action>
Creare i due componenti di entry point della Car App Library, entrambi nel package `com.sed.tachimetro.car`.

1. `TachimetroCarAppService.kt`: `class TachimetroCarAppService : CarAppService()` con due override:
   - `override fun createHostValidator(): HostValidator = HostValidator.ALLOW_ALL_HOSTS_VALIDATOR`
   - `override fun onCreateSession(): Session = TachimetroCarSession()`
   Sopra `createHostValidator()` aggiungere un commento inline esplicito e cercabile che marca il debito: validatore permissivo accettabile solo per lo scaffold e per il test DHU di questa fase; la sostituzione con una allow-list reale e' scope esplicito della Fase 11 (ROADMAP.md Phase 11 SC1) e NON va anticipata qui. Il commento deve contenere la stringa letterale `Fase 11` cosi' da essere greppabile come marcatore di debito.

2. `TachimetroCarSession.kt`: `class TachimetroCarSession : Session()` con un solo override:
   - `override fun onCreateScreen(intent: Intent): Screen = SpeedScreen(carContext)`
   Usare `android.content.Intent` importato esplicitamente in cima al file (convenzione import del progetto: import espliciti, mai wildcard). Nessuna logica di routing sull'`intent`: l'app ha un solo schermo, nessun deep link.

3. Nessuno dei due file deve costruire un `GpsSpeedProvider`, tenere riferimenti a `MainActivity`, o avviare raccolte di dati: la sorgente e' Application-scoped (Piano 01) e il collector vive in `SpeedScreen` (Task 3).

4. KDoc di classe su entrambi, nello stile del progetto: cosa fa, chi lo invoca (l'host Android Auto via Binder), e per `TachimetroCarAppService` anche il fatto che e' `exported` per necessita' di piattaforma.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && grep -c 'class TachimetroCarAppService : CarAppService()' app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt && grep -c 'ALLOW_ALL_HOSTS_VALIDATOR' app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt && grep -c 'Fase 11' app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt && grep -c 'class TachimetroCarSession : Session()' app/src/main/java/com/sed/tachimetro/car/TachimetroCarSession.kt && grep -c 'SpeedScreen(carContext)' app/src/main/java/com/sed/tachimetro/car/TachimetroCarSession.kt</automated>
  </verify>
  <acceptance_criteria>
    - `TachimetroCarAppService.kt` dichiara `class TachimetroCarAppService : CarAppService()` con override di `createHostValidator()` che restituisce `HostValidator.ALLOW_ALL_HOSTS_VALIDATOR` e override di `onCreateSession()` che restituisce `TachimetroCarSession()`
    - `TachimetroCarAppService.kt` contiene la stringa `Fase 11` in un commento sopra `createHostValidator()`
    - `TachimetroCarSession.kt` dichiara `class TachimetroCarSession : Session()` con `onCreateScreen(intent: Intent): Screen` che restituisce `SpeedScreen(carContext)`
    - Nessuno dei due file contiene `GpsSpeedProvider`: `grep -c 'GpsSpeedProvider' app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt app/src/main/java/com/sed/tachimetro/car/TachimetroCarSession.kt` restituisce 0 per entrambi
    - Nessun import wildcard: `grep -c 'import .*\*$' app/src/main/java/com/sed/tachimetro/car/TachimetroCarAppService.kt app/src/main/java/com/sed/tachimetro/car/TachimetroCarSession.kt` restituisce 0 per entrambi
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>L'host Android Auto puo' bindare TachimetroCarAppService, ottenere una Session e ricevere SpeedScreen come schermo iniziale.</done>
</task>

<task type="auto">
  <name>Task 3: SpeedScreen — template della velocita' aggiornato a 1 Hz</name>
  <files>app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt righe 207-228 (`setupGpsCollection()`: la forma esatta `lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { ... } }` da portare) e righe 371-386 (`updatePlaceholder()`: la ramificazione su SpeedState da rispecchiare)
    - app/src/main/java/com/sed/tachimetro/car/CarSpeedContent.kt (la funzione pura da consumare — NON riscrivere il `when` su SpeedState qui)
    - app/src/main/java/com/sed/tachimetro/TachimetroApplication.kt (la proprieta' `gpsSpeedProvider` da leggere)
    - app/src/main/java/com/sed/tachimetro/gps/GpsSpeedProvider.kt righe 130-147 (il ticker a 1 Hz che gia' ritma le emissioni — motivo per cui non serve un timer lato auto)
    - app/src/main/java/com/sed/tachimetro/car/TachimetroCarSession.kt (il costruttore atteso: `SpeedScreen(carContext)`)
    - .planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-UI-SPEC.md (sezioni "Content Contract", "States", "Refresh Cadence & Quota Verification")
  </read_first>
  <action>
Creare `app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt`, package `com.sed.tachimetro.car`, come `class SpeedScreen(carContext: CarContext) : Screen(carContext)`.

1. Accesso alla sorgente condivisa: leggere il provider da `carContext.applicationContext as? TachimetroApplication` (cast SAFE, non forzato — convenzione "Error Handling" di CLAUDE.md: preferire un default sicuro a un'eccezione). Se il cast fallisce, il provider resta null e lo schermo mostra permanentemente lo stato "Ricerca segnale..." invece di andare in crash. Non tenere mai `carContext` in una proprieta' che sopravviva allo `Screen`.

2. Stato locale: una `private var latestState: SpeedState = SpeedState.Searching` — il valore piu' recente da cui `onGetTemplate()` costruisce il template. Inizializzata a `Searching` cosi' che il primo template mostrato prima di qualsiasi fix sia gia' quello corretto (AA-02).

3. Raccolta reattiva, in un blocco `init`, con la stessa forma di `MainActivity.setupGpsCollection()`:
   - `lifecycleScope.launch { repeatOnLifecycle(Lifecycle.State.STARTED) { ... } }`
   - dentro, PRIMA di collezionare, un gate difensivo sul permesso: `ContextCompat.checkSelfPermission(carContext, Manifest.permission.ACCESS_FINE_LOCATION) == PackageManager.PERMISSION_GRANTED`. Se il permesso non e' concesso, NON collezionare e non fare altro. Motivo concreto: `GpsSpeedProvider` e' annotato `@Suppress("MissingPermission")` e chiama `requestLocationUpdates` senza controlli propri, quindi collezionare senza permesso solleverebbe una `SecurityException` dentro il `callbackFlow` e farebbe crashare lo schermo auto. Il gate essendo dentro `repeatOnLifecycle(STARTED)` viene rivalutato ogni volta che lo schermo torna in stato STARTED, quindi un permesso concesso sul telefono viene raccolto al rientro senza riavvio.
   - se concesso: `provider.state.collect { state -> latestState = state; invalidate() }`
   - commento inline che documenta D-05/AA-03: la cadenza e' esattamente 1 Hz perche' ritmata dal ticker interno di `GpsSpeedProvider` (GpsSpeedProvider.kt:130-136); NON creare un timer, un `delay()` o un throttle lato auto in questa fase — l'eventuale throttle e' la mitigazione contingente D-07, che scatta solo se la verifica DHU del Piano 03 fallisce e solo dopo rinegoziazione esplicita di SC3 con l'utente
   - commento che rimanda alla Fase 9 per la richiesta esplicita del permesso via `CarContext.requestPermissions()`: qui il gate e' solo difensivo, nessuna UX di permesso viene inventata (UI-SPEC.md "Permission not yet granted")

4. `override fun onGetTemplate(): Template`:
   - incrementare un contatore `private var templateBuildCount = 0L` e, dentro `if (BuildConfig.DEBUG)`, emettere una riga `android.util.Log.d` con tag costante `TachimetroCar` e messaggio nel formato `onGetTemplate #<n> content=<Speed|Searching>`. Questa riga e' il segnale che lo script di verifica del Piano 03 conta per misurare la cadenza effettiva dei refresh (D-10). Il gate `BuildConfig.DEBUG` e' obbligatorio: nessun valore di velocita' deve finire in logcat nelle build di release (T-08-07).
   - costruire la `Row` a partire da `carSpeedContent(latestState)` con un `when` esaustivo sui due sottotipi di `CarSpeedContent`:
     - `CarSpeedContent.Speed` -> `Row.Builder().setTitle(content.kmh.toString()).addText(carContext.getString(R.string.unit_kmh)).build()` — D-01: solo le cifre nel titolo, unita' in uno slot separato, mai concatenata
     - `CarSpeedContent.Searching` -> `Row.Builder().setTitle(carContext.getString(R.string.car_searching_gps_signal)).build()` — nessun `addText`, l'unita' non va mostrata quando non c'e' un valore (UI-SPEC.md "States")
   - nessuna icona nella Row (UI-SPEC.md Content Contract: "Icon inside the Row: None")
   - restituire `PaneTemplate.Builder(Pane.Builder().addRow(row).build()).setHeaderAction(Action.APP_ICON).build()` — D-03: nessun titolo testuale ne' branding dentro lo schermo; `PaneTemplate` richiede almeno uno tra titolo e header action, e `Action.APP_ICON` soddisfa il vincolo senza mostrare testo
   - NON aggiungere azioni, pulsanti, `setActionStrip` o elementi interattivi: REQUIREMENTS.md "Out of Scope" esclude pulsanti e schermate di impostazioni sullo schermo auto
   - NON mostrare velocita' massima o distanza percorsa: restano funzioni solo del telefono (out of scope di milestone)

5. Nessun accesso a `MaxSpeedStore`, `DistanceStore`, `reduceMax`, `reduceDistance` o `ScreenOnPreferenceStore` da questo file: gli effetti collaterali di persistenza restano esclusivamente in `MainActivity.updatePlaceholder()`. Duplicarli qui farebbe contare due volte la distanza quando entrambi gli schermi sono attivi.

6. Import espliciti, mai wildcard. KDoc di classe che spiega: unico collector lato auto dello `StateFlow` condiviso, cadenza ereditata dal provider, contenuto delegato a `carSpeedContent()`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && ./gradlew.bat :app:assembleDebug --console=plain -q && grep -c 'carSpeedContent(' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt && grep -c 'repeatOnLifecycle(Lifecycle.State.STARTED)' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt && grep -c 'invalidate()' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt && grep -c 'setHeaderAction(Action.APP_ICON)' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt && grep -c 'checkSelfPermission' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt && grep -c 'BuildConfig.DEBUG' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt && test "$(grep -c 'MaxSpeedStore\|DistanceStore\|reduceMax\|reduceDistance\|ScreenOnPreferenceStore\|setActionStrip\|setTitle(carContext.getString(R.string.app_name))' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt)" -eq 0 && test "$(grep -c 'delay(' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt)" -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `SpeedScreen.kt` dichiara `class SpeedScreen(carContext: CarContext) : Screen(carContext)` con `override fun onGetTemplate(): Template`
    - Il provider e' ottenuto con un cast sicuro: il file contiene `applicationContext as? TachimetroApplication` (con `as?`, non `as` forzato)
    - Il blocco `init` contiene `lifecycleScope.launch` con `repeatOnLifecycle(Lifecycle.State.STARTED)` e, al suo interno, un controllo `ContextCompat.checkSelfPermission(...) == PackageManager.PERMISSION_GRANTED` che precede la `collect`
    - La `collect` chiama `invalidate()` a ogni emissione
    - `onGetTemplate()` costruisce il risultato con `PaneTemplate.Builder(...)` + `.setHeaderAction(Action.APP_ICON)` e NON chiama `setTitle` sul `PaneTemplate.Builder` (D-03)
    - Il ramo Speed produce una `Row` con `setTitle(<cifre>)` e `addText(getString(R.string.unit_kmh))`; il ramo Searching produce una `Row` con `setTitle(getString(R.string.car_searching_gps_signal))` e senza `addText`
    - Il logging e' racchiuso in `if (BuildConfig.DEBUG)` e usa il tag `TachimetroCar` con un messaggio contenente `onGetTemplate #`
    - `grep -c 'MaxSpeedStore\|DistanceStore\|reduceMax\|reduceDistance\|ScreenOnPreferenceStore\|setActionStrip' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt` restituisce 0
    - `grep -c 'delay(' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt` restituisce 0 (nessun throttle lato auto in questa fase, D-05)
    - `grep -c 'import .*\*$' app/src/main/java/com/sed/tachimetro/car/SpeedScreen.kt` restituisce 0
    - `./gradlew.bat :app:assembleDebug` termina con `BUILD SUCCESSFUL`
  </acceptance_criteria>
  <done>SpeedScreen collega lo StateFlow condiviso, ricostruisce il PaneTemplate a ogni emissione (1 Hz), mostra le cifre con unita' separata quando c'e' un fix e "Ricerca segnale..." altrimenti, e non collezionerebbe senza permesso concesso.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Processo host Android Auto <-> `TachimetroCarAppService` (Binder IPC) | Confine principale della fase: un processo esterno binda un componente esportato dell'app e riceve contenuto generato da dati di localizzazione. |
| `SpeedScreen` <-> `GpsSpeedProvider` (in-process) | Punto in cui i dati di localizzazione grezzi vengono ridotti prima di attraversare il confine IPC. |
| Processo app <-> logcat di sistema | Il conteggio dei refresh introduce output diagnostico. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05 | Spoofing | `<service android:exported="true">` + `HostValidator.ALLOW_ALL_HOSTS_VALIDATOR` | accept | `exported="true"` e' un requisito di piattaforma (senza di esso l'host non puo' bindare, AA-01 e' irrealizzabile). Il validatore permissivo accetta qualunque processo che bindi il servizio. Accettato solo per questa fase perche': (a) non viene prodotta nessuna build di release in questo piano, (b) il dato esposto e' il solo valore km/h intero corrente, senza coordinate, identificatori o storico, (c) la sostituzione con un `HostValidator` reale e' scope esplicito e gia' pianificato della Fase 11 (ROADMAP.md Phase 11 SC1). Tracciabilita' verificabile: il commento sopra `createHostValidator()` contiene il marcatore `Fase 11`. |
| T-08-06 | Information Disclosure | dati che attraversano il confine IPC verso l'host | mitigate | Attraversa il confine solo il contenuto derivato prodotto da `carSpeedContent()`: un intero km/h o una stringa fissa. Latitudine, longitudine, accuratezza e l'oggetto `Location` restano confinati in `GpsSpeedProvider`. Verificabile: `SpeedScreen.kt` non importa `android.location` e consuma solo `SpeedState`. |
| T-08-07 | Information Disclosure | log di conteggio refresh in logcat | mitigate | Il `Log.d` e' racchiuso in `if (BuildConfig.DEBUG)` (abilitato dal Piano 01), quindi assente dalle build di release. Verificabile: `grep -c 'BuildConfig.DEBUG' SpeedScreen.kt` >= 1 e nessun `Log.` fuori da quel blocco. |
| T-08-08 | Elevation of Privilege | `SpeedScreen` avviato dall'host prima della concessione di ACCESS_FINE_LOCATION | mitigate | Gate `ContextCompat.checkSelfPermission(...) == PERMISSION_GRANTED` prima della `collect`. Senza di esso `requestLocationUpdates` (annotato `@Suppress("MissingPermission")` in GpsSpeedProvider.kt:81) solleverebbe `SecurityException` nel `callbackFlow`, facendo crashare lo schermo auto. Il gate e' dentro `repeatOnLifecycle(STARTED)` quindi viene rivalutato a ogni rientro in STARTED. |
| T-08-09 | Tampering | `automotive_app_desc.xml` / dichiarazioni manifest | mitigate | Il descrittore dichiara solo `<uses name="template" />`; nessun `<uses-feature android:name="android.hardware.type.automotive" />` e nessun artifact `app-automotive`, per evitare che l'app venga esposta come app Android Automotive OS nativa (PITFALLS.md Pitfall 3). Verificabile con il grep negativo nell'acceptance del Task 1. |
| T-08-10 | Elevation of Privilege | superficie dei permessi | mitigate | Zero nuovi `<uses-permission>`: il percorso POI/template non richiede `NAVIGATION_TEMPLATES` ne' `ACCESS_SURFACE`. Verificabile: `grep -c '<uses-permission' AndroidManifest.xml` == 1. |
| T-08-SC | Tampering | installazioni da package manager | n/a | Nessuna installazione da npm/pip/cargo in questo piano; l'unica dipendenza esterna e' stata pinnata e valutata nel Piano 01 (T-08-SC). |
</threat_model>

<verification>
1. `./gradlew.bat :app:assembleDebug` -> BUILD SUCCESSFUL
2. `./gradlew.bat :app:testDebugUnitTest` -> nessuna regressione nei test JVM esistenti
3. `grep -c '<uses-permission' app/src/main/AndroidManifest.xml` -> 1
4. `grep -c 'androidx.car.app.category.POI' app/src/main/AndroidManifest.xml` -> 1
5. Il manifest merged dell'APK debug contiene il service: `./gradlew.bat :app:processDebugMainManifest` e poi ispezione di `app/build/intermediates/merged_manifests/debug/**/AndroidManifest.xml` per la presenza di `TachimetroCarAppService` e della category POI
6. Il comportamento del telefono resta invariato: `./gradlew.bat :app:installDebug` e verifica manuale che l'app si avvii, chieda il permesso e mostri velocita'/MAX/distanza come in v1.1 (verifica formale nel Piano 03)
</verification>

<success_criteria>
- L'APK debug dichiara `TachimetroCarAppService` come servizio esportato di categoria `androidx.car.app.category.POI` con descrittore a template e `minCarApiLevel` 1
- `SpeedScreen` esiste, consuma lo `StateFlow` condiviso da `TachimetroApplication`, chiama `invalidate()` a ogni emissione e costruisce un `PaneTemplate` con una sola `Row`
- Il contenuto della Row rispetta il Content Contract: cifre nel titolo + "km/h" separato quando c'e' un fix, "Ricerca segnale..." senza unita' altrimenti, nessuna icona, nessun titolo/branding a livello di template
- Nessun nuovo permesso, nessun throttle lato auto, nessuna logica di massimo/distanza duplicata sullo schermo auto
</success_criteria>

<output>
Create `.planning/phases/08-fondamenta-condivise-e-velocit-sullo-schermo-auto/08-02-SUMMARY.md` when done
</output>
