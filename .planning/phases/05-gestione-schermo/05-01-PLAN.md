---
phase: 05-gestione-schermo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/res/values/strings.xml
  - app/src/main/res/color/switch_track_tint.xml
  - app/src/main/res/color/switch_thumb_tint.xml
  - app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt
  - app/src/main/res/layout/activity_main.xml
  - app/src/main/java/com/sed/tachimetro/MainActivity.kt
autonomous: true
requirements: [SCRN-01, SCRN-02, SCRN-03]

must_haves:
  truths:
    - "L'utente vede in basso a sinistra uno switch (SwitchCompat) sempre visibile con etichetta 'Sempre acceso', speculare verticalmente all'area MAX in alto a sinistra (D-01/D-02/D-03)"
    - "Con lo switch ON, la finestra ha FLAG_KEEP_SCREEN_ON attivo e lo schermo non si spegne; con OFF il flag viene rimosso — applicazione immediata al tap, senza riavvio (D-06/SCRN-02)"
    - "Ogni cambio di stato dello switch scrive immediatamente la preferenza booleana su SharedPreferences, così sopravvive a chiusura app e riavvio telefono (D-07/SCRN-03)"
    - "Al primissimo avvio (nessuna preferenza salvata) lo stato iniziale è ON se il telefono è in ricarica, OFF altrimenti; il valore derivato viene persistito una sola volta (D-04/D-05)"
    - "Ad ogni avvio successivo lo switch riflette esclusivamente la preferenza persistita — lo stato di ricarica NON viene più ricontrollato (D-05)"
    - "Lo stato checked dello switch viene impostato in onCreate PRIMA di registrare l'OnCheckedChangeListener, così il listener non scatta durante l'inizializzazione e non c'è flash di stato (UI-SPEC)"
    - "Lo switch rispetta gli insets di status bar / navigation bar / display cutout sul lato bottom+left in entrambi gli orientamenti (applyScreenSwitchWindowInsets, specchio di applyMaxAreaWindowInsets)"
    - "Track e thumb dello switch sono in scala di grigi (bianco/alpha su nero) — nessun colorAccent blu/viola di default AppCompat; nessuna nuova dipendenza Gradle (SwitchCompat da androidx.appcompat)"
    - "Tutte le stringhe passano da getString() e sono in italiano (UI-05); nessuna animazione app-authored (UI-04)"
  artifacts:
    - path: "app/src/main/res/values/strings.xml"
      provides: "Stringa italiana keep_screen_on_label ('Sempre acceso')"
      contains: "keep_screen_on_label"
    - path: "app/src/main/res/color/switch_track_tint.xml"
      provides: "ColorStateList grayscale per il track (checked #80FFFFFF / unchecked #33FFFFFF)"
      contains: "state_checked"
    - path: "app/src/main/res/color/switch_thumb_tint.xml"
      provides: "ColorStateList bianco opaco per il thumb in entrambi gli stati"
      contains: "#FFFFFF"
    - path: "app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt"
      provides: "Wrapper SharedPreferences per un Boolean nullable (null = nessuna preferenza ancora salvata, D-05)"
      contains: "fun read(): Boolean?"
    - path: "app/src/main/res/layout/activity_main.xml"
      provides: "SwitchCompat keepScreenOnSwitch bottom-left, sempre visibile, 48dp min, tint grayscale"
      contains: "@+id/keepScreenOnSwitch"
    - path: "app/src/main/java/com/sed/tachimetro/MainActivity.kt"
      provides: "Wiring: default da ricarica al primo avvio, checked prima del listener, FLAG_KEEP_SCREEN_ON immediato, persistenza, insets"
      contains: "ScreenOnPreferenceStore"
  key_links:
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt onCreate"
      to: "ScreenOnPreferenceStore.read() ?: isCharging()"
      via: "lettura preferenza persistita con fallback allo stato di ricarica solo al primo avvio (D-04/D-05)"
      pattern: "screenOnStore\\.read\\(\\)"
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt keepScreenOnSwitch"
      to: "applyKeepScreenOn + ScreenOnPreferenceStore.write"
      via: "setOnCheckedChangeListener: applica FLAG_KEEP_SCREEN_ON e persiste immediatamente (D-06/D-07)"
      pattern: "setOnCheckedChangeListener"
    - from: "app/src/main/java/com/sed/tachimetro/MainActivity.kt applyKeepScreenOn"
      to: "window.addFlags/clearFlags(FLAG_KEEP_SCREEN_ON)"
      via: "controllo effettivo dello spegnimento schermo (SCRN-02)"
      pattern: "FLAG_KEEP_SCREEN_ON"
---

<objective>
Aggiungere il controllo "Schermo sempre acceso". Un nuovo `keepScreenOnSwitch` (SwitchCompat) in basso a sinistra — speculare verticalmente all'area MAX in alto a sinistra — è sempre visibile e permette all'utente di scegliere se mantenere lo schermo acceso durante l'uso. Il cambio di stato applica/rimuove immediatamente `FLAG_KEEP_SCREEN_ON` sulla finestra e persiste la preferenza booleana su SharedPreferences. Al primissimo avvio (nessuna preferenza salvata) lo stato iniziale è derivato dallo stato di ricarica del telefono (ON se in ricarica, OFF altrimenti), e quel default viene persistito una volta sola; da lì in poi conta solo il valore salvato.

Purpose: Soddisfa SCRN-01 (toggle sempre-acceso/automatico), SCRN-02 (blocco effettivo dello spegnimento con FLAG_KEEP_SCREEN_ON) e SCRN-03 (persistenza tra sessioni), rispettando le decisioni D-01..D-07 del CONTEXT e il contratto visivo 05-UI-SPEC.
Output: strings.xml + 2 ColorStateList + activity_main.xml aggiornati, nuovo ScreenOnPreferenceStore.kt, MainActivity.kt cablato. Verifica su device nel piano 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-gestione-schermo/05-CONTEXT.md
@.planning/phases/05-gestione-schermo/05-UI-SPEC.md

<interfaces>
<!-- Contratti che l'executor deve usare direttamente — nessuna esplorazione aggiuntiva del codice necessaria. -->

Pattern SharedPreferences esistente da replicare (read-only, NON modificare):
  From app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt:
    class MaxSpeedStore(context: Context) {
      private val prefs = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)
      fun read(): Int; fun write(value: Int)
      companion object { const val PREFS_NAME = "tachimetro_prefs"; const val KEY_MAX_SPEED = "max_speed_kmh" }
    }
  Nota: riusare LO STESSO file di preferenze "tachimetro_prefs" con una chiave DIVERSA ("keep_screen_on") — nessun conflitto.

Punti di integrazione esistenti in MainActivity.kt (Fase 3/4):
  - onCreate(): setContentView(R.layout.activity_main); enableImmersiveFullscreen();
    findViewById per messageText/unitText/retryButton; applyUnitTextWindowInsets();
    findViewById per maxSpeedText/resetMaxButton; applyMaxAreaWindowInsets();
    maxSpeedStore = MaxSpeedStore(applicationContext); currentMax = maxSpeedStore.read(); updateMaxArea();
    poi crea gpsSpeedProvider e avvia la raccolta STARTED-scoped; infine checkAndRequestPermission().
  - applyMaxAreaWindowInsets(): pattern di riferimento per insets top+left, da specchiare sul lato bottom+left.
    Legge baseParams (topMargin/marginStart) UNA VOLTA prima del listener, poi somma
    maxOf(systemBars, cutout) su ogni evento insets. Usa ViewCompat.setOnApplyWindowInsetsListener.
  - Le funzioni showReady()/showDenied()/updatePlaceholder() gestiscono la visibilità di unitText/area MAX
    in base allo stato permesso/GPS. keepScreenOnSwitch NON deve MAI essere toccato da queste funzioni
    (D-03: sempre visibile, stato-indipendente).

Nuova API interna esposta da questo piano (Task 1), consumata da MainActivity (Task 3):
  package com.sed.tachimetro.screen
  class ScreenOnPreferenceStore(context: Context) {
    fun read(): Boolean?          // null = nessuna preferenza salvata (primo avvio, D-05)
    fun write(keepOn: Boolean)    // scrittura immediata (apply())
  }
</interfaces>

<constraints>
- minSdk = 30 confermato in app/build.gradle.kts. SwitchCompat (androidx.appcompat.widget.SwitchCompat), SharedPreferences, BatteryManager, WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON, ConstraintLayout sono tutti API/widget GIÀ disponibili — NESSUNA nuova dipendenza Gradle (CONTEXT Established Patterns / UI-SPEC Registry Safety).
- AGP 9.1.1 con supporto Kotlin integrato: NON aggiungere il plugin classico `org.jetbrains.kotlin.android` (incompatibilità documentata).
- Root ConstraintLayout in activity_main.xml dichiara già `xmlns:android`, `xmlns:app`, `xmlns:tools` — nessun namespace nuovo da aggiungere.
- keepScreenOnSwitch è SEMPRE `VISIBLE` in ogni stato (ready, reading, searching, permesso negato/negato-permanente): NON aggiungere alcuna logica di visibilità condizionale per questo widget (D-03/UI-SPEC).
- NON modificare messageText, unitText, retryButton, maxSpeedText, resetMaxButton (id, vincoli, margini, autosize, visibilità, tint) — questa fase aggiunge solo il nuovo switch.
- Nessuna animazione/transizione app-authored (UI-04): solo lo slide nativo del thumb del SwitchCompat, non pilotato da codice.
- Tint del track/thumb SOLO in scala di grigi (bianco/alpha su nero): NON lasciare il default `colorControlActivated`/`colorAccent` AppCompat (blu/viola) — override obbligatorio via app:trackTint/app:thumbTint (UI-02).
- Passare `applicationContext` a ScreenOnPreferenceStore (pattern WR-04: mai un riferimento all'Activity in oggetti a vita lunga).
- Riusare lo stesso file di preferenze "tachimetro_prefs" di MaxSpeedStore con una chiave nuova e distinta ("keep_screen_on").
</constraints>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stringa italiana, due ColorStateList grayscale e ScreenOnPreferenceStore</name>
  <files>app/src/main/res/values/strings.xml, app/src/main/res/color/switch_track_tint.xml, app/src/main/res/color/switch_thumb_tint.xml, app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt</files>
  <read_first>
    - app/src/main/res/values/strings.xml (stato attuale — aggiungere in coda, non riscrivere le stringhe esistenti)
    - app/src/main/java/com/sed/tachimetro/maxspeed/MaxSpeedStore.kt (pattern SharedPreferences da replicare per un Boolean)
    - .planning/phases/05-gestione-schermo/05-UI-SPEC.md (sezioni Color, Copywriting Contract, Registry Safety)
  </read_first>
  <action>
    (A) In `app/src/main/res/values/strings.xml`, aggiungere ESATTAMENTE questa stringa (italiano, UI-05) prima del tag di chiusura `</resources>`. NON modificare/rinominare le stringhe esistenti:
      <string name="keep_screen_on_label">Sempre acceso</string>

    (B) Creare `app/src/main/res/color/switch_track_tint.xml` (la cartella `res/color/` non esiste ancora — crearla). ColorStateList grayscale per il track, con lo stato più specifico per primo:
      <?xml version="1.0" encoding="utf-8"?>
      <selector xmlns:android="http://schemas.android.com/apk/res/android">
          <item android:state_checked="true" android:color="#80FFFFFF" />
          <item android:color="#33FFFFFF" />
      </selector>

    (C) Creare `app/src/main/res/color/switch_thumb_tint.xml`. ColorStateList bianco opaco in entrambi gli stati (la posizione del thumb, non il colore, comunica ON/OFF):
      <?xml version="1.0" encoding="utf-8"?>
      <selector xmlns:android="http://schemas.android.com/apk/res/android">
          <item android:color="#FFFFFF" />
      </selector>

    (D) Creare `app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt` (nuovo package `com.sed.tachimetro.screen`, specchio del package `maxspeed`). Replica il pattern di MaxSpeedStore ma persiste un Boolean e distingue "nessuna preferenza salvata" (null) da "false salvato" — indispensabile per il default da stato di ricarica al primo avvio (D-05). Usa `prefs.contains(KEY)` per distinguere i due casi. Riusa lo STESSO file "tachimetro_prefs" con una chiave nuova:

      package com.sed.tachimetro.screen

      import android.content.Context

      /**
       * D-07: persistenza della preferenza "schermo sempre acceso" (un Boolean) via SharedPreferences
       * app-private, stesso file "tachimetro_prefs" di MaxSpeedStore ma con chiave distinta.
       * read() restituisce null quando NESSUNA preferenza è ancora stata salvata (primo avvio, D-05),
       * così il chiamante può derivare il default dallo stato di ricarica solo in quel caso.
       */
      class ScreenOnPreferenceStore(context: Context) {
          // WR-04: il chiamante passa applicationContext, mai un'Activity.
          private val prefs = context.getSharedPreferences(PREFS_NAME, Context.MODE_PRIVATE)

          /** null = nessuna preferenza salvata (D-05: primo avvio); altrimenti il Boolean persistito. */
          fun read(): Boolean? =
              if (prefs.contains(KEY_KEEP_SCREEN_ON)) prefs.getBoolean(KEY_KEEP_SCREEN_ON, false) else null

          /** D-06/D-07: scrittura immediata (apply(), fuori dal main thread) — un solo Boolean economico. */
          fun write(keepOn: Boolean) {
              prefs.edit().putBoolean(KEY_KEEP_SCREEN_ON, keepOn).apply()
          }

          companion object {
              const val PREFS_NAME = "tachimetro_prefs"
              const val KEY_KEEP_SCREEN_ON = "keep_screen_on"
          }
      }
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'name="keep_screen_on_label">Sempre acceso<' app/src/main/res/values/strings.xml` restituisce 0.
    - `app/src/main/res/color/switch_track_tint.xml` esiste e contiene sia `state_checked="true"` sia `#80FFFFFF` sia `#33FFFFFF`.
    - `app/src/main/res/color/switch_thumb_tint.xml` esiste e contiene `#FFFFFF`.
    - `app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt` esiste, dichiara `package com.sed.tachimetro.screen`, contiene `fun read(): Boolean?`, `prefs.contains(KEY_KEEP_SCREEN_ON)`, `const val PREFS_NAME = "tachimetro_prefs"` e `const val KEY_KEEP_SCREEN_ON = "keep_screen_on"`.
    - Le stringhe esistenti in strings.xml sono invariate (nessuna rinominata/rimossa).
    - `./gradlew.bat :app:assembleDebug` completa senza errori.
  </acceptance_criteria>
  <done>Stringa italiana aggiunta, i due ColorStateList grayscale creati in res/color/, ScreenOnPreferenceStore.kt compila con read() nullable e chiave "keep_screen_on" sul file "tachimetro_prefs"; assembleDebug verde.</done>
</task>

<task type="auto">
  <name>Task 2: Aggiungere keepScreenOnSwitch (SwitchCompat) in basso a sinistra nel layout</name>
  <files>app/src/main/res/layout/activity_main.xml</files>
  <read_first>
    - app/src/main/res/layout/activity_main.xml (stato attuale — evolvere in place; osservare maxSpeedText/resetMaxButton da specchiare sul bordo inferiore)
    - .planning/phases/05-gestione-schermo/05-UI-SPEC.md (Layout Contract, Spacing Scale, Typography, Color)
  </read_first>
  <action>
    In `app/src/main/res/layout/activity_main.xml` aggiungere UN nuovo figlio al ConstraintLayout root, dopo `resetMaxButton` (l'ordine tra fratelli non conta per ConstraintLayout). NON modificare messageText, unitText, retryButton, maxSpeedText, resetMaxButton (nessun cambio a id, vincoli, margini, autosize, visibilità).

    Aggiungere keepScreenOnSwitch — SwitchCompat ancorato all'angolo bottom-left, speculare a maxSpeedText sul lato start (D-02), sempre visibile (nessun `android:visibility="gone"`, D-03), etichetta baked-in via android:text (D-01), 16dp margini start/bottom (UI-SPEC Spacing md), 48dp min touch target (UI-SPEC), 16sp Regular, singola riga, tint grayscale (UI-02):

      <androidx.appcompat.widget.SwitchCompat
          android:id="@+id/keepScreenOnSwitch"
          android:layout_width="wrap_content"
          android:layout_height="wrap_content"
          android:layout_marginStart="16dp"
          android:layout_marginBottom="16dp"
          android:minHeight="48dp"
          android:maxLines="1"
          android:singleLine="true"
          android:text="@string/keep_screen_on_label"
          android:textColor="@android:color/white"
          android:textSize="16sp"
          app:thumbTint="@color/switch_thumb_tint"
          app:trackTint="@color/switch_track_tint"
          app:layout_constraintBottom_toBottomOf="parent"
          app:layout_constraintStart_toStartOf="parent"
          tools:text="Sempre acceso" />

    Note vincolanti:
    - Usare il nome completo `androidx.appcompat.widget.SwitchCompat` (non `Switch`) per garantire il tint via app:thumbTint/app:trackTint su tutta la gamma minSdk 30+.
    - NESSUN attributo di visibilità: il widget deve restare VISIBLE in ogni stato (D-03).
    - I vincoli sono Start+Bottom su parent (specchio dei Start+Top di maxSpeedText sul bordo opposto).
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'androidx.appcompat.widget.SwitchCompat' app/src/main/res/layout/activity_main.xml` restituisce 0.
    - Il nodo keepScreenOnSwitch contiene `@+id/keepScreenOnSwitch`, `app:layout_constraintBottom_toBottomOf="parent"`, `app:layout_constraintStart_toStartOf="parent"`, `android:layout_marginStart="16dp"`, `android:layout_marginBottom="16dp"`, `android:minHeight="48dp"`, `android:textSize="16sp"`, `app:thumbTint="@color/switch_thumb_tint"`, `app:trackTint="@color/switch_track_tint"`, `android:text="@string/keep_screen_on_label"`.
    - keepScreenOnSwitch NON contiene alcun attributo `android:visibility`.
    - Gli id esistenti @+id/messageText, @+id/unitText, @+id/retryButton, @+id/maxSpeedText, @+id/resetMaxButton sono ancora presenti e invariati.
    - `./gradlew.bat :app:assembleDebug` completa senza errori.
  </acceptance_criteria>
  <done>SwitchCompat keepScreenOnSwitch presente nel layout, ancorato bottom-left con margini 16dp, 48dp min, tint grayscale, etichetta italiana, sempre visibile; assembleDebug verde e gli elementi esistenti intatti.</done>
</task>

<task type="auto">
  <name>Task 3: Cablare MainActivity — default da ricarica, FLAG_KEEP_SCREEN_ON, persistenza, insets</name>
  <files>app/src/main/java/com/sed/tachimetro/MainActivity.kt</files>
  <read_first>
    - app/src/main/java/com/sed/tachimetro/MainActivity.kt (stato attuale — osservare onCreate, applyMaxAreaWindowInsets, e come maxSpeedStore viene letto prima della raccolta GPS)
    - app/src/main/java/com/sed/tachimetro/screen/ScreenOnPreferenceStore.kt (API read()/write() creata nel Task 1)
    - .planning/phases/05-gestione-schermo/05-CONTEXT.md (decisioni D-04/D-05/D-06/D-07)
    - .planning/phases/05-gestione-schermo/05-UI-SPEC.md (timing "no flash", Window insets row)
  </read_first>
  <action>
    Modificare `app/src/main/java/com/sed/tachimetro/MainActivity.kt`. NON toccare il flusso permessi, la raccolta GPS, l'area MAX, gli insets esistenti.

    (A) Aggiungere gli import mancanti (in cima, nei gruppi coerenti con lo stile esistente):
      import android.content.IntentFilter
      import android.os.BatteryManager
      import android.view.WindowManager
      import androidx.appcompat.widget.SwitchCompat
      import com.sed.tachimetro.screen.ScreenOnPreferenceStore

    (B) Aggiungere due campi di istanza accanto agli altri lateinit (es. dopo `resetMaxButton`):
      private lateinit var keepScreenOnSwitch: SwitchCompat
      private lateinit var screenOnStore: ScreenOnPreferenceStore

    (C) In `onCreate()`, DOPO il blocco che inizializza l'area MAX (dopo `updateMaxArea()`) e PRIMA della creazione di gpsSpeedProvider, aggiungere questo blocco. L'ordine è vincolante: leggere la preferenza, derivare il default da ricarica SOLO al primo avvio, impostare `isChecked` PRIMA di registrare il listener (così il listener non scatta in init e non c'è flash — UI-SPEC), applicare il flag, poi registrare il listener, infine gli insets:

      screenOnStore = ScreenOnPreferenceStore(applicationContext)
      keepScreenOnSwitch = findViewById(R.id.keepScreenOnSwitch)
      // D-04/D-05: se nessuna preferenza è salvata (primo avvio), il default deriva dallo stato di
      // ricarica; quel valore derivato viene persistito UNA sola volta, così dagli avvii successivi
      // conta solo la scelta salvata (lo stato di ricarica non viene più ricontrollato).
      val savedKeepOn = screenOnStore.read()
      val keepOn = savedKeepOn ?: isDeviceCharging()
      if (savedKeepOn == null) {
          screenOnStore.write(keepOn)
      }
      // Impostare checked PRIMA del listener: nessun trigger in init, nessun flash (UI-SPEC).
      keepScreenOnSwitch.isChecked = keepOn
      applyKeepScreenOn(keepOn)
      // D-06/D-07: il cambio applica immediatamente il flag e persiste la preferenza su disco.
      keepScreenOnSwitch.setOnCheckedChangeListener { _, isChecked ->
          applyKeepScreenOn(isChecked)
          screenOnStore.write(isChecked)
      }
      applyScreenSwitchWindowInsets()

    (D) Aggiungere tre metodi privati (accanto agli altri helper, es. dopo applyMaxAreaWindowInsets()):

      // SCRN-02/D-06: applica o rimuove FLAG_KEEP_SCREEN_ON sulla finestra corrente. Immediato,
      // nessun riavvio richiesto. FLAG_KEEP_SCREEN_ON è la via documentata e leggera per impedire
      // lo spegnimento schermo senza permessi WAKE_LOCK.
      private fun applyKeepScreenOn(keepOn: Boolean) {
          if (keepOn) {
              window.addFlags(WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)
          } else {
              window.clearFlags(WindowManager.LayoutParams.FLAG_KEEP_SCREEN_ON)
          }
      }

      // D-04: legge lo stato di ricarica corrente dal broadcast sticky ACTION_BATTERY_CHANGED
      // (registerReceiver(null, ...) restituisce subito l'ultimo Intent sticky, nessun receiver da
      // deregistrare). Usato SOLO al primo avvio per derivare il default dello switch.
      private fun isDeviceCharging(): Boolean {
          val batteryStatus = registerReceiver(null, IntentFilter(Intent.ACTION_BATTERY_CHANGED))
          val status = batteryStatus?.getIntExtra(BatteryManager.EXTRA_STATUS, -1) ?: -1
          return status == BatteryManager.BATTERY_STATUS_CHARGING ||
              status == BatteryManager.BATTERY_STATUS_FULL
      }

      // Specchio di applyMaxAreaWindowInsets() per l'angolo bottom-left: somma l'inset live
      // systemBars/displayCutout bottom+left sui margini base XML, così lo switch non finisce mai
      // dietro la navigation bar o un cutout inferiore/sinistro, in entrambi gli orientamenti.
      // Listener dedicato: gli insets differiscono per angolo (non riusare quello di maxSpeedText).
      private fun applyScreenSwitchWindowInsets() {
          val baseParams = keepScreenOnSwitch.layoutParams as ConstraintLayout.LayoutParams
          val baseBottom = baseParams.bottomMargin
          val baseStart = baseParams.marginStart
          ViewCompat.setOnApplyWindowInsetsListener(keepScreenOnSwitch) { view, insets ->
              val systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars())
              val cutout = insets.getInsets(WindowInsetsCompat.Type.displayCutout())
              val extraBottom = maxOf(systemBars.bottom, cutout.bottom)
              val extraStart = maxOf(systemBars.left, cutout.left)
              val lp = view.layoutParams as ConstraintLayout.LayoutParams
              lp.bottomMargin = baseBottom + extraBottom
              lp.marginStart = baseStart + extraStart
              view.layoutParams = lp
              insets
          }
      }

    (E) NON aggiungere alcuna riga che imposti la visibilità di keepScreenOnSwitch in showReady()/showDenied()/updatePlaceholder(): lo switch resta VISIBLE in ogni stato (D-03).
  </action>
  <verify>
    <automated>./gradlew.bat :app:assembleDebug</automated>
  </verify>
  <acceptance_criteria>
    - MainActivity.kt contiene gli import `android.os.BatteryManager`, `android.view.WindowManager`, `androidx.appcompat.widget.SwitchCompat`, `com.sed.tachimetro.screen.ScreenOnPreferenceStore`, `android.content.IntentFilter`.
    - Contiene `private lateinit var keepScreenOnSwitch: SwitchCompat` e `private lateinit var screenOnStore: ScreenOnPreferenceStore`.
    - Contiene `screenOnStore.read()` e il fallback `?: isDeviceCharging()`, con `screenOnStore.write(keepOn)` eseguito solo quando `savedKeepOn == null`.
    - `keepScreenOnSwitch.isChecked = keepOn` compare PRIMA di `keepScreenOnSwitch.setOnCheckedChangeListener` nel file (ordine verificabile: la riga isChecked precede la riga setOnCheckedChangeListener).
    - Contiene i metodi `applyKeepScreenOn`, `isDeviceCharging`, `applyScreenSwitchWindowInsets`, e `FLAG_KEEP_SCREEN_ON` con sia `addFlags` sia `clearFlags`.
    - `grep -c 'keepScreenOnSwitch.visibility' app/src/main/java/com/sed/tachimetro/MainActivity.kt` restituisce 0 (nessuna logica di visibilità sullo switch).
    - `./gradlew.bat :app:assembleDebug` completa senza errori.
  </acceptance_criteria>
  <done>MainActivity legge la preferenza, deriva il default dallo stato di ricarica solo al primo avvio (persistendolo una volta), imposta checked prima del listener, applica/rimuove FLAG_KEEP_SCREEN_ON immediatamente al tap, persiste ogni cambio, e applica gli insets bottom-left; lo switch resta sempre visibile; assembleDebug verde.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Utente → app (tap sullo switch) | Unico "input" applicativo: un Boolean generato dal tap dell'utente sul proprio device. Nessun input testuale, nessun parsing, nessuna superficie iniettabile. |
| OS → app (broadcast sticky ACTION_BATTERY_CHANGED) | Al primo avvio l'app legge lo stato di ricarica dal broadcast sticky di sistema. Sorgente = Android stesso (broadcast protetto di sistema), non un'app terza. |
| App ↔ disco (SharedPreferences MODE_PRIVATE) | Persistenza di un Boolean su file app-private. Nessun dato condiviso con altre app, nessuna PII, nessun segreto. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | SharedPreferences boolean ("keep_screen_on") | accept | File app-private (MODE_PRIVATE). Solo un device con root o l'utente stesso potrebbe alterarlo; l'unico effetto è cambiare una preferenza UX reversibile. `getBoolean` restituisce sempre un Boolean valido (nessun valore malformato possibile, a differenza dell'Int di MaxSpeedStore che richiedeva sanitize), quindi nessuna sanitizzazione necessaria. Nessun impatto di sicurezza. |
| T-05-01-02 | Information Disclosure | isDeviceCharging() via ACTION_BATTERY_CHANGED sticky broadcast | accept | `registerReceiver(null, IntentFilter(ACTION_BATTERY_CHANGED))` legge un broadcast di sistema protetto e restituisce solo lo stato di ricarica del device dell'utente. Nessun dato lascia l'app, nessuna PII, nessun permesso richiesto. Lettura locale one-shot al primo avvio. |
| T-05-01-03 | Denial of Service (battery) | FLAG_KEEP_SCREEN_ON | accept | Lo schermo sempre acceso consuma batteria, ma è un tradeoff UX ESPLICITAMENTE scelto dall'utente (CONTEXT D-04) e completamente reversibile con un tap. Non è una minaccia di sicurezza ma una decisione di prodotto già approvata. FLAG_KEEP_SCREEN_ON non richiede il permesso WAKE_LOCK e non tiene sveglia la CPU a schermo spento. |

Nessuna minaccia ad alta severità in questa fase: nessuna chiamata di rete, nessuna autenticazione, nessuna PII, nessun input non fidato da terze parti. L'unico input è un Boolean dal tap dell'utente e la lettura del proprio stato di ricarica da un broadcast di sistema protetto. Le tre voci sopra sono tutte a bassa severità con disposizione "accept" motivata. Nessun blocco al ship.
</threat_model>

<verification>
- `./gradlew.bat :app:assembleDebug` verde dopo ogni task.
- Presenza dei nuovi artefatti e assenza di regressioni sugli elementi esistenti — vedi acceptance_criteria per grep puntuali.
- Verifica comportamentale completa (applicazione immediata del flag, persistenza post-chiusura e post-riavvio, default da ricarica al primo avvio, insets nei due orientamenti) delegata al checkpoint on-device del piano 05-02.
</verification>

<success_criteria>
- SCRN-01: keepScreenOnSwitch sempre visibile in basso a sinistra con etichetta italiana "Sempre acceso".
- SCRN-02: FLAG_KEEP_SCREEN_ON applicato/rimosso immediatamente al tap (D-06).
- SCRN-03: preferenza booleana persistita su SharedPreferences ad ogni cambio (D-07); default da stato di ricarica solo al primo avvio (D-04/D-05).
- Nessuna nuova dipendenza Gradle; tint grayscale (nessun blu/viola AppCompat); nessuna regressione sugli elementi Fase 3/4.
</success_criteria>

<output>
Create `.planning/phases/05-gestione-schermo/05-01-SUMMARY.md` when done
</output>
