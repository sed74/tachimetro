---
phase: 260902-qr8-aggiorna-tutti-i-file-nella-cartella-pla
plan: 02
subsystem: playstore-publishing
tags: [docs, playstore, compliance, android-auto, release-2.0]
requires:
  - "playstore/ riallineato dal piano 01"
  - "bump versionCode = 3 / versionName = 2.0 (commit 69de1e2)"
provides:
  - "Pacchetto playstore/ allineato alla versione 2.0 con supporto Android Auto"
  - "Note di rilascio 2.0 bilingui (release_notes_v2.0.txt)"
  - "Dichiarazione del quarto valore persistito (contatore rifiuti permesso lato auto)"
  - "Nota interna di rischio su ALLOW_ALL_HOSTS_VALIDATOR / Fase 11"
affects:
  - "playstore/**"
tech-stack:
  added: []
  patterns:
    - "Separazione interno/pubblico verificata da gate grep automatico"
    - "Note di rilascio: file piatti per-locale + archivio bilingue versionato"
key-files:
  created:
    - playstore/release_notes/release_notes_v2.0.txt
  modified:
    - playstore/listing/it/short_description.txt
    - playstore/listing/en/short_description.txt
    - playstore/listing/it/full_description.txt
    - playstore/listing/en/full_description.txt
    - playstore/release_notes/it.txt
    - playstore/release_notes/en.txt
    - playstore/data_safety.md
    - playstore/privacy_policy.html
    - playstore/content_rating.md
    - playstore/README.md
decisions:
  - "content_rating.md verificato e NON riscritto: le risposte IARC restano corrette per la 2.0 (nessun permesso nuovo, nessun INTERNET, nessuna condivisione posizione). Aggiunta solo la nota permessa sulla categoria 'Auto e veicoli'"
  - "La nota sul gap ALLOW_ALL_HOSTS_VALIDATOR resta confinata a playstore/README.md: non e' una voce di sicurezza dei dati (riguarda il binding degli host al car service, non la raccolta dati)"
  - "I 5 bullet Android Auto derivano 1:1 da AA-01..AA-04/CONN-01/CONN-02; il bullet sullo stile deciso dall'host dichiara esplicitamente la nuance D-13 invece di sottintenderla"
metrics:
  duration: ~5 min
  completed: 2026-09-02
  tasks: 3
  files: 11
---

# Quick Task 260902-qr8 Plan 02: Allineamento playstore/ alla v2.0 con Android Auto Summary

Riallineamento completo del pacchetto di pubblicazione `playstore/` alla versione 2.0 (`versionCode = 3`), annunciando il supporto Android Auto con cinque capacita' derivate 1:1 dai requisiti verificati, dichiarando il quarto valore persistito e confinando il gap di hardening della Fase 11 al solo README interno.

## Cosa e' stato fatto

**Task 1 — testi rivolti all'utente** (commit `c1b3344`)
Short description IT/EN riscritte per citare Android Auto restando entro gli 80 caratteri (IT 77 byte, EN 68 byte). Sezione `ANDROID AUTO` con esattamente cinque bullet inserita fra `FUNZIONALITÀ PRINCIPALI`/`KEY FEATURES` e `PRIVACY AL PRIMO POSTO`/`PRIVACY FIRST` in entrambe le full description; sezioni requisiti estese con la nota su auto/head unit compatibile. `release_notes/it.txt` ed `en.txt` sovrascritte per intero con i contenuti 2.0 (nessun residuo 1.1) e creato `release_notes_v2.0.txt` bilingue nello stesso formato dell'archivio 1.1, che resta intatto.

**Task 2 — documenti di compliance** (commit `e9f6f99`)
`data_safety.md` passa da "tre" a "quattro" voci persistite, con il contatore dei rifiuti del permesso lato auto (`CarPermissionDenialStore`) come quarto punto; precisato che la posizione GPS gia' raccolta alimenta anche lo schermo auto senza raccolta aggiuntiva, permessi nuovi o trasmissione; confermato che il manifest 2.0 dichiara ancora solo `ACCESS_FINE_LOCATION`. `privacy_policy.html` ha ora 4 `<li>` per lingua (8 totali) e una frase su Android Auto in entrambe le sezioni "Dove vanno i dati"/"Where the data goes"; i due placeholder email sono intatti e nessun nome di classe/package e' stato introdotto.

**Task 3 — README interno** (commit `9f8159c`)
Intestazione, tabella `Contenuto`, passi 1/3/6 riferiti alla 2.0. Eliminata la sezione "Attenzione — questo pacchetto riguarda la sola v1.1" e ogni istruzione di costruire dal tag `v1.1` (ora falsa: produrrebbe un binario privo delle funzionalita' descritte). Aggiunta la sezione "Rischio noto accettato per questo rilascio (nota interna)" su `ALLOW_ALL_HOSTS_VALIDATOR`/Fase 11, con la motivazione esplicita del perche' non va copiata altrove.

## Esito dei gate automatici

| Gate | Task | Esito |
|------|------|-------|
| GATE1_OK | 1 | **PASSED** — limiti caratteri rispettati, sezioni ANDROID AUTO presenti, note 2.0 senza residui 1.1, archivio v1.1 conservato |
| GATE2_OK | 2 | **PASSED** — quattro store nominati in data_safety, 8 `<li>` nella privacy policy, contatore e Android Auto in entrambe le lingue, placeholder email intatti |
| GATE3_OK | 3 | **PASSED** — nessuna menzione di HostValidator/ALLOW_ALL_HOSTS né di nomi implementativi nei file pubblici; README allineato alla 2.0 senza l'istruzione del tag v1.1 |

Verifica complessiva del piano: `git status --short playstore/` pulito dopo i commit, `git diff --stat app/` vuoto (nessun sorgente toccato, `app/build.gradle.kts` incluso), nessun file sotto `playstore/apk/`, `screenshots/` o `graphics/` modificato. I file cambiati sono esattamente gli 11 elencati in `files_modified`, senza cancellazioni.

## content_rating.md: verificato, non riscritto

Confrontate tutte le risposte IARC con il comportamento reale della 2.0: nessun permesso nuovo, nessun `INTERNET` (il blocco `<queries>` e' contribuito dal manifest di `androidx.car.app` in fase di merge, non dall'app), nessuna condivisione posizione, nessun UGC, nessuna interazione fra utenti, nessuna pubblicita'. **Nessuna risposta e' risultata sbagliata**: la tabella e la classificazione attesa non sono state modificate. L'unico intervento e' la nota permessa dal piano sulla categoria primaria "Auto e veicoli", resa ancora piu' appropriata dal supporto Android Auto.

## Tracciabilita' dei bullet Android Auto

| Bullet | Requisito |
|--------|-----------|
| Velocita' sullo schermo auto, aggiornata ogni secondo | AA-01 + AA-03 |
| "Ricerca segnale..." quando manca il GPS | AA-02 |
| Permesso concedibile dallo schermo auto | AA-04 |
| Messaggio neutro + rilascio schermo acceso, ripristino alla disconnessione | CONN-01 + CONN-02 |
| Aspetto deciso da Android Auto | Nuance D-13 di AA-01 (08-CONTEXT.md) |

Nessun bullet senza requisito corrispondente; nessuna capacita' inventata (niente navigazione turn-by-turn, mappe, impostazioni in auto o pulsanti sullo schermo auto).

## Deviations from Plan

**1. [Rule 1 - Bug] Corretta una quarta menzione residua di "tre voci" in data_safety.md**
- **Found during:** Task 2
- **Issue:** Oltre ai punti indicati dal piano, la voce "Gli utenti possono richiedere la cancellazione dei dati?" elencava ancora solo tre valori locali (`max velocità sessione, distanza percorsa, preferenza schermo`), restando factualmente incompleta dopo l'aggiunta del quarto.
- **Fix:** Aggiunto il contatore dei rifiuti del permesso lato auto all'elenco.
- **Files modified:** `playstore/data_safety.md`
- **Commit:** `e9f6f99`

**2. [Rule 3 - Blocking] Worktree allineato a master prima di iniziare**
- **Found during:** Setup, prima del Task 1
- **Issue:** Il worktree era 3 commit indietro rispetto a `master` e non conteneva il lavoro del piano 01 (`2848ecd`) ne' il bump di versione (`69de1e2`): eseguire il piano 02 su quella base avrebbe prodotto conflitti e avrebbe riscritto testi gia' corretti dal piano 01.
- **Fix:** `git merge --ff-only master`. Il branch non aveva commit propri, quindi il fast-forward e' stato non distruttivo (nessun `reset --hard`, nessun `clean`).
- **Files modified:** nessuno (solo avanzamento del branch)
- **Commit:** n/a

**3. [Rule 3 - Blocking] Gate eseguiti con path esplicito invece di `$(git rev-parse --show-toplevel)`**
- **Found during:** Task 1
- **Issue:** La sandbox di isolamento del worktree rifiuta i comandi che annidano `git rev-parse` in una command substitution.
- **Fix:** Eseguiti gli stessi identici gate sostituendo `cd "$(git rev-parse --show-toplevel)"` con `cd` alla radice del worktree scritta per esteso. Nessuna modifica alla logica di verifica.
- **Files modified:** nessuno
- **Commit:** n/a

## Passi manuali ancora aperti

Nessuno di questi e' stato eseguito — restano tutti a carico dell'utente:

1. **Rigenerazione APK/AAB alla 2.0** — build da `HEAD` (o dal tag `2.0` quando esistera'), da depositare come `apk/tachimetro-2.0-unsigned.apk` e `apk/tachimetro-2.0.aab`, rimuovendo i due file 1.0 obsoleti con `git rm`.
2. **Firma release** — creare `keystore.properties` dalle credenziali reali; senza, la build resta non firmata senza fallire.
3. **Screenshot v2.0** — cattura manuale su device reale (icona di ricarica e distanza percorsa) **piu' la cattura dello schermo Android Auto**, che richiede una sessione DHU o un head unit fisico: non automatizzabile in questo ambiente.
4. **Hosting privacy policy** — serve un URL pubblico (GitHub Pages o hosting statico).
5. **Sostituzione dei due placeholder email** in `privacy_policy.html` (`[inserire indirizzo email di contatto]` e `[insert contact email address]`), lasciati intatti di proposito.
6. **Form Play Console** — scheda store, sicurezza dei dati, classificazione contenuti e privacy policy vanno compilati a mano.

## Rischio noto in uscita

`TachimetroCarAppService.createHostValidator()` restituisce ancora `HostValidator.ALLOW_ALL_HOSTS_VALIDATOR`: il car service accetta il binding da qualunque host Android Auto. Sostituirlo e' lavoro della Fase 11, non iniziata. L'utente ne e' informato e ha scelto di pubblicare comunque la 2.0. La nota e' documentata **solo** in `playstore/README.md` e il gate automatico di Task 3 verifica che non trapeli in nessun file rivolto all'utente o a Play Console.

## Known Stubs

Nessuno. Il piano modifica esclusivamente file di testo: i due placeholder email in `privacy_policy.html` sono intenzionali e tracciati come passo manuale aperto n. 5, per esplicita istruzione del piano.

## Self-Check: PASSED

File creati verificati presenti:
- `playstore/release_notes/release_notes_v2.0.txt` — FOUND
- `playstore/release_notes/release_notes_v1.1.txt` — FOUND (archivio conservato, non cancellato)

Commit verificati in `git log`:
- `c1b3344` — FOUND (Task 1)
- `e9f6f99` — FOUND (Task 2)
- `9f8159c` — FOUND (Task 3)

Tutti e tre i gate automatici (GATE1_OK, GATE2_OK, GATE3_OK) rieseguiti e passati.
